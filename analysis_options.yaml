# cspell: ignore ktlint tearoffs interps setstate WCAG listview inlines datetimes initstate maybeof mediaquery singlechildscrollview scrollview expando futureor vsync firestore scrollables sizedbox
#
# saropa_lints ^1.5.1 - 628+ lint rules for Flutter and Dart
# https://pub.dev/packages/saropa_lints/changelog
#
# Configuration optimized for: DART UTILITY LIBRARY (not a Flutter app)

analyzer:
  exclude:
    - "**/*.bak"
    - "**/*.freezed.dart"
    - "**/*.g.dart"
    - "**/*.old"
    - ".crashlytics/**"
    - ".dart_tool/**"
    - ".dev/**"
    - ".github/**"
    - ".idea/**"
    - ".vs/**"
    - ".vscode/**"
    - "android/**"
    - "assets/**"
    - "bin/cache/**"
    - "build/**"
    - "dependency_overrides/**"
    - "example/**"
    - "ios/**"
    - "ktlint"
    - "lib/generated_plugin_registrant.dart"
    - "scripts/**"
    - "test/.test_coverage.dart"

  language:
    strict-casts: true
    strict-inference: true
    strict-raw-types: true

  errors:
    cast_nullable_to_non_nullable: warning
    missing_required_param: error
    missing_return: error
    record_literal_one_positional_no_trailing_comma: error
    collection_methods_unrelated_type: warning
    unrelated_type_equality_checks: warning
    parameter_assignments: warning
    todo: ignore

  strong-mode:
    implicit-dynamic: false
    implicit-casts: false

formatter:
  page_width: 100
  trailing_commas: preserve

# =============================================================================
# SAROPA LINTS - UTILITY LIBRARY CONFIG
# =============================================================================
# Using 'insanity' tier (ALL 500+ rules enabled).
# Disabling rules that don't apply to this Dart utility library (no Flutter).
# =============================================================================

linter:
  rules:
    avoid_classes_with_only_static_members: false
    avoid_positional_boolean_parameters: false
    flutter_style_todos: false
    lines_longer_than_80_chars: false
    public_member_api_docs: false
    sort_pub_dependencies: false

plugins:
  saropa_lints:
    version: "5.0.2"
    # ═══════════════════════════════════════════════════════════════════
    # SAROPA LINTS CONFIGURATION
    # ═══════════════════════════════════════════════════════════════════
    # Regenerate with: dart run saropa_lints:init --tier comprehensive
    # Tier: comprehensive (1596 of 1777 rules enabled)
    # Lint rules are disabled by default. Set to true to enable.
    # User customizations are preserved unless --reset is used
    #
    # Tiers (cumulative):
    #   1. essential    - Critical: crashes, security, memory leaks
    #   2. recommended  - Essential + accessibility, performance
    #   3. professional - Recommended + architecture, testing
    #   4. comprehensive - Professional + thorough coverage
    #   5. pedantic     - All rules (pedantic, highly opinionated)
    #   +  stylistic    - Opt-in only (formatting, ordering)
    #
    # Disabled platforms: macos, web, windows, linux
    #
    # Settings (max_issues, platforms, packages) are in analysis_options_custom.yaml
    # ═══════════════════════════════════════════════════════════════════

    diagnostics:
      #
      # ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
      #   ◆ USER CUSTOMIZATIONS
      # ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
      #
      # These rules have been manually configured and will be preserved
      # when regenerating. Use --reset to discard these customizations.

      avoid_barrel_files: false  # [INFO] File contains only export statements (barrel file). Barrel files increase build times by pulling in unused code and obscure dependency tracking, making it harder to identify which modules depend on which implementations. {v6}
      avoid_non_ascii_symbols: false  # [INFO] String contains non-ASCII characters. Non-ASCII characters can cause encoding issues and may be hard to distinguish visually (e.g., different types of spaces). {v4}
      avoid_static_state: false  # [WARNING] Static mutable state persists across hot-reloads and tests, causing stale data and inconsistent behavior. Tests fail unpredictably due to shared state leaking between runs, and production bugs become hard to reproduce across different app sessions and isolates. {v4}
      avoid_unmarked_public_class: false  # [INFO] Public class lacks an explicit class modifier. Dart 3.0 introduced class modifiers (base, final, interface, sealed). For API stability, public classes should declare their inheritance intent. {v3}
      no_empty_string: false  # [INFO] Using empty string literals ("") in your code can be ambiguous and may indicate a missing value or a placeholder. Relying on empty strings for logic can lead to subtle bugs and makes intent unclear to readers. {v6}
      no_magic_number_in_tests: false  # [INFO] Unexplained numeric literal in test file obscures the purpose of expected values and assertions. When a test fails, readers cannot tell whether the number is an arbitrary fixture value, a meaningful boundary, or a calculated expected result, making failures harder to diagnose and fix. {v4}
      no_magic_string_in_tests: false  # [INFO] Unexplained string literal in test file obscures the purpose of expected values and assertions. When a test fails, readers cannot tell whether the string is an arbitrary fixture, a meaningful expected output, or a format-specific value, making failures harder to diagnose. {v5}
      prefer_compute_for_heavy_work: false  # [INFO] Heavy computation such as encryption, compression, or parsing runs synchronously on the main UI thread. This blocks the rendering pipeline, freezing animations, dropping frames, and making the app unresponsive to touch input for the duration of the operation. On lower-end devices, this delay is especially pronounced and triggers ANR warnings on Android. {v5}
      prefer_descriptive_test_name: false  # [INFO] Test name must be descriptive. Test names must be descriptive and follow conventions. Test names don't follow the expected format. This reduces test maintainability and makes it harder to identify which behavior failed when tests break. {v7}
      prefer_prefixed_global_constants: false  # [INFO] Global constant must have a descriptive prefix. Global constants must be prefixed with 'k' or similar. {v5}
      prefer_secure_random: false  # [WARNING] Random() uses a predictable pseudo-random number generator that produces reproducible sequences from a known seed. Tokens, passwords, encryption keys, or nonces generated with Random() can be predicted by attackers, enabling session hijacking, credential guessing, and cryptographic attacks. {v3}
      prefer_static_method: false  # [INFO] Method does not reference any instance members and could be static. Non-static methods that ignore instance state mislead readers into thinking the method depends on object state, and they prevent calling the method without an instance. {v4}
      require_https_only_test: false  # [INFO] HTTP URL detected in test file. Consider using HTTPS even in test data. {v3}
      require_test_description_convention: false  # [INFO] Test description should explain what is being tested and expected behavior. Test descriptions should explain WHAT is being tested and WHAT the expected behavior is. This helps with test maintenance and debugging. {v3}

      #
      # ════════════════════════════════════════════════════════════════════════
      #   ✓ ENABLED RULES (COMPREHENSIVE TIER)
      # ════════════════════════════════════════════════════════════════════════
      #

      #
      # --- TIER 1: ESSENTIAL (284 rules) ---
      #
      always_remove_listener: true  # [WARNING] Listener added via addListener() but no matching removeListener() call found in dispose(). Orphaned listeners retain references to the widget, preventing garbage collection and causing memory leaks that accumulate as users navigate between screens. {v3}
      avoid_android_cleartext_traffic: true  # [WARNING] HTTP URL detected. Android 9+ blocks cleartext traffic by default. This request will fail silently. {v2}
      avoid_animation_rebuild_waste: true  # [WARNING] AnimatedBuilder wraps too much of the widget tree, causing unnecessary rebuilds and wasted CPU cycles. This can degrade animation performance, increase battery usage, and make your app feel sluggish, especially on lower-end devices. {v3}
      avoid_api_key_in_code: true  # [ERROR] Hardcoded keys are extractable from app binaries, enabling unauthorized API access and billing abuse. {v3}
      avoid_app_links_sensitive_params: true  # [ERROR] Deep link params are logged by OS and analytics, exposing tokens in crash reports and logs. {v2}
      avoid_assert_in_production: true  # [WARNING] assert() is compiled out of release builds by the Dart compiler. Any validation, input checking, or security enforcement using assert() will silently stop running in production, allowing invalid data, unauthorized access, or corrupted state to pass through unchecked. {v2}
      avoid_asset_manifest_json: true  # [ERROR] The 'AssetManifest.json' file was removed in Flutter 3.38.0 (PR #172594). Code that loads this path at runtime — such as rootBundle.loadString('AssetManifest.json') — will throw a FlutterError because the file no longer exists in the built app bundle. Migrate to the typed AssetManifest API (AssetManifest.loadFromAssetBundle) or use 'AssetManifest.bin' for the binary format. {v1}
      avoid_assigning_notifiers: true  # [ERROR] Reassigning Notifier breaks provider contract. State updates are lost and listeners receive stale data. {v3}
      avoid_async_callback_in_fake_async: true  # [ERROR] Async callback uses real time, making tests hang or timeout instead of using controlled fake time. {v2}
      avoid_async_in_build: true  # [ERROR] Build method should never be async. This will cause rendering issues. {v2}
      avoid_auth_in_query_params: true  # [ERROR] Query params are logged in server logs, browser history, and referrer headers, leaking auth tokens. {v3}
      avoid_bloc_context_dependency: true  # [WARNING] Bloc depending on BuildContext couples business logic to the UI layer. This makes the Bloc untestable in isolation, prevents reuse across widgets, and can cause crashes when the context becomes invalid after the widget is removed from the tree. {v6}
      avoid_bloc_event_mutation: true  # [ERROR] If BLoC events are mutable, they can be modified during processing, causing race conditions, unpredictable state changes, and hard-to-debug bugs. This breaks the contract of event immutability and can destabilize your app. {v3}
      avoid_blocking_main_thread: true  # [WARNING] Synchronous I/O blocks the main isolate, freezing the UI and causing jank or ANR (Application Not Responding) errors on mobile platforms. Even brief blocking degrades perceived performance and user experience significantly. {v1}
      avoid_builder_index_out_of_bounds: true  # [WARNING] itemBuilder accesses list without bounds check. If the index is out of bounds due to list changes, this will cause runtime exceptions, app crashes, and unpredictable UI behavior. This is a common source of production bugs in dynamic lists and can lead to negative user reviews. {v6}
      avoid_cached_image_unbounded_list: true  # [WARNING] CachedNetworkImage used inside a scrollable list without memCacheWidth or memCacheHeight. All images are cached at full resolution, causing excessive memory usage that can lead to OOM crashes on lower-end devices. In a long list, this quickly exhausts available memory. {v1}
      avoid_catch_all: true  # [WARNING] Bare catch clause without an on-type hides the specific error type being caught and silently swallows critical failures like OutOfMemoryError and StackOverflowError. This can mask fatal programming bugs, making them impossible to detect in crash reports or monitoring systems. {v3}
      avoid_catch_exception_alone: true  # [WARNING] Using "on Exception catch" without an "on Object catch" fallback silently misses all Error types including StateError, TypeError, and RangeError. These programming errors will crash the app without being logged or reported, making production debugging extremely difficult. {v2}
      avoid_circular_dependencies: true  # [WARNING] Service class constructor depends on another service of the same architectural layer (both end with Service, Repository, Controller, etc.). This creates circular dependency chains that break initialization order, prevent unit testing with mocks, and produce tightly coupled modules that cannot be modified or deployed independently. {v5}
      avoid_circular_provider_deps: true  # [ERROR] Circular dependencies between providers cause stack overflows, infinite loops, and unpredictable initialization order. This leads to runtime crashes, hard-to-debug errors, and broken state management. If not fixed, your app may crash or behave unpredictably in production. Always design provider graphs to be acyclic for reliability and maintainability. {v1}
      avoid_circular_redirects: true  # [WARNING] GoRoute redirect callback always returns a path and never returns null, creating an unconditional redirect that forms an infinite loop. The router exhausts the redirect limit and throws an exception, crashing the app or permanently locking users out of all navigation when the redirect chain has no termination condition. {v2}
      avoid_clearing_form_on_error: true  # [WARNING] Form fields are cleared when validation fails, destroying all user input. Users lose their partially-correct data and must re-enter everything from scratch. This frustrating pattern increases form abandonment, wastes user time, and creates a hostile experience that drives users away from completing the form submission. {v2}
      avoid_clipboard_sensitive: true  # [WARNING] Clipboard contents persist and are readable by other apps, exposing passwords and tokens. {v4}
      avoid_color_only_meaning: true  # [WARNING] Color is used as the sole visual indicator to convey meaning or state, which is inaccessible to approximately 8% of men and 0.5% of women with color vision deficiency. WCAG 1.4.1 (Use of Color) requires that color is never the only visual means of conveying information, indicating an action, or distinguishing a visual element. Always pair color with a secondary cue such as an icon, text label, underline, or pattern. {v1}
      avoid_conditional_hooks: true  # [ERROR] Hook function called conditionally. Hooks must be called unconditionally in the same order. {v2}
      avoid_context_across_async: true  # [ERROR] BuildContext used after await crashes if widget was disposed during the async gap. Check mounted first. {v6}
      avoid_context_after_await_in_static: true  # [ERROR] BuildContext used after await in static method. Context may be invalid after async gap. {v4}
      avoid_context_after_navigation: true  # [WARNING] BuildContext accessed after an awaited navigation call without a mounted check. The widget that owns this context is likely disposed by the time the await completes, and calling ScaffoldMessenger.of(), Navigator.of(), or Theme.of() on a disposed context throws a FlutterError that crashes the app or produces silent failures in release mode. {v3}
      avoid_context_dependency_in_callback: true  # [WARNING] BuildContext-dependent call (Theme.of, MediaQuery.of, Navigator.of, ScaffoldMessenger.of, etc.) is used inside an asynchronous callback such as Future.then(), Future.delayed(), or Timer. After an async gap the originating widget may have been unmounted, making the captured BuildContext stale and causing a "Looking up a deactivated widget's ancestor" exception at runtime. {v2}
      avoid_datetime_now_in_tests: true  # [WARNING] DateTime.now() in tests produces non-deterministic values that vary between runs, causing flaky assertions that pass locally but fail in CI, making test failures impossible to reproduce reliably. {v2}
      avoid_deep_link_sensitive_params: true  # [ERROR] Deep link query parameter contains sensitive data (password, token, secret, API key, or credential), creating a security breach. Deep link URLs are recorded in system logs, browser history, HTTP referrer headers, and analytics platforms, permanently exposing credentials to attackers who gain access to those logs or the device history. {v4}
      avoid_deprecated_crypto_algorithms: true  # [WARNING] The use of outdated cryptographic algorithms such as MD5, SHA1, DES, 3DES, and RC4 exposes your application to well-known attacks. These algorithms have been broken by the security community: MD5 and SHA1 are vulnerable to collision and preimage attacks, allowing attackers to forge digital signatures or tamper with data. DES and 3DES have insufficient key lengths and are susceptible to brute-force attacks, while RC4 is vulnerable to several biases and key recovery attacks. Continuing to use these algorithms puts all encrypted or hashed data at risk of compromise, regardless of other security measures. {v2}
      avoid_dialog_context_after_async: true  # [ERROR] If you call Navigator.pop() or use BuildContext after an await, the widget may have been disposed, causing a "Looking up deactivated widget" crash. This leads to app instability and poor user experience. {v5}
      avoid_dialog_in_build: true  # [ERROR] Calling showDialog (or similar) inside build() will cause your app to enter an infinite loop, repeatedly showing dialogs and freezing the UI. This results in a poor user experience and may crash the app. {v2}
      avoid_double_for_money: true  # [ERROR] Floating point causes rounding errors in money calculations. $0.1 + $0.2 != $0.3, causing financial loss. {v4}
      avoid_duplicate_bloc_event_handlers: true  # [ERROR] Second handler for same event type is ignored, causing silent bugs when expected logic runs. {v2}
      avoid_duplicate_map_keys: true  # [WARNING] Duplicate key in map literal silently overwrites the earlier value, causing data loss and unpredictable behavior. Only the last value assigned to the key will persist in the resulting map. {v4}
      avoid_duplicate_widget_keys: true  # [WARNING] Multiple widgets in a list share the same Key value. Flutter uses keys to match old widgets with new widgets during reconciliation. Duplicate keys cause the framework to reuse the wrong element, leading to stale state, broken animations, and incorrect widget ordering after list mutations. {v4}
      avoid_dynamic_code_loading: true  # [ERROR] Dynamic code loading at runtime bypasses compile-time dependency verification, allowing execution of unverified code and creating supply chain attack vectors. {v3}
      avoid_dynamic_sql: true  # [ERROR] Constructing SQL queries by concatenating or interpolating user input directly into the query string exposes your application to SQL injection attacks. Attackers can craft input that alters the structure of your query, allowing them to read, modify, or delete arbitrary data, escalate privileges, or compromise the entire database. SQL injection is one of the most critical and well-documented security vulnerabilities in software development, and has led to major data breaches across industries. {v3}
      avoid_eval_like_patterns: true  # [ERROR] Dynamic code execution allows arbitrary code injection, enabling attackers to execute malicious code. {v7}
      avoid_excessive_rebuilds_animation: true  # [WARNING] The builder callback of this animation widget contains too many widget constructors, causing the entire subtree to rebuild on every animation frame (typically 60 times per second). This wastes CPU cycles, increases battery drain, and degrades animation smoothness. Only widgets that actually change during the animation should be inside the builder; static content should be passed via the child parameter or moved outside. {v1}
      avoid_existing_instances_in_bloc_provider: true  # [ERROR] Returning an existing bloc instance from BlocProvider(create: ...) causes the bloc to be closed when the provider disposes, even if it is still used elsewhere. This can lead to unexpected state loss, runtime errors, and hard-to-debug bugs. Always use the correct provider pattern for new vs. existing blocs. {v2}
      avoid_expanded_outside_flex: true  # [ERROR] Expanded, Flexible, and Spacer set FlexParentData on their child, which only RenderFlex (Row, Column, Flex) can read during layout. Placing them inside any other parent — Stack, Center, Padding, LimitedBox, SizedBox, etc. — throws an unrecoverable "Incorrect use of ParentDataWidget" FlutterError at runtime. This also happens indirectly when a widget's build() returns Expanded and the widget is later wrapped by a non-Flex container, breaking the Flex→Expanded parent chain. {v7}
      avoid_external_storage_sensitive: true  # [ERROR] External storage is world-readable on Android, exposing credentials to any installed app. {v4}
      avoid_fixed_size_in_scaffold_body: true  # [WARNING] Scaffold body has a Column with text input fields but no ScrollView. The keyboard will cause overflow. {v2}
      avoid_flashing_content: true  # [WARNING] Flashing >3Hz can trigger seizures in users with photosensitive epilepsy. WCAG 2.3.1 compliance required. {v4}
      avoid_functions_in_register_singleton: true  # [WARNING] registerSingleton expects an instance, not a factory function. {v3}
      avoid_future_in_build: true  # [WARNING] Creating Future in build() causes repeated async calls on every rebuild. {v2}
      avoid_getx_build_context_bypass: true  # [ERROR] Get.context or Get.overlayContext bypasses Flutter's BuildContext propagation. This hides widget tree dependencies, makes code untestable without GetX runtime, and circumvents Flutter's fundamental context-based service location pattern. The retrieved context may be stale or from an unexpected part of the widget tree, causing subtle bugs. {v2}
      avoid_getx_context_outside_widget: true  # [WARNING] Get.context or Get.overlayContext used outside widget class. This is unsafe and may cause crashes. {v2}
      avoid_global_key_in_build: true  # [ERROR] Creating a GlobalKey inside the build() method causes it to be recreated on every rebuild, which results in lost widget state, broken references, and unpredictable UI behavior. This can cause your app to lose user input or fail to maintain state across rebuilds. {v4}
      avoid_go_router_inline_creation: true  # [WARNING] GoRouter instance created inside build() is destroyed and recreated on every widget rebuild. This resets all navigation state including the current route stack, destroys in-flight transitions, breaks hot reload during development, and causes users to lose their navigation position whenever the parent widget rebuilds. {v3}
      avoid_hardcoded_config: true  # [WARNING] Hardcoded configuration value detected. Embedding URLs, ports, API keys, or feature flags directly in source code makes the app inflexible across environments (dev, staging, production) and forces a rebuild for every configuration change, increasing deployment risk. {v4}
      avoid_hardcoded_credentials: true  # [ERROR] Hardcoding credentials (such as passwords, API keys, or tokens) in source code exposes them to anyone with access to the codebase, including public repositories and version control history. This can lead to unauthorized access, data breaches, and compromised systems. Credentials should always be stored securely and never committed to version control. {v5}
      avoid_hardcoded_encryption_keys: true  # [ERROR] Hardcoded encryption keys present in source code or binaries can be easily extracted by attackers using reverse engineering tools. Once exposed, these keys allow adversaries to decrypt all user data protected by the key, resulting in a complete compromise of confidentiality for every user of your application. This vulnerability is especially critical in mobile and web apps, where binaries are distributed to end users. Hardcoded keys are often found in test code, examples, or as quick fixes, but they must never be used in production or shipped code. Attackers routinely scan for such patterns, and automated tools can detect and extract these secrets within minutes of release. {v5}
      avoid_hidden_interactive: true  # [ERROR] This interactive element uses excludeFromSemantics, making it completely inaccessible to screen readers and users with assistive technologies. Excluding interactive child elements from the semantics tree prevents users with disabilities from discovering or activating key actions, which fails accessibility standards and can break critical workflows. {v3}
      avoid_hive_field_index_reuse: true  # [ERROR] Reusing Hive field indexes across different fields or types can corrupt your database, cause data loss, and make migrations impossible. This can result in users losing critical data or experiencing app crashes after updates. Always assign unique field indexes and never change them after release. See https://docs.hivedb.dev/#/adapters/fields. {v3}
      avoid_hive_synchronous_in_ui: true  # [WARNING] Synchronous Hive box operation (get, put, delete, add) is called inside a build() or initState() method. These operations perform disk I/O on the main isolate, blocking frame rendering and causing visible UI jank. On slower devices or with large boxes, this can trigger ANR (Application Not Responding) dialogs, force-close the app, and create a poor user experience that leads to negative app store reviews. {v2}
      avoid_hooks_outside_build: true  # [ERROR] Hook function called outside of build method. Hooks must only be called from build(). {v2}
      avoid_ignoring_ssl_errors: true  # [ERROR] Bypassing or ignoring SSL/TLS certificate errors disables the core security guarantees of HTTPS, allowing man-in-the-middle attackers to intercept, modify, or steal all data sent over the network. This exposes user credentials, tokens, and sensitive personal data to silent compromise, and is a leading cause of real-world security breaches. SSL validation must never be disabled in production code. {v3}
      avoid_inherited_widget_in_initstate: true  # [WARNING] InheritedWidget accessed in initState(), where the widget is not yet fully mounted in the element tree. This call returns stale or missing data and does not subscribe to updates, so the widget never rebuilds when the inherited value changes. {v4}
      avoid_instantiating_in_bloc_value_provider: true  # [ERROR] Creating a new bloc instance inside BlocProvider.value prevents the bloc from being automatically closed, leading to memory leaks and unpredictable state. This is a critical resource management issue that can degrade app performance and reliability. {v2}
      avoid_instantiating_in_value_provider: true  # [ERROR] Creating a new instance inside Provider.value prevents proper lifecycle management, leading to memory leaks, resource retention, and unpredictable behavior. This is a critical issue for stateful objects like ChangeNotifiers and ValueListenables. {v2}
      avoid_ios_in_app_browser_for_auth: true  # [ERROR] OAuth URL detected in WebView. Google and Apple block OAuth via in-app WebView for security reasons. {v2}
      avoid_isar_enum_field: true  # [ERROR] Storing enums directly in Isar collections is dangerous: renaming or reordering enum values will silently corrupt your data, breaking existing records and causing unpredictable bugs. {v3}
      avoid_keyboard_overlap: true  # [INFO] TextField may be hidden behind the soft keyboard when no viewInsets handling is detected. Users cannot see what they are typing, leading to input errors, frustration, and accessibility failures on devices where the keyboard covers a large portion of the screen. {v5}
      avoid_late_final_reassignment: true  # [WARNING] Late final field has multiple assignment paths, which throws a LateInitializationError at runtime on the second write. The compiler cannot catch this statically, so the crash only surfaces during execution of the specific code path that triggers the duplicate assignment. {v4}
      avoid_listen_in_async: true  # [WARNING] Using context.watch() inside an async callback triggers widget rebuilds during asynchronous execution, creating stale closures that capture outdated state. This causes data races where async operations complete with wrong values, leading to corrupted state or duplicate side effects. {v4}
      avoid_loading_full_pdf_in_memory: true  # [WARNING] Loading an entire PDF file into memory can cause out-of-memory errors, especially on mobile devices with limited RAM. This can crash the app, degrade performance, and prevent users from viewing large documents. {v3}
      avoid_logging_sensitive_data: true  # [ERROR] Logging sensitive data such as passwords, tokens, or personally identifiable information (PII) exposes users to privacy risks and can result in data leaks if logs are accessed by unauthorized parties. Sensitive information in logs may appear in crash reports, debug output, or be inadvertently shared with third parties, leading to compliance violations and loss of user trust. Always sanitize logs to prevent exposure of confidential data. {v6}
      avoid_long_running_isolates: true  # [WARNING] Long-running isolate detected. iOS kills background tasks after ~30 seconds. Design tasks to complete quickly. {v3}
      avoid_losing_stack_trace: true  # [WARNING] Rethrowing without preserving the stack trace loses the original error location and call chain. Production debugging becomes impossible because crash reports show only the rethrow site, not where the error actually originated. Always capture and forward the full stack trace. {v2}
      avoid_misused_hooks: true  # [WARNING] Hook function is called inside a callback or closure, which violates the rules of hooks. Hooks must be called at the top level of the build method, never inside callbacks (onPressed, onTap), event handlers, or nested functions. Hooks called inside closures execute unpredictably because the closure may run zero, one, or many times per build, breaking hook state tracking and causing runtime errors or lost state. {v1}
      avoid_mixed_environments: true  # [ERROR] Mixing production and development configuration in your codebase creates serious security vulnerabilities. Debug APIs may expose sensitive data, development endpoints can corrupt production databases, and credentials may leak to unauthorized users. This can result in data breaches, compliance violations, and loss of user trust. Environment-specific configuration must be strictly separated and managed. {v4}
      avoid_mounted_in_setstate: true  # [WARNING] Checking the mounted property inside a setState callback is an anti-pattern. If the widget is not mounted, setState should not be called at all. Placing the check inside the callback can lead to subtle bugs where partial state updates execute before the mounted check runs. {v5}
      avoid_multiple_material_apps: true  # [ERROR] Multiple MaterialApp (or CupertinoApp) widgets in the tree create separate Navigator stacks, separate Theme contexts, and independent Locale/MediaQuery scopes. This breaks navigation (pushNamed cannot reach routes in the other app), causes theme inconsistencies, and doubles memory usage for shared resources. {v2}
      avoid_mutable_field_in_equatable: true  # [ERROR] Equatable classes should only have final fields. Mutable fields break value equality, causing bugs in collections, state management, and UI updates. Changing a field after object creation makes == and hashCode unreliable. {v4}
      avoid_nested_scaffolds: true  # [WARNING] Nested Scaffold widget detected inside another Scaffold. This creates duplicate app bars, floating action buttons, and bottom navigation, leading to broken layout, gesture conflicts, and a confusing user experience. {v5}
      avoid_notification_same_id: true  # [WARNING] Static or hardcoded notification ID causes newer notifications to silently replace older ones using the same identifier. Users will miss important alerts, messages, and time-sensitive updates without any indication that previous notifications were overwritten, leading to lost information and degraded communication reliability. {v3}
      avoid_notification_silent_failure: true  # [WARNING] Notification operation without error handling. Failures will be silent and hard to debug. {v2}
      avoid_null_assertion: true  # [INFO] Null assertion operator (!) throws a runtime exception if the value is null, crashing the app without a meaningful error message. The resulting _CastError provides no context about which value was null or why, making production crashes difficult to diagnose from error reports alone. {v7}
      avoid_openai_key_in_code: true  # [ERROR] Hardcoded OpenAI keys are extractable from binaries, enabling API abuse charged to your account. {v2}
      avoid_optional_field_crash: true  # [ERROR] JSON field accessed with direct bracket notation on a Map that may contain null values. When the API response omits an optional field, this direct access throws a runtime exception that crashes the app. Defensive null-aware access with ?[] prevents unexpected null pointer errors and provides graceful handling of incomplete or malformed JSON responses. {v2}
      avoid_overlapping_animations: true  # [WARNING] Multiple animations targeting the same property (e.g., opacity, position, scale) at the same time cause visual jitter, unpredictable results, and confusing UI behavior. Overlapping animations can make transitions hard to follow and degrade user experience, especially for users with cognitive or visual disabilities. {v4}
      avoid_parameter_mutation: true  # [WARNING] Parameter object is being mutated. This modifies the caller's data. {v2}
      avoid_path_traversal: true  # [WARNING] File paths constructed from user input may allow path traversal attacks (e.g., "../"), enabling access to sensitive files outside the intended directory. This is a critical security risk. {v7}
      avoid_permission_handler_null_safety: true  # [ERROR] Deprecated pre-null-safety permission_handler API detected. The PermissionHandler() constructor and PermissionGroup enum were removed in permission_handler 8.0+. Using these deprecated APIs prevents migration to null-safe versions and causes compile errors when updating the package. The modern API uses Permission.camera.status and Permission.camera.request() instead. {v2}
      avoid_positioned_outside_stack: true  # [ERROR] Positioned widget used outside of a Stack. This causes a ParentData crash at runtime. {v5}
      avoid_print_error: true  # [WARNING] Using print() for error logging in a catch block. In production, print() output is not captured by crash reporting services like Crashlytics or Sentry, making errors invisible to monitoring dashboards. Errors logged only via print() are effectively lost and cannot trigger alerts or be tracked over time. {v2}
      avoid_print_in_release: true  # [ERROR] Using print() in production exposes debug information to end users, can leak sensitive data, and negatively impacts performance. Print statements are not optimized for release builds and may clutter logs, making it harder to diagnose real issues. This can also violate privacy policies and app store guidelines. {v3}
      avoid_provider_value_rebuild: true  # [WARNING] Provider.value with inline object creation recreates the notifier on every widget build. This discards accumulated user data, resets form state, and can trigger infinite rebuild loops that freeze the UI as each rebuild creates a new instance that triggers another rebuild. {v6}
      avoid_purchase_in_sandbox_production: true  # [ERROR] Hardcoded IAP environment URL detected. Sandbox receipts fail in production and vice versa. {v3}
      avoid_recursive_widget_calls: true  # [ERROR] Widget build method creates a new instance of itself, triggering infinite recursion. This crashes the app with a stack overflow as Flutter repeatedly builds the same widget, consuming all available stack frames within milliseconds. {v6}
      avoid_redirect_injection: true  # [WARNING] Redirect URL read from a parameter or user input without domain validation creates an open redirect vulnerability. Attackers craft URLs pointing to your app that redirect victims to phishing sites, credential harvesters, or malware downloads while appearing to originate from your trusted domain. {v5}
      avoid_ref_in_build_body: true  # [ERROR] Using ref.read() in build() does not trigger widget rebuilds when the provider changes, leading to stale UI, missed updates, and confusing bugs. This breaks the reactive model of Riverpod and can cause your app to display outdated information. {v4}
      avoid_ref_in_dispose: true  # [ERROR] Using ref in dispose() is unsafe because the provider may already be destroyed, leading to runtime errors, crashes, or accessing invalid state. This can cause unpredictable bugs and app instability. {v1}
      avoid_ref_inside_state_dispose: true  # [ERROR] Ref may already be disposed when dispose() runs, causing "already disposed" errors or crashes. {v3}
      avoid_ref_read_inside_build: true  # [WARNING] ref.read() called inside build() bypasses Riverpod reactivity. The widget will not rebuild when the provider state changes, resulting in stale data displayed to the user. This creates inconsistent UI state that fails silently and produces hard-to-diagnose rendering errors across dependent widgets. {v2}
      avoid_ref_watch_outside_build: true  # [ERROR] ref.watch() detected outside build() method, breaking the Riverpod widget lifecycle. Subscriptions created outside build() leak memory, produce stale data, and cause missed UI updates that lead to inconsistent state and hard-to-debug rendering errors across dependent widgets. {v3}
      avoid_retaining_disposed_widgets: true  # [ERROR] Non-widget class stores a reference to a Widget or State object. After the widget is disposed, this reference prevents garbage collection and may cause use-after-dispose crashes. Widget and State objects are tied to the Flutter lifecycle and should not be retained outside the widget tree. This creates a strong reference that outlives the widget's lifecycle. {v2}
      avoid_riverpod_state_mutation: true  # [WARNING] Riverpod state mutated directly instead of replaced. Direct mutations don't trigger Notifier rebuilds, causing the UI to display stale, inconsistent data. Listeners and consumers will not receive the updated values. {v2}
      avoid_scroll_listener_in_build: true  # [WARNING] Scroll listener registered in build() accumulates duplicate subscriptions on every widget rebuild. Each rebuild adds another listener that is never removed, causing memory leaks, duplicate callback executions, and progressively degrading scroll performance as listeners compound over the widget lifecycle. {v2}
      avoid_scrollable_in_intrinsic: true  # [ERROR] Scrollable widget inside IntrinsicHeight/IntrinsicWidth causes a geometry loop crash. Scrollables have no natural size. {v2}
      avoid_sensitive_data_in_clipboard: true  # [WARNING] Copying sensitive data such as passwords, tokens, or personal information to the clipboard exposes users to data theft, as other apps or malicious actors can access clipboard contents. This is a significant privacy and security risk, especially on shared or compromised devices. See https://owasp.org/www-project-top-ten/2017/A3_2017-Sensitive_Data_Exposure.html and https://api.flutter.dev/flutter/services/Clipboard-class.html. {v3}
      avoid_sensitive_in_logs: true  # [ERROR] Logging sensitive data (such as passwords, tokens, or personal information) exposes users to credential theft, privacy violations, and compliance failures (e.g., OWASP A09). Attackers or support staff may access logs and extract secrets, leading to data breaches. {v5}
      avoid_setstate_in_build: true  # [ERROR] Calling setState inside the build() method causes the widget to rebuild recursively, leading to stack overflows, app crashes, and unpredictable UI behavior. This makes your app unstable and difficult to debug. Always trigger state changes outside build(). {v6}
      avoid_shared_prefs_in_isolate: true  # [ERROR] SharedPreferences used in isolate context. SharedPreferences does not work in isolates. {v2}
      avoid_shared_prefs_sensitive_data: true  # [ERROR] SharedPreferences stores data as plaintext XML, readable via backup extraction or rooted device. {v3}
      avoid_shrink_wrap_expensive: true  # [WARNING] shrinkWrap: true disables list virtualization, forcing Flutter to measure and lay out every item upfront instead of lazily building only visible items. For lists with many children, this causes slow initial rendering, high memory consumption, and jank during scrolling as the framework processes the entire list eagerly. {v5}
      avoid_spacer_in_wrap: true  # [ERROR] Spacer/Expanded inside Wrap causes a flex paradox crash. Wrap does not support flex-based sizing. {v3}
      avoid_sqflite_type_mismatch: true  # [ERROR] SQLite type may not match Dart type. Booleans are stored as INTEGER (0/1), DateTime as TEXT/INTEGER. {v2}
      avoid_storing_context: true  # [ERROR] Storing a BuildContext in a field or variable for later use is dangerous because the context may become invalid after the widget is disposed or rebuilt. Using a stale context can cause exceptions, show dialogs on the wrong screen, or trigger subtle bugs. This is a common source of crashes and hard-to-diagnose UI issues in Flutter apps. {v4}
      avoid_storing_passwords: true  # [ERROR] SharedPreferences stores passwords in plaintext, readable by anyone with device access or backup. {v4}
      avoid_storing_sensitive_unencrypted: true  # [ERROR] Unencrypted sensitive data exposed via device backup extraction or rooted device access, enabling identity theft. {v4}
      avoid_stream_in_build: true  # [ERROR] Creating a Stream or StreamController inside a widget's build() method causes a new stream instance to be created on every rebuild of that widget. This leads to multiple overlapping subscriptions, memory leaks, and lost or duplicated events, making the widget's state unpredictable and difficult to debug. Always manage streams as persistent fields in the State class, not as local variables in build(). See https://docs.flutter.dev/cookbook/networking/web-sockets#using-streambuilder. {v2}
      avoid_supabase_anon_key_in_code: true  # [ERROR] Hardcoded keys can be extracted from app binary. Attackers gain direct access to your Supabase project. {v2}
      avoid_swallowing_exceptions: true  # [WARNING] Catch block swallows exception without logging, rethrowing, or handling it. Silent failures hide production bugs, break monitoring and alerting systems, and make it impossible to diagnose issues reported by users. Every caught exception must be acknowledged. {v5}
      avoid_table_cell_outside_table: true  # [ERROR] TableCell used outside of a Table widget. This causes a ParentData crash at runtime. {v4}
      avoid_test_sleep: true  # [WARNING] Real sleep() or Future.delayed() call in test code blocks the test runner, slows the suite, and produces flaky failures when system load varies. Tests with real delays waste CI minutes and give inconsistent results across machines. {v5}
      avoid_textfield_in_row: true  # [ERROR] TextField/TextFormField inside a Row without width constraints causes an unbounded width crash. {v3}
      avoid_throw_in_finally: true  # [WARNING] Throw statement inside a finally block silently replaces the original exception. If the try block throws an error, the finally throw overwrites it, making the root cause invisible in crash logs and error handlers. {v5}
      avoid_token_in_url: true  # [ERROR] Tokens in URLs are logged in browser history, server logs, and referrer headers, exposing credentials. {v5}
      avoid_unassigned_stream_subscriptions: true  # [WARNING] Stream subscription created by listen() is not assigned to a variable. Without a reference to the StreamSubscription, you cannot cancel it during dispose, which causes memory leaks, prevents garbage collection, and allows callbacks to fire after the StatefulWidget has been destroyed and unmounted from the widget tree. {v5}
      avoid_unawaited_future: true  # [WARNING] Not awaiting a Future means any errors or exceptions it throws may be silently lost, leading to missed bugs and unreliable app behavior. This is especially risky in production code where error reporting is critical. {v3}
      avoid_unbounded_cache_growth: true  # [WARNING] Map or collection used as a cache has no maximum size constraint. Without eviction logic, the cache grows indefinitely as new entries are added, consuming increasing amounts of device memory. This eventually exhausts available RAM and crashes the app with an out-of-memory error, particularly on mobile devices with limited memory resources. {v4}
      avoid_unbounded_listview_in_column: true  # [ERROR] Scrollable widget inside a Column without Expanded/Flexible causes an unbounded constraints crash. {v3}
      avoid_uncaught_future_errors: true  # [WARNING] Fire-and-forget Future called without error handling. Any exception thrown by this Future is silently lost or crashes the app via the global error handler. Without .catchError(), try-catch, or .ignore(), async failures become invisible in production and extremely difficult to diagnose from crash logs. {v5}
      avoid_unconstrained_dialog_column: true  # [WARNING] Column inside a dialog without mainAxisSize: MainAxisSize.min can overflow and push buttons off-screen. {v2}
      avoid_undisposed_instances: true  # [WARNING] Disposable object (e.g., TextEditingController, AnimationController, StreamController) created but no matching dispose() call found. Undisposed instances retain listeners, streams, and platform resources, causing memory leaks that grow with each widget rebuild or navigation. {v7}
      avoid_unreachable_for_loop: true  # [WARNING] For loop with impossible bounds will never execute, resulting in dead code. This often indicates a logic error, typo, or incorrect increment direction. Unreachable loops can hide bugs, confuse maintainers, and lead to missed updates or calculations. {v3}
      avoid_unsafe_collection_methods: true  # [WARNING] Calling .first, .last, or .single on an empty collection throws a StateError at runtime, crashing the app. This is especially dangerous when the collection comes from an API response, database query, or user input where emptiness cannot be guaranteed at compile time. {v9}
      avoid_unsafe_deserialization: true  # [WARNING] Unvalidated JSON from untrusted sources can crash the app, inject malicious data, or corrupt application state. Attackers can exploit this vulnerability to trigger unexpected behavior, bypass security checks, or cause data loss by crafting malicious payloads. {v4}
      avoid_unsafe_reduce: true  # [WARNING] Calling reduce() on an empty collection throws a StateError at runtime, crashing the app. Unlike fold(), reduce() has no initial value and requires at least one element to operate. {v6}
      avoid_unsafe_where_methods: true  # [WARNING] Calling firstWhere, lastWhere, or singleWhere without an orElse callback throws a StateError when no element matches the predicate. This crashes the app at runtime, especially when filtering data from external sources where matches are not guaranteed. {v4}
      avoid_unverified_native_library: true  # [ERROR] Loading native libraries from dynamic or absolute paths bypasses package verification, allowing attackers to substitute malicious native code. {v3}
      avoid_user_controlled_urls: true  # [WARNING] Allowing user-controlled URLs to be loaded without validation exposes your app to security vulnerabilities such as phishing, open redirects, and malicious content injection. This can compromise user data and app integrity. Always validate and sanitize URLs before loading or displaying them. See https://owasp.org/www-community/attacks/Unvalidated_Redirects_and_Forwards_Cheat_Sheet and https://pub.dev/packages/webview_flutter#security-considerations. {v3}
      avoid_weak_cryptographic_algorithms: true  # [WARNING] Weak or deprecated cryptographic algorithm detected (e.g. MD5, SHA-1). These algorithms have known collision vulnerabilities that allow attackers to forge hashes, compromising data integrity verification, password storage, and digital signature validation. {v5}
      avoid_yield_in_on_event: true  # [ERROR] yield breaks Bloc 8.0+ concurrency and event ordering, causing unpredictable state updates. {v3}
      check_is_not_closed_after_async_gap: true  # [ERROR] Emitting to closed Bloc throws StateError, crashing the app when widget is disposed during async. {v2}
      dispose_class_fields: true  # [WARNING] Class has disposable fields but no dispose/close method. This may cause memory leaks. {v2}
      dispose_provided_instances: true  # [WARNING] Provider creates a disposable instance without a dispose callback, causing memory leaks. Undisposed stream subscriptions and controllers persist after the parent StatefulWidget is removed from the tree, continuing to hold resources and fire callbacks that trigger setState-after-dispose errors. {v4}
      emit_new_bloc_state_instances: true  # [ERROR] Mutating state object breaks equality checks, preventing BlocBuilder from detecting changes. {v2}
      incorrect_firebase_event_name: true  # [ERROR] Invalid event name is silently dropped by Firebase Analytics. Your analytics data will be incomplete. {v2}
      incorrect_firebase_parameter_name: true  # [ERROR] Invalid parameter names are silently dropped by Firebase. Event data will be missing fields. {v2}
      list_all_equatable_fields: true  # [WARNING] Equatable class has fields not included in props. Equality checks fail silently when these fields differ, causing inconsistent behavior in collections, comparisons, and UI updates. Two objects with different field values will be treated as equal, leading to missed rebuilds, incorrect deduplication, and subtle bugs that are extremely hard to trace. {v3}
      missing_test_assertion: true  # [WARNING] Test without assertions always passes, giving false confidence. Bugs slip through undetected. {v5}
      pass_existing_future_to_future_builder: true  # [WARNING] Creating new Future in FutureBuilder restarts the async operation on every widget rebuild. This causes duplicate network calls, database queries, and slow UI with visible loading states. {v8}
      pass_existing_stream_to_stream_builder: true  # [WARNING] New Stream created inline in the StreamBuilder constructor. Every build() call creates a fresh stream, discarding the previous subscription and triggering an infinite rebuild loop as each new stream emits its initial value. {v7}
      prefer_copy_with_for_state: true  # [ERROR] Directly modifying BLoC state breaks immutability, leading to unpredictable UI updates, missed rebuilds, and subtle bugs that surface only in production. The BLoC pattern relies on immutable state transitions to guarantee that every emit triggers a rebuild; mutating fields in place silently bypasses this contract. {v4}
      prefer_correct_package_name: true  # [ERROR] Library directive name does not follow Dart naming conventions. Library and package names must use lowercase_with_underscores format: start with a lowercase letter, use only lowercase letters, digits, and underscores. Names with hyphens, dots, uppercase letters, or starting with digits break pub publish, import resolution, and IDE tooling. {v2}
      prefer_expanded_at_call_site: true  # [ERROR] Expanded/Flexible/Spacer returned from build() forces flex layout on all callers, breaking reuse in non-flex contexts. If this widget is placed inside a Stack, SingleChildScrollView, or any non-flex parent, the Expanded wrapper triggers a runtime ParentDataWidget error and crashes the app. {v4}
      prefer_firebase_remote_config_defaults: true  # [INFO] Missing defaults cause null/zero values when fetch fails, breaking app behavior. {v3}
      prefer_html_escape: true  # [WARNING] Displaying user-generated content in a WebView without proper HTML escaping exposes your app to cross-site scripting (XSS) attacks. Malicious input can execute scripts, steal user data, or compromise device security. Always sanitize and escape user content before rendering it in a web context. {v4}
      prefer_image_picker_max_dimensions: true  # [WARNING] pickImage() called without maxWidth or maxHeight parameters loads full-resolution camera images (12+ megapixels on modern devices). Decoding these large images into memory causes OutOfMemoryError crashes on lower-end devices, excessive memory consumption that triggers OS app kills, and slow image processing. {v2}
      prefer_lazy_loading_images: true  # [WARNING] Image.network in ListView builder without lazy loading. All images load immediately, wasting bandwidth and memory. {v2}
      prefer_pending_intent_flags: true  # [ERROR] PendingIntent without FLAG_IMMUTABLE or FLAG_MUTABLE crashes on Android 12+. {v2}
      prefer_platform_io_conditional: true  # [ERROR] Platform class from dart:io throws UnsupportedError at runtime on Flutter web because dart:io is unavailable in browser environments. Accessing Platform.isAndroid, Platform.isIOS, or any Platform property without first checking kIsWeb crashes the web app immediately with an unrecoverable runtime error. {v2}
      prefer_secure_random_for_crypto: true  # [WARNING] The default Random() constructor in Dart uses a seed based on the current system time, making its output predictable and vulnerable to attack. If Random() is used to generate cryptographic keys, initialization vectors (IVs), nonces, tokens, or any value intended to protect sensitive data, attackers can reproduce the same sequence of random numbers and break your security. This flaw has led to real-world breaches where cryptographic protections were bypassed due to weak randomness. Only a cryptographically secure random number generator (CSPRNG) can provide the unpredictability required for security-critical operations. {v3}
      prefer_timeout_on_requests: true  # [WARNING] Making HTTP requests without a timeout can cause your app to hang indefinitely if the server is slow or unresponsive. This degrades user experience and can block critical app flows. {v3}
      prefer_try_parse_for_dynamic_data: true  # [ERROR] Using parse methods (like int.parse or double.parse) on dynamic or user-provided data can throw exceptions and crash your app if the input is invalid. This exposes your app to stability and security risks, especially when handling external or untrusted data. Always use tryParse to safely handle potentially malformed input and provide a fallback for invalid values. See https://dart.dev/guides/libraries/library-tour#numbers. {v4}
      prefer_utc_for_storage: true  # [WARNING] Local time stored without UTC conversion causes incorrect values when restored in different timezones. {v4}
      provide_correct_intl_args: true  # [ERROR] Intl.message args count does not match placeholders. This will cause runtime errors. {v2}
      require_android_permission_request: true  # [ERROR] Permission-gated API called without runtime permission request. Android 6+ will deny access or crash. {v2}
      require_animation_controller_dispose: true  # [ERROR] Neglecting to dispose of an AnimationController when a widget is removed from the tree causes memory leaks and can lead to performance degradation, as the controller continues to consume resources and tick animations in the background. This can eventually crash your app or cause unexpected behavior. Always dispose of AnimationControllers to maintain optimal app performance. See https://api.flutter.dev/flutter/animation/AnimationController/dispose.html. {v2}
      require_animation_disposal: true  # [WARNING] Failing to dispose an AnimationController in the dispose() method causes it to retain resources, listeners, and animation frames, resulting in memory leaks and degraded performance. This is especially problematic in widgets that are frequently created and destroyed, such as in lists or navigation stacks, and can lead to app instability or crashes. Dispose every AnimationController to keep Flutter apps robust. {v7}
      require_animation_ticker_disposal: true  # [ERROR] If you do not stop your Ticker in dispose(), it will continue running and leak memory, causing slowdowns and error messages. This can degrade performance and lead to crashes in long-running apps. {v2}
      require_app_startup_error_handling: true  # [WARNING] runApp() is called without runZonedGuarded or FlutterError.onError. Uncaught errors during app startup will crash the application silently with no crash report sent to monitoring services. Users see a blank or frozen screen with no diagnostic information available to the development team. {v2}
      require_apple_sign_in: true  # [ERROR] Third-party login detected without Sign in with Apple. iOS apps with social login must offer Sign in with Apple. {v2}
      require_apple_signin_nonce: true  # [ERROR] Omitting a cryptographic nonce when using Apple Sign-In exposes your app to replay attacks. Attackers can intercept a valid authorization token and reuse it to impersonate the user, gaining unauthorized access to their account and sensitive data. Apple’s security documentation strongly recommends using a unique, random nonce for every authentication request to prevent these attacks and ensure user safety. {v4}
      require_auto_route_full_hierarchy: true  # [WARNING] Using push() in auto_route bypasses the route hierarchy, so parent shell routes and wrapper widgets are not applied. This causes missing bottom navigation bars, app bars, or layout scaffolds. Use navigate() instead to push the full route tree including all parent wrappers. {v1}
      require_auto_route_guard_resume: true  # [WARNING] An AutoRouteGuard.onNavigation override must call resolver.next() on every code path. Missing calls cause navigation to hang silently with no error message, leaving the user stuck on the current page. Ensure both the if and else branches call resolver.next(true) or resolver.next(false) respectively. {v1}
      require_background_message_handler: true  # [WARNING] Push notifications received when app is terminated are silently dropped without handler. {v3}
      require_baseline_text_baseline: true  # [ERROR] CrossAxisAlignment.baseline requires a textBaseline property. Omitting it causes an assertion failure at runtime. {v2}
      require_bloc_close: true  # [ERROR] If you do not close your Bloc or Cubit in the StatefulWidget dispose() method, it will leak memory, keep stream subscriptions active, and cause app slowdowns or crashes. Always close Blocs and Cubits to prevent leaks and unexpected behavior after the widget tree is rebuilt. {v3}
      require_bloc_manual_dispose: true  # [WARNING] Bloc or Cubit holds StreamController or Timer fields but does not override close() to dispose them. Undisposed resources cause memory leaks that accumulate across navigation, eventually increasing memory pressure until the operating system kills the app. {v5}
      require_cache_key_determinism: true  # [ERROR] Cache key uses non-deterministic values (e.g., DateTime.now, Random, hashCode, or UUID). This causes cache misses, duplicated resources, and unpredictable behavior. Cache keys must uniquely and consistently identify the same resource for the same input. Using unstable values breaks cache integrity and wastes memory. {v4}
      require_cached_image_dimensions: true  # [WARNING] CachedNetworkImage without cache dimensions loads full-resolution images into memory, causing out-of-memory errors and app crashes on devices with limited RAM. Large images (such as high-resolution photos from modern cameras) can consume hundreds of megabytes when decoded, quickly exhausting available memory. {v4}
      require_camera_dispose: true  # [ERROR] Undisposed camera holds hardware exclusively, blocking other apps from accessing camera until app restart. {v2}
      require_camera_permission_check: true  # [ERROR] Camera initialized without permission check. This crashes on iOS and throws on Android. {v3}
      require_catch_logging: true  # [WARNING] Catch block swallows the exception without logging or rethrowing. Security-relevant errors such as authentication failures, permission violations, and data corruption go completely undetected. This prevents incident response, makes debugging production issues impossible, and hides potential attack activity. {v2}
      require_data_encryption: true  # [WARNING] Unencrypted sensitive data exposes credentials to attackers via device access or backup extraction. {v5}
      require_database_migration: true  # [WARNING] Database models must support versioned migrations to handle schema changes safely. Without migration logic, updates to the schema can break data, cause runtime errors, and result in data loss or corruption. This is a critical reliability and maintainability issue for any persistent storage solution. {v4}
      require_date_format_specification: true  # [WARNING] DateTime.parse may fail on server dates due to format mismatches. Unspecified formats can cause runtime errors, silent failures, and broken data flows. This is a common source of bugs in internationalized and backend-driven apps. {v2}
      require_deep_link_fallback: true  # [ERROR] Deep link handler navigates to content without verifying the target exists or is accessible. When a deep link references deleted, restricted, or invalid content, the app either crashes with a null reference error or displays a blank screen. Users tapping expired links in emails, notifications, or shared messages encounter a broken experience. {v9}
      require_deep_link_validation: true  # [WARNING] Deep link parameter used without validation allows attackers to craft malicious URLs that inject arbitrary data into your app. Unvalidated deep link parameters can cause crashes from unexpected types, unauthorized access to restricted screens, or execution of unintended actions on behalf of the user. {v3}
      require_default_config: true  # [WARNING] Accessing configuration values without providing a default or fallback can cause runtime crashes if the value is missing or misconfigured. This leads to unpredictable app behavior, poor user experience, and failed startup. It may also result in app store rejection for reliability issues. {v4}
      require_dio_error_handling: true  # [WARNING] Making a Dio HTTP request without wrapping it in a try-catch block means any DioException (such as network errors, timeouts, or invalid responses) will crash the app and leave users with no feedback. This is especially problematic in production apps where network conditions are unpredictable. Always handle Dio errors to provide a robust and user-friendly experience. See https://pub.dev/packages/dio#handling-errors. {v3}
      require_dio_timeout: true  # [WARNING] Creating a Dio instance for network requests without setting connectTimeout and receiveTimeout in BaseOptions can cause HTTP requests to hang indefinitely if the server is slow or unresponsive. This can freeze the UI, degrade user experience, and make error recovery impossible, especially in mobile apps with unreliable networks. Always configure timeouts to ensure your app remains responsive and can handle network failures gracefully. See https://pub.dev/packages/dio#timeouts. {v3}
      require_error_handling_in_async: true  # [WARNING] Async provider without error handling allows exceptions to propagate unhandled. Unhandled errors in FutureProvider or StreamProvider surface as uncaught exceptions that crash the app or leave the UI in a permanent loading state with no recovery path. {v3}
      require_error_identification: true  # [WARNING] Error or alert states are indicated only by color, which is not accessible to users with color vision deficiencies. All error indicators must include a non-color cue such as an icon, text label, or pattern for full accessibility. {v2}
      require_error_state: true  # [ERROR] If your BLoC state hierarchy does not include an error state, failures will be unhandled, leading to crashes or missing error UI. This makes your app less robust and harder to debug. {v3}
      require_error_widget: true  # [WARNING] FutureBuilder/StreamBuilder must handle error state to prevent silent failures, blank screens, or cryptic UI. Without error handling, users may see no feedback when data fails to load, leading to confusion, poor UX, and support burden. This can also mask backend or network issues during development. {v4}
      require_fcm_token_refresh_handler: true  # [WARNING] FCM tokens expire periodically. Without onTokenRefresh handling, push notifications will stop working. {v3}
      require_feature_flag_default: true  # [WARNING] Missing default causes null/zero when remote config fetch fails, breaking expected feature behavior. {v3}
      require_field_dispose: true  # [WARNING] Disposable field may not be properly disposed. {v3}
      require_file_close_in_finally: true  # [WARNING] Unclosed file handle on exception leaks file descriptor, exhausting system limits. {v4}
      require_file_path_sanitization: true  # [WARNING] File path constructed from parameter without sanitization. Path traversal attack possible with ../ {v2}
      require_firebase_composite_index: true  # [ERROR] Firebase Realtime Database query uses orderByChild with filtering but may lack a .indexOn rule. Without the index the SDK downloads all data and sorts client-side, causing severe performance degradation on large datasets. {v1}
      require_firebase_init_before_use: true  # [WARNING] Firebase services crash if accessed before initializeApp() completes. App fails on startup. {v2}
      require_firestore_index: true  # [ERROR] Compound Firestore query may need a composite index. Query will fail at runtime without the required index. {v2}
      require_focus_node_dispose: true  # [WARNING] FocusNode created but not disposed. Undisposed focus nodes retain listeners and focus tree references, causing memory leaks and stale focus behavior that accumulates as users navigate between screens with form inputs. {v6}
      require_form_key: true  # [WARNING] Without GlobalKey, validate() and save() calls fail because FormState cannot be accessed. {v2}
      require_form_key_in_stateful_widget: true  # [WARNING] GlobalKey<FormState> created inside the build() method is re-instantiated on every widget rebuild triggered by setState(). Each rebuild creates a new key, causing the form to lose all current field values, validation state, and user input. This produces a frustrating user experience where typed data disappears unpredictably during state changes. {v2}
      require_geolocator_permission_check: true  # [ERROR] Accessing location without checking permission will crash your app on iOS and may cause unpredictable behavior on Android. This can result in app store rejection and poor user experience. {v2}
      require_getit_registration_order: true  # [WARNING] GetIt registration uses a dependency not yet registered at this point in the setup sequence. This causes runtime errors when the service locator attempts to resolve an unregistered type, resulting in app crashes during startup or lazy initialization that are difficult to reproduce and debug in production. {v3}
      require_getx_permanent_cleanup: true  # [WARNING] Get.put(permanent: true) registers a navigation, scroll, or animation controller that will never be automatically deleted. This can cause memory leaks if you do not manually clean up. Instances registered as permanent remain in memory for the lifetime of the app unless explicitly deleted. This is especially risky for feature-specific or page-scoped controllers that may not always be needed. {v3}
      require_getx_worker_dispose: true  # [WARNING] GetX Worker field has no corresponding dispose() call in onClose(). Without disposing the Worker, its internal stream subscription remains active after the GetxController is destroyed. This creates a memory leak where the Worker, its closure, and all captured references are retained in memory indefinitely. {v2}
      require_hive_adapter_registration_order: true  # [ERROR] Opening box before registering adapters throws HiveError. Adapters must be registered first. {v2}
      require_hive_encryption_key_secure: true  # [ERROR] Hardcoded key defeats encryption. Anyone decompiling the app can decrypt all stored user data. {v2}
      require_hive_initialization: true  # [INFO] Hive.openBox() or Hive.openLazyBox() was called without a visible Hive.init() or Hive.initFlutter() call in this file. Opening a box before initialization throws a HiveError at runtime. {v4}
      require_hive_nested_object_adapter: true  # [ERROR] Nested custom type without adapter causes runtime crash when Hive tries to serialize the object. {v2}
      require_hive_type_adapter: true  # [WARNING] Hive cannot serialize this object without @HiveType annotation. Storing will throw a HiveError at runtime. {v2}
      require_hive_web_subdirectory: true  # [ERROR] Hive.initFlutter() called without a subDir parameter. On web platforms, Hive stores data in IndexedDB. Without an explicit subdirectory, multiple apps deployed on the same domain will share the same Hive storage namespace, causing data conflicts, overwrites, and potential data loss between applications. This is especially critical for web deployments. {v2}
      require_https_for_ios: true  # [WARNING] HTTP URL will be blocked by iOS App Transport Security unless exception is configured. {v2}
      require_https_only: true  # [WARNING] HTTP URL detected in network request. Unencrypted HTTP traffic is vulnerable to interception, modification, and eavesdropping by any attacker on the network path. Sensitive data including credentials, personal information, and session tokens transmitted over HTTP can be silently captured in plaintext. {v4}
      require_image_cache_dimensions: true  # [WARNING] Image.network loads the full-resolution image into memory without cacheWidth or cacheHeight constraints. A 4000x3000 pixel photo decodes to approximately 48MB of uncompressed bitmap data in memory. Without cache dimensions, displaying multiple images causes excessive memory consumption that leads to out-of-memory crashes on lower-end devices. {v6}
      require_image_stream_dispose: true  # [WARNING] ImageStream listener is not removed in the dispose() method. The listener retains a reference to the widget State object, preventing garbage collection after the widget is removed from the tree. This creates a memory leak where decoded image data and the entire widget state remain allocated indefinitely, consuming device memory. {v2}
      require_immutable_bloc_state: true  # [ERROR] If your BLoC state is mutable, it causes unpredictable UI updates, breaks state comparison, and leads to missed widget rebuilds. This results in subtle bugs, inconsistent UI, and hard-to-maintain code. {v5}
      require_initial_state: true  # [ERROR] If a BLoC or Cubit does not provide an initial state, it will throw a LateInitializationError at runtime when BlocBuilder or BlocConsumer tries to read the state. This causes your app to crash and makes debugging difficult. {v3}
      require_intl_args_match: true  # [ERROR] The placeholders in your Intl.message string do not match the arguments provided in the args list. Missing, extra, or mismatched arguments cause runtime errors, broken translations, and can result in untranslated or incorrect messages in production. This is a common source of i18n bugs and can break localization workflows. {v3}
      require_intl_plural_rules: true  # [WARNING] Manual pluralization logic using if/else or ternary expressions on count values. Languages such as Russian, Arabic, and Polish have complex plural categories (zero, one, two, few, many, other) that simple singular/plural branching cannot handle. This produces grammatically incorrect text for international users and breaks translation workflows. {v2}
      require_ios_app_tracking_transparency: true  # [ERROR] Advertising/tracking SDK detected. iOS 14.5+ requires App Tracking Transparency permission before tracking users. {v2}
      require_ios_data_protection: true  # [WARNING] Sensitive file storage detected. Consider using iOS Data Protection to encrypt files when device is locked. {v2}
      require_ios_face_id_usage_description: true  # [WARNING] Biometric authentication detected. iOS requires NSFaceIDUsageDescription in Info.plist for Face ID. {v4}
      require_ios_healthkit_authorization: true  # [WARNING] HealthKit data access detected. Ensure authorization is requested before reading or writing health data. {v2}
      require_ios_keychain_for_credentials: true  # [ERROR] Credential storage in SharedPreferences detected. Use iOS Keychain (flutter_secure_storage) for sensitive data. {v2}
      require_ios_local_notification_permission: true  # [WARNING] Local notification scheduling detected. Ensure iOS notification permission is requested before scheduling. {v2}
      require_ios_permission_description: true  # [WARNING] Missing Info.plist keys cause App Store rejection or instant crash when the permission is requested. {v5}
      require_ios_privacy_manifest: true  # [WARNING] API requires an iOS Privacy Manifest entry (iOS 17+). Missing PrivacyInfo.xcprivacy declarations for required-reason APIs will cause automatic App Store rejection during review, block new releases, and may trigger runtime permission failures on user devices. {v3}
      require_ios_receipt_validation: true  # [WARNING] In-app purchase detected. Ensure receipt is validated with server, not just locally. {v2}
      require_key_for_reorderable: true  # [ERROR] Without unique keys, reordering fails silently or shows wrong items after drag-and-drop. {v3}
      require_late_initialization_in_init_state: true  # [WARNING] Late field initialized in build() recreates the object on every setState() call. AnimationController instances restart their animations, StreamSubscription objects create duplicate listeners, and the resulting UI jank becomes visible to users. This also wastes memory by allocating new objects on every widget rebuild cycle. {v3}
      require_lifecycle_observer: true  # [WARNING] Long-running timers, streams, or background tasks must be paused or stopped when the app is backgrounded. Failing to observe lifecycle changes can drain battery, consume CPU, and cause stale callbacks to execute when the app resumes, leading to inconsistent state and poor user experience. This may also violate platform guidelines and result in app store rejection. {v4}
      require_location_permission_rationale: true  # [WARNING] Location permission requested without showing rationale. Users may deny without understanding why. {v2}
      require_map_idle_callback: true  # [INFO] Data fetching triggered in onCameraMove fires on every frame during map pan and zoom gestures. This spams backend APIs with hundreds of redundant requests per second, causes severe performance degradation with UI jank, and wastes user bandwidth and battery on mobile devices. {v3}
      require_media_loading_state: true  # [INFO] VideoPlayer displayed without checking isInitialized shows a black rectangle or crashes. The video player needs time to load the video before the build method can render it properly. {v4}
      require_media_player_dispose: true  # [ERROR] Undisposed media controller holds audio/video hardware, blocking other apps and draining battery. This can cause resource leaks, app crashes, and poor user experience. {v2}
      require_minimum_contrast: true  # [WARNING] Low contrast text excludes users with low vision, cataracts, or age-related sight loss who cannot distinguish foreground from background. WCAG 2.1 Success Criterion 1.4.3 requires a minimum contrast ratio of 4.5:1 for normal text and 3:1 for large text. Failing to meet this threshold makes content unreadable for millions of users worldwide. {v1}
      require_mock_http_client: true  # [WARNING] Real HTTP calls in tests create network dependencies that cause flaky failures and slow CI pipelines. Tests become non-deterministic, failing on timeout when servers are slow and passing inconsistently across environments, making test results unreliable for merge decisions. {v5}
      require_mounted_check: true  # [ERROR] Calling setState after an await without checking if the StatefulWidget is still mounted in the widget tree throws a "setState called after dispose" error. This leads to runtime exceptions, app instability, and hard-to-debug crashes, especially in async code. {v4}
      require_mounted_check_after_await: true  # [ERROR] setState called after await without mounted check. Widget may have been disposed. {v2}
      require_native_resource_cleanup: true  # [WARNING] Unfreed native memory leaks outside Dart GC, causing permanent memory loss until app restart. {v3}
      require_notification_channel_android: true  # [WARNING] Android notification is missing a channel ID or description. Without these, notifications may not appear or may be grouped incorrectly on Android 8.0+ devices, reducing reliability and user engagement. {v2}
      require_page_controller_dispose: true  # [ERROR] Not disposing of a PageController when a widget is removed from the tree can cause memory leaks and continued resource usage, leading to performance issues and potential crashes in long-running apps. This is especially critical in apps with dynamic navigation or frequent page changes. See https://api.flutter.dev/flutter/widgets/PageController/dispose.html. {v4}
      require_provider_dispose: true  # [WARNING] Provider creating a ChangeNotifier without a dispose callback leaks listener registrations and memory. Over time, leaked notifiers accumulate stale listeners that fire after the StatefulWidget is removed from the tree, causing setState-after-dispose errors and increasing memory pressure. {v4}
      require_provider_scope: true  # [ERROR] If your Riverpod app is missing ProviderScope at the root, any attempt to access providers will throw runtime exceptions and crash the app. This makes the app unusable and breaks all provider-based state management. {v2}
      require_purchase_restoration: true  # [ERROR] In-app purchase detected without restore functionality. App Store requires "Restore Purchases" for non-consumables. {v2}
      require_purchase_verification: true  # [ERROR] In-app purchase without server verification detected. Client-side verification can be bypassed by attackers. {v2}
      require_qr_content_validation: true  # [WARNING] QR code scan result used without validation creates a security vulnerability. Malicious QR codes can contain crafted URLs that redirect to phishing sites, inject JavaScript through URL schemes, trigger automatic downloads, or launch unintended app actions. Users scanning codes in public places are especially vulnerable. {v2}
      require_qr_permission_check: true  # [ERROR] If you open the QR scanner without first requesting camera permission, your app will show a black screen on iOS or crash on some Android devices. This breaks user experience and can cause app store rejection. {v2}
      require_riverpod_error_handling: true  # [WARNING] AsyncValue from a Riverpod provider accessed without handling error and loading states. When the async operation fails or is in progress, accessing .value directly throws a StateError or returns null, causing crashes or displaying stale data. Users see broken UI instead of loading indicators or error messages. {v2}
      require_route_guards: true  # [WARNING] Protected route path (profile, settings, admin, payment) lacks an authentication guard. Without a redirect callback, unauthorized users can access sensitive pages directly via deep link or URL manipulation, exposing personal data, financial information, or admin controls to unauthenticated sessions. {v3}
      require_scroll_controller_dispose: true  # [ERROR] ScrollController created but not disposed. Undisposed scroll controllers retain listeners and scroll position state, causing memory leaks that accumulate as users navigate between screens with scrollable content. {v5}
      require_search_debounce: true  # [WARNING] Calling a search API on every keystroke can overwhelm your backend, degrade performance, and create a poor user experience. Without debouncing, users may see lag, rate limits, or unnecessary network traffic. {v2}
      require_secure_key_generation: true  # [ERROR] Encryption key generated from a predictable source. Key.fromLength() uses dart:math Random which is NOT cryptographically secure. Hardcoded byte arrays in Uint8List.fromList() or List.filled() produce deterministic keys extractable from compiled binaries. Predictable keys compromise all data encrypted with them and cannot be rotated without data loss. {v2}
      require_secure_password_field: true  # [WARNING] Password input fields must disable suggestions and autocorrect to prevent sensitive information from being stored in device dictionaries or suggested to users inappropriately. Failing to do so increases the risk of password leakage, accidental exposure, and poor user privacy. Always configure password fields to maximize security and user trust. {v5}
      require_secure_storage_for_auth: true  # [ERROR] Auth tokens in SharedPreferences leak via backup extraction, enabling account takeover. {v4}
      require_speech_stop_on_dispose: true  # [ERROR] Unreleased SpeechToText keeps microphone active, draining battery and blocking other apps. {v2}
      require_sqflite_whereargs: true  # [ERROR] SQL queries using string interpolation are vulnerable to SQL injection attacks. User input can modify queries, leak data, or corrupt the database. Always use whereArgs to safely pass parameters. {v3}
      require_sse_subscription_cancel: true  # [ERROR] If a widget (such as a StatefulWidget managing a live data feed) opens a Server-Sent Events (SSE) or EventSource connection but does not cancel it in dispose(), the connection remains open after the widget is removed. This can result in orphaned network connections, wasted bandwidth, and memory leaks, especially in dashboards, chat clients, or any UI with dynamic SSE usage. Always close SSE/EventSource connections in the correct State object’s dispose() method. See https://docs.flutter.dev/perf/memory#dispose-resources. {v4}
      require_stepper_validation: true  # [INFO] Stepper onStepContinue callback does not validate form input before proceeding. This can allow users to advance with incomplete or invalid data, leading to errors or inconsistent state. {v3}
      require_stream_controller_close: true  # [ERROR] Failing to close a StreamController in the dispose() method leaves the stream open, causing memory leaks, connection handle exhaustion, and potential app crashes. Unclosed streams can accumulate events and listeners, degrading performance and making debugging difficult. Proper closure is essential for robust, production-quality Flutter apps. {v5}
      require_stream_controller_dispose: true  # [ERROR] Not closing a StreamController in the StatefulWidget dispose method will leak memory, keep listeners active, and cause app slowdowns, crashes, or persistent background activity. This leads to stream subscription exhaustion, battery drain, and unpredictable bugs, especially in production apps with frequent widget tree rebuilds. Unclosed controllers also prevent garbage collection and block app updates. {v7}
      require_stream_subscription_cancel: true  # [WARNING] StreamSubscription field without cancel(). Callbacks fire after State disposal, causing setState errors and memory leaks. {v4}
      require_subscription_status_check: true  # [WARNING] Premium/subscription content displayed without status verification. Subscription may have expired. {v3}
      require_supabase_realtime_unsubscribe: true  # [ERROR] Unsubscribed channel keeps WebSocket open, leaking connections and receiving stale updates. {v2}
      require_tab_controller_dispose: true  # [ERROR] Undisposed TabController leaks AnimationController, causing memory exhaustion over time. {v2}
      require_tab_controller_length_sync: true  # [ERROR] TabController length mismatch throws RangeError when switching tabs, crashing the app. {v2}
      require_test_widget_pump: true  # [ERROR] After simulating a tap or text entry in a widget test, failing to call pump() or pumpAndSettle() means the widget tree will not rebuild, so your test may pass even though the user experience is broken. Always process pending build events to ensure your test reflects real app behavior. {v3}
      require_text_editing_controller_dispose: true  # [ERROR] When a State class creates a TextEditingController for a TextField, search bar, or form input and fails to dispose of it in dispose(), the controller’s internal listeners and text buffers will persist after the widget is removed from the widget tree. This can cause memory leaks, input lag, and unpredictable or stale text field behavior, especially in dynamic forms, chat UIs, or lists where widgets are frequently created and destroyed. In severe cases, this can lead to app slowdowns or crashes due to uncollected resources. Always dispose of every TextEditingController in the owning State object’s dispose() method to ensure proper cleanup. See: https://api.flutter.dev/flutter/widgets/TextEditingController-class.html {v3}
      require_type_adapter_registration: true  # [WARNING] Hive box opened with a custom type but the corresponding TypeAdapter may not be registered. Unregistered adapters cause HiveError at runtime, preventing all read and write operations on the box, which leads to app crashes and complete data inaccessibility. {v3}
      require_unique_iv_per_encryption: true  # [ERROR] Using a static or reused initialization vector (IV) with the same encryption key enables attackers to detect patterns in your encrypted data. When the same IV is used for multiple encryption operations, identical plaintexts will always produce identical ciphertexts, making it possible for adversaries to infer relationships between messages, perform replay attacks, or even recover plaintexts in some encryption modes. This breaks the fundamental guarantee of confidentiality provided by encryption and has led to serious vulnerabilities in real-world systems. {v4}
      require_unknown_route_handler: true  # [WARNING] MaterialApp or CupertinoApp defines routes or onGenerateRoute but does not provide an onUnknownRoute handler. When a user navigates to an undefined route path via deep link, push notification, or programmatic navigation, the app throws an unhandled exception and crashes instead of showing a helpful error screen. {v4}
      require_url_validation: true  # [WARNING] Uri.parse on user input without scheme validation enables SSRF attacks. Attackers can make your app connect to internal servers, databases, or use malicious protocols to exfiltrate data. {v5}
      require_value_notifier_dispose: true  # [ERROR] If you do not dispose a ValueNotifier, it will leak memory, keep listeners attached, and trigger updates on a StatefulWidget that has already been removed from the widget tree. This causes memory leaks, unexpected UI updates, and hard-to-find bugs in your app. {v6}
      require_vsync_mixin: true  # [ERROR] AnimationController is missing a vsync parameter. Without vsync, animations run without frame synchronization, causing visual tearing, wasted CPU cycles, and degraded user experience. This can lead to janky motion and battery drain, especially on mobile devices. Always provide vsync: this and mix in SingleTickerProviderStateMixin to ensure smooth, efficient animations and proper animation controller lifecycle management. {v4}
      require_websocket_close: true  # [WARNING] Unclosed WebSocket leaks connections and continues receiving data after widget disposal, causing errors. {v5}
      require_websocket_message_validation: true  # [WARNING] Unvalidated WebSocket messages crash when malformed data arrives, or enable injection attacks. {v3}
      require_websocket_reconnection: true  # [WARNING] WebSocket connection without reconnection logic will stay permanently disconnected after network interruptions, server restarts, or mobile network handoffs. Users will see stale data, miss real-time updates, and have no indication that the live connection has dropped until they manually refresh or restart the app. {v4}
      require_webview_ssl_error_handling: true  # [ERROR] If your WebView does not handle SSL certificate errors, it may silently accept invalid or malicious certificates, exposing users to man-in-the-middle attacks. Users may unknowingly submit sensitive information (such as passwords or payment details) to attackers, resulting in account compromise, data theft, or financial loss. Proper SSL error handling is essential for secure in-app browsing. {v4}
      require_workmanager_result_return: true  # [ERROR] Missing return value makes WorkManager assume failure, triggering unnecessary retries. {v2}
      use_ref_and_state_synchronously: true  # [WARNING] Using ref after await risks accessing disposed provider, causing runtime errors or stale data. {v2}
      use_ref_read_synchronously: true  # [WARNING] ref.read() after await may access a disposed or invalidated provider, causing crashes or stale reads. {v2}

      #
      # --- TIER 2: RECOMMENDED (525 rules) ---
      #
      avoid_accessing_collections_by_constant_index: true  # [WARNING] Collection accessed by a constant index inside a loop body. This retrieves the same element on every iteration, which is wasteful and usually indicates a logic error where the loop variable was intended as the index instead. {v5}
      avoid_analytics_in_build: true  # [WARNING] Analytics calls inside build() are triggered on every rebuild, resulting in duplicate events, inaccurate tracking data, degraded app performance, and inflated backend costs. This can skew business metrics, produce misleading dashboards, and make it difficult to analyze real user behavior. Persistent analytics spam may also trigger rate limits or data quality flags in analytics platforms. {v3}
      avoid_animation_in_build: true  # [ERROR] Creating an AnimationController inside the build() method causes a new controller to be instantiated on every widget rebuild, leading to severe memory leaks, degraded animation performance, and unpredictable UI behavior. Previous controllers are never disposed, which can crash your app or exhaust system resources. AnimationControllers must be long-lived and managed at the widget level, not recreated per frame. This is a critical resource management issue in Flutter. {v2}
      avoid_assigning_to_static_field: true  # [WARNING] Instance method modifies a static field, coupling instance behavior to global state. This makes the class unpredictable because any instance can silently alter shared state, causing race conditions in concurrent code and making tests unreliable. {v3}
      avoid_assignments_as_conditions: true  # [WARNING] Assignment operator (=) used inside a condition expression where a comparison (==) was likely intended. The condition silently assigns a new value instead of testing the current one, causing incorrect control flow and overwriting the original variable value. {v4}
      avoid_async_call_in_sync_function: true  # [WARNING] Async function called inside a synchronous function without handling the returned Future. The Future is silently discarded, so any errors thrown by the async operation are swallowed and any result is lost, making failures invisible. {v5}
      avoid_auth_state_in_prefs: true  # [WARNING] SharedPreferences stores tokens in plaintext, exposing credentials on rooted devices or backups. {v2}
      avoid_auto_play_media: true  # [WARNING] Media that auto-plays on page load can be disorienting, distracting, or even harmful for users with disabilities, especially those with cognitive or sensory sensitivities. Auto-play also reduces user control and can violate accessibility guidelines. {v2}
      avoid_bitwise_operators_with_booleans: true  # [WARNING] Bitwise operator on boolean. Unlike &&/||, this does not short-circuit. Using & or | with booleans is likely a mistake; use && or || instead. Bitwise operators don't short-circuit and can cause unexpected behavior. {v4}
      avoid_bloc_business_logic_in_ui: true  # [WARNING] UI code such as showDialog or Navigator calls inside a Bloc breaks separation of concerns and makes the Bloc untestable without a widget tree. Business logic becomes coupled to the UI framework, preventing reuse across platforms and complicating unit testing. {v5}
      avoid_bloc_emit_after_close: true  # [ERROR] Calling emit() after an await may throw an exception if the Bloc has been closed, leading to runtime errors and unpredictable state changes. This can cause crashes or silent failures, especially in asynchronous event handlers. Always check that the Bloc is still open before emitting new states after an await. {v1}
      avoid_bloc_event_in_constructor: true  # [ERROR] Adding a BLoC event in the constructor runs it before listeners are attached, causing missed state updates and unpredictable app behavior. This can result in lost events, bugs that are hard to trace, and inconsistent UI state. {v5}
      avoid_bloc_in_bloc: true  # [WARNING] BLoC directly calling another BLoC creates tight coupling between state managers. This makes unit testing difficult, risks circular dependencies, and breaks the unidirectional data flow pattern that BLoC relies on for predictable state management. {v5}
      avoid_bloc_listen_in_build: true  # [ERROR] Using BlocProvider.of in build() with listen:true causes the widget to rebuild on every state change, leading to performance issues and unpredictable UI updates. This can make your app less efficient and harder to maintain. {v3}
      avoid_bloc_public_fields: true  # [WARNING] Public field in Bloc. Keep internals private. Bloc internals must be private. Public fields expose implementation details and allow external modification of state. {v2}
      avoid_bloc_state_mutation: true  # [ERROR] Direct mutation bypasses equality checks, preventing UI rebuild and causing stale data display. {v1}
      avoid_blocking_database_ui: true  # [WARNING] Database operation executed in build() or on the main UI thread blocks rendering until the query completes. Users experience frozen screens, unresponsive touch input, and dropped animation frames. Long-running queries can trigger ANR dialogs on Android and watchdog termination on iOS. {v2}
      avoid_bluetooth_scan_without_timeout: true  # [WARNING] Infinite Bluetooth scan drains battery and may run until app termination. {v3}
      avoid_bottom_type_in_patterns: true  # [WARNING] Pattern uses a bottom type (void, Never, or Null) that either never matches any value or only matches null. This creates dead code in switch cases and if-case expressions, silently hiding logic errors. {v4}
      avoid_bottom_type_in_records: true  # [WARNING] Record field uses void/Never/Null which cannot hold useful values. Record fields with bottom types are usually mistakes. Record contains bottom types (void, Never, Null). {v4}
      avoid_build_context_in_providers: true  # [WARNING] Storing BuildContext in providers can cause memory leaks. BuildContext is stored in providers or state managers. This violates the widget lifecycle, risking setState-after-dispose errors or silent state corruption. {v6}
      avoid_cached_image_web: true  # [WARNING] CachedNetworkImage is used inside a kIsWeb branch where it provides no caching benefit. On the web platform there is no local file system, so cached_network_image falls back to a plain Image.network internally. You carry an extra dependency and misleading widget name for zero benefit. The browser's native HTTP cache (Cache-Control, ETag) already handles image caching on web. {v1}
      avoid_canvas_operations_in_build: true  # [WARNING] Canvas operations (drawing, painting) should only be performed in CustomPainter.paint(), not in build() or other widget methods. Doing this in build() can cause severe performance issues, unpredictable rendering, visual glitches, and broken platform optimizations. This may result in app store rejection for poor graphics handling and negative user feedback due to laggy or broken UI. {v3}
      avoid_cascade_after_if_null: true  # [WARNING] Cascade after ?? may have unexpected precedence. Using cascade after ?? without parentheses can lead to unexpected behavior because ?? has lower precedence than cascade. {v5}
      avoid_casting_to_extension_type: true  # [WARNING] Cast to extension type bypasses the constructor invariants and type safety guarantees. Extension types are erased at runtime, so the cast always succeeds regardless of whether the value satisfies the extension type constraints, silently producing an invalid wrapper. {v5}
      avoid_catching_generic_exception: true  # [WARNING] Catching Exception or Object swallows all errors including programming bugs, assertion failures, and unexpected states that should crash visibly. This masks root causes, making bugs harder to diagnose and allowing the app to continue in a corrupted state. {v4}
      avoid_change_notifier_in_widget: true  # [WARNING] ChangeNotifier created inside build() is re-instantiated on every widget rebuild, losing all accumulated state and listener registrations. This causes flickering UI, lost user input, and wasted allocations that trigger unnecessary garbage collection. {v5}
      avoid_closure_memory_leak: true  # [WARNING] Closure capturing setState retains a strong reference to the State object, preventing garbage collection after the widget is disposed. The entire parent widget subtree leaks memory, and calling setState on an unmounted widget throws a framework error that crashes the app during subsequent navigation. {v3}
      avoid_collection_equality_checks: true  # [WARNING] Comparing collections with == uses reference equality. This can cause false positives/negatives, leading to logic errors and unexpected app behavior. Collections (List, Set, Map) use reference equality by default, not value equality. Use listEquals, setEquals, or mapEquals instead. {v5}
      avoid_collection_methods_with_unrelated_types: true  # [WARNING] Collection method called with unrelated type. Using contains, indexOf, remove, etc. with a type that can never match the collection's element type is likely a bug. {v5}
      avoid_color_only_indicators: true  # [WARNING] Color-only status indicator fails for approximately 8% of men and 0.5% of women who have color vision deficiency and cannot distinguish red from green, making the UI completely inaccessible to colorblind users. Relying solely on color to convey meaning violates WCAG 1.4.1 (Use of Color), which requires a secondary visual cue such as an icon, text label, or pattern to communicate status information. {v6}
      avoid_conditions_with_boolean_literals: true  # [INFO] Boolean literal used in a logical expression where it has no effect or makes the result constant. Expressions like x || true are always true and x && false are always false, indicating dead code or a logic error where a variable was intended. {v6}
      avoid_constant_assert_conditions: true  # [WARNING] Assert condition evaluates to a compile-time constant, making it either always pass (true) or always fail (false). A constant assert provides no runtime safety check and misleads readers into thinking a dynamic invariant is being verified. {v5}
      avoid_constant_conditions: true  # [WARNING] Condition evaluates to a compile-time constant, making one branch unreachable dead code. This usually indicates a logic error where the condition was intended to be dynamic, or leftover debugging code that was never cleaned up. {v4}
      avoid_constant_switches: true  # [WARNING] Switch expression matches against a compile-time constant, so only one case ever executes. The switch adds structural complexity without providing any branching value, and the unreachable cases are dead code that misleads readers. {v5}
      avoid_context_in_async_static: true  # [WARNING] BuildContext in async static method may become invalid during async operations. {v2}
      avoid_context_in_initstate_dispose: true  # [WARNING] BuildContext used in initState or dispose may reference an unmounted widget, causing runtime errors or silent failures. In initState the widget tree is not yet fully built, and in dispose the widget has been removed, so context-dependent lookups (Theme.of, Navigator.of) can return stale or invalid data. {v7}
      avoid_contradictory_expressions: true  # [WARNING] Contradictory conditions detected where two expressions cannot both be true simultaneously. This creates unreachable code paths that silently skip intended logic, indicating a logic error that may cause incorrect behavior or missed edge cases. {v3}
      avoid_controller_in_build: true  # [WARNING] Creating controllers (e.g., TextEditingController, AnimationController) inside the build() method causes a new instance to be created on every rebuild. This leads to memory leaks, lost state, and degraded performance, as old controllers are never disposed. {v5}
      avoid_creating_vector_in_update: true  # [WARNING] Creating new Vector objects (Vector2, Vector3, etc.) inside update() causes frequent garbage collection (GC) churn every frame, leading to performance degradation, frame drops, and increased CPU usage. In real-time games, this can cause stuttering, input lag, and poor user experience, especially on lower-end devices. {v2}
      avoid_database_in_build: true  # [WARNING] Running database queries inside build() causes the query to execute on every rebuild, leading to repeated database hits, slow UI, increased backend load, and degraded app performance. This can also cause inconsistent data, race conditions, and higher costs for cloud databases. {v4}
      avoid_datetime_parse_unvalidated: true  # [WARNING] DateTime.parse throws on invalid input. Unvalidated parsing can crash the app, cause silent failures, and break data flows. This is a common source of bugs in date handling and can lead to missed events or corrupted records. {v3}
      avoid_debug_print: true  # [WARNING] debugPrint bypasses structured logging, making it impossible to filter, search, or disable output per environment. Debug statements left in production code expose internal state to device logs, degrade performance through I/O overhead, and create noise that obscures real issues during troubleshooting. {v4}
      avoid_deep_widget_nesting: true  # [INFO] Widget tree exceeds 15 levels of nesting. Deeply nested widget trees are hard to read, maintain, and debug. They often indicate a need to extract widgets into separate components. {v5}
      avoid_deeply_nested_widgets: true  # [INFO] Widget tree is too deeply nested, exceeding the recommended nesting depth. Deep nesting reduces build method readability and increases the risk of layout overflow errors at runtime. {v6}
      avoid_di_in_widgets: true  # [WARNING] GetIt.I access in widgets creates hidden dependencies that break testability and widget reuse. Direct service locator calls tightly couple widgets to the DI container, making it impossible to substitute mock dependencies in tests and preventing widget extraction to other packages. {v3}
      avoid_double_slash_imports: true  # [WARNING] Import path contains double slashes. Double slashes in import paths cause resolution failures on some platforms and may silently import the wrong file, leading to runtime errors or missing symbols. {v5}
      avoid_double_tap_submit: true  # [WARNING] Submit button without double-tap protection can fire the onPressed callback multiple times before the first submission completes. This causes duplicate API requests, duplicate database entries, double charges in payment flows, and race conditions that corrupt application state. {v4}
      avoid_duplicate_exports: true  # [WARNING] File is exported multiple times. Duplicate exports increase compilation time, confuse IDE auto-imports, and may cause unexpected symbol visibility when show/hide combinators differ between duplicates. {v5}
      avoid_duplicate_mixins: true  # [WARNING] Mixin is applied multiple times in the same class declaration. Duplicate mixin applications have no additional effect and indicate a copy-paste error or misunderstanding of the class hierarchy. {v5}
      avoid_duplicate_named_imports: true  # [WARNING] Import is declared multiple times with different prefixes. Multiple imports of the same URI with different prefixes create ambiguity about which prefix to use, increase cognitive load, and may cause IDE auto-import confusion. {v6}
      avoid_duplicate_number_elements: true  # [WARNING] Duplicate numeric element in collection literal typically indicates a copy-paste error or logic mistake. In Sets, the duplicate is silently ignored, producing a smaller collection than expected. {v2}
      avoid_duplicate_object_elements: true  # [WARNING] Duplicate object reference or literal (bool, null, identifier) in collection typically indicates a copy-paste error. In Sets, the duplicate is silently ignored, producing a smaller collection than expected. {v2}
      avoid_duplicate_patterns: true  # [WARNING] Same pattern appears multiple times in a switch or if-case chain. Duplicate patterns mean the second occurrence is unreachable dead code because the first match always wins, indicating a copy-paste error or incomplete refactoring. {v4}
      avoid_duplicate_string_elements: true  # [WARNING] Duplicate string element in collection literal typically indicates a copy-paste error or incomplete refactoring. In Sets, the duplicate is silently ignored, producing a smaller collection than expected. {v2}
      avoid_duplicate_switch_case_conditions: true  # [WARNING] Same condition value appears in multiple switch cases. The second occurrence is unreachable because the first matching case always executes, making the duplicate case dead code that indicates a copy-paste error or incomplete refactoring. {v4}
      avoid_dynamic_json_chains: true  # [ERROR] Deep dynamic access throws NoSuchMethodError or TypeError at runtime when any nested key is missing. Chaining multiple dynamic map accesses (e.g., json["a"]["b"]["c"]) is fragile and will crash if any key is missing or null. This leads to runtime exceptions, broken features, and poor user experience. Always check each level for null before accessing the next. {v3}
      avoid_empty_build_when: true  # [WARNING] buildWhen callback always returns true, which is the default behavior. The callback adds code without filtering any rebuilds, defeating the purpose of the optimization that buildWhen provides in BlocBuilder and BlocListener. {v2}
      avoid_empty_spread: true  # [INFO] Empty spread operator (...[] or ...{}) adds nothing to your collection and may confuse readers. This is often a leftover from refactoring or copy-paste and serves no purpose. {v4}
      avoid_empty_test_groups: true  # [INFO] Test group has an empty body with no test cases. Empty groups clutter the test suite, provide false organization, and reduce confidence in actual test coverage. {v4}
      avoid_empty_text_widgets: true  # [INFO] Text widget with an empty string ('') still occupies space based on the inherited text style's line height, creating invisible layout artifacts. It also participates in accessibility announcements, confusing screen readers with blank text nodes that convey no information. {v2}
      avoid_enum_values_by_index: true  # [WARNING] Enum value accessed by numeric index on the .values list. If enum members are reordered or new values are inserted, the index silently resolves to the wrong constant, causing incorrect behavior that the compiler cannot catch. {v4}
      avoid_equal_expressions: true  # [WARNING] Both sides of the binary expression are identical, meaning the comparison always produces the same result (true for ==, false for >, <). This is almost always a copy-paste bug where the developer intended to compare two different values, and the redundant comparison masks the real logic error in the code. {v5}
      avoid_exception_in_constructor: true  # [WARNING] Throwing in a constructor creates a partially constructed object that can leak resources and leave dependent fields uninitialized. Callers cannot easily recover because the constructor has already failed midway through initialization. {v3}
      avoid_expensive_build: true  # [WARNING] Expensive operations in build() run on every frame, causing jank and dropped frames during animations. {v2}
      avoid_find_child_in_build: true  # [INFO] Creating findChildIndexCallback as a new closure inside the build method allocates a new function object on every rebuild. Since SliverChildBuilderDelegate uses this callback to optimize child reordering, a new instance defeats the identity check and forces unnecessary child lookups on every frame. {v4}
      avoid_firestore_in_widget_build: true  # [WARNING] Performing Firestore operations (get, collection, doc) inside build() causes the query to run on every rebuild, leading to excessive database reads, slow UI, increased backend costs, and potential quota exhaustion. This can also cause inconsistent data, race conditions, and degraded user experience, especially in dynamic UIs. {v2}
      avoid_firestore_unbounded_query: true  # [WARNING] Firestore query without limit() returns unbounded data from the entire collection. This triggers excessive bandwidth consumption, inflated Firestore read costs, slow UI rendering, and out-of-memory crashes on low-end devices when the collection grows large. {v3}
      avoid_flaky_tests: true  # [WARNING] Test contains patterns that may cause flakiness. Flaky tests fail intermittently, eroding confidence in the test suite. This rule detects common causes of test flakiness. {v4}
      avoid_form_without_key: true  # [WARNING] Form widget without a GlobalKey<FormState> makes it impossible to call validate(), save(), or reset() on the form state programmatically. Without a key, you cannot trigger field validation on submit, retrieve form values, or reset the form to its initial state. {v4}
      avoid_freezed_json_serializable_conflict: true  # [ERROR] Combining Freezed and json_serializable annotations incorrectly can result in code generation conflicts, leading to broken serialization, runtime errors, or data loss. This can cause your app to fail when parsing or serializing JSON, especially in production environments. Always follow the recommended integration patterns to ensure reliable code generation. See https://pub.dev/packages/freezed#json_serializable. {v2}
      avoid_future_ignore: true  # [WARNING] Calling Future.ignore() discards all errors and exceptions from the Future, causing failures to go unnoticed. This can result in silent bugs, missed exceptions, and unreliable app behavior, especially in production where error reporting is critical. {v6}
      avoid_future_then_in_async: true  # [INFO] Using .then() inside async function. Prefer await for consistency. Using .then() inside an async function mixes two async patterns. Prefer await for cleaner, more readable code. {v2}
      avoid_future_tostring: true  # [WARNING] Future.toString() returns 'Instance of Future', not the resolved value. Logs show useless output, error messages fail to include actual data, and debugging async code becomes nearly impossible. {v7}
      avoid_generic_exceptions: true  # [INFO] Generic Exception thrown instead of a specific error type. This prevents callers from distinguishing between different failure modes, forces broad catch-all blocks, and makes error traceability across services impossible. Specific exception types enable targeted recovery and clearer crash reports. {v4}
      avoid_generic_key_in_url: true  # [WARNING] Sensitive data embedded in URL query parameters is logged by web servers, proxy caches, browser history, and analytics tools. This exposes API keys, tokens, and credentials in access logs and referrer headers, where they persist indefinitely and can be harvested by attackers with log access. {v4}
      avoid_gesture_conflict: true  # [WARNING] Nested GestureDetector widgets compete in the gesture arena, causing unpredictable behavior where the inner detector wins some gestures and the outer wins others depending on timing. This leads to missed taps, swallowed swipes, and inconsistent interaction behavior that is difficult to debug. {v6}
      avoid_gesture_without_behavior: true  # [INFO] GestureDetector without explicit HitTestBehavior defaults to deferToChild, which only detects taps on painted pixels of the child. Empty areas within the GestureDetector bounds are ignored, causing missed taps that confuse users. Specifying the behavior makes the tap target boundaries explicit and predictable. {v5}
      avoid_getx_dialog_snackbar_in_controller: true  # [WARNING] Get.snackbar/dialog in controller couples UI to business logic and prevents testing. {v2}
      avoid_global_keys_in_state: true  # [WARNING] GlobalKey in StatefulWidget persists across hot reload. Move to State class instead. {v3}
      avoid_global_state: true  # [WARNING] Mutable global state detected. Top-level mutable variables create hidden dependencies between functions, make unit testing unreliable because tests share state, and cause race conditions in concurrent or isolate-based code. {v5}
      avoid_go_router_push_replacement_confusion: true  # [INFO] context.go() used to navigate to a detail route with a dynamic ID parameter. go() replaces the entire navigation stack, destroying the back button history and preventing users from returning to the previous screen. {v2}
      avoid_gradient_in_build: true  # [WARNING] Creating Gradient in build() prevents reuse and causes allocations. This leads to unnecessary memory usage, slower UI performance, and increased battery drain. {v3}
      avoid_hardcoded_api_urls: true  # [WARNING] Hardcoded API URLs prevent switching between development, staging, and production environments, making code inflexible and error-prone. This practice also risks leaking sensitive endpoints and complicates maintenance. Always extract API URLs to configuration constants (e.g., ApiConfig.baseUrl) to enable environment switching and improve security. {v5}
      avoid_hardcoded_layout_values: true  # [INFO] Hardcoded numeric value in layout widget prevents responsive adaptation across screen sizes and text scales. Fixed pixel values that look correct on one device may cause overflow, clipping, or wasted space on devices with different screen densities, orientations, or accessibility font size settings. {v6}
      avoid_hardcoded_signing_config: true  # [WARNING] Hardcoding keystore paths, passwords, or signing configuration in source code exposes them to reverse engineering. These values are extractable from compiled binaries and enable attackers to sign malicious builds. {v3}
      avoid_hardcoded_test_delays: true  # [WARNING] Test has hardcoded delay which makes tests slow and flaky. This weakens test quality, making failures harder to diagnose and reducing confidence in the test suite. {v5}
      avoid_hardcoded_text_styles: true  # [INFO] Inline TextStyle with hardcoded fontSize, fontWeight, and color values creates scattered styling that drifts from the design system over time. Every hardcoded style must be updated individually when the design changes, and inconsistencies between screens become difficult to detect during code review. {v5}
      avoid_hive_binary_storage: true  # [INFO] Storing Uint8List/binary data in Hive. This degrades performance for large files. {v2}
      avoid_hive_box_name_collision: true  # [WARNING] Generic Hive box name may cause collision. Use a specific name. Box names must be unique across the application. Using the same name for different types causes data corruption or type errors. {v2}
      avoid_icon_buttons_without_tooltip: true  # [WARNING] This IconButton does not provide a tooltip, making it inaccessible to screen readers and users with visual impairments. Without a tooltip, users cannot understand the button’s purpose, which reduces usability and fails accessibility standards. Tooltips are essential for describing the action of icon-only buttons. {v5}
      avoid_ignoring_return_values: true  # [INFO] Return value of this invocation is ignored. Discarding return values can hide bugs where an important result (a Future, a boolean success flag, or a parsed value) is silently lost. Assign the result to a variable or remove the call if it is truly unnecessary. {v1}
      avoid_image_buttons_without_tooltip: true  # [WARNING] This image-only button does not provide an accessible label, making it invisible to screen readers and users with visual impairments. Without a tooltip or semantic label, users cannot understand or interact with the button, which fails accessibility standards and excludes blind users from key actions. {v5}
      avoid_implicitly_nullable_extension_types: true  # [WARNING] Extension type is implicitly nullable. Extension types that don't implement Object can be implicitly nullable, which may lead to unexpected behavior. {v4}
      avoid_incorrect_image_opacity: true  # [WARNING] Wrapping an Image widget in an Opacity widget is inefficient because it forces the framework to allocate an offscreen buffer, render the image into it, and then composite the buffer with reduced alpha. This extra compositing pass increases GPU memory usage and slows frame rendering, especially on lower-end devices. The Image widget natively supports opacity through its color and colorBlendMode properties, which apply the alpha during the image decode stage without an extra compositing layer. {v6}
      avoid_incorrect_uri: true  # [WARNING] URI string appears to be malformed or contains invalid characters. Malformed URIs cause runtime exceptions when parsed by Uri.parse(), leading to unhandled errors in network requests, routing logic, or deep link handling. {v4}
      avoid_ios_13_deprecations: true  # [WARNING] Deprecated iOS API detected. This API is deprecated since iOS 13 and may cause App Store rejection. {v2}
      avoid_ios_battery_drain_patterns: true  # [WARNING] Pattern detected that may cause excessive battery drain. iOS shows high battery usage in Settings. {v2}
      avoid_ios_continuous_location_tracking: true  # [INFO] Continuous location tracking detected with high accuracy. Consider using lower accuracy or distance filters to save battery. {v2}
      avoid_ios_force_unwrap_in_callbacks: true  # [WARNING] Force unwrap on MethodChannel result detected. Native code may return null unexpectedly, causing crashes. {v2}
      avoid_ios_hardcoded_device_model: true  # [WARNING] Hardcoded iOS device model detected. Device-specific code breaks when new devices are released. {v3}
      avoid_ios_hardcoded_keyboard_height: true  # [WARNING] Hardcoded bottom padding may be for keyboard. iOS keyboard height varies by device and input type. {v2}
      avoid_ios_hardcoded_status_bar: true  # [WARNING] Hardcoded status bar height (20, 44, 47, or 59) may cause UI issues on different iOS devices. {v2}
      avoid_ios_simulator_only_code: true  # [WARNING] iOS Simulator-only code pattern detected. This code may not work on real iOS devices. {v2}
      avoid_ios_wifi_only_assumption: true  # [WARNING] Large download without connectivity check. Users may have expensive cellular plans. {v2}
      avoid_isar_clear_in_production: true  # [ERROR] Calling isar.clear() will permanently delete ALL user data in the database. If this code runs in production, users will lose their data irreversibly, leading to catastrophic data loss. {v3}
      avoid_isar_transaction_nesting: true  # [ERROR] Calling writeTxn inside another writeTxn causes a deadlock: Isar cannot acquire a second write lock while the first is held. This will freeze your app and block all database writes until a restart. {v1}
      avoid_json_encode_in_build: true  # [WARNING] jsonEncode in build() is expensive and causes jank. JSON encoding is expensive. Doing it in build() causes performance issues because build() is called frequently (60fps during animations). {v2}
      avoid_jwt_decode_client: true  # [WARNING] Decoding JWT tokens on the client for authorization decisions is insecure because the client cannot verify token signatures. Attackers can craft or modify JWT claims to bypass permission checks, escalate privileges, and access restricted features or data without valid server-issued credentials. {v4}
      avoid_keywords_in_wildcard_pattern: true  # [WARNING] Pattern variable uses a Dart keyword. Using keywords in wildcard patterns can be confusing. This pattern matching usage can cause unexpected behavior or miss important type information. {v4}
      avoid_large_images_in_memory: true  # [INFO] Image loaded without size constraints decodes at full resolution into GPU memory regardless of display size. A single uncompressed 4K image can consume 30-50 MB of memory, and lists of such images quickly exhaust available memory, causing OOM crashes on mobile devices. {v5}
      avoid_large_list_copy: true  # [INFO] List.from() and toList() allocate a new list and copy every element, doubling memory consumption for the duration of the operation. For large collections, this triggers garbage collection pauses that freeze the UI and degrade scrolling performance. The unnecessary allocation pressure accumulates rapidly in loops or frequently called methods. List<T>.from() with type args and structurally-required .toList() are exempt. {v4}
      avoid_late_context: true  # [WARNING] BuildContext in late field initializer captures a stale reference that may become invalid after rebuilds. Late fields are initialized once on first access, but BuildContext changes whenever the widget rebuilds, so the captured context points to an outdated element that may no longer exist in the tree. {v8}
      avoid_late_for_nullable: true  # [ERROR] Nullable type declared with late keyword. Since the type already accepts null, the late keyword adds no initialization safety and instead introduces a hidden crash path: accessing the variable before assignment throws LateInitializationError rather than returning null. {v4}
      avoid_layout_builder_in_scrollable: true  # [WARNING] LayoutBuilder inside scrollable causes performance issues. This layout configuration can trigger RenderFlex overflow errors or unexpected visual behavior at runtime. {v6}
      avoid_listview_children_for_large_lists: true  # [WARNING] ListView with many children loads all items. Use ListView.builder. ListView(children: [..]) builds all children immediately. For lists with more than 20 items, use ListView.builder to improve performance through virtualization. {v2}
      avoid_listview_without_item_extent: true  # [INFO] ListView.builder should specify itemExtent to improve scroll performance. This layout configuration can trigger RenderFlex overflow errors or unexpected visual behavior at runtime. {v5}
      avoid_manual_date_formatting: true  # [WARNING] Manual date formatting is error-prone, ignores locale, and can produce incorrect or confusing output for international users. This can break compliance, cause user confusion, and lead to support issues in global apps. {v3}
      avoid_map_keys_contains: true  # [INFO] Calling .keys.contains() allocates an iterable of all keys and performs a linear search, while .containsKey() uses the map hash table for O(1) lookup. This wastes memory and CPU cycles on every call. {v5}
      avoid_map_markers_in_build: true  # [WARNING] Creating map markers in build() causes flickering. Creating markers in build() causes recreation on every rebuild, leading to flickering and performance issues. Cache markers. {v3}
      avoid_material2_fallback: true  # [INFO] Explicitly setting useMaterial3: false forces the app back to the deprecated Material 2 design system, which will receive no new component updates or accessibility improvements. Material 2 components may also be removed in future Flutter releases, creating a migration burden. {v3}
      avoid_mediaquery_in_build: true  # [INFO] MediaQuery.of(context) subscribes to all MediaQueryData changes (size, padding, orientation, brightness, text scaling), causing unnecessary rebuilds when only one property is needed. Specific accessors like sizeOf or paddingOf subscribe to only the relevant property, significantly reducing rebuild frequency. {v6}
      avoid_memory_intensive_operations: true  # [WARNING] String concatenation using += inside a loop creates a new String object on every iteration, resulting in O(n squared) memory allocations. Each iteration copies the entire accumulated string, causing excessive garbage collection pressure, UI thread pauses, and visible jank in animations or scrolling. {v2}
      avoid_misnamed_padding: true  # [INFO] A parameter or field named "padding" is being used to provide margin (spacing outside a widget) rather than actual padding (spacing inside a widget). This can confuse maintainers and lead to incorrect UI adjustments, as padding and margin serve distinct layout purposes in Flutter. {v2}
      avoid_missing_completer_stack_trace: true  # [WARNING] Completer.completeError() called without passing the stack trace as the second argument. Without the original stack trace, error reports show only the completeError() call site instead of the actual failure origin, making debugging asynchronous errors significantly harder. {v4}
      avoid_missing_enum_constant_in_map: true  # [INFO] Map literal keyed by enum values does not include all enum constants. When a new enum value is added, this map silently returns null for the missing key instead of producing a compile-time error, leading to unexpected null values or fallback behavior at runtime. {v3}
      avoid_missing_interpolation: true  # [WARNING] String concatenation using the + operator combines a string literal with a variable or expression. String interpolation ($variable or ${expression}) is the idiomatic Dart approach that is more readable, less error-prone (no accidental space omission between segments), and avoids creating intermediate String objects for each + operation, improving both clarity and performance in concatenation-heavy code paths.
      avoid_misused_set_literals: true  # [INFO] Set literal may be misused. Empty `{}` without type annotation creates a Map, not a Set. {v2}
      avoid_misused_test_matchers: true  # [WARNING] Raw literal used as a test matcher instead of a proper matcher function. Using literals (true, false, null) as the second argument to expect() produces poor failure messages that show "Expected: true, Actual: false" instead of descriptive matcher output. Proper matchers like isTrue, isFalse, isNull, hasLength, equals, and isA provide clearer assertion semantics and significantly better diagnostic output when tests fail, making debugging faster. {v1}
      avoid_money_arithmetic_on_double: true  # [WARNING] Floating point arithmetic on double values introduces rounding errors in financial calculations. For example, 0.1 + 0.2 yields 0.30000000000000004 instead of 0.3. This causes users to see incorrect totals, be charged wrong amounts, and produces accounting discrepancies that compound over multiple transactions and are difficult to trace. {v3}
      avoid_motion_without_reduce: true  # [INFO] Animation should respect disableAnimations preference. Users with vestibular disorders may have enabled "Reduce Motion" in their accessibility settings. Respect this preference for non-essential animations. {v2}
      avoid_multiple_autofocus: true  # [WARNING] Multiple widgets with autofocus: true causes unpredictable behavior. Only one widget can have autofocus at a time. Multiple autofocus widgets cause unpredictable focus behavior. {v2}
      avoid_multiple_stream_listeners: true  # [WARNING] Multiple listen() calls detected on the same non-broadcast stream. Single-subscription streams only allow one active listener at a time. Adding a second listener throws a StateError at runtime, which crashes your app and makes the stream connection unusable for all subscribers. {v2}
      avoid_navigation_in_build: true  # [ERROR] Navigation in build() triggers during rebuild, causing infinite navigation loops or flickering screens. {v2}
      avoid_navigator_context_issue: true  # [ERROR] Using context from GlobalKey for navigation can fail if widget is not in tree. {v2}
      avoid_navigator_push_unnamed: true  # [INFO] Navigator.push used with an inline route constructor instead of a named route. Inline routes prevent deep linking, break URL-based navigation, and make route management unmaintainable. Users cannot share or bookmark specific screens, and analytics tools cannot track page views accurately. {v3}
      avoid_nested_assignments: true  # [INFO] Assignment expression embedded inside another expression (e.g. condition, argument, or return). Nested assignments obscure the data flow and make it unclear whether the intent is comparison, assignment, or both, increasing the risk of logic errors. {v7}
      avoid_nested_providers: true  # [WARNING] Provider created inside Consumer or builder callback. Deeply nested provider trees are hard to reason about and maintain. Flatten with MultiProvider and avoid provider-in-provider patterns where possible. {v4}
      avoid_nested_routes_without_parent: true  # [WARNING] context.go() navigates to a path with 3+ segments, which places users deep in the route hierarchy without parent routes on the stack. The back button skips intermediate screens, breaking expected navigation flow and disorienting users. {v2}
      avoid_nested_scrollables: true  # [WARNING] Nested scrollable widgets can cause scroll conflicts. This layout configuration can trigger RenderFlex overflow errors or unexpected visual behavior at runtime. {v2}
      avoid_nested_scrollables_conflict: true  # [WARNING] Nested scrollable without explicit physics causes gesture conflicts. Nested scrollable widgets cause gesture conflicts. The inner scrollable must have NeverScrollableScrollPhysics to let the outer one handle scrolling. {v3}
      avoid_non_final_exception_class_fields: true  # [WARNING] Mutable exception fields allow modification after throw, corrupting error data for handlers. {v4}
      avoid_not_encodable_in_to_json: true  # [WARNING] Value is not JSON-encodable and will cause runtime error. {v2}
      avoid_notification_payload_sensitive: true  # [WARNING] Sensitive data in notifications exposes passwords, tokens, or PII on the lock screen. Anyone nearby can read this information without unlocking the device, creating a security vulnerability. {v4}
      avoid_notification_spam: true  # [WARNING] Notification inside loop detected. Sending too many notifications causes users to disable notifications or uninstall. {v2}
      avoid_nullable_async_value_pattern: true  # [INFO] Nullable access on AsyncValue bypasses the type system error and loading state handling. Accessing .value directly returns null during loading and error states, which can propagate nulls through the UI and hide error conditions that users should see. {v3}
      avoid_nullable_interpolation: true  # [WARNING] Nullable value in string interpolation produces the literal text 'null' instead of a meaningful fallback. Users may see 'Hello null' or 'Order #null' in the UI, which looks like a bug and erodes trust in the application quality and data integrity. {v5}
      avoid_nullable_parameters_with_default_values: true  # [INFO] Parameter with default value must not be nullable. If a parameter has a default value, it doesn't need to be nullable. Quick fix available: Adds a comment to flag for manual review. {v5}
      avoid_only_rethrow: true  # [WARNING] Catch-rethrow with no handling is dead code that adds nesting and complexity without providing any value. {v4}
      avoid_opacity_animation: true  # [WARNING] Animating the Opacity widget via setState triggers a full rebuild of the child subtree on every frame, which is expensive for complex children. FadeTransition applies opacity changes directly on the compositing layer without rebuilding, achieving the same visual effect with significantly less CPU and GPU overhead. {v3}
      avoid_opacity_misuse: true  # [INFO] Use AnimatedOpacity for opacity animations. Opacity is used for animations instead of AnimatedOpacity. This layout configuration can trigger RenderFlex overflow errors or unexpected visual behavior at runtime. {v6}
      avoid_passing_async_when_sync_expected: true  # [WARNING] Async callback passed to a sync-only method such as forEach, map, or where. The returned Future is silently discarded, which means any errors thrown inside the callback are lost and never reported. This makes debugging extremely difficult and can hide critical failures in your application logic. {v4}
      avoid_passing_bloc_to_bloc: true  # [WARNING] Bloc should not depend on another Bloc. This creates tight coupling and makes testing difficult. {v2}
      avoid_passing_build_context_to_blocs: true  # [WARNING] BuildContext in Bloc couples UI to business logic and makes testing difficult. {v2}
      avoid_prefs_for_large_data: true  # [WARNING] SharedPreferences loads its entire file into memory on first access, making it unsuitable for large datasets. Storing collections or large JSON blobs here causes slow app startup, excessive memory consumption, UI freezes, and potential data corruption on write failures. {v3}
      avoid_provider_in_widget: true  # [ERROR] Declaring a provider inside a widget class breaks Riverpod's global state model, leading to multiple provider instances, lost state, and unpredictable bugs. This can make your app behave inconsistently and is hard to debug. {v2}
      avoid_provider_listen_false_in_build: true  # [INFO] Provider.of() is called with listen: false inside a build() method. This prevents the widget from rebuilding when the provided value changes, causing the UI to display stale data that does not reflect the current application state. Users see outdated information until something else triggers a rebuild, creating confusing and inconsistent UI behavior that is difficult to debug. {v2}
      avoid_provider_recreate: true  # [ERROR] Creating a Provider inside a frequently rebuilding build() method causes the provider to be recreated, losing its state and causing unexpected behavior. This can result in lost user input, bugs, and degraded app performance. {v3}
      avoid_push_replacement_misuse: true  # [WARNING] `[HEURISTIC]` pushReplacement removes current route from stack. User cannot go back. {v2}
      avoid_real_dependencies_in_tests: true  # [WARNING] Test uses real network/database call. Use mocks instead. Tests must be fast, reliable, and not depend on external systems. Use mocks or fakes for external dependencies. {v3}
      avoid_real_network_calls_in_tests: true  # [WARNING] Test may be making real network calls. Tests should mock external dependencies for reliability. This weakens test quality, making failures harder to diagnose and reducing confidence in the test suite. {v5}
      avoid_real_timer_in_widget_test: true  # [WARNING] Real Timer in widget test causes flaky results and slow execution due to actual time delays. Timer callbacks execute asynchronously outside the test framework control, leading to race conditions where assertions run before or after the timer fires depending on system load. {v6}
      avoid_rebuild_on_scroll: true  # [WARNING] Scroll listener registered inside build() is re-added on every widget rebuild without removing the previous one. Duplicate listeners accumulate over time, firing multiple callbacks per scroll event, causing memory leaks, compounding performance degradation, and eventually crashes from excessive callback execution. {v3}
      avoid_redundant_async: true  # [INFO] Declaring a function async without using await adds unnecessary Future wrapping and microtask scheduling overhead. This can make code harder to read, debug, and maintain, and may introduce subtle timing bugs. {v7}
      avoid_redundant_async_on_load: true  # [INFO] async onLoad() without await adds unnecessary overhead. OnLoad() is async but doesn't contain any await. {v2}
      avoid_redundant_await: true  # [INFO] Awaiting a non-Future expression is redundant and misleading. It wraps the value in a resolved Future only to immediately unwrap it, adding an unnecessary microtask delay and confusing readers who expect asynchronous behavior. Remove the await keyword to clarify that the expression is synchronous. {v1}
      avoid_redundant_null_check: true  # [INFO] Comparing a non-nullable value to null is redundant because the result is always the same at compile time. This often indicates a type signature that should be nullable, or dead code that can be removed. With sound null safety, the compiler already guarantees non-nullable variables cannot be null. {v1}
      avoid_referencing_discarded_variables: true  # [WARNING] Variable prefixed with underscore is referenced after declaration. The underscore prefix signals that the value is intentionally discarded, so reading it later contradicts the naming convention and confuses developers who expect underscore-prefixed variables to be unused. {v5}
      avoid_refresh_without_await: true  # [WARNING] RefreshIndicator onRefresh callback must return a Future that completes when data loading finishes. Without awaiting async operations, the refresh spinner dismisses immediately while data is still loading, leaving users confused about whether the refresh succeeded or failed and showing stale content. {v4}
      avoid_regex_in_loop: true  # [WARNING] RegExp object constructed inside a loop body is re-compiled on every iteration, wasting CPU cycles on repeated pattern parsing. Regex compilation is expensive relative to matching, and this overhead multiplies with large data sets, causing noticeable jank in UI-driven code. {v6}
      avoid_return_await_db: true  # [WARNING] Returning directly from a database/IO write skips yieldToUI(). {v3}
      avoid_returning_null_for_future: true  # [INFO] Returning null from a synchronous function declared to return Future<T> where T is non-nullable causes a runtime null error when the future is awaited. Use Future.value() to wrap the default, or make the function async to let Dart handle the future wrapping automatically. {v1}
      avoid_returning_null_for_void: true  # [INFO] Returning null from a void function is redundant and misleading. Void functions should either use a bare return statement or simply let execution fall through. An explicit return null suggests the caller expects a value, which contradicts the void return type and confuses readers. {v1}
      avoid_returning_value_from_cubit_methods: true  # [INFO] Cubit methods should emit states, not return values. This bypasses reactive state management. {v2}
      avoid_riverpod_notifier_in_build: true  # [WARNING] Creating a Riverpod Notifier inside the build method reinstantiates it on every widget rebuild, discarding all accumulated state. Users experience lost form input, reset scroll positions, and flickering UI as the notifier repeatedly initializes from scratch. {v5}
      avoid_scaffold_messenger_after_await: true  # [WARNING] Using ScaffoldMessenger.of(context) after await may use an invalid context. This violates the widget lifecycle, risking setState-after-dispose errors or silent state corruption. {v8}
      avoid_screenshot_sensitive: true  # [INFO] Sensitive screen allows screenshots and screen recording. Financial and authentication screens should disable screenshots using platform APIs to prevent sensitive data exposure. {v5}
      avoid_self_assignment: true  # [WARNING] Variable is assigned to itself (x = x), which has no effect and indicates a copy-paste error or incomplete refactor. The assignment executes at runtime but produces no state change, wasting CPU cycles and obscuring the developer's actual intent. This dead code makes maintenance harder because readers must determine whether the assignment was intentional. {v5}
      avoid_self_compare: true  # [WARNING] Variable is compared to itself (x == x, x > x, etc.), which always produces a constant result (true for ==, false for inequality operators) unless the value is NaN. This is almost always a copy-paste bug where the developer intended to compare two distinct values, and the redundant comparison hides the real logic error in the code. {v4}
      avoid_semantics_exclusion: true  # [WARNING] excludeFromSemantics: true detected without accompanying documentation. Screen reader users will encounter hidden content without understanding why it was excluded, making it difficult to debug accessibility issues or verify the exclusion was intentional. {v3}
      avoid_set_state_in_dispose: true  # [ERROR] setState in dispose() throws "setState called after dispose" error, crashing the app during navigation. {v2}
      avoid_shadowed_extension_methods: true  # [WARNING] Extension method has the same name as an instance method on the target class. The instance method always takes precedence, so the extension method is never called through normal dispatch, making it dead code that misleads developers. {v3}
      avoid_shadowing_type_parameters: true  # [WARNING] A method type parameter with the same name as the enclosing class type parameter creates a new, unrelated type that shadows the outer one. This causes confusing compile errors where values of the "same" type T are incompatible. Rename the method type parameter to avoid the collision. {v1}
      avoid_shrink_wrap_in_lists: true  # [WARNING] Using 'shrinkWrap: true' inside a nested scrollable (such as a ListView within another scrollable) can cause significant performance issues. It forces the inner list to compute the size of all its children, leading to poor scroll performance and increased memory usage, especially with large or dynamic lists. {v4}
      avoid_shrinkwrap_in_scrollview: true  # [WARNING] shrinkWrap: true on a scrollable list disables virtualization, forcing Flutter to lay out and render every child widget immediately regardless of visibility. This causes severe jank, high memory usage, and slow initial rendering for lists with more than a few dozen items, degrading the user experience. {v6}
      avoid_single_child_column_row: true  # [INFO] Column/Row with single child is unnecessary. A Column or Row has only a single child. This layout configuration can trigger RenderFlex overflow errors or unexpected visual behavior at runtime. {v5}
      avoid_singlechildscrollview_with_column: true  # [WARNING] SingleChildScrollView with Column may cause layout issues. SingleChildScrollView wraps a Column with Expanded children. This layout configuration can trigger RenderFlex overflow errors or unexpected visual behavior at runtime. {v6}
      avoid_small_touch_targets: true  # [INFO] Touch target under 44px violates WCAG 2.5.5 (Target Size). Users with motor impairments, tremors, or limited dexterity will struggle to tap accurately, causing frustration and excluding them from core functionality. The minimum recommended touch target size is 48x48 logical pixels to ensure reliable interaction across all ability levels. {v5}
      avoid_snackbar_in_build: true  # [WARNING] Calling showSnackBar inside build() causes a snackbar to be shown on every rebuild, flooding the user with duplicate messages and overwhelming the snackbar queue. This leads to poor user experience, lost context, and can make the app feel buggy or unresponsive. It may also result in app store rejection for UX violations and negative user reviews due to notification spam. {v3}
      avoid_stack_without_positioned: true  # [WARNING] Stack child without Positioned. Layout may be unexpected. Non-first Stack children without explicit positioning use the Stack's default alignment, which may produce unexpected overlap. The first child is skipped because it typically defines the Stack's base size. {v2}
      avoid_state_constructors: true  # [WARNING] State class must not have a constructor body. Constructor logic runs before the framework initializes the State object, risking errors that bypass the widget lifecycle contract. {v4}
      avoid_stateful_test_setup: true  # [WARNING] setUp must not mutate shared state. Shared mutable state can cause test pollution. This weakens test quality, making failures harder to diagnose and reducing confidence in the test suite. {v4}
      avoid_stateless_widget_initialized_fields: true  # [WARNING] StatelessWidget must not have initialized fields. This violates the widget lifecycle, risking setState-after-dispose errors or silent state corruption. {v4}
      avoid_storing_user_data_in_auth: true  # [WARNING] Large object in setCustomClaims. Claims are for roles, not data storage. Firebase custom claims are meant for access control, not user data storage. They're limited to 1000 bytes and are included in every auth token. {v2}
      avoid_stream_subscription_in_field: true  # [WARNING] If a StreamSubscription is stored as a field in a State class but not properly canceled in dispose(), the subscription will continue to receive events even after the widget is removed from the widget tree. This can cause memory leaks, unexpected UI updates, and subtle bugs, especially in dynamic lists or navigation flows where widgets are frequently created and destroyed. Always cancel subscriptions in the correct State object’s dispose() method. See https://docs.flutter.dev/perf/memory#dispose-resources. {v4}
      avoid_stream_tostring: true  # [WARNING] Calling toString() on a Stream returns only the type name (e.g., 'Instance of Stream'), not the actual stream data. This leads to data loss and can mislead developers or users expecting meaningful output. {v5}
      avoid_string_concatenation_for_l10n: true  # [WARNING] String concatenation may break word order in other languages. HEURISTIC: String concatenation breaks word order in RTL languages and languages with different grammatical structures. {v2}
      avoid_string_concatenation_l10n: true  # [WARNING] String concatenation in UI breaks word order for translations. {v2}
      avoid_string_concatenation_loop: true  # [INFO] Each += creates new String object, causing O(n²) memory allocations in loops. {v3}
      avoid_string_substring: true  # [WARNING] substring() throws RangeError if start or end indices are out of bounds, causing runtime crashes when input lengths vary. This is especially dangerous with user input, API responses, or dynamically sized strings where the length cannot be guaranteed at compile time. {v3}
      avoid_sync_on_every_change: true  # [WARNING] `[HEURISTIC]` API call in onChanged callback may fire on every keystroke. Add a debounce timer to batch rapid inputs before syncing. {v2}
      avoid_test_coupling: true  # [WARNING] Test appears to depend on shared mutable state from other tests. Tests must be independent and run in any order. Depending on state from previous tests creates fragile, unreliable tests. {v2}
      avoid_test_print_statements: true  # [INFO] Print statement in test file clutters output. Print statements in tests add noise to test output and can hide actual test failures. Use proper logging or debugging tools instead. {v2}
      avoid_text_scale_factor: true  # [WARNING] textScaleFactor is deprecated since Flutter 3.16. It applies a linear multiplier that cannot express non-linear text scaling used by accessibility settings on modern platforms. The replacement textScaler API supports both linear and non-linear scaling, ensuring correct rendering for users with accessibility needs. {v4}
      avoid_text_scale_factor_ignore: true  # [WARNING] Setting textScaleFactor to 1.0 on a text child element overrides user accessibility settings, preventing users from increasing text size for readability. This excludes users with low vision and fails accessibility standards. Respecting user-configured text scaling in the widget tree is essential for inclusive design. {v3}
      avoid_throw_in_catch_block: true  # [WARNING] Throwing a new error in a catch block without preserving the original stack trace makes debugging much harder. This can hide the root cause of failures and lead to incomplete error logs, making it difficult to diagnose and fix issues. {v6}
      avoid_time_limits: true  # [INFO] Short duration may disadvantage users who need more time. Consider longer durations or manual dismissal. {v2}
      avoid_unassigned_late_fields: true  # [WARNING] Late field has no assignment in any constructor, initializer, or lifecycle method. Accessing an unassigned late field throws a LateInitializationError at runtime, crashing the app at a point that the compiler cannot check statically. {v4}
      avoid_unbounded_constraints: true  # [WARNING] Column/Row in SingleChildScrollView may have unbounded constraints. Expanded/Flexible children in an unbounded scroll axis throw RenderFlex overflow errors at runtime. Crash path — Expanded/Flexible in unbounded scroll axis throws RenderFlex overflow. Even 10+ violations need immediate attention. {v8}
      avoid_unconditional_break: true  # [WARNING] Unconditional break or continue at the start of a loop body causes the loop to execute at most once. This defeats the purpose of the loop construct and usually indicates a logic error where a condition was accidentally omitted. {v5}
      avoid_unconstrained_box_misuse: true  # [WARNING] UnconstrainedBox in constrained parent may cause overflow. UnconstrainedBox removes constraints which can cause overflow. UnconstrainedBox is used improperly causing overflow. {v5}
      avoid_uncontrolled_text_field: true  # [INFO] TextField without a TextEditingController loses programmatic access to the input value, making it impossible to pre-fill, clear, validate on demand, or read the text outside of onChanged. This leads to fragile state management and unexpected behavior during form submissions. {v6}
      avoid_unguarded_debug: true  # [WARNING] debugPrint() is not guarded by a debug mode check. {v3}
      avoid_unknown_pragma: true  # [WARNING] Unrecognized pragma annotation detected. Unknown pragmas are silently ignored by the Dart compiler, which means the intended optimization or behavior hint has no effect and may mislead developers into thinking the code is optimized when it is not. {v3}
      avoid_unnecessary_conditionals: true  # [WARNING] Conditional expression or if statement has a condition that always evaluates to the same boolean value. The unnecessary branch adds dead code that will never execute, and it misleads readers into thinking the outcome can vary at runtime. {v4}
      avoid_unnecessary_consumer_widgets: true  # [INFO] ConsumerWidget does not use ref parameter. If a widget extends ConsumerWidget but doesn't use ref, it must be a regular StatelessWidget instead. {v2}
      avoid_unnecessary_hook_widgets: true  # [INFO] HookWidget without any hook calls. Use StatelessWidget instead. If a widget extends HookWidget but doesn't call any hook functions, it must be a regular StatelessWidget instead. {v2}
      avoid_unnecessary_reassignment: true  # [WARNING] Variable is assigned the same value it already holds (x = x). Self-assignment has no effect and typically indicates a copy-paste error or a missing right-hand-side expression that was intended. {v5}
      avoid_unnecessary_setstate: true  # [WARNING] setState called in lifecycle method where not needed. This violates the widget lifecycle, risking setState-after-dispose errors or silent state corruption. {v6}
      avoid_unnecessary_statements: true  # [WARNING] Statement produces a value or expression result that is never used and has no side effects. Dead statements clutter the code, mislead readers into thinking meaningful work is being done, and may indicate a missing assignment or function call. {v4}
      avoid_unnecessary_type_assertions: true  # [INFO] Unnecessary type assertion. The expression is already known to be of this type. {v2}
      avoid_unnecessary_type_casts: true  # [INFO] Unnecessary type cast. The expression is already of this type. {v2}
      avoid_unrelated_type_assertions: true  # [WARNING] Type assertion can never be true. The types are unrelated. {v2}
      avoid_unrelated_type_casts: true  # [ERROR] Casting between unrelated types (such as String to int) will always throw a runtime error, leading to crashes and unpredictable behavior. This often indicates a logic error or misunderstanding of the type system. Always ensure types are compatible before casting to prevent runtime failures and improve code safety. {v3}
      avoid_unsafe_cast: true  # [WARNING] Direct cast with "as" may throw at runtime. Direct casting with as can throw if the value is null or wrong type. Prefer is check first or use as? for nullable result. {v5}
      avoid_unused_constructor_parameters: true  # [WARNING] A constructor parameter that is not assigned to any field, forwarded to a super constructor, or referenced in the constructor body is dead code. It adds to the public API surface without serving any purpose and confuses callers who pass values that are silently discarded. {v1}
      avoid_unused_generics: true  # [WARNING] Type parameter is declared but never used in the class or method body. Unused generics add false complexity and mislead callers into thinking the type affects behavior. {v4}
      avoid_unused_instances: true  # [WARNING] Object instance created but never assigned to a variable or used in an expression. The constructor runs its side effects (if any) but the resulting object is immediately garbage-collected, wasting memory allocation and usually indicating a missing assignment. {v5}
      avoid_url_launcher_simulator_tests: true  # [INFO] URL launcher test with tel:/mailto:/sms:/facetime:/maps: scheme may fail on simulator. These schemes are not supported in emulators and will cause test failures. {v3}
      avoid_validation_in_build: true  # [WARNING] Complex or async validation running inside build() executes on every keystroke and widget rebuild. This causes visible input lag as network calls fire per character, floods the backend API with excessive requests, degrades performance with unnecessary widget rebuilds, and frustrates users who see delayed responses while typing in form fields. {v3}
      avoid_variable_shadowing: true  # [INFO] Declaration shadows a declaration from an outer scope. Shadowing occurs when a nested scope declares a variable with the same name as one in an enclosing scope. This can lead to confusion about which variable is being referenced and is a common source of subtle bugs. {v3}
      avoid_very_long_length_files: true  # [INFO] File exceeds 1000 code lines (comments and blank lines excluded). Files this large significantly slow down IDE indexing, increase build times, and make it nearly impossible to understand the full scope of changes during code review. {v4}
      avoid_very_long_length_test_files: true  # [INFO] Test file exceeds 2000 code lines (comments and blank lines excluded). Even with test files' higher tolerance for length, 2000+ lines indicates the file is testing too much. Split into separate test files organized by feature, screen, or use case. {v2}
      avoid_void_async: true  # [WARNING] Async functions returning void silently swallow exceptions — callers cannot catch errors or await completion. Changing the return type to Future<void> lets errors propagate and enables callers to coordinate execution order. This is especially dangerous in error-handling paths where silent failure masks bugs. {v1}
      avoid_websocket_memory_leak: true  # [WARNING] WebSocketChannel is not closed in dispose(). This leaves network connections open, wasting bandwidth, battery, and server resources. Unclosed sockets can also cause app crashes or server-side issues. {v3}
      avoid_webview_file_access: true  # [WARNING] WebView file access enabled (allowFileAccess: true) creates a critical security vulnerability. Malicious web content loaded in the WebView can read local files including user data, cached credentials, and app configuration, then exfiltrate them to attacker-controlled servers without user consent or visible indication. {v3}
      avoid_webview_insecure_content: true  # [ERROR] Allowing mixed (HTTP and HTTPS) content in a WebView exposes users to man-in-the-middle attacks, data interception, and content tampering. Insecure content can compromise the confidentiality and integrity of user data. Always restrict WebView to load only secure (HTTPS) content. {v3}
      avoid_wildcard_cases_with_enums: true  # [WARNING] Switch on an enum uses a default or wildcard case, suppressing exhaustiveness checking. When new enum values are added, the compiler will not flag this switch as incomplete, allowing the new case to silently fall into the default branch instead of being explicitly handled. {v5}
      avoid_wildcard_cases_with_sealed_classes: true  # [WARNING] Switch on a sealed class uses a default or wildcard case, suppressing exhaustiveness checking. When new subtypes are added to the sealed hierarchy, the compiler will not flag this switch as incomplete, allowing unhandled subtypes to silently fall through. {v4}
      avoid_work_in_paused_state: true  # [WARNING] Timer.periodic without lifecycle handling. Will run when app is backgrounded. Timers and periodic callbacks should pause when the app is backgrounded to save battery and avoid unexpected behavior. {v2}
      check_mounted_after_async: true  # [WARNING] setState() after await without mounted check. State may be disposed during async gap, causing "setState() called after dispose()" crash. {v5}
      dispose_provider_instances: true  # [WARNING] Provider creating a disposable instance without a dispose callback leaks stream subscriptions and controllers. These undisposed resources accumulate across navigation, increasing memory usage and leaving background listeners that fire after the parent StatefulWidget is removed from the tree. {v4}
      dispose_widget_fields: true  # [WARNING] Field requires disposal but dispose method is missing or incomplete. Fields that need disposal are not disposed. This violates the widget lifecycle, risking setState-after-dispose errors or silent state corruption. {v2}
      function_always_returns_null: true  # [WARNING] Function returns null on every code path, making the return type effectively void. Callers that check or use the return value are performing dead logic, and the nullable return type misleads developers into thinking the function can return meaningful data. {v6}
      match_class_name_pattern: true  # [INFO] Class name does not follow expected pattern. Class names should follow naming conventions for their purpose. {v4}
      move_variable_closer_to_its_usage: true  # [INFO] Variable declared far from its first use, with many unrelated statements in between. This forces readers to hold the variable in memory while reading irrelevant code, reducing comprehension and increasing the risk of accidental reuse or shadowing. {v7}
      no_equal_arguments: true  # [WARNING] The same identifier is passed as multiple positional arguments to a function call, which typically indicates a copy-paste error where distinct values were intended (e.g., setPosition(x, x) instead of setPosition(x, y)). This silent bug produces incorrect behavior that passes compilation but yields wrong results at runtime. {v4}
      no_equal_conditions: true  # [WARNING] Same condition appears more than once in an if/else-if chain. The duplicate branch is unreachable because the first occurrence already handles all cases where the condition is true, making the repeated check dead code. {v5}
      no_equal_nested_conditions: true  # [WARNING] Inner condition is identical to an enclosing outer condition. The nested check is always true at that point because the outer condition already guarantees it, making the inner branch redundant dead logic that adds nesting depth without any behavioral effect. {v4}
      no_equal_then_else: true  # [WARNING] If and else branches contain identical code, so the condition has no effect on behavior. The branch structure misleads readers into thinking different paths produce different results, when in fact the outcome is the same regardless of the condition. {v4}
      nullify_after_dispose: true  # [INFO] Nullable disposable field must be set to null after disposal. When a nullable disposable field (Timer?, StreamSubscription?, etc.) is disposed/canceled, it's good practice to also set it to null. This: - Helps garbage collection - Prevents accidental reuse of disposed resources - Makes it clear the resource has been cleaned up. {v7}
      pass_correct_accepted_type: true  # [WARNING] Argument type does not match the parameter type declared by the @Accept annotation. Passing an incompatible type circumvents the annotation contract, which may cause runtime cast failures or incorrect behavior in the called function. {v4}
      prefer_abstraction_injection: true  # [INFO] Injecting concrete implementation. Prefer injecting abstract types for testability. {v2}
      prefer_action_button_tooltip: true  # [INFO] IconButton without a tooltip is inaccessible to screen reader users who cannot see the icon and have no text description of the button action. Tooltips also appear on long-press (mobile) and hover (desktop), providing discoverability for all users, not just those using assistive technology. {v2}
      prefer_actions_and_shortcuts: true  # [INFO] RawKeyboardListener requires manual key code matching and does not integrate with the Flutter intent system, making keyboard shortcuts invisible to accessibility tools and impossible to discover via keyboard shortcut overlays. The Actions/Shortcuts system provides composable, discoverable, and testable keyboard handling. {v6}
      prefer_adaptive_icons: true  # [INFO] Icon has a hardcoded size value. The default 24px icon size is optimized for phones but is too small on tablets and too large on watches or compact layouts. Hardcoded sizes ignore device form factor, accessibility settings (large text mode), and theme overrides. Use IconTheme to provide contextual sizing, or derive the size from MediaQuery or the surrounding layout constraints. {v1}
      prefer_adequate_spacing: true  # [INFO] Adjacent interactive buttons detected without spacing. Users may accidentally tap the wrong button, especially those with motor impairments or using touch interfaces where precision is limited. {v2}
      prefer_align_over_container: true  # [INFO] Container with only alignment creates unnecessary intermediate widgets. Align is a single-purpose widget with fewer allocations and faster layout computation. {v4}
      prefer_api_pagination: true  # [INFO] API fetches all items without pagination. May cause memory issues. Loading thousands of items at once is slow and memory-intensive. Use pagination with limit/offset or cursor-based pagination. {v2}
      prefer_asset_image_for_local: true  # [WARNING] FileImage reads from the device filesystem at runtime and is intended for user-generated content, not bundled assets. Using FileImage for assets bypasses Flutter asset resolution (density variants, locale fallbacks), is not supported on web, and fails if the file path does not exist on the target device. {v3}
      prefer_async_init_state: true  # [INFO] Using .then().setState() pattern in initState. Store the Future in a field and use FutureBuilder to manage loading states declaratively. {v2}
      prefer_avatar_loading_placeholder: true  # [INFO] CircleAvatar uses a network-loaded backgroundImage without providing onBackgroundImageError or a child widget as fallback. When the network image fails to load due to connectivity issues, invalid URLs, server errors, or slow connections, the avatar displays an empty or broken state with no visual feedback to the user. This creates a confusing UI where the user sees a blank circle instead of meaningful placeholder content like initials or a default icon. {v2}
      prefer_bloc_hydration: true  # [INFO] Bloc uses SharedPreferences for persistence. Consider using HydratedBloc for automatic state persistence. {v2}
      prefer_broadcast_stream: true  # [WARNING] Stream from StreamController is single-subscription. Multiple listeners will cause an error. {v2}
      prefer_cached_image_cache_manager: true  # [INFO] CachedNetworkImage without CacheManager. Cache may grow unbounded. Without a custom CacheManager, cached images can grow unbounded and consume excessive storage. Configure limits for production apps. {v2}
      prefer_change_notifier_proxy_provider: true  # [INFO] ChangeNotifierProvider.create accesses another provider. Use ChangeNotifierProxyProvider instead. {v2}
      prefer_clip_behavior: true  # [INFO] Widget does not specify clipBehavior explicitly. Default clipping wastes rendering performance when no overflow occurs, and obscures whether overflow is intentionally allowed or prevented. {v4}
      prefer_clipboard_feedback: true  # [INFO] Clipboard.setData should provide user feedback. Clipboard operations should provide user feedback (SnackBar, Toast, etc.) to confirm the action was successful. Without feedback, users won't know if the copy succeeded. {v2}
      prefer_const_widgets_in_lists: true  # [INFO] Widget list recreated on every rebuild. If elements are constant, the entire list can be const. This layout configuration can trigger RenderFlex overflow errors or unexpected visual behavior at runtime. {v4}
      prefer_constrained_box_over_container: true  # [INFO] Container with only constraints should use ConstrainedBox instead. Use ConstrainedBox widget when Container is only used for constraints. This makes the intent clearer and is more efficient. {v3}
      prefer_consumer_over_provider_of: true  # [INFO] Provider.of in build. Use Consumer for granular rebuilds. Provider.of<T>(context) is used in build method. This pattern increases maintenance cost and the likelihood of introducing bugs during future changes. {v2}
      prefer_correct_bloc_provider: true  # [INFO] Using context.read() in BlocProvider.create returns an existing bloc. Use BlocProvider.value instead. {v2}
      prefer_correct_for_loop_increment: true  # [INFO] Non-standard for loop increment (e.g., i += 2, i = i + 3) reduces readability and can hide off-by-one errors. Standard i++ makes the iteration pattern immediately recognizable to all developers. {v2}
      prefer_correct_type_name: true  # [WARNING] Type name is not UpperCamelCase. Nonstandard type names reduce code readability and break Dart conventions. Quick fix available: Adds a comment to flag for manual review. {v4}
      prefer_cursor_for_buttons: true  # [INFO] Interactive widget on web/desktop without a pointer cursor leaves the default arrow cursor, giving users no visual indication that the element is clickable. This violates web platform conventions and reduces discoverability of interactive elements, especially for GestureDetector-based custom buttons. {v6}
      prefer_data_masking: true  # [INFO] Unmasked sensitive data visible in UI and screenshots. Sensitive data (SSN, credit cards, passwords) must be partially masked when displayed to prevent shoulder surfing. {v4}
      prefer_define_hero_tag: true  # [WARNING] Hero widget without an explicit tag defaults to the widget itself, causing conflicts when multiple Hero widgets exist on the same screen. Duplicate tags trigger runtime assertion errors that crash the app during navigation transitions. {v4}
      prefer_delayed_permission_prompt: true  # [WARNING] Permission request detected in main() or initState(). Asking too early reduces acceptance rates. {v2}
      prefer_dispose_before_new_instance: true  # [WARNING] Reassigning without dispose leaks the old instance. Listeners and resources remain active forever. {v4}
      prefer_dropdown_initial_value: true  # [WARNING] The 'value' parameter on DropdownButtonFormField was deprecated in Flutter 3.35.0 (PR #170805) in favor of 'initialValue'. The old 'value' parameter conflicted with FormField.value and caused confusing behavior when set to null — for example, the selected value was not properly reset. The renamed 'initialValue' parameter has identical behavior but clearer semantics that it only sets the initial selection, not a controlled value. {v1}
      prefer_explicit_json_keys: true  # [INFO] Use @JsonKey for field name mapping. Manual key mapping in fromJson is error-prone and verbose. Use @JsonKey annotation with json_serializable for clarity. {v2}
      prefer_firebase_auth_persistence: true  # [INFO] Firebase Auth on web defaults to session persistence. Prefer setting LOCAL persistence. By default, Firebase Auth on web uses session persistence, meaning users are logged out when they close the browser tab. For "remember me" functionality, you need to explicitly set persistence to LOCAL. {v2}
      prefer_fit_cover_for_background: true  # [INFO] Background DecorationImage without BoxFit.cover may show letterboxing, stretching, or empty space around the image on different screen aspect ratios. BoxFit.cover ensures the image fills the entire container while maintaining its aspect ratio, which is the standard behavior for background images. {v3}
      prefer_foundation_platform_check: true  # [INFO] Use defaultTargetPlatform in widget code. In widget code, defaultTargetPlatform from foundation is safer and allows for testing overrides. Platform requires dart:io which doesn't exist on web. {v2}
      prefer_fractional_sizing: true  # [INFO] Use FractionallySizedBox for percentage-based sizing. FractionallySizedBox is cleaner for percentage-based layouts. Percentage-based sizing uses hardcoded calculations. {v5}
      prefer_freezed_for_data_classes: true  # [INFO] Data class with manual copyWith/equals. Consider using @freezed to eliminate boilerplate. {v2}
      prefer_go_router_extra_typed: true  # [INFO] go_router extra parameter passes a Map or dynamic value instead of a typed class. Untyped extras require unsafe casts at the destination route, causing runtime ClassCastException crashes when the map structure changes or keys are misspelled. {v2}
      prefer_hive_lazy_box: true  # [INFO] Consider using LazyBox for potentially large collections. Regular Box loads all entries into memory. {v3}
      prefer_hive_value_listenable: true  # [INFO] Manual setState after Hive changes is error-prone. Manually calling setState after Hive updates is error-prone. Use ValueListenableBuilder with box.listenable() for reactive updates. {v3}
      prefer_ignore_pointer: true  # [INFO] AbsorbPointer blocks underlying widgets - is IgnorePointer better? - AbsorbPointer: Absorbs events and prevents them from reaching widgets behind it. Use when you need to block underlying interactions. - IgnorePointer: Lets events pass through completely. Use when you just want to disable this widget's interaction. {v4}
      prefer_image_cropping: true  # [INFO] Profile/avatar image picked without cropping. Raw photos may have wrong aspect ratio for profile display. {v2}
      prefer_image_picker_multi_selection: true  # [INFO] pickImage in loop. Use pickMultiImage for batch selection. Use pickMultiImage instead of loop calling pickImage. This can cause resource exhaustion, performance degradation, or application instability. {v2}
      prefer_inkwell_over_gesture: true  # [INFO] GestureDetector with onTap provides no visual feedback when tapped, leaving users unsure whether their tap registered. InkWell produces the Material Design ripple effect that confirms interaction, improving perceived responsiveness and matching platform conventions users expect. {v4}
      prefer_intrinsic_dimensions: true  # [INFO] Use IntrinsicWidth/Height for content-based sizing. IntrinsicWidth/Height can help with sizing widgets to their content. IntrinsicWidth/Height could improve layout. {v5}
      prefer_ios_safe_area: true  # [INFO] Scaffold body without SafeArea may have content hidden by iOS notch or Dynamic Island. {v2}
      prefer_json_serializable: true  # [INFO] Data class with manual JSON serialization. Manual parsing is error-prone and hard to maintain. {v2}
      prefer_keep_alive: true  # [INFO] Use AutomaticKeepAliveClientMixin to preserve state. Without AutomaticKeepAliveClientMixin, tab content is rebuilt when switching tabs, losing scroll position and state. {v5}
      prefer_large_touch_targets: true  # [WARNING] Touch target is smaller than 48px. This makes it difficult for users with motor impairments. {v2}
      prefer_late_final: true  # [INFO] Late variable is never reassigned after its initial assignment, so it can be declared as late final for stronger immutability guarantees. {v3}
      prefer_listview_builder: true  # [WARNING] Use ListView.builder to improve performance. This layout configuration can trigger RenderFlex overflow errors or unexpected visual behavior at runtime. {v3}
      prefer_marker_clustering: true  # [INFO] Many markers cause frame drops and memory issues. Displaying hundreds of markers individually causes performance issues. Use marker clustering to improve performance and UX. {v3}
      prefer_matcher_over_equals: true  # [INFO] Use matchers instead of direct equality to improve messages. Matchers provide better error messages and more expressive tests. Expect uses equality instead of matchers. {v7}
      prefer_merge_semantics: true  # [INFO] Row or Column containing Icon and Text detected without MergeSemantics wrapper. Screen readers will announce each element separately instead of as a single cohesive label, forcing users to piece together fragmented information. {v3}
      prefer_mock_http: true  # [WARNING] Use mock HTTP client in tests instead of real network calls. Real network calls make tests slow and flaky. This weakens test quality, making failures harder to diagnose and reducing confidence in the test suite. {v5}
      prefer_multi_bloc_provider: true  # [INFO] Nested BlocProviders should use MultiBlocProvider instead. Use MultiBlocProvider when providing multiple blocs to reduce nesting and improve readability. {v2}
      prefer_multi_provider: true  # [INFO] Nested Providers should use MultiProvider instead. Use MultiProvider when providing multiple objects to reduce nesting and improve readability. Nested Provider widgets are used. {v2}
      prefer_number_format: true  # [INFO] Number displayed without locale formatting. Different locales use different decimal/grouping separators. {v2}
      prefer_on_field_submitted: true  # [INFO] Form field must have onFieldSubmitted to handle keyboard action. This callback fires when the user presses the keyboard action button (e.g., Done, Next). Add it to move focus to the next field or submit the form for a smooth keyboard-driven workflow. {v3}
      prefer_on_pop_with_result: true  # [WARNING] The 'onPop' callback on Route and PopNavigatorRouterDelegateMixin was deprecated in Flutter 3.35.0 (PR #169700) in favor of 'onPopWithResult'. The deprecated 'onPop' callback does not receive the pop result value, which means callers cannot inspect or react to data returned by the popped route. The replacement 'onPopWithResult' provides the result as a parameter, enabling proper data flow between routes. {v1}
      prefer_overriding_parent_equality: true  # [WARNING] Subclass overrides == without incorporating the parent class equality check. If the parent class compares fields that the subclass ignores, two objects may be considered equal even though their parent fields differ, breaking the transitivity contract of ==. {v4}
      prefer_padding_over_container: true  # [INFO] Container with only padding creates unnecessary intermediate widgets (DecoratedBox, ConstrainedBox). Padding is a single-purpose widget with fewer allocations and faster layout. {v4}
      prefer_page_storage_key: true  # [INFO] Use PageStorageKey to preserve scroll position. This layout configuration can trigger RenderFlex overflow errors or unexpected visual behavior at runtime. {v5}
      prefer_positioned_directional: true  # [INFO] Use PositionedDirectional for RTL support. PositionedDirectional respects text direction for RTL languages. Positioned is used instead of PositionedDirectional. {v3}
      prefer_ref_watch_over_read: true  # [WARNING] ref.read in build() won't rebuild widget when provider changes. ref.read doesn't subscribe to changes - widget won't rebuild when provider updates. Use ref.watch in build methods for reactive updates. {v2}
      prefer_regex_validation: true  # [INFO] Form field with basic empty check but label suggests structured data (email/phone/url). Use regex validation. {v2}
      prefer_return_await: true  # [INFO] Returning a Future directly from an async function skips error propagation and stack trace preservation, making debugging harder. This can hide the source of exceptions and complicate error handling. {v6}
      prefer_riverpod_family_for_params: true  # [INFO] Provider uses nullable state for parameterized data. Use .family instead. Using state to pass parameters to providers is error-prone. The .family modifier provides type-safe parameter passing. {v2}
      prefer_route_settings_name: true  # [INFO] MaterialPageRoute without RouteSettings.name. Analytics and debugging will be harder. {v2}
      prefer_safe_area_aware: true  # [INFO] Content may overlap device notch or system UI. This layout configuration can trigger RenderFlex overflow errors or unexpected visual behavior at runtime. {v4}
      prefer_scalable_text: true  # [INFO] Text uses a fixed font size and does not respect user accessibility settings. This prevents users who rely on larger text for readability from accessing your content. All text should scale with the system text scale factor for accessibility compliance. {v2}
      prefer_setup_teardown: true  # [INFO] Duplicated test setup code. Use setUp()/tearDown(). Repeated setup code in tests violates DRY and makes maintenance harder. Use setUp() and tearDown() for common test initialization. {v6}
      prefer_shared_prefs_async_api: true  # [INFO] Legacy SharedPreferences.getInstance() detected. Consider using SharedPreferencesAsync for new code. {v2}
      prefer_simpler_boolean_expressions: true  # [INFO] Boolean expression contains redundant negations or can be reduced using De Morgan's law. Complex negated compound expressions are harder to read and more error-prone during modification than their simplified equivalents. {v3}
      prefer_single_container: true  # [INFO] Nested decoration widgets could be a single Container. Using nested Padding, DecoratedBox, etc. is verbose when Container provides all these features in one widget. {v2}
      prefer_single_setstate: true  # [INFO] Multiple setState calls cause unnecessary rebuilds. Multiple setState calls are made in the same method. This increases build() cost, causing unnecessary widget rebuilds that degrade scroll performance. {v2}
      prefer_sized_box_for_whitespace: true  # [INFO] Container creates unnecessary intermediate widgets (DecoratedBox, ConstrainedBox) when used only for whitespace. SizedBox is a single lightweight widget with fewer allocations and faster layout. {v3}
      prefer_skeleton_over_spinner: true  # [INFO] CircularProgressIndicator for content loading. Prefer skeleton loaders. This pattern increases maintenance cost and the likelihood of introducing bugs during future changes. {v2}
      prefer_sliver_app_bar: true  # [INFO] Use SliverAppBar inside CustomScrollView, not AppBar. SliverAppBar integrates with CustomScrollView for scroll-based effects like collapsing, floating, and pinning. {v5}
      prefer_sliver_list: true  # [WARNING] Use SliverList instead of ListView inside CustomScrollView. Using ListView inside CustomScrollView creates nested scrollables. Use SliverList for proper sliver composition. {v6}
      prefer_specific_cases_first: true  # [WARNING] General switch case appears before a more specific case. In Dart, the first matching case wins, so a broad pattern placed early shadows narrower patterns below it, making those specific cases unreachable dead code. {v4}
      prefer_spring_animation: true  # [INFO] CurvedAnimation with a physics-like curve (bounceOut, elasticOut, bounceIn, elasticIn, etc.) is being used where a SpringSimulation would produce smoother, more natural-feeling motion. CurvedAnimation uses a fixed duration that cannot respond to user input velocity, causing disconnects between gesture speed and animation behavior that feel artificial and jarring to users. {v2}
      prefer_sqflite_column_constants: true  # [INFO] String literal column name may contain typos. String literals for column names are error-prone. Use constants for compile-time checking. This file handling can cause data corruption, resource leaks, or permission errors. {v2}
      prefer_stream_distinct: true  # [INFO] Stream.listen() without .distinct() may process duplicate values unnecessarily. {v3}
      prefer_system_theme_default: true  # [INFO] Hardcoded ThemeMode ignores user's OS dark mode preference. Using ThemeMode.light or ThemeMode.dark ignores user's OS preference. Default to ThemeMode.system to respect user settings, with option to override. {v3}
      prefer_test_find_by_key: true  # [INFO] find.byType() is fragile. Use find.byKey() for reliable widget testing. Using find.byType can be fragile when widget structure changes. Using find.byKey with ValueKey or Key is more reliable and explicit. {v3}
      prefer_test_wrapper: true  # [INFO] Widget test should wrap with MaterialApp/CupertinoApp. Most widgets need MaterialApp ancestor for theming and localization. Note: This rule ignores teardown patterns where simple widgets like SizedBox, Container, or Placeholder are pumped to unmount the widget tree before disposal. {v4}
      prefer_text_input_action: true  # [INFO] TextField without textInputAction. Keyboard action button unclear. Setting textInputAction helps users navigate forms efficiently with the keyboard action button (Done, Next, Search, etc.). {v2}
      prefer_transaction_for_batch: true  # [INFO] Multiple sequential database writes. Use transaction for atomicity. Individual writes are slower and can leave data inconsistent. Use transactions or batch writes for multiple related changes. {v3}
      prefer_transform_over_container: true  # [INFO] Container with only transform must be a Transform. When Container only has a transform property, use Transform widget instead to improve semantics and performance. {v2}
      prefer_tween_sequence: true  # [INFO] Multiple chained animations are implemented without using TweenSequence, which can lead to complex, error-prone code and unpredictable animation timing. TweenSequence simplifies sequential animations, making them easier to manage and ensuring smooth transitions. Use TweenSequence for sequential property changes to improve maintainability and user experience. {v2}
      prefer_typed_route_params: true  # [INFO] Route parameter from pathParameters or queryParameters is passed directly without type conversion. All route parameters are strings at runtime, and passing them where an int, double, or bool is expected causes type mismatch errors or silent data corruption. {v3}
      prefer_url_launcher_fallback: true  # [INFO] launchUrl called without a fallback. If the scheme is unsupported, the user gets no feedback and cannot complete the action. This leads to user frustration and failed actions. {v2}
      prefer_using_list_view: true  # [WARNING] Column inside SingleChildScrollView. A "Column" is being used inside a "SingleChildScrollView" Flutter to pre-render the entire list, bypassing "Lazy Loading" optimizations. This layout configuration can also trigger RenderFlex overflow errors or unexpected visual behavior at runtime. {v6}
      prefer_value_listenable_builder: true  # [INFO] Simple single-value state managed with setState causes the entire widget subtree to rebuild on every change. ValueListenableBuilder isolates rebuilds to only the affected subtree, significantly reducing unnecessary widget tree comparisons, improving frame rendering performance, and lowering battery consumption. {v3}
      prefer_video_loading_placeholder: true  # [INFO] Video player must have a loading placeholder. Video widgets should show a placeholder while loading to provide visual feedback and prevent layout shifts. {v2}
      prefer_where_or_null: true  # [INFO] Using firstWhere/lastWhere/singleWhere with an orElse callback is verbose and harder to read. The *OrNull variant from package:collection combined with ?? produces cleaner, more idiomatic Dart code. {v4}
      prefer_wrap_over_overflow: true  # [INFO] Row with many children may overflow - Use Wrap. DefaultTextStyle provides consistent typography without repeating styles. Text widgets are not wrapped with DefaultTextStyle. {v5}
      proper_getx_super_calls: true  # [ERROR] Omitting a call to super in GetxController lifecycle methods (onInit, onReady, onClose) breaks the controller lifecycle, causing incomplete initialization, missed cleanup, and unpredictable behavior. This can lead to memory leaks, resource retention, and subtle bugs that are hard to diagnose. {v2}
      require_accessible_images: true  # [WARNING] Image element lacks accessibility handling, making it invisible to screen readers and users with visual impairments. Without a semanticLabel describing the image content, assistive technologies skip over it entirely, leaving users unable to understand the visual context. This violates WCAG 1.1.1 (Non-text Content), which requires all meaningful images to have a text alternative. {v2}
      require_analytics_error_handling: true  # [INFO] Analytics method call without error handling. Analytics SDKs (Firebase Analytics, Mixpanel, Amplitude) can throw when the SDK is not initialized, the network is unavailable, or event parameters are invalid. A failed analytics call should never crash the app or interrupt user workflows — wrap analytics calls in try-catch and silently log failures instead. {v1}
      require_android_12_splash: true  # [INFO] Flutter splash screen widget detected. Android 12+ shows system splash first, causing potential double-splash. {v2}
      require_app_lifecycle_handling: true  # [INFO] Timer or subscription detected without lifecycle handling. Stop background work when app is inactive to save battery. {v4}
      require_async_value_order: true  # [INFO] AsyncValue.when() has non-standard parameter order. The standard order is data, error, loading. Incorrect order makes code harder to read and may indicate confusion about the API. {v2}
      require_auth_check: true  # [WARNING] Missing auth check allows unauthorized access to protected user data and privileged operations. {v5}
      require_auto_dispose: true  # [ERROR] Not using autoDispose with Riverpod providers can cause memory leaks, as providers and their resources may remain in memory after they are no longer needed. This can lead to increased memory usage and degraded app performance. {v2}
      require_autofill_hints: true  # [INFO] Form field must have autofillHints to improve user experience. The autofillHints parameter enables system autofill to help users complete forms faster. This improves UX especially for common fields like email, password, name, and address. {v2}
      require_avatar_alt_text: true  # [WARNING] Avatar or profile image is missing a semanticLabel or alt text. Screen readers cannot describe the image, making it inaccessible to users who rely on assistive technology. All meaningful images must have a descriptive label. {v2}
      require_avatar_fallback: true  # [WARNING] CircleAvatar with NetworkImage fails silently when image load fails. Users will see a broken or blank avatar with no indication of the error, leading to confusion, poor UX, and missed identity cues. This can also mask backend or connectivity issues during development. {v3}
      require_badge_count_limit: true  # [INFO] Badge count exceeds 99. Use "99+" pattern for large numbers. Large numbers in badges are hard to read and look unprofessional. The convention is to show "99+" for counts above 99. {v2}
      require_badge_semantics: true  # [WARNING] Badge or status indicator is missing accessibility semantics. Without a semantic label, screen readers cannot announce the badge count or its meaning to users with visual impairments, hiding important notification or status information from a significant portion of your users. {v4}
      require_ble_disconnect_handling: true  # [WARNING] BLE connections must handle disconnect events to maintain app stability and resource management. Ignoring disconnects can lead to stale UI (showing devices as connected when they are not), resource leaks (unreleased connections or streams), and user confusion when devices unexpectedly disappear or fail to reconnect. This can also cause battery drain and degraded reliability, especially in apps that manage multiple devices. {v3}
      require_bloc_initial_state: true  # [ERROR] Missing initial state throws LateInitializationError when BlocBuilder tries to read state. {v1}
      require_bluetooth_state_check: true  # [WARNING] Bluetooth Low Energy (BLE) operations must check the adapter state before attempting connections, scans, or service discovery. Failing to check adapter state can result in failed connections, wasted battery due to repeated attempts, degraded user experience, and hard-to-debug errors—especially on devices where Bluetooth is disabled or unavailable. This can also cause your app to be rejected during app store review for reliability issues. {v3}
      require_button_loading_state: true  # [INFO] Button triggering an async operation without a loading state indicator leaves users unsure whether their tap was registered. They may tap again (causing duplicate requests), navigate away (abandoning the operation), or assume the app is frozen. A loading state provides essential feedback for async interactions. {v5}
      require_button_semantics: true  # [WARNING] Custom interactive elements (e.g., GestureDetector, InkWell) are missing Semantics with button: true. Without proper semantics, assistive technologies cannot identify these child elements as buttons, making them inaccessible to screen reader users who rely on the widget tree for navigation. {v2}
      require_cache_expiration: true  # [WARNING] Cache implementation lacks expiration logic. Caches without TTL serve stale data indefinitely. Implement expiration to ensure data freshness. Unreleased memory grows over time, increasing garbage collection pressure and risking out-of-memory crashes. {v2}
      require_cached_image_error_widget: true  # [INFO] CachedNetworkImage without errorWidget. Broken images show nothing. Network images can fail. Always provide fallback for broken images. CachedNetworkImage is used without error widget. {v2}
      require_cached_image_placeholder: true  # [INFO] CachedNetworkImage without placeholder. User sees blank during load. Placeholders provide visual feedback while the image loads. This image handling causes excessive memory usage, visual artifacts, or slow load times. {v2}
      require_calendar_timezone_handling: true  # [WARNING] device_calendar Event is missing an explicit timeZone. This can cause events to appear at the wrong time for users in different time zones, leading to missed or misaligned appointments. {v2}
      require_change_notifier_dispose: true  # [ERROR] Failing to dispose a ChangeNotifier (or ValueNotifier, etc.) in the dispose() method causes it to retain all listeners and references in the widget tree, resulting in memory leaks and potential crashes when notifications are sent to unmounted widgets. This is a common source of subtle bugs and degraded performance in Flutter apps. {v4}
      require_clipboard_paste_validation: true  # [INFO] Clipboard data used without validation. Pasted content can be unexpected format or malicious. Validate clipboard data before using it. This creates a security vulnerability that attackers can exploit to compromise user data or application integrity. {v2}
      require_connectivity_error_handling: true  # [WARNING] Connectivity check without error handling. checkConnectivity() can throw platform exceptions. {v2}
      require_connectivity_subscription_cancel: true  # [ERROR] If you do not cancel your connectivity subscription, your app will leak memory and system resources. This can cause crashes, slowdowns, and unpredictable behavior, especially after repeated hot reloads or navigation. {v3}
      require_copy_with_null_handling: true  # [WARNING] copyWith with ?? operator cannot set nullable fields to null. Standard copyWith pattern can't distinguish between "not provided" and "explicitly null". Use a sentinel value or wrapper class to support setting nullable fields back to null. {v2}
      require_currency_code_with_amount: true  # [INFO] Money amount without currency information. Amounts without currency are ambiguous. Always pair amounts with currency codes. This monetary calculation can produce rounding errors that accumulate, causing financial discrepancies. {v2}
      require_currency_formatting_locale: true  # [INFO] NumberFormat.currency() without a locale can format currency inconsistently for users. Currency formatting depends heavily on locale. Without explicit locale, the format may not match user expectations. {v2}
      require_dark_mode_testing: true  # [WARNING] MaterialApp missing darkTheme. App won't adapt to dark mode. Apps should support dark mode for accessibility and user preference. Without darkTheme, the app won't adapt when the user enables dark mode. {v2}
      require_database_close: true  # [WARNING] Unclosed database connection leaks resources and may exhaust connection pool, causing app failures. {v6}
      require_database_index: true  # [INFO] Query on non-indexed field causes full table scan. Queries on non-indexed fields are slow. Add indices for fields used in where clauses, especially in large collections. {v4}
      require_debouncer_cancel: true  # [ERROR] Uncanceled debounce timer fires after dispose, calling setState on unmounted widget causing crashes. {v1}
      require_default_text_style: true  # [INFO] Multiple sibling Text widgets repeat the same TextStyle, creating redundant style objects and making future style changes error-prone since each instance must be updated independently. DefaultTextStyle applies a shared style to all descendant Text widgets automatically, keeping the code DRY and consistent. {v5}
      require_deprecation_message: true  # [INFO] Deprecated annotation should include migration guidance. Missing documentation makes the API harder to use correctly and increases onboarding time. {v6}
      require_dialog_barrier_consideration: true  # [INFO] Destructive confirmation dialog (delete, remove, logout) defaults to barrierDismissible: true, allowing users to accidentally dismiss the dialog by tapping outside. This can silently skip the confirmation step, or worse, leave the user unsure whether the action was confirmed or canceled. {v2}
      require_dialog_barrier_dismissible: true  # [INFO] Default barrier behavior may allow accidental dismissal, losing unsaved user input. {v3}
      require_dialog_tests: true  # [INFO] Dialog test may be incomplete. Ensure pumpAndSettle after showing dialog. Dialogs require special handling: tap to open, find within dialog context, test dismiss behavior. Don't forget barrier dismiss tests. {v2}
      require_did_update_widget_check: true  # [INFO] didUpdateWidget triggers updates without checking if properties changed, causing unnecessary rebuilds. didUpdateWidget receives the old widget for comparison. Without comparing oldWidget to widget, you might trigger unnecessary updates. {v3}
      require_dio_response_type: true  # [WARNING] Dio download without explicit responseType may corrupt binary data. Dio defaults responseType to JSON, which causes issues when downloading files or handling binary responses. {v2}
      require_disabled_state: true  # [INFO] Button without custom disabled styling uses the framework default gray appearance, which may not match your app design system or provide sufficient contrast. Customizing the disabled state ensures visual consistency, communicates the disabled status clearly, and maintains your brand identity across all button states. {v5}
      require_dispose_implementation: true  # [WARNING] Controllers, subscriptions, or timers without cleanup cause memory leaks and setState errors. {v3}
      require_dispose_pattern: true  # [WARNING] Class has StreamController, AnimationController, or other disposable fields but no cleanup method. These controllers leak memory and can crash when accessed after disposal. {v4}
      require_drag_feedback: true  # [INFO] Draggable without a feedback widget shows no visual representation of the dragged item under the user finger/cursor. The original child disappears (replaced by childWhenDragging or nothing) while the drag area appears empty, making it impossible for the user to see what they are dragging or where to drop it. {v4}
      require_empty_results_state: true  # [INFO] List with search-related name missing empty state check. Search results should show a helpful message when no results are found. Empty ListView/GridView with no indicator confuses users. {v2}
      require_envied_obfuscation: true  # [WARNING] Envied environment variable generated without obfuscation stores secrets as plaintext string constants in the compiled binary. Attackers can extract API keys, database URLs, and authentication tokens using basic reverse engineering tools, enabling unauthorized access to your backend services and third-party APIs. {v2}
      require_equatable_copy_with: true  # [INFO] Equatable class lacks copyWith method. Without copyWith, creating modified copies requires manually constructing new instances with all fields, leading to verbose code and errors when fields are added or removed from the class. {v2}
      require_equatable_props_override: true  # [ERROR] Without props override, equality defaults to identity comparison, breaking state deduplication. {v1}
      require_error_case_tests: true  # [INFO] Test file has no error case tests. Consider adding tests for exceptions. {v4}
      require_feature_flag_type_safety: true  # [INFO] Feature flag accessed with a raw string literal key. String-based lookups are error-prone: typos compile successfully but fail silently at runtime, renames require a fragile codebase-wide search-and-replace, and there is no compile-time guarantee the flag name exists. {v1}
      require_file_exists_check: true  # [INFO] File read operation should check exists() or use try-catch. File operations on non-existent files throw exceptions. Always verify the file exists or wrap in try-catch to handle missing files gracefully. {v2}
      require_firebase_error_handling: true  # [WARNING] Firebase operation without error handling. Firebase calls can fail. {v2}
      require_flutter_riverpod_not_riverpod: true  # [ERROR] Flutter apps should use flutter_riverpod, not riverpod package directly. {v2}
      require_flutter_riverpod_package: true  # [ERROR] Base riverpod package lacks Flutter widgets (ProviderScope, ConsumerWidget), causing errors. {v1}
      require_form_auto_validate_mode: true  # [INFO] Form should specify autovalidateMode for consistent UX. Forms should specify how validation is triggered for consistent UX. The autovalidateMode parameter controls when the form fields validate. {v2}
      require_form_validation: true  # [WARNING] TextFormField inside a Form without a validator function bypasses the form validation pipeline entirely. Calling formKey.currentState.validate() will always return true for this field, allowing invalid or empty input to reach the backend, causing data integrity issues and poor user feedback. {v3}
      require_freezed_arrow_syntax: true  # [ERROR] The fromJson factory in a @freezed class must use arrow syntax (=>) to ensure correct code generation. Using block syntax ({}) breaks the generated code, causing runtime errors, missing serialization, and build_runner failures. This leads to broken deserialization and hard-to-debug bugs in your models. {v2}
      require_freezed_lint_package: true  # [INFO] File uses Freezed. Add freezed_lint package for specialized linting. Heuristic warning: This rule cannot verify if freezed_lint is in your pubspec.yaml. It simply reminds developers using Freezed to Add freezed_lint to their dev_dependencies. Disable this rule if you have already installed freezed_lint. {v2}
      require_freezed_private_constructor: true  # [ERROR] Missing private constructor breaks code generation, causing build_runner to fail. {v2}
      require_getx_lazy_put: true  # [INFO] Consider using Get.lazyPut() for controllers that may not be needed immediately. This improves startup performance. {v2}
      require_go_router_error_handler: true  # [INFO] GoRouter instance lacks an errorBuilder or errorPageBuilder parameter. When a user navigates to an undefined path via deep link, push notification, or typo, the router displays a blank white screen with no explanation or way to recover. {v3}
      require_go_router_fallback_route: true  # [ERROR] GoRouter configuration without errorBuilder or errorPageBuilder has no fallback for unmatched routes. When users navigate to a non-existent path via deep link, push notification, or typo, the router throws an unhandled exception that crashes the app instead of showing a helpful error page. {v2}
      require_golden_test: true  # [INFO] Add golden test for visual verification. Golden tests catch visual regressions automatically. Visual widgets must have golden tests. This weakens test quality, making failures harder to diagnose and reducing confidence in the test suite. {v3}
      require_google_fonts_fallback: true  # [WARNING] GoogleFonts should specify fontFamilyFallback. Google Fonts may fail to load on slow connections or offline. Without a fallback, text may be invisible or use system default unexpectedly. {v2}
      require_google_signin_error_handling: true  # [WARNING] Google Sign-In call without error handling crashes when the user cancels the sign-in flow, the network is unavailable, or Google Play Services are outdated. Users see an unhandled exception crash screen instead of a friendly error message, causing frustration and potential data loss in unsaved work. {v3}
      require_graphql_error_handling: true  # [WARNING] GraphQL query and mutation results must check hasException before accessing data. Failing to check for errors can cause runtime crashes, display incomplete or misleading data, and make debugging difficult. This is a common source of silent failures and poor user experience in apps using GraphQL. {v3}
      require_graphql_operation_names: true  # [INFO] Defining anonymous GraphQL queries, mutations, or subscriptions (without an operation name) makes debugging, error tracking, and persisted queries more difficult. Operation names are essential for identifying and managing GraphQL operations in large codebases and production environments. {v2}
      require_hero_tag_uniqueness: true  # [ERROR] Using duplicate Hero tags within the same navigation context causes Hero animations to fail, resulting in visual glitches and confusing user experiences. This can break navigation transitions and reduce the perceived quality of your app. Ensure each Hero tag is unique within a given Navigator to maintain smooth and predictable animations. See https://docs.flutter.dev/ui/animations/hero-animations#the-hero-tag. {v3}
      require_hive_field_default_value: true  # [WARNING] @HiveField on nullable field without defaultValue. This can cause existing data to fail to load, trigger runtime exceptions, and break migrations. Missing defaults may result in silent data loss or corrupted records after schema changes. {v2}
      require_hover_states: true  # [INFO] Interactive widget on web/desktop without hover feedback feels unresponsive to mouse users. Hover states are a fundamental interaction pattern on these platforms, signaling clickability and providing visual confirmation that the cursor is over the target. Missing hover states make the app feel like a mobile port rather than a native desktop experience. {v6}
      require_http_status_check: true  # [WARNING] HTTP response body is used without first checking the status code. This can result in undetected failures, silent data corruption, or security issues if error responses are parsed as valid data. Always check if (response.statusCode == 200) before parsing response.body to ensure only successful responses are processed and errors are handled appropriately. {v5}
      require_https_over_http: true  # [ERROR] HTTP transmits data in plain text. Attackers can intercept credentials, tokens, and user data. {v1}
      require_image_description: true  # [WARNING] Image without semanticLabel is announced as "image" by screen readers, providing no useful information. {v2}
      require_image_error_builder: true  # [WARNING] Network image without errorBuilder shows a broken image icon or blank space when the URL is invalid, the server is down, or the network is unavailable. This creates a poor user experience with no indication of what went wrong and no way to retry loading the image. {v3}
      require_image_loading_placeholder: true  # [INFO] Image.network without loadingBuilder shows blank space during load. Network images take time to download. Without a loading indicator, users see an empty space which creates a poor user experience. {v4}
      require_image_picker_permission_android: true  # [WARNING] Missing CAMERA permission causes SecurityException crash when user tries to take a photo. {v3}
      require_image_picker_permission_ios: true  # [WARNING] Missing Info.plist entries cause app rejection by App Store or instant crash when accessing photos. {v3}
      require_image_semantics: true  # [WARNING] Image constructor lacks a semanticLabel parameter. Screen readers cannot describe this image to users with visual impairments, making the content inaccessible. This violates WCAG 1.1.1 (Non-text Content) and may exclude your app from accessibility-mandated markets. {v6}
      require_integration_test_setup: true  # [WARNING] Integration test missing IntegrationTestWidgetsFlutterBinding.ensureInitialized(). Integration tests need IntegrationTestWidgetsFlutterBinding.ensureInitialized() in main(). Without it, tests hang or crash on device. {v2}
      require_integration_test_timeout: true  # [WARNING] Integration test without timeout. May hang CI indefinitely. Long-running tests without timeouts can hang CI indefinitely. Always set timeouts on integration tests to catch infinite loops. {v2}
      require_interval_timer_cancel: true  # [ERROR] Timer.periodic without cancel(). Timer keeps firing after State disposal, draining battery and causing setState errors. {v2}
      require_intl_currency_format: true  # [WARNING] Currency value formatted manually using string interpolation with currency symbols. Symbol placement (prefix vs suffix), decimal separator (period vs comma), and digit grouping vary by locale. Manual formatting produces incorrect output for international users, causing financial confusion and misread amounts that undermine trust. {v3}
      require_intl_date_format_locale: true  # [WARNING] DateFormat created without an explicit locale parameter. The format output varies unpredictably across devices and platforms because each uses a different system default locale. Users in different regions see dates in unexpected formats (e.g., MM/DD/YYYY vs DD/MM/YYYY), leading to confusion about whether 01/02 means January 2nd or February 1st. {v2}
      require_ios_accessibility_large_text: true  # [INFO] Hardcoded font size may not respect iOS Dynamic Type. Use theme text styles for accessibility. {v2}
      require_ios_app_group_capability: true  # [INFO] App extension data sharing detected. Ensure App Groups capability is enabled in Xcode for both main app and extensions. {v2}
      require_ios_app_review_prompt_timing: true  # [WARNING] App review request detected in initialization context. Do not request reviews on first launch or during startup. {v2}
      require_ios_callkit_integration: true  # [WARNING] VoIP call handling detected. iOS requires CallKit integration for native call UI. Without CallKit, incoming calls will not appear on the lock screen, call audio routing will fail, and Apple will reject your app from the App Store during review. {v4}
      require_ios_deployment_target_consistency: true  # [WARNING] API requiring iOS 15+ detected. Ensure minimum deployment target matches or add version guards. {v2}
      require_ios_dynamic_island_safe_zones: true  # [WARNING] Fixed top padding (44pt or 59pt) detected. Dynamic Island height varies by device. Use MediaQuery.padding.top instead. {v2}
      require_ios_low_power_mode_handling: true  # [INFO] Heavy animation or background activity detected. Consider checking iOS Low Power Mode and adapting behavior. {v2}
      require_ios_method_channel_cleanup: true  # [WARNING] MethodChannel handler set without cleanup. Set handler to null in dispose() to prevent memory leaks. {v2}
      require_ios_minimum_version_check: true  # [INFO] iOS version-specific API detected. Ensure iOS version is checked before using this API. {v2}
      require_ios_multitasking_support: true  # [INFO] Fixed layout detected. iPads support Split View and Slide Over. Layouts should adapt to window size changes. {v2}
      require_ios_nfc_capability_check: true  # [WARNING] NFC usage detected. Not all iOS devices support NFC. Check capability before use. {v2}
      require_ios_photo_library_add_usage: true  # [WARNING] Photo saving detected. iOS requires NSPhotoLibraryAddUsageDescription for saving photos (separate from read permission). {v2}
      require_ios_photo_library_limited_access: true  # [INFO] Photo library access detected. Handle iOS 14+ limited access mode where user may only grant access to selected photos. {v2}
      require_ios_push_notification_capability: true  # [INFO] Push notification usage detected. Ensure Push Notifications capability is enabled in Xcode and APNs is configured. {v3}
      require_isar_collection_annotation: true  # [ERROR] This class is missing the @collection annotation, so Isar will not generate an adapter for it. As a result, any attempt to persist or query this type will fail at runtime, and your build will break with missing adapter errors. {v1}
      require_isar_id_field: true  # [ERROR] Every Isar collection must have an "Id? id" field as the primary key. Without it, Isar cannot uniquely identify records, and code generation will fail, causing your build to break and all database operations to be unusable. {v1}
      require_isar_nullable_field: true  # [ERROR] Isar TypeAdapters bypass constructors during hydration. Non-nullable fields trigger fatal TypeErrors when encountering NULL values from legacy disk records created in previous app versions where the field did not exist. {v3}
      require_json_decode_try_catch: true  # [WARNING] jsonDecode throws on malformed JSON. Unhandled exceptions can crash the app, cause silent data loss, and make debugging difficult. This is a common source of runtime errors in networked and user-input scenarios. {v2}
      require_key_for_collection: true  # [WARNING] List items in dynamic collections (ListView, GridView, etc.) must have a Key to preserve child widget state (e.g., TextField input, animations) when the list reorders or updates. Missing keys can cause UI bugs, loss of user input, broken animations, and confusing user experiences. This is a common source of hard-to-debug Flutter widget tree issues. {v4}
      require_keyboard_action_type: true  # [INFO] Text field must have textInputAction to improve UX. The textInputAction parameter controls the keyboard action button (e.g., Next, Done). This improves form navigation and UX. {v2}
      require_keyboard_dismiss_on_scroll: true  # [INFO] Scroll view must have keyboardDismissBehavior for form UX. ScrollViews containing text fields should specify how the keyboard dismisses when the user scrolls. This provides better UX for forms in scrollable content. {v2}
      require_keyboard_visibility_dispose: true  # [WARNING] Uncanceled subscription keeps firing callbacks after dispose, causing setState errors. {v2}
      require_location_timeout: true  # [WARNING] Location request without timeout can hang indefinitely if GPS is unavailable, freezing the app. {v3}
      require_logout_cleanup: true  # [WARNING] Incomplete logout cleanup leaves session tokens, cached user data, and authentication state accessible in local storage. On shared or stolen devices, the next user can access the previous account, personal data, and session tokens without re-authenticating, enabling unauthorized account access. {v4}
      require_long_press_callback: true  # [INFO] Interactive list item without onLongPress misses a standard mobile interaction pattern for secondary actions. Users expect long-press to reveal context menus, selection mode, or additional options. Omitting this leaves no discoverable path to bulk actions like delete, share, or move. {v3}
      require_mock_verification: true  # [INFO] Mock is stubbed but never verified. Creating mocks without verification means the test doesn't check behavior. This weakens test quality, making failures harder to diagnose and reducing confidence in the test suite. {v4}
      require_network_status_check: true  # [INFO] `[HEURISTIC]` Network call without connectivity check. Verify network status before making requests to avoid silent failures. {v2}
      require_notification_for_long_tasks: true  # [WARNING] Long-running operation detected without progress notification. Silent background work may be killed by OS. {v2}
      require_notification_handler_top_level: true  # [ERROR] If your notification handler is not a top-level function, background messages will be silently dropped when the app is terminated or in the background. Users will miss important notifications and your app may fail compliance checks. {v3}
      require_notify_listeners: true  # [WARNING] ChangeNotifier method modifies state properties but does not call notifyListeners(). Widgets listening to this notifier will not rebuild to reflect the updated state, displaying stale data to the user. This creates silent data synchronization bugs that are difficult to diagnose because the state appears correct in debug tools. {v5}
      require_null_safe_extensions: true  # [WARNING] Extension method on a nullable type does not handle null receivers. This can cause runtime exceptions. Extension methods on nullable types should check for null. {v4}
      require_null_safe_json_access: true  # [ERROR] Accessing values from a JSON map without checking for key existence or null values can throw exceptions (such as NoSuchMethodError or TypeError) if the key is missing or the value is null. This is a common source of runtime crashes and unstable code, especially when dealing with data from APIs, user input, or external sources. Null-safe access is essential for robust, production-quality Dart and Flutter applications. {v4}
      require_number_format_locale: true  # [WARNING] NumberFormat without explicit locale. 1,234.56 vs 1.234,56 varies by device. Consequence: Numbers may be formatted incorrectly for users in different locales, leading to misinterpretation. {v3}
      require_openai_error_handling: true  # [WARNING] OpenAI API call without error handling crashes when the service returns rate limit errors (429), the API is temporarily unavailable (503), or the request exceeds token limits. Users see an unhandled exception crash screen instead of graceful fallback behavior, causing lost context and a broken experience. {v3}
      require_orientation_handling: true  # [INFO] MaterialApp without orientation handling. May break in landscape. This widget pattern increases complexity and makes the widget tree harder to maintain and debug. {v3}
      require_pagination_loading_state: true  # [INFO] Paginated list triggers loadMore but shows no loading indicator. Infinite scroll lists should show a loading indicator when fetching the next page of results. {v2}
      require_pdf_error_handling: true  # [WARNING] PDF loading operation lacks try-catch error handling. PDF files can be corrupted, missing, or have invalid format. Without error handling, failures crash the application instead of showing user-friendly error messages. {v3}
      require_pdf_loading_indicator: true  # [INFO] PDF viewer should provide loading feedback. PDF loading can be slow, especially for large documents or over network. Users should see progress feedback during load. {v2}
      require_permission_manifest_android: true  # [WARNING] Runtime permission request without manifest entry always fails. Feature silently stops working. {v3}
      require_permission_permanent_denial_handling: true  # [WARNING] Permission request does not handle permanent denial. Users who permanently deny a permission are stuck with no way to re-enable it from within the app, causing frustration and feature abandonment. {v3}
      require_permission_plist_ios: true  # [WARNING] iOS requires usage descriptions in Info.plist. App crashes or gets rejected from App Store without them. {v3}
      require_price_localization: true  # [INFO] Hardcoded price detected. Prices vary by region and currency. Users may see incorrect amounts. {v3}
      require_provider_generic_type: true  # [ERROR] Missing generic type causes runtime cast errors when Provider returns dynamic instead of expected type. {v1}
      require_qr_scan_feedback: true  # [INFO] QR scan callback should provide user feedback. Users need confirmation that their scan was successful. Haptic feedback (vibration) or visual feedback (flash, animation) improves user experience and prevents double-scanning. {v2}
      require_receive_port_close: true  # [ERROR] Leaving a ReceivePort open after it is no longer needed can cause memory leaks and keep isolates alive unnecessarily, leading to wasted resources and potential app instability. This can also prevent garbage collection of related objects. See https://dart.dev/guides/libraries/concurrency#isolates. {v2}
      require_refresh_indicator: true  # [INFO] List displaying remote data without pull-to-refresh forces users to navigate away and back to see updated content. This common UX pattern is expected on both platforms, and its absence makes the app feel unresponsive when data becomes stale. Users have no self-service way to recover from a failed initial load. {v5}
      require_refresh_indicator_on_lists: true  # [INFO] ListView without RefreshIndicator. Users can't pull to refresh. Pull-to-refresh is a standard mobile UX pattern. Lists with dynamic content should support manual refresh. {v3}
      require_request_timeout: true  # [WARNING] HTTP request is missing a timeout configuration, which means it may hang indefinitely if the server does not respond. This can cause the app to freeze, degrade user experience, and waste socket connections and memory. Always add .timeout(Duration(seconds: 30)) or configure timeout in client options to ensure requests fail gracefully and users are informed of network issues. {v4}
      require_responsive_breakpoints: true  # [INFO] Using magic numbers for breakpoints in MediaQuery makes code unclear and hard to maintain. Readers won’t know what the number means. {v2}
      require_resume_state_refresh: true  # [INFO] App handles paused state but not resumed, leaving UI stale after returning from background. When app returns from background, data may be stale and must be refreshed. Missing resumed handling leads to outdated UI. {v2}
      require_riverpod_async_value_guard: true  # [WARNING] Try-catch in async provider. Use AsyncValue.guard for consistent error handling. AsyncValue.guard provides better error handling and state management for async operations in Riverpod providers. {v2}
      require_riverpod_lint: true  # [INFO] Project uses Riverpod but riverpod_lint is not configured. The official riverpod_lint package catches Riverpod-specific mistakes that general linters miss. Use it alongside saropa_lints for complete coverage. {v2}
      require_rtl_layout_support: true  # [WARNING] Hardcoded left/right directional value detected. This layout will render incorrectly for RTL (right-to-left) languages such as Arabic, Hebrew, Persian, and Urdu, affecting over 500 million users worldwide. Use directional equivalents (start/end) that automatically adapt to the text direction, ensuring your layout works correctly in both LTR and RTL contexts. {v1}
      require_safe_area_handling: true  # [INFO] Scaffold body without safe area handling allows content to render behind device notches, camera cutouts, rounded corners, and home indicators. On modern devices with non-rectangular displays, this causes text and interactive elements to be obscured or unreachable. {v2}
      require_safe_json_parsing: true  # [WARNING] JSON parsing may throw on missing keys. JSON parsing should handle missing or null values gracefully to avoid runtime exceptions. This weakens type safety, allowing errors to reach runtime where they crash instead of being caught at compile time. {v4}
      require_screen_size_tests: true  # [INFO] Widget test does not verify behavior at multiple screen sizes. Responsive layouts can break on different devices, and single-size tests miss overflow and layout errors at runtime. {v3}
      require_scroll_controller: true  # [INFO] Add ScrollController for scroll tracking. ScrollController is needed for infinite scroll and scroll position tracking. Scrollable lists must have a ScrollController. {v3}
      require_scroll_physics: true  # [INFO] Scrollable widget should specify scroll physics. This layout configuration can trigger RenderFlex overflow errors or unexpected visual behavior at runtime. {v5}
      require_search_loading_indicator: true  # [INFO] Search callback without loading state management. When triggering a search (API call), users need feedback that something is happening. Missing loading state causes confusion. {v2}
      require_secure_keyboard: true  # [WARNING] Password field detected without obscureText: true. The password is displayed as plain text on screen, exposing credentials to shoulder surfing attacks in public spaces. Nearby observers can read the password directly from the display, compromising user account security and violating OWASP M1 credential protection guidelines. {v3}
      require_secure_storage_auth_data: true  # [ERROR] Plaintext auth tokens enable session hijacking via device backup or physical access. {v3}
      require_secure_storage_error_handling: true  # [WARNING] Secure storage operation without error handling. May fail on some devices. {v3}
      require_semantic_label_icons: true  # [WARNING] Icon child element is missing a semanticLabel, making it invisible or meaningless to screen readers and assistive technologies. All icons that convey information or actions in the widget tree must have a descriptive semanticLabel for accessibility compliance. {v2}
      require_semantics_label: true  # [WARNING] This Semantics child element with a button, link, or other interactive role is missing a label, making it inaccessible to screen readers. Without a label, users cannot understand the purpose of the interactive element, which reduces usability and fails accessibility standards. Labels are essential for describing the action or purpose of interactive elements in the widget tree. {v5}
      require_shared_prefs_key_constants: true  # [INFO] String literal used as SharedPreferences key. Use named constants. Using string literals for keys is error-prone and makes refactoring difficult. Define keys as constants for type safety and easier maintenance. {v4}
      require_shared_prefs_null_handling: true  # [WARNING] SharedPreferences getter with null assertion (!) crashes with a NoSuchMethodError if the key does not exist. This is a common source of production crashes on first launch, after app updates that add new preference keys, or when storage is cleared by the OS under memory pressure. {v3}
      require_shared_prefs_prefix: true  # [INFO] SharedPreferences usage detected. Consider calling SharedPreferences.setPrefix() to avoid key conflicts. {v2}
      require_snackbar_duration: true  # [INFO] Default duration varies by platform, causing inconsistent UX across devices. {v3}
      require_socket_close: true  # [ERROR] Not closing a Socket keeps the TCP connection open indefinitely, occupying server connection slots, leaking file descriptors, and potentially exhausting OS resources. This can prevent new network connections and degrade app or system performance. {v2}
      require_step_count_indicator: true  # [INFO] Multi-step flow with 3+ conditional step views lacks a progress indicator. Users have no visibility into how many steps remain, leading to abandonment when the process feels open-ended. {v2}
      require_stream_error_handling: true  # [WARNING] Stream.listen() is called without providing an onError callback or wrapping the stream in a try-catch. Unhandled stream errors propagate as uncaught exceptions, which crash your app in production and terminate the stream connection permanently, preventing any further data from being received. {v2}
      require_supabase_error_handling: true  # [WARNING] Supabase operation called without error handling crashes when the network is unavailable, authentication tokens expire, or the database rejects the query. Users see an unhandled exception crash screen instead of a friendly error message, causing data loss and a broken user experience. {v3}
      require_super_dispose_call: true  # [ERROR] Missing super.dispose() prevents parent State cleanup, causing memory leaks and broken widget lifecycle. {v1}
      require_super_init_state_call: true  # [ERROR] Missing super.initState() skips parent initialization, breaking framework contracts and causing subtle bugs. {v2}
      require_svg_error_handler: true  # [WARNING] SvgPicture without errorBuilder shows blank on invalid SVG. SVG loading can fail for various reasons. Without an error builder, the UI may break or show nothing when an SVG fails to load. {v3}
      require_test_assertions: true  # [WARNING] Test has no assertions. Tests without assertions don't actually verify anything. This rule uses simple string matching on the test body source code for fast detection. {v4}
      require_test_isolation: true  # [WARNING] Mutable top-level variable may cause test coupling. Tests must not share mutable state. Use setUp/tearDown to reset state between tests for proper isolation. {v4}
      require_text_input_type: true  # [INFO] TextField without keyboardType. Users may see wrong keyboard. Setting the appropriate keyboardType improves user experience by showing the right keyboard layout for the expected input. {v2}
      require_text_overflow_handling: true  # [WARNING] Text displaying dynamic content (user input, API data, translations) without overflow handling can break layouts when the text is longer than expected. Unbounded text pushes sibling widgets off-screen, triggers RenderFlex overflow errors in Row/Column contexts, and creates inconsistent UI across locales with varying text lengths. {v3}
      require_timer_cancellation: true  # [WARNING] Timer or StreamSubscription must be canceled in dispose(). Timers and stream subscriptions that aren't canceled will continue running after the widget is disposed, causing: - Crashes if they call setState on a disposed widget - Memory leaks from retained references - Wasted CPU cycles. {v5}
      require_timezone_display: true  # [INFO] DateFormat includes time components but no timezone indicator. Users may misinterpret displayed times when the timezone is ambiguous, leading to missed appointments, incorrect scheduling, and poor user experience across time zones. {v1}
      require_url_launcher_can_launch_check: true  # [INFO] launchUrl called without canLaunchUrl check. May fail silently on unsupported schemes, confusing users and breaking expected flows. Check canLaunchUrl before launchUrl to improve error messages. Without this check, launchUrl may fail silently or throw cryptic platform exceptions. {v2}
      require_url_launcher_encoding: true  # [WARNING] URL passed to launchUrl or canLaunchUrl contains string interpolation without Uri.encodeComponent(). Unencoded special characters (spaces, ampersands, Unicode) produce malformed URLs that fail to open, display incorrect content, or enable URL injection attacks where user input manipulates the destination path or query parameters. {v2}
      require_url_launcher_queries_android: true  # [WARNING] Without <queries> in manifest, canLaunchUrl returns false on Android 11+ even for installed apps. {v3}
      require_url_launcher_schemes_ios: true  # [WARNING] Without LSApplicationQueriesSchemes, canLaunchUrl returns false on iOS even for available URL schemes. {v3}
      require_validator_return_null: true  # [ERROR] Non-null return on valid input shows error message even when field is correct, confusing users. Validator functions in forms must return null for valid input. Returning a non-null value causes error messages to display even when the field is correct, leading to user frustration and broken form validation. {v3}
      require_video_player_controller_dispose: true  # [ERROR] When a State class creates a VideoPlayerController for a video widget (such as a video player, gallery, carousel, or story view) and does not dispose of it in dispose(), the controller will keep video and audio resources alive after the widget is removed from the widget tree. This can cause memory leaks, background playback, battery drain, and even prevent other apps from acquiring audio focus. In video-heavy UIs or dynamic lists, this can quickly exhaust device resources and degrade app performance. Always dispose of every VideoPlayerController in the owning State object’s dispose() method to ensure proper cleanup. See: https://pub.dev/packages/video_player#disposing {v3}
      require_websocket_error_handling: true  # [WARNING] WebSocket stream.listen() is missing an onError handler. Unhandled errors can crash your app, disconnect users, and make debugging network issues difficult. All WebSocket listeners must handle errors to ensure robust, production-quality networking. {v3}
      require_widgets_binding_callback: true  # [WARNING] showDialog/showModalBottomSheet in initState without addPostFrameCallback may fail. {v2}
      require_workmanager_constraints: true  # [WARNING] WorkManager task registered without constraints runs unconditionally regardless of network availability, battery level, or charging state. This drains battery during low-power conditions, consumes metered mobile data, and causes failed network connection requests when connectivity is unavailable, wasting battery, memory, and processing resources. {v3}
      require_workmanager_for_background: true  # [WARNING] Periodic task detected without workmanager. Dart isolates die when app backgrounds. Use workmanager for reliable background tasks. {v2}
      require_wss_over_ws: true  # [ERROR] ws:// transmits data unencrypted. Attackers can intercept, read, and modify WebSocket messages in transit. {v1}
      require_yield_after_db_write: true  # [WARNING] Database or I/O write without a following yieldToUI() call may block the UI thread and cause visible frame drops. Write operations acquire exclusive locks that starve the framework of time to paint. {v1}
      suggest_yield_after_db_read: true  # [INFO] Bulk database or I/O read without a following yieldToUI() call. Deserializing large payloads on the main isolate can cause frame drops during data-heavy workflows. {v1}
      use_setstate_synchronously: true  # [WARNING] setState called after async gap without mounted check. Quick fix available: Wraps the setState call in if (mounted) { .. }. This violates the widget lifecycle, risking setState-after-dispose errors or silent state corruption. {v9}

      #
      # --- TIER 3: PROFESSIONAL (622 rules) ---
      #
      always_remove_getx_listener: true  # [WARNING] GetX worker is not assigned to a variable for cleanup. This will cause a memory leak. {v2}
      avoid_absorb_pointer_misuse: true  # [INFO] AbsorbPointer blocks ALL touch events. Prefer IgnorePointer instead. This layout configuration can trigger RenderFlex overflow errors or unexpected visual behavior at runtime. {v3}
      avoid_accessing_other_classes_private_members: true  # [WARNING] Accessing a private member of another class in the same file. In Dart, underscore-prefixed members are private to the library (file), not to the class. This means classes in the same file can reach into each other's internal state — but this violates encapsulation and creates maintenance debt. When the code is later refactored (classes split into different files), these accesses break. {v1}
      avoid_adjacent_strings: true  # [INFO] Adjacent string literals detected without an explicit concatenation operator. Dart implicitly joins adjacent strings, which can mask accidental line breaks or missing commas in list literals, leading to silently merged values that are difficult to debug. {v4}
      avoid_always_null_parameters: true  # [INFO] Parameter is explicitly passed as null at every call site. Passing null as a constant argument adds noise, makes the call site harder to read, and defeats the purpose of optional parameters, which default to null when omitted. {v4}
      avoid_android_task_affinity_default: true  # [INFO] Starting Android activity via platform channel. Verify taskAffinity is set in AndroidManifest.xml. {v2}
      avoid_animation_in_large_list: true  # [WARNING] Animation widget inside ListView builder. All items run animations even when off-screen, causing performance issues. {v2}
      avoid_auto_route_context_navigation: true  # [WARNING] String-based context.push() or context.go() is used in an auto_route project. auto_route provides typed route classes for navigation that are compile-time checked, support deep linking, and work correctly with nested routers. String-based navigation bypasses all of these benefits and can push onto the wrong router in nested navigation setups, causing confusing back-button behavior and incorrect URL updates on web. {v1}
      avoid_auto_route_keep_history_misuse: true  # [WARNING] replaceAll() or popUntilRoot() clears the navigation stack outside an authentication flow. replaceAll destroys the user's entire navigation history so the back button no longer works as expected. popUntilRoot pops every intermediate route. These are appropriate for login, logout, and onboarding flows but are a common source of bugs when used for general navigation. The user loses their place and cannot navigate back to where they were. {v1}
      avoid_autoplay_audio: true  # [INFO] Autoplay is blocked on iOS/web and annoys users. Autoplaying audio is blocked on iOS/web and annoying to users. Require explicit user interaction to start playback. {v3}
      avoid_badge_without_meaning: true  # [INFO] Displaying a badge with a count of 0 provides no useful information to users and can create visual noise or confusion. Badges are intended to highlight actionable or noteworthy items, and showing them when empty diminishes their value. {v2}
      avoid_behavior_subject_last_value: true  # [WARNING] Accessing .value on a BehaviorSubject that may be closed. BehaviorSubject retains its last emitted value even after close() is called. Reading .value on a closed subject returns stale data that no longer reflects the current state, which can cause subtle logic errors and prevents garbage collection of the retained object. If replay behavior is not needed, consider using PublishSubject instead. {v1}
      avoid_bloc_public_methods: true  # [INFO] Public method in Bloc. Use events via add() instead. Bloc should only expose add() for events. Other public methods break the event-driven architecture and make testing harder. {v2}
      avoid_border_all: true  # [INFO] Prefer Border.fromBorderSide for const borders. This layout configuration can trigger RenderFlex overflow errors or unexpected visual behavior at runtime. {v6}
      avoid_brightness_check_for_theme: true  # [INFO] Manually checking Theme.of(context).brightness to pick light/dark colors bypasses the colorScheme system, which already provides semantically correct colors for each theme mode. Brightness checks are fragile, miss high-contrast mode, and scatter color logic throughout the codebase instead of centralizing it in the theme. {v2}
      avoid_business_logic_in_ui: true  # [INFO] Widget class contains methods named with calculation or transformation verbs (calculate, compute, validate, process, etc.), indicating business logic embedded in the UI layer. This violates separation of concerns, making the logic impossible to reuse across screens, untestable without widget infrastructure, and tightly coupled to Flutter framework classes. {v5}
      avoid_cache_stampede: true  # [WARNING] Async method uses a Map cache without in-flight request deduplication. When multiple concurrent callers miss the cache simultaneously, each triggers a separate expensive fetch (cache stampede / thundering herd). Dart's async event loop means multiple awaits can interleave and all see the empty cache before any write completes. This causes redundant API calls, wasted bandwidth, potential server overload, and possible data corruption from race conditions. {v1}
      avoid_cached_isar_stream: true  # [ERROR] Caching or storing Isar/single-subscription streams in variables or fields will cause runtime errors: these streams can only be listened to once and must be created inline each time. If you cache them, your app will throw a StateError or fail to update as expected. {v2}
      avoid_calling_of_in_build: true  # [INFO] Multiple .of(context) lookups walk the InheritedWidget ancestor chain on every rebuild call. Each lookup traverses the widget tree upward, adding cumulative overhead that slows frame rendering. This is especially costly when called multiple times in a single build method for the same provider type. {v3}
      avoid_capturing_this_in_callbacks: true  # [INFO] Closure callback captures a reference to the entire enclosing object instance via implicit this. This prevents garbage collection of the object and all its fields for as long as the callback exists. Long-lived callbacks such as stream listeners, timers, or global event handlers create memory leaks that accumulate over the app session lifetime. {v4}
      avoid_circular_di_dependencies: true  # [WARNING] Potential circular dependency detected. Circular dependencies cause runtime errors or infinite loops during dependency resolution. Circular dependencies are detected in DI registration. {v5}
      avoid_circular_imports: true  # [WARNING] Circular import detected. This file is part of an import cycle. {v2}
      avoid_clip_during_animation: true  # [WARNING] A Clip widget is used inside an animated widget, causing expensive rasterization on every animation frame. ClipRRect, ClipOval, and ClipPath force the GPU to re-clip content 60+ times per second during animation, which leads to janky motion and dropped frames. ClipPath is especially costly because it forces software rasterization. Move the clip outside the animated scope or use BoxDecoration.borderRadius for rounded corners. {v1}
      avoid_closure_capture_leaks: true  # [WARNING] setState() is called inside a Timer or Future.delayed callback without a mounted check. The closure captures the State object, and if the widget is disposed before the callback fires, calling setState throws a "setState() called after dispose()" error at runtime. This is a common source of crashes in production, especially on slow networks or with periodic timers. Always check mounted before calling setState in async callbacks. {v1}
      avoid_collapsible_if: true  # [INFO] Nested if statement without an else clause can be merged with its parent if. The extra nesting level adds indentation and cognitive load without any logical benefit, making the combined condition harder to scan than a single flattened check. {v5}
      avoid_collection_mutating_methods: true  # [WARNING] Mutating a collection in-place inside setState keeps the same object reference, which some state management solutions may not detect. Additionally, mutating shared collections breaks immutable-first architectures. Use the spread operator to create a new collection instead of modifying the existing one. {v1}
      avoid_complex_arithmetic_expressions: true  # [INFO] Arithmetic expression has more than 4 operators. Complex arithmetic expressions are hard to understand and maintain. Extract parts into named variables. {v4}
      avoid_complex_conditions: true  # [INFO] Condition has more than 3 logical operators. Complex conditions with many logical operators are hard to understand. Extract parts into named boolean variables. {v3}
      avoid_complex_loop_conditions: true  # [INFO] Loop condition contains too many operators or nested expressions, making it difficult to reason about when the loop terminates. Complex conditions increase the risk of off-by-one errors and infinite loops that are hard to diagnose. {v4}
      avoid_context_in_static_methods: true  # [INFO] BuildContext in static method. Consider instance method or extension instead. {v3}
      avoid_continuous_location_updates: true  # [WARNING] Continuous location stream without distance filter. GPS polling drains battery rapidly — a typical app consumes 10-15% battery per hour with continuous updates. Most use cases (ride tracking, delivery, fitness) work well with a distance filter (50-100m) that only fires when the user moves significantly. Use getPositionStream with a distanceFilter or switch to geofencing for area-based triggers. {v1}
      avoid_cross_feature_dependencies: true  # [INFO] File inside a feature module imports from a different feature module directory, creating a direct cross-feature dependency. This breaks feature isolation, prevents independent development and testing of features, and makes it impossible to extract or remove a feature without cascading changes across the codebase. Cross-feature coupling also increases merge conflicts and deployment risks. {v5}
      avoid_datetime_comparison_without_precision: true  # [INFO] Direct equality comparison (== or !=) on DateTime objects is unreliable because two timestamps representing the same logical moment almost never share identical microsecond values. Network latency, clock drift, serialization rounding, and independent construction all introduce sub-second differences that cause equality checks to silently fail, leading to missed cache hits, duplicate entries, and incorrect conditional logic. {v2}
      avoid_declaring_call_method: true  # [INFO] call() method makes class callable but hides intent. Code reads ambiguously. A class declares a call() method. {v5}
      avoid_deep_nesting: true  # [WARNING] Code nested more than 5 levels deep is difficult to read, test, and maintain. Deep nesting increases cyclomatic complexity and makes control flow hard to follow. Refactor by extracting helper methods, using early returns, or restructuring conditional logic into guard clauses. {v1}
      avoid_default_tostring: true  # [INFO] Class relies on default toString() implementation which returns unhelpful output like "Instance of 'ClassName'". During debugging, logging, or error messages, developers see meaningless object identifiers instead of the actual state values needed to diagnose issues. {v5}
      avoid_digit_separators: true  # [INFO] Unnecessary digit separator in small number. Digit separators in small numbers don't improve readability. {v6}
      avoid_dio_debug_print_production: true  # [WARNING] Using Dio LogInterceptor in production exposes sensitive request and response data—including authentication tokens, user information, and API payloads—to device logs. This can lead to data leaks, privacy violations, and compliance issues, especially on shared or rooted devices. Always restrict debug logging to development builds only. {v3}
      avoid_dio_form_data_leak: true  # [INFO] FormData with file. Ensure proper cleanup of file resources. FormData with file streams must be cleaned up to avoid resource leaks. FormData with files is not properly cleaned up. {v3}
      avoid_direct_data_access_in_ui: true  # [WARNING] Widget class declares a field typed as Repository, DataSource, ApiClient, or similar data-layer class, creating a direct coupling between the UI and data implementation. This bypasses the domain/business logic layer, making the widget untestable in isolation, difficult to refactor when data sources change, and prone to leaking data concerns into the presentation layer. {v5}
      avoid_duplicate_cascades: true  # [WARNING] Duplicate cascade operation detected on the same target. Identical cascade members are likely copy-paste errors that produce redundant side effects and increase maintenance risk. {v4}
      avoid_duplicate_constant_values: true  # [INFO] Multiple constants share the same value in this scope. Duplicate constant definitions increase maintenance cost because changes must be applied to every copy, and inconsistent updates lead to subtle logic errors when the values diverge. {v3}
      avoid_duplicate_initializers: true  # [INFO] Same initialization expression appears in multiple initializer list entries. Duplicate expressions waste computation, increase the risk of inconsistent updates when one copy is changed but others are missed, and obscure the intended initialization logic. {v4}
      avoid_duplicate_string_literals: true  # [INFO] String literal appears 3+ times in this file. Consider extracting to a constant. {v1}
      avoid_duplicate_string_literals_pair: true  # [INFO] String literal appears 2+ times in this file. Consider extracting to a constant. {v1}
      avoid_dynamic_json_access: true  # [WARNING] Chained dynamic JSON access without null checks throws NoSuchMethodError at runtime when any intermediate key is missing or null. This causes unhandled crashes in production when API responses deviate from the expected schema, with no compile-time safety net. {v6}
      avoid_dynamic_type: true  # [INFO] 'dynamic' type disables static type checking, hiding errors until runtime. Method calls on dynamic values are never verified by the compiler, so typos, missing methods, and wrong argument types only surface as NoSuchMethodError crashes in production. Exempt: type arguments (e.g. List<dynamic>, Map<String, dynamic>), closure parameters, and for-in loop variables. {v5}
      avoid_elevation_opacity_in_dark: true  # [INFO] High elevation (>4) without brightness check. Shadows look poor in dark mode. Elevation shadows appear differently in dark mode. Material Design recommends using surface overlays instead of shadows in dark themes. {v2}
      avoid_empty_setstate: true  # [INFO] setState callback is empty — state was likely modified before this call. An empty setState(() {}) still triggers a rebuild, but moving state changes inside the callback makes the intent clearer. {v5}
      avoid_encryption_key_in_memory: true  # [INFO] Encryption key stored as a persistent class field remains in process memory for the lifetime of the object. Memory dumps, debugging tools, or memory-scanning malware can extract the key, compromising all data encrypted with it. Keys in memory survive garbage collection and are visible in heap snapshots. {v2}
      avoid_equatable_datetime: true  # [WARNING] DateTime in Equatable props may cause flaky equality checks. DateTime comparisons can fail due to microsecond differences. Compare truncated or formatted values instead. {v2}
      avoid_equatable_nested_equality: true  # [WARNING] Including a mutable collection (List, Map, or Set) directly in Equatable props can cause non-deterministic equality checks and unstable hash codes. Equatable performs shallow comparison, so if the collection mutates after construction, two "equal" instances may later compare as different. Wrap collections with List.unmodifiable or use immutable types. {v1}
      avoid_excessive_bottom_nav_items: true  # [INFO] BottomNavigationBar with more than 5 items crowds the UI. More than 5 bottom navigation items crowds the UI and makes tap targets too small for comfortable use. {v3}
      avoid_excessive_expressions: true  # [INFO] Expression has excessive complexity (>5 operators). Complex expressions are hard to read and maintain. This excessive complexity makes the code harder to understand, test, and maintain. {v4}
      avoid_expanded_as_spacer: true  # [INFO] Use Spacer() instead of Expanded with empty child. This layout configuration can trigger RenderFlex overflow errors or unexpected visual behavior at runtime. {v6}
      avoid_expando_circular_references: true  # [WARNING] Creating a circular reference by storing a reference to an Expando key inside its own value prevents Dart’s garbage collector from reclaiming memory, resulting in memory leaks. This subtle bug can cause your app’s memory usage to grow over time, degrade performance, and eventually lead to crashes or out-of-memory errors, especially in long-running or data-intensive applications. Such leaks are difficult to detect and debug, making it critical to avoid circular references when using Expando for metadata or caching. {v5}
      avoid_expensive_computation_in_build: true  # [WARNING] Expensive computation detected inside build() method. Build runs on every frame during animations and on every state change, so heavy operations here cause jank, dropped frames, and sluggish UI responsiveness. Users will experience visible stuttering especially during transitions and scrolling. {v2}
      avoid_explicit_pattern_field_name: true  # [INFO] Explicit pattern field name matches variable name. Explicit field names are used in pattern matching when they match the variable name. This pattern matching usage can cause unexpected behavior or miss important type information. {v3}
      avoid_extensions_on_records: true  # [INFO] Extension on record type. Records lack identity for extension discovery. This pattern matching usage can cause unexpected behavior or miss important type information. {v4}
      avoid_find_all: true  # [INFO] Generic finder (Text, Container, etc.) matches many widgets. Use specific finder. Using generic finders like find.byType(Text) matches many widgets and makes tests fragile. Use more specific finders. {v2}
      avoid_find_by_text: true  # [INFO] Prefer find.byKey() for widget interactions instead of find.text(). - find.text() is appropriate for verifying displayed content. - find.byKey() is preferred for tapping/interacting with widgets because it's stable when text changes (e.g., i18n, A/B testing). {v3}
      avoid_firebase_realtime_in_build: true  # [WARNING] Creating Firebase stream/listener in build causes multiple subscriptions. {v2}
      avoid_firebase_user_data_in_auth: true  # [WARNING] Accessing more than 5 distinct custom claim keys from a Firebase ID token suggests profile data is being stored in custom claims. Custom claims are limited to 1000 bytes total and included in every authenticated request, bloating token size and slowing network calls. Move profile data to Firestore and keep only security-critical fields like role and permissions in claims. {v1}
      avoid_fitted_box_for_text: true  # [INFO] FittedBox scales Text widgets uniformly, shrinking the entire text to fit the container. This ignores the user accessibility text scaling preference, can render text unreadably small on narrow screens, and defeats the purpose of responsive text layout. Use text-specific overflow handling instead. {v3}
      avoid_fixed_dimensions: true  # [INFO] Fixed pixel dimensions may not work on all screen sizes. Fixed pixel dimensions break on different screen sizes. Use responsive sizing with Flexible, Expanded, FractionallySizedBox, or constraints. {v4}
      avoid_flexible_outside_flex: true  # [WARNING] The Flexible or Expanded widget is being used outside of a Row, Column, or Flex parent. This breaks layout expectations and can cause runtime errors or unexpected UI behavior, as Flexible/Expanded are only designed to work within Flex-based widgets. Using them elsewhere will not provide the intended flexible sizing and may result in layout exceptions. {v6}
      avoid_font_weight_as_number: true  # [INFO] Numeric FontWeight values like w400 or w700 are less readable and harder to maintain than their named equivalents. Named constants (normal, bold) convey semantic intent, reduce lookup effort during code review, and align with design system terminology used by designers. {v2}
      avoid_form_in_alert_dialog: true  # [INFO] Form placed directly inside AlertDialog loses all field values, validation state, and user input when the dialog rebuilds due to a parent setState() or keyboard appearance. Because the dialog builder creates a new Form on each build call, there is no StatefulWidget to preserve form state across rebuilds, producing a frustrating experience where typed data disappears. {v2}
      avoid_form_without_unfocus: true  # [INFO] Form submission handler does not call unfocus() before processing. The on-screen keyboard stays open after submission, blocking the success message, navigation, or dialog that confirms the action to the user. This creates a confusing experience where users cannot see feedback and may repeatedly tap the submit button. {v2}
      avoid_freezed_any_map_issue: true  # [WARNING] A @freezed class with a fromJson factory that lacks @JsonSerializable(anyMap: true) will throw a cast error when deserializing nested maps from Firestore or other APIs that return Map<dynamic, dynamic>. Adding anyMap: true makes the generated fromJson accept any map type safely. {v1}
      avoid_freezed_for_logic_classes: true  # [INFO] Freezed annotation on logic class. Freezed is meant for data classes, not Blocs/Services. {v2}
      avoid_full_sync_on_every_launch: true  # [WARNING] Bulk data fetch detected in initState. Downloading the entire dataset on every launch is slow and wastes bandwidth. This causes unnecessary network traffic and increases startup time, especially on slow connections. Use delta sync with timestamps or change feeds to only fetch what changed since the last sync. {v1}
      avoid_function_type_in_records: true  # [INFO] Inline function type in record reduces readability. Function types in records are hard to read and maintain. Use a typedef instead. This pattern matching usage can cause unexpected behavior or miss important type information. {v5}
      avoid_generics_shadowing: true  # [INFO] Generic type parameter shadows a top-level declaration. This class design reduces clarity and can lead to incorrect object initialization. {v4}
      avoid_gesture_detector_in_scrollview: true  # [WARNING] GestureDetector around scrollable can cause gesture conflicts. This layout configuration can trigger RenderFlex overflow errors or unexpected visual behavior at runtime. {v6}
      avoid_get_find_in_build: true  # [ERROR] Calling Get.find() inside the build method is inefficient and can cause unnecessary object creation and performance issues. This leads to wasted memory allocations on every rebuild and makes your app less responsive. {v3}
      avoid_getit_in_build: true  # [WARNING] GetIt service locator in build() hides dependencies. Service locator calls in build() hide dependencies and make testing difficult. Inject dependencies via constructor instead. {v2}
      avoid_getx_global_navigation: true  # [WARNING] GetX global navigation (Get.to, Get.off) bypasses widget context. GetX navigation methods bypass the widget tree's context, making testing and navigation state management difficult. {v2}
      avoid_getx_rx_inside_build: true  # [WARNING] Creating .obs reactive variables inside build() allocates a new Rx instance on every rebuild. Each instance leaks because it is never disposed, and the widget observes a fresh variable each time, losing all previous state and accumulating orphaned subscriptions. {v4}
      avoid_getx_rx_nested_obs: true  # [WARNING] Nesting GetX reactive types like Rx<List<RxString>> causes double-notification when either the outer or inner observable changes, leading to redundant widget rebuilds, confusing update behavior, and potential infinite loops. Use flat reactive types like RxList<String> instead of wrapping collections in multiple Rx layers. {v1}
      avoid_getx_static_context: true  # [WARNING] GetX static context method used. Hard to unit test. Get.offNamed and Get.dialog use static context internally which cannot be unit tested. Prefer abstraction for testability. {v2}
      avoid_getx_static_get: true  # [WARNING] Get.find<T>() is a static service locator call that creates a hidden dependency on the global GetX container. This pattern makes the class impossible to unit test in isolation because there is no way to substitute a mock without initializing the full GetX dependency graph. It also obscures the true dependency count of the class, making it harder to detect god-object violations and architectural boundary breaches. {v2}
      avoid_global_key_misuse: true  # [INFO] Multiple GlobalKey instances in a single class indicate overuse of an expensive mechanism. Each GlobalKey prevents Flutter from efficiently diffing the widget tree, forces cross-tree reference tracking, and can cause unexpected widget reparenting that corrupts state and degrades rendering performance. {v2}
      avoid_global_riverpod_providers: true  # [ERROR] Defining providers at the global scope creates implicit dependencies, makes testing difficult, and can cause state to leak between tests or app sessions. This leads to flaky tests, unpredictable bugs, and hard-to-maintain code. {v5}
      avoid_go_router_string_paths: true  # [INFO] String literal used as a navigation path in go_router. Hardcoded path strings are error-prone, bypass compile-time validation, and break silently when route definitions change. {v3}
      avoid_god_class: true  # [WARNING] Class declares more than 15 fields or 20 methods, violating the Single Responsibility Principle. God classes accumulate unrelated responsibilities, making them difficult to understand, test, and maintain. Changes to one responsibility risk breaking others, and the class becomes a merge-conflict magnet as multiple developers modify it concurrently. {v6}
      avoid_graphql_string_queries: true  # [INFO] Raw GraphQL string queries lack type safety and compile-time validation. Raw GraphQL string queries are error-prone, lack type safety, and cannot be validated at compile time. Use code generation tools like graphql_codegen or artemis to generate type-safe query objects. {v2}
      avoid_hardcoded_app_name: true  # [INFO] App name must not be hardcoded in UI. App names may need to vary by market or locale. App name or branding is hardcoded. {v4}
      avoid_hardcoded_asset_paths: true  # [INFO] Hardcoded asset path string is error-prone: typos produce silent runtime failures, path changes require find-and-replace across the codebase, and the compiler cannot verify the asset exists. Centralized asset references enable compile-time safety and single-source-of-truth for all asset paths. {v5}
      avoid_hardcoded_colors: true  # [INFO] Hardcoded color value bypasses the theme system, breaking dark mode and dynamic theming. When users switch themes, hardcoded colors remain unchanged, creating contrast issues, illegible text, and an inconsistent visual experience that can fail accessibility requirements. {v6}
      avoid_hardcoded_config_test: true  # [INFO] Hardcoded configuration detected in test file. Consider using a const or shared test helper. {v2}
      avoid_hardcoded_delays: true  # [WARNING] Hardcoded delay in test is flaky. Use pumpAndSettle() instead. Hardcoded delays are flaky - too short and tests fail intermittently, too long and tests waste time. Use pumpAndSettle() or wait for conditions. {v1}
      avoid_hardcoded_duration: true  # [INFO] Hardcoded Duration values make it difficult to maintain consistent timing across the app and can lead to subtle bugs when timings need to be updated globally. This practice reduces maintainability and increases the risk of inconsistent user experiences. Always extract Duration values to named constants for clarity, reusability, and easier updates. {v2}
      avoid_hardcoded_feature_flags: true  # [INFO] Hardcoded if(true)/if(false) suggests incomplete feature flag. Hardcoded conditions like if (true) or if (false) suggest incomplete feature flag implementation or dead code. {v2}
      avoid_hardcoded_locale: true  # [INFO] Hardcoded locale ignores user's device settings. Apps should respect the user's device locale settings. {v6}
      avoid_hardcoded_locale_strings: true  # [WARNING] `[HEURISTIC]` Hardcoded string in Text widget. Use localization for user-visible text. {v2}
      avoid_hardcoded_strings_in_ui: true  # [INFO] Hardcoded user-facing string. Cannot be translated to other languages. User-facing strings should use localization (l10n) instead of hardcoded text for proper internationalization. {v4}
      avoid_high_cyclomatic_complexity: true  # [WARNING] Functions with cyclomatic complexity exceeding 15 have too many branching paths, making them difficult to understand, test exhaustively, and maintain over time. Each branch doubles the testing surface area. Simplify by extracting helper methods or using polymorphism instead of conditionals. {v2}
      avoid_hive_datetime_local: true  # [WARNING] Storing a local DateTime value in Hive without first converting to UTC causes timezone-dependent bugs. When the value is read back on a device in a different timezone, the timestamp will be interpreted incorrectly. Always call .toUtc() before storing DateTime values in Hive to ensure consistent behavior across timezones. {v1}
      avoid_hive_large_single_entry: true  # [WARNING] Storing a large collection or binary blob as a single Hive entry forces Hive to load the entire object into memory at once, causing high memory usage and slow reads. Large Uint8List or List<Model> values should be split into individual keyed entries so Hive can load them on demand. {v1}
      avoid_hive_type_modification: true  # [WARNING] Reusing a @HiveField index causes silent data corruption because Hive maps indices to binary offsets. When two fields share an index, reading from disk returns the wrong data. Once a field index is assigned it must never be reused, even if the original field is deleted. Mark retired indices with a comment instead. {v1}
      avoid_icon_size_override: true  # [INFO] Setting icon size directly on individual Icon widgets scatters sizing values throughout the codebase, causing inconsistencies when the design system changes. IconTheme provides a single point of control for icon sizing within a subtree, keeping all icons consistent and easier to update. {v2}
      avoid_identical_exception_handling_blocks: true  # [INFO] Multiple catch blocks contain identical handling code. Duplicated exception handling increases maintenance burden because changes must be applied to every copy, and missed updates lead to inconsistent error recovery behavior. {v5}
      avoid_if_with_many_branches: true  # [INFO] If/else-if chain has more than 4 branches, making the control flow difficult to follow. Long chains are error-prone because conditions are checked sequentially and later branches depend on all previous conditions being false. {v5}
      avoid_image_picker_large_files: true  # [WARNING] pickImage without imageQuality allows raw photos that can be 10+ MB in size, creating slow uploads and wasteful bandwidth consumption. Large image files cause network timeouts, drain battery, consume expensive mobile data, and may trigger out-of-memory errors when the app attempts to process them. {v5}
      avoid_image_picker_quick_succession: true  # [WARNING] Image picker method called without a loading guard. Calling pickImage() or pickVideo() while another picker is already active throws a PlatformException (ALREADY_ACTIVE) that crashes the app. Users who double-tap "Choose Photo" or press it during a slow gallery load will trigger this crash. Add a boolean loading flag that prevents concurrent picker invocations. {v1}
      avoid_image_picker_without_source: true  # [WARNING] ImagePicker called without specifying an ImageSource shows a blank or empty picker dialog on some Android devices and older iOS versions. Users see a non-functional dialog and cannot select or capture images, resulting in a broken feature that provides no error feedback or alternative selection path. {v2}
      avoid_image_repeat: true  # [INFO] ImageRepeat tiles the image across the available space, which is rarely the intended behavior for photos or icons and usually signals a misconfigured decoration. Tiled images consume additional GPU memory for the repeated texture and can produce visual artifacts at tile boundaries on different screen densities. {v4}
      avoid_image_without_cache: true  # [INFO] Image.network without cacheWidth or cacheHeight decodes the full-resolution image into memory, even when displayed at a smaller size. A 4000x3000 photo decoded at full resolution consumes ~48 MB of GPU memory, causing excessive memory usage and potential out-of-memory crashes on low-end devices. {v6}
      avoid_immediately_invoked_functions: true  # [INFO] Function is immediately invoked after definition. Immediately invoked function expressions (IIFE) can be confusing and usually indicate the code must be refactored. {v4}
      avoid_incomplete_copy_with: true  # [INFO] copyWith() is missing fields. Copied objects will lose data for those fields. All non-final fields must be included in copyWith for complete copying. {v4}
      avoid_inconsistent_digit_separators: true  # [INFO] Digit separators are not grouped consistently. Digit separators should group digits by 3 for readability. {v4}
      avoid_infinite_scroll_duplicate_requests: true  # [WARNING] Scroll listener triggers data loading without a loading guard. When the user scrolls to the bottom, this fires multiple simultaneous requests because the listener fires continuously while at max extent. This wastes bandwidth, causes duplicate items in the list, and may trigger API rate limits. Check an isLoading flag before loading. {v1}
      avoid_internal_dependency_creation: true  # [INFO] Dependency created internally instead of being injected. Cannot substitute mock implementations for testing. This tight coupling reduces testability and makes the component harder to reuse. {v5}
      avoid_inverted_boolean_checks: true  # [INFO] Boolean expression negated with ! when a direct opposite operator exists. Negated conditions add a mental inversion step for readers and are more error-prone during modification, especially in compound expressions with multiple negations. {v4}
      avoid_ios_debug_code_in_release: true  # [INFO] Debug code detected. Ensure this is conditionally compiled out for release builds. {v2}
      avoid_ios_deprecated_uikit: true  # [WARNING] Deprecated UIKit API pattern detected in platform channel code. Platform channel code (Swift/Objective-C) that uses deprecated UIKit APIs will generate warnings during Xcode builds and may be rejected by App Store in future iOS versions. {v2}
      avoid_ios_hardcoded_bundle_id: true  # [INFO] Hardcoded bundle ID detected. Bundle IDs should come from configuration, not hardcoded strings. {v2}
      avoid_ios_misleading_push_notifications: true  # [INFO] Marketing notification pattern detected. Push notifications must be relevant to user interests to comply with Apple guidelines. {v2}
      avoid_isar_schema_breaking_changes: true  # [ERROR] Renaming, removing, or changing the type of a field in an Isar collection without using @Name will break migrations. This can cause data loss, failed upgrades, or app crashes for existing users. {v1}
      avoid_json_in_main: true  # [INFO] jsonDecode on main thread blocks UI for large payloads (100KB+). jsonDecode() for large payloads (>100KB) blocks the main thread. Use compute() to parse JSON in a background isolate. {v2}
      avoid_large_bloc: true  # [INFO] Bloc with 7+ event handlers becomes difficult to test and reason about. Blocs handling too many responsibilities become hard to maintain. Keep Blocs focused on a single domain. {v2}
      avoid_large_isolate_communication: true  # [INFO] Sending large objects between isolates is expensive. Sending large objects between isolates requires copying, which is expensive. Use SendPort with TransferableTypedData for large data. {v3}
      avoid_large_objects_in_state: true  # [WARNING] Unbounded List, Map, Set, or ByteData field declared in a State class grows without limit as data accumulates. Without pagination or size constraints, this allocates excessive memory that degrades scroll performance, increases garbage collection pressure, and eventually crashes the app with an out-of-memory error on devices with limited RAM. {v4}
      avoid_late_keyword: true  # [INFO] Field declared with the 'late' keyword defers initialization checking to runtime. If the field is accessed before assignment, Dart throws a LateInitializationError that crashes the app, bypassing the null safety guarantees the type system provides at compile time. {v7}
      avoid_layout_builder_misuse: true  # [INFO] LayoutBuilder should use constraints in its builder. LayoutBuilder is used inefficiently. This layout configuration can trigger RenderFlex overflow errors or unexpected visual behavior at runtime. {v6}
      avoid_layout_passes: true  # [WARNING] Using IntrinsicWidth or IntrinsicHeight causes Flutter to perform two layout passes for affected child widgets in the build tree, which significantly hurts performance, especially in complex UIs or lists. This can lead to dropped frames, laggy animations, and poor user experience. Prefer using CrossAxisAlignment.stretch, Expanded, or fixed dimensions to avoid extra layout computation. {v2}
      avoid_loading_flash: true  # [INFO] Loading indicator shown without delay. Fast responses will cause jarring visual flash. {v2}
      avoid_local_functions: true  # [INFO] Local function declared inside another function. Local functions increase nesting depth, cannot be tested independently, and make the enclosing function harder to read and reason about. {v5}
      avoid_long_event_handlers: true  # [INFO] Bloc event handler is too long. Extract logic to separate methods. Long event handlers indicate the handler is doing too much. Extract business logic to separate methods or services for testability. {v1}
      avoid_long_functions: true  # [INFO] Function body exceeds 100 code lines (comments and blank lines excluded). Long functions are harder to understand, test in isolation, and debug because they typically handle multiple concerns and have complex control flow paths. {v5}
      avoid_long_length_files: true  # [INFO] File exceeds 500 code lines (comments and blank lines excluded). Files this long are difficult to navigate, increase code review time, and frequently indicate that the file handles multiple unrelated concerns. {v4}
      avoid_long_length_test_files: true  # [INFO] Test file exceeds 1000 code lines (comments and blank lines excluded). A 1000+ line test file is difficult to navigate and likely tests multiple distinct features. Extract test groups into separate files organized by feature area. {v2}
      avoid_long_parameter_list: true  # [INFO] Function has too many parameters (max 5). Functions with many parameters are difficult to call correctly, hard to test with all combinations, and indicate the function may be doing too much work. {v6}
      avoid_long_records: true  # [INFO] Record has more than 5 fields, reducing readability. Records with excessive fields lose their clarity advantage over classes and become harder to destructure, understand, and maintain. {v3}
      avoid_medium_length_files: true  # [INFO] File exceeds 300 code lines (comments and blank lines excluded). Files beyond this threshold often contain multiple responsibilities, which makes navigation harder and increases merge conflict risk in team development. {v4}
      avoid_medium_length_test_files: true  # [INFO] Test file exceeds 600 code lines (comments and blank lines excluded). At 600+ lines, a test file may be covering too many scenarios or features. Split tests by domain, widget, or use case to improve maintainability and faster test runs. {v2}
      avoid_merged_semantics_hiding_info: true  # [INFO] MergeSemantics hides interactive elements from screen readers, making buttons/inputs unusable. {v5}
      avoid_misleading_documentation: true  # [INFO] Documentation does not match the method name or code behavior. Mismatched docs confuse maintainers and lead to incorrect usage. {v4}
      avoid_missed_calls: true  # [INFO] Function reference passed without parentheses where a call was likely intended. Without the () invocation, the function is not executed and the reference is silently discarded, meaning the intended side effect or return value is lost. {v5}
      avoid_missing_image_alt: true  # [INFO] This Image widget is missing a semanticLabel, which is essential for accessibility. Without a semanticLabel, screen readers cannot describe the image to visually impaired users, making your app less inclusive and potentially non-compliant with accessibility standards. {v4}
      avoid_mixing_named_and_positional_fields: true  # [INFO] Record mixes named and positional fields. Mixing named and positional fields in records reduces readability. This pattern matching usage can cause unexpected behavior or miss important type information. {v4}
      avoid_multi_assignment: true  # [INFO] Multiple chained assignments detected. Chained assignments reduce readability and can be confusing. This excessive complexity makes the code harder to understand, test, and maintain. {v4}
      avoid_mutable_rx_variables: true  # [WARNING] Reassigning an Rx variable with = replaces the entire reactive wrapper, breaking all existing Obx listeners that still reference the old instance. The build method stops receiving updates because child widgets observe a stale object, leading to a frozen interface that appears unresponsive. {v4}
      avoid_navigator_push_without_route_name: true  # [INFO] Anonymous Navigator.push with inline MaterialPageRoute scatters route definitions throughout the codebase, making it impossible to see all routes in one place, complicating deep linking, and preventing analytics from tracking navigation paths by name. {v4}
      avoid_negated_conditions: true  # [INFO] Condition uses negation (!) where a positive equivalent exists. Negated conditions force readers to mentally invert the logic, increasing cognitive load and the likelihood of misreading compound boolean expressions during code review. {v4}
      avoid_negations_in_equality_checks: true  # [INFO] Equality check is wrapped in a negation operator !(a == b) instead of using the direct != operator. The negated form adds unnecessary cognitive overhead, increases nesting depth, and is harder to scan during code review. The != operator expresses the same intent more clearly and concisely. {v4}
      avoid_nested_conditional_expressions: true  # [INFO] Ternary expression contains another ternary, creating a multi-level conditional that is difficult to parse visually. Nested ternaries lack clear precedence at a glance and are a common source of logic errors during editing. {v4}
      avoid_nested_extension_types: true  # [INFO] Extension type wraps another extension type, creating multiple layers of zero-cost abstraction. Each layer adds indirection to the representation type, making the code harder to reason about and increasing the chance of applying the wrong extension methods to the underlying value. {v3}
      avoid_nested_futures: true  # [WARNING] Future<Future<T>> detected. Outer Future resolves to inner Future, not value - requires double await. Consequence: This pattern makes code harder to read, maintain, and debug, and can introduce subtle async bugs and runtime errors. {v4}
      avoid_nested_navigators_misuse: true  # [WARNING] Nested Navigator without WillPopScope/PopScope. Back button may behave unexpectedly. {v2}
      avoid_nested_records: true  # [INFO] Nested record type creates deeply coupled structure that is difficult to read, maintain, and refactor. Inner record positions become ambiguous (e.g., value.$1.$2), and changes to the inner record layout silently break all access sites without a compile-time error in many cases. {v5}
      avoid_nested_shorthands: true  # [INFO] Deeply nested shorthand syntax (e.g., cascades inside ternaries inside null-aware operators) reduces readability and makes expressions difficult to understand at a glance. Each nesting level multiplies cognitive load, increasing the risk of logic errors during code review and maintenance. {v5}
      avoid_nested_streams_and_futures: true  # [WARNING] Stream<Future<T>> or Future<Stream<T>> detected. Complex to consume - each item needs await or stream needs await. This increases cognitive load and can lead to memory leaks, unclosed stream subscriptions, or missed events. {v4}
      avoid_nested_switch_expressions: true  # [INFO] Switch expression contains another switch expression, creating multi-level branching that is extremely difficult to read and reason about. Each nesting level multiplies the number of result paths, making the expression fragile and hard to maintain. {v4}
      avoid_nested_switches: true  # [INFO] Switch statement contains another switch statement, creating multi-dimensional branching that is hard to follow and test. Each nesting level multiplies the number of code paths, making exhaustive testing impractical and bugs harder to locate. {v4}
      avoid_nested_try: true  # [INFO] Try-catch block nested inside another try-catch, creating layered error handling that is difficult to trace. Nested try blocks obscure which catch clause handles which error, increasing the risk of swallowing exceptions or applying the wrong recovery logic. {v4}
      avoid_non_empty_constructor_bodies: true  # [INFO] Constructor body has logic. Final fields cannot be set in body, only initializers. Constructor bodies with logic can be harder to understand. Prefer using initializer lists or factory constructors. {v4}
      avoid_notifier_constructors: true  # [WARNING] Notifier constructors break Riverpod lifecycle management. Initialization logic is skipped during rebuild. {v2}
      avoid_nullable_tostring: true  # [INFO] Calling toString() on a nullable value. Calling .toString() on a nullable value without null check. This type usage can cause unexpected runtime behavior or weaken static analysis effectiveness. {v4}
      avoid_obs_outside_controller: true  # [WARNING] .obs used outside a Getx stream controller (GetxController or GetxService) creates observables without proper lifecycle management. These observables cause memory leaks because they are never disposed when the widget tree rebuilds. {v3}
      avoid_one_field_records: true  # [INFO] Single-field record adds unnecessary wrapping overhead; the value type alone is simpler and equally expressive. The extra parentheses and trailing comma add syntactic noise, and the field can only be accessed via positional notation ($1), which obscures intent compared to a plain variable. {v5}
      avoid_over_fetching: true  # [INFO] Fetching the full object but only using a few fields, wasting bandwidth and serialization time. This network pattern wastes bandwidth and server resources, increasing latency and data costs for users. {v5}
      avoid_overengineered_bloc_states: true  # [INFO] More than 5 state subclasses adds complexity without benefit. Harder to maintain and test. Separate states for "loading" and "idle" when a boolean would suffice. Simpler state machines are easier to reason about. {v2}
      avoid_passing_default_values: true  # [INFO] Argument explicitly passes a value that matches the parameter default (e.g. empty list, false, 0). Passing the default adds noise to the call site without changing behavior, and if the library updates its default, this call site will not benefit from the change. {v4}
      avoid_passing_self_as_argument: true  # [WARNING] Object passed as an argument to its own method, creating a self-referential call. This pattern often indicates a logic error and can lead to infinite recursion, stack overflow, or unexpected mutation of the object state during method execution. {v4}
      avoid_pop_without_result: true  # [WARNING] Navigator.push awaits a result but does not handle the null case when the user dismisses the route by pressing the back button or using a system gesture. Accessing properties on a null result throws a runtime exception that crashes the app. This creates a fragile navigation flow that fails under normal user interaction patterns. {v2}
      avoid_positional_record_field_access: true  # [INFO] Positional record field access ($1, $2) is cryptic and forces readers to count positions to understand the code. As records grow, positional access becomes increasingly error-prone and makes refactoring dangerous, since reordering fields silently changes which value each accessor returns. {v5}
      avoid_print_in_production: true  # [WARNING] print() call found in production widget code. Print statements bypass structured logging, cannot be filtered by severity, pollute the console in release builds, and may inadvertently leak sensitive data. They also add unnecessary I/O overhead in production. {v5}
      avoid_production_config_in_tests: true  # [WARNING] Test may be using production configuration. Tests should use test-specific configuration. This weakens test quality, making failures harder to diagnose and reducing confidence in the test suite. {v4}
      avoid_provider_in_init_state: true  # [WARNING] Accessing Provider in initState() may fail because the widget context is not fully mounted in the element tree. This can throw a ProviderNotFoundException or return stale data, causing initialization logic to operate on incorrect values or crash on first render. {v4}
      avoid_provider_of_in_build: true  # [ERROR] Using Provider.of in build() causes the widget to rebuild every time the provider changes, which can lead to performance issues and unnecessary UI updates. This can make your app less efficient and harder to maintain. {v3}
      avoid_qr_scanner_always_active: true  # [INFO] QR scanner should pause when app is backgrounded. Camera for QR scanning drains battery significantly. Without proper lifecycle handling, the camera stays active when the app is backgrounded or the screen is turned off. {v2}
      avoid_raw_keyboard_listener: true  # [INFO] RawKeyboardListener is deprecated since Flutter 3.18. It uses the legacy RawKeyEvent system that does not correctly handle key mapping across platforms, missing modifier keys and producing inconsistent key codes. The replacement KeyboardListener uses the modern KeyEvent system with proper platform key mapping. {v4}
      avoid_recursive_calls: true  # [INFO] Function contains a direct recursive call to itself. Without a guaranteed base case or depth limit, unbounded recursion exhausts the call stack and crashes the application with a StackOverflowError, which cannot be caught in Dart. {v5}
      avoid_recursive_tostring: true  # [WARNING] toString() method references itself through $this or this.toString(), creating infinite recursion. The runtime repeatedly invokes toString() until the call stack overflows, crashing the application with an unrecoverable StackOverflowError. {v5}
      avoid_redundant_else: true  # [INFO] Else clause follows a branch that already exits via return, throw, break, or continue. The else keyword is redundant because the code after the if block only runs when the condition is false, adding unnecessary nesting and indentation. {v3}
      avoid_redundant_positional_field_name: true  # [INFO] Positional record field uses redundant default name. Positional record field has an explicit name that matches the default positional field name pattern ($1, $2, etc.). {v4}
      avoid_redundant_pragma_inline: true  # [INFO] Pragma inline annotation applied to a trivial method that the compiler already inlines automatically. Redundant annotations add noise to the codebase, and overusing pragma inline can prevent the compiler from making better optimization decisions. {v5}
      avoid_repaint_boundary_misuse: true  # [INFO] RepaintBoundary around const/static content provides no benefit. RepaintBoundary is used around static content. This layout configuration can trigger RenderFlex overflow errors or unexpected visual behavior at runtime. {v6}
      avoid_returning_cascades: true  # [INFO] Return statement contains a cascade expression (..operator), which obscures the actual return value and can confuse readers about whether the returned object is the cascade target or the result of the last cascade operation. Separating the cascade from the return makes the control flow explicit and easier to debug. {v4}
      avoid_returning_void: true  # [INFO] Explicit void return is redundant and can mask accidental side-effect returns. Writing return at the end of a void function adds visual noise without changing behavior, and in rare cases may hide a mistaken attempt to return a value from a void-typed function. {v6}
      avoid_riverpod_for_network_only: true  # [INFO] Provider adds unnecessary indirection for a simple network client. Harder to debug. Using Riverpod just to access a network layer when direct injection would suffice adds unnecessary complexity. {v2}
      avoid_riverpod_navigation: true  # [INFO] Riverpod provider managing navigation. Navigation belongs in widgets, not state management. {v2}
      avoid_secure_storage_large_data: true  # [WARNING] `[HEURISTIC]` Storing large data in secure storage. It's designed for small secrets like tokens. {v3}
      avoid_sequential_awaits: true  # [INFO] Multiple sequential awaits on independent operations. Total time is sum of all; could run in parallel. {v2}
      avoid_service_locator_in_widgets: true  # [WARNING] Service locator in widget hides dependencies. Cannot mock in widget tests. This reduces testability, maintainability, and makes code harder to refactor. {v5}
      avoid_service_locator_overuse: true  # [INFO] Service locator (e.g. GetIt.instance) called directly in a widget hides dependencies, makes the widget untestable without the full service container, and couples the UI layer to a specific DI framework. Constructor injection makes dependencies explicit and enables easy mocking in tests. {v6}
      avoid_setstate_in_large_state_class: true  # [INFO] setState() in a large State class causes expensive full rebuilds. Consider breaking into smaller widgets or using granular state. {v1}
      avoid_shared_prefs_large_data: true  # [WARNING] SharedPreferences setString called with serialized data (jsonEncode, json.encode, toJson, toString). SharedPreferences loads the entire XML/plist file into memory on first access. Storing JSON blobs or serialized objects wastes memory, degrades app startup time, and risks exceeding platform limits. Use a proper database (Hive, Isar, sqflite) for structured or large data. {v1}
      avoid_similar_names: true  # [INFO] Variable name differs from another in-scope variable by only one or two characters. Near-identical names increase the risk of accidentally using the wrong variable, producing subtle bugs that pass code review because the names look correct at a glance. {v4}
      avoid_single_field_destructuring: true  # [INFO] Single-field destructuring adds syntactic overhead without the readability benefit of multi-field destructuring. The pattern syntax (e.g., final (:name) = obj;) is unfamiliar to many Dart developers and introduces unnecessary complexity when accessing just one property. {v5}
      avoid_singleton_for_scoped_dependencies: true  # [INFO] Scoped data as singleton. State will persist across sessions/screens. Some dependencies must be scoped to a specific lifecycle (e.g., per request, per screen) rather than being global singletons. {v6}
      avoid_singleton_pattern: true  # [INFO] Class uses the singleton pattern (static instance field, factory constructor, and private constructor). Singletons hide dependencies, prevent mocking in unit tests, make it impossible to run tests in parallel with isolated state, and create implicit global state that is difficult to reason about and reset between test cases. {v5}
      avoid_sized_box_expand: true  # [INFO] SizedBox.expand() fills all available space unconditionally, causing unpredictable layout overflow in constrained parents. Inside a Column, Row, or other flex widget without explicit constraints, expand() can trigger unbounded height/width errors or silently push sibling widgets off-screen. {v3}
      avoid_slow_collection_methods: true  # [INFO] sync* generator used for a simple collection that yields a small, fixed number of elements. Generator functions have overhead from creating state machines and lazy iterables that exceeds the cost of building a plain list for small collections. {v5}
      avoid_snackbar_queue_buildup: true  # [INFO] Prefer clearing snackbars before showing new ones. Multiple snackbars can queue up, leading to poor UX where users see stale messages. Prefer clearing or hiding previous snackbars before showing new ones. {v2}
      avoid_sqflite_reserved_words: true  # [WARNING] Using SQLite reserved words as column names in SQL statements can cause syntax errors, failed migrations, and data loss. This is a common source of hard-to-diagnose database bugs and can break schema upgrades or queries. {v3}
      avoid_stateful_widget_in_list: true  # [INFO] StatefulWidget created inside a ListView.builder callback loses its State when scrolled off-screen and recreated, causing input fields to reset, animations to restart, and expanded/collapsed states to revert. The framework cannot preserve State for widgets without stable keys in a lazily-built list. {v6}
      avoid_stateful_without_state: true  # [WARNING] Using a StatefulWidget without any state fields adds unnecessary complexity, increases lifecycle overhead, and can confuse maintainers. This leads to harder-to-read code, wasted memory allocations for the State object, and potential performance issues. {v6}
      avoid_static_route_config: true  # [WARNING] Static router configuration prevents hot reload. Route changes require full restart. {v2}
      avoid_stream_sync_events: true  # [WARNING] Stream event added synchronously right after controller creation. Listeners may not be attached yet. {v2}
      avoid_string_concatenation_in_ui: true  # [INFO] String concatenation breaks internationalization. Word order varies by language, so concatenation breaks i18n. {v4}
      avoid_synchronous_file_io: true  # [WARNING] Synchronous file I/O blocks the main thread, causing UI freezes and dropped frames during disk access. {v4}
      avoid_test_implementation_details: true  # [INFO] Test verifies internal calls. Test behavior, not implementation. Tests that verify internal method calls break when you refactor. Test observable behavior (outputs, state changes) instead. {v3}
      avoid_text_in_images: true  # [INFO] Image path suggests embedded text that cannot be localized. Text in images cannot be translated. This prevents proper localization, causing text to display incorrectly for users in non-English locales. {v5}
      avoid_text_span_in_build: true  # [INFO] TextSpan recreated inside the build() method forces the Flutter rendering pipeline to recalculate expensive text layout metrics on every widget rebuild. This includes glyph positioning, line breaking, and paragraph layout. The repeated computation causes visible jank during scrolling and animations, especially for RichText with multiple styled spans. {v4}
      avoid_throw_objects_without_tostring: true  # [INFO] Thrown objects without a useful toString() method produce cryptic error logs that hinder troubleshooting. When caught in production, these errors display unhelpful messages like "Instance of MyClass" instead of actionable details, making it nearly impossible to diagnose root causes from crash reports. {v6}
      avoid_tight_coupling_with_getx: true  # [INFO] Class with 5+ GetX usages becomes tightly coupled and difficult to unit test. Using GetX for everything leads to tight coupling and hard-to-test code. Use only necessary features. {v2}
      avoid_too_many_dependencies: true  # [WARNING] Constructor has >5 dependencies. Class likely violates Single Responsibility. Classes with many dependencies often violate single responsibility. Break the class into smaller, focused components. {v5}
      avoid_top_level_members_in_tests: true  # [INFO] Public top-level member in test file leaks implementation details and pollutes the library namespace. Other files can accidentally import test helpers, creating fragile dependencies on code that was never designed for reuse and may change without notice. {v3}
      avoid_ui_in_domain_layer: true  # [WARNING] Domain-layer file (models, entities, or domain directory) references Flutter UI types such as Widget, BuildContext, Color, or TextStyle. This couples domain logic to the Flutter framework, preventing code reuse in non-Flutter contexts (CLI tools, server-side Dart, packages), breaking testability without widget infrastructure, and violating clean architecture boundaries. {v5}
      avoid_unassigned_fields: true  # [INFO] Field declared without an initializer and no constructor or method assigns it a value. Reading this field returns the default value (null for nullable types), which may cause unexpected NullPointerExceptions or logic errors if the caller expects a meaningful value. {v4}
      avoid_unconstrained_images: true  # [INFO] Image without sizing constraints causes layout shifts on load. This layout configuration can trigger RenderFlex overflow errors or unexpected visual behavior at runtime. {v2}
      avoid_unnecessary_block: true  # [INFO] Unnecessary nested code blocks add clutter and reduce readability. They rarely serve a purpose and are often left from copy-paste or refactoring. {v4}
      avoid_unnecessary_call: true  # [INFO] Using .call() on a function or callable is redundant. Dart automatically calls .call() when you use (). {v4}
      avoid_unnecessary_collections: true  # [INFO] Wrapping an existing collection literal with List.of() or Set.of() creates a redundant copy, wasting memory and adding unnecessary overhead. The literal itself already produces the correct collection type. {v4}
      avoid_unnecessary_compare_to: true  # [INFO] Using compareTo() == 0 for equality checking is unnecessarily verbose when the == operator expresses the same comparison directly. The compareTo method is designed for ordering (less than, greater than), and using it for equality adds cognitive overhead, increases code length, and obscures the developer's intent of a simple equality check. {v4}
      avoid_unnecessary_constructor: true  # [INFO] Empty constructors are redundant—Dart provides a default constructor automatically. Leaving them in adds noise and may confuse readers. {v5}
      avoid_unnecessary_continue: true  # [INFO] Continue statement at the end of a loop body has no effect because the loop would proceed to the next iteration anyway. The redundant statement adds noise and can mislead readers into thinking it skips code that follows. {v3}
      avoid_unnecessary_digit_separators: true  # [INFO] Unnecessary or poorly placed digit separator. Digit separators should improve readability, not hinder it. {v5}
      avoid_unnecessary_enum_arguments: true  # [INFO] Providing an argument to an enum constructor that matches the default value is unnecessary and can make the code harder to read. It may also mislead readers into thinking the value is intentionally different from the default. {v4}
      avoid_unnecessary_enum_prefix: true  # [INFO] Using the enum type name as a prefix when referencing enum values inside the enum declaration is redundant. Dart allows you to reference enum values directly within the enum, and including the prefix adds unnecessary verbosity. {v4}
      avoid_unnecessary_extends: true  # [INFO] All Dart classes implicitly extend Object, so explicitly writing extends Object is unnecessary and adds clutter to your class declaration. This can confuse readers and is never required. {v4}
      avoid_unnecessary_futures: true  # [INFO] Async function has no await expressions and gains nothing from being async. The unnecessary Future wrapper adds overhead and misleads callers about asynchronous behavior. {v4}
      avoid_unnecessary_gesture_detector: true  # [WARNING] GestureDetector wraps a child widget but has no gesture callbacks (onTap, onDoubleTap, onLongPress, etc.) defined, making it a redundant wrapper that adds an unnecessary layer to the widget tree and confuses maintainers reading the code. {v6}
      avoid_unnecessary_getter: true  # [INFO] Getter just returns a final field without additional logic. This unnecessary code increases cognitive load without providing functional benefit. {v6}
      avoid_unnecessary_if: true  # [INFO] If statement returns true in one branch and false in the other, which is equivalent to returning the condition expression directly. The extra branching adds visual complexity without changing the result. {v4}
      avoid_unnecessary_late_fields: true  # [INFO] Field marked as late but already assigned in the constructor or initializer list. The late keyword is redundant here and misleads readers into thinking initialization is deferred, while also disabling the compile-time guarantee that the field is always initialized. {v5}
      avoid_unnecessary_length_check: true  # [INFO] Use isNotEmpty instead of length comparison. Quick fix available: Replaces with .isEmpty or .isNotEmpty. {v4}
      avoid_unnecessary_local_late: true  # [INFO] Local variable declared as late but assigned a value on the same line. The late keyword is designed for deferred initialization, so using it on an immediately initialized variable is misleading and disables the compile-time check that ensures the variable is assigned. {v5}
      avoid_unnecessary_local_variable: true  # [INFO] Variable is declared, used once, and returned on the next line. This intermediate variable adds a line of code without improving readability and forces readers to track an extra name through the function. {v4}
      avoid_unnecessary_negations: true  # [INFO] Unnecessary negation can be simplified. This unnecessary code increases cognitive load without providing functional benefit. {v5}
      avoid_unnecessary_nullable_fields: true  # [INFO] Nullable field is always assigned a non-null value across all constructors and assignment sites. The unnecessary nullable type forces every read site to handle null even though it can never occur, adding redundant null checks and obscuring the actual data flow. {v4}
      avoid_unnecessary_nullable_parameters: true  # [INFO] Parameter declared as nullable but null is never passed at any call site. The unnecessary nullable type forces every usage within the function body to handle a null case that cannot occur, adding defensive checks and reducing code clarity. {v4}
      avoid_unnecessary_nullable_return_type: true  # [INFO] Return type is nullable but function never returns null. Unnecessary nullability forces callers to add redundant null checks, reducing code clarity and type safety. {v3}
      avoid_unnecessary_overrides: true  # [INFO] Method override only delegates to super without adding any logic. Unnecessary overrides clutter the class, obscure the inheritance chain, and add a maintenance burden because developers must inspect each override to confirm it does nothing beyond the parent implementation. {v4}
      avoid_unnecessary_overrides_in_state: true  # [INFO] Override only calls super with no additional logic. State class has unnecessary overrides. This violates the widget lifecycle, risking setState-after-dispose errors or silent state corruption. {v4}
      avoid_unnecessary_patterns: true  # [INFO] Pattern matching syntax used where it does not narrow types or destructure values. The pattern adds syntactic complexity without any type-safety benefit, making the code harder to read compared to a plain variable declaration or assignment. {v5}
      avoid_unnecessary_return: true  # [INFO] Unnecessary return statement at end of void function. This return pattern causes unexpected control flow and makes function behavior harder to predict. {v5}
      avoid_unnecessary_stateful_widgets: true  # [INFO] StatefulWidget may be unnecessary. If a State class never calls setState and has no mutable state, it should probably be a StatelessWidget. This violates the widget lifecycle, risking setState-after-dispose errors or silent state corruption. {v6}
      avoid_unnecessary_super: true  # [INFO] Unnecessary super() call with no arguments. Super call is unnecessary in constructor. This unnecessary code increases cognitive load without providing functional benefit. {v4}
      avoid_unnecessary_to_list: true  # [INFO] .toList() may be unnecessary here. Lazy iterables are more efficient. Calling .toList() after .map(), .where(), .take(), etc. creates an intermediate list that may not be needed. Dart's lazy iterables are more memory efficient. {v3}
      avoid_unrestricted_text_field_length: true  # [INFO] TextField without maxLength allows unbounded input, enabling users to paste megabytes of text that can freeze the UI, exhaust memory, and create oversized payloads for backend APIs. Setting maxLength protects against denial-of-service scenarios and enforces data integrity constraints. {v6}
      avoid_unused_after_null_check: true  # [INFO] Variable is null-checked in a condition but never referenced inside the guarded block. The null check implies the variable is needed, so the missing reference likely indicates a logic error where the intended usage was accidentally omitted. {v3}
      avoid_unused_assignment: true  # [INFO] Variable is assigned a value that is never read before being overwritten or going out of scope. The assignment wastes computation, and the unused result often signals a logic error where the value was meant to be used in a subsequent expression or return statement. {v3}
      avoid_unused_callback_parameters: true  # [INFO] Callback parameter is declared but never referenced in the closure body, adding visual noise and misleading readers into thinking the value is needed. Unused parameters also trigger analyzer warnings and obscure the actual data flow of the callback. {v4}
      avoid_unused_parameters: true  # [INFO] Function parameter is declared but never referenced in the function body. Unused parameters add cognitive overhead for callers who must provide a value that has no effect, and they mask API design issues — either remove the parameter or complete the implementation that was supposed to use it. {v6}
      avoid_vague_test_descriptions: true  # [INFO] Test description is too vague. Test names should clearly describe what is being tested. This weakens test quality, making failures harder to diagnose and reducing confidence in the test suite. {v5}
      avoid_watch_in_callbacks: true  # [ERROR] Using watch in callbacks (like onPressed or onTap) creates new subscriptions on every call, leading to memory leaks, redundant widget rebuilds, and degraded app performance. This can cause your app to slow down or even crash over time. {v4}
      avoid_websocket_without_heartbeat: true  # [INFO] WebSocket without heartbeat/ping. Dead connections won't be detected. WebSocket connections can silently fail. Periodic pings help detect dead connections and keep firewalls from closing idle sockets. {v3}
      avoid_webview_javascript_enabled: true  # [WARNING] WebView with JavaScript enabled expands the attack surface to cross-site scripting (XSS) vulnerabilities. Malicious scripts injected through untrusted content can steal session tokens, access device APIs, redirect users to phishing pages, and exfiltrate sensitive data without the user's knowledge. {v5}
      avoid_widget_creation_in_loop: true  # [INFO] Widgets created in .map() or a loop are all instantiated eagerly on every rebuild, including those currently off-screen. This causes jank, high memory usage, and slow rendering for long lists because Flutter allocates and lays out every item upfront instead of lazily constructing only visible items. {v3}
      avoid_wrapping_in_padding: true  # [INFO] Widget has its own padding property, avoid wrapping in Padding. This layout configuration can trigger RenderFlex overflow errors or unexpected visual behavior at runtime. {v6}
      check_for_equals_in_render_object_setters: true  # [WARNING] RenderObject setter should check equality before updating. RenderObject property setters should check if the new value equals the old value before updating and marking needs layout/paint. {v5}
      consistent_update_render_object: true  # [WARNING] updateRenderObject may be missing property updates from createRenderObject. When a RenderObjectWidget creates a RenderObject with properties, the updateRenderObject method should update all those same properties. {v4}
      dispose_getx_fields: true  # [WARNING] Undisposed Worker keeps timer running after GetxController closes, causing memory leaks and stale updates. {v2}
      function_always_returns_same_value: true  # [INFO] Function returns the same value on every code path regardless of input. The function body adds complexity without varying the output, suggesting the logic branches are incomplete or the function can be replaced by a constant. {v5}
      limit_max_imports: true  # [INFO] File has more than 20 imports. A high import count signals the file depends on too many modules, which increases coupling, makes refactoring risky, and slows incremental compilation. {v3}
      match_base_class_default_value: true  # [INFO] Overridden method parameter has a different default value than the parent class. Callers using the parent type see the parent default, while callers using the subtype see the override default, creating inconsistent behavior depending on the variable type. {v3}
      match_getter_setter_field_names: true  # [INFO] Getter/setter name should match the backing field. Getter/setter must have the same name as the backing field (without underscore). {v4}
      match_lib_folder_structure: true  # [INFO] Test file location should mirror lib folder structure. This naming violation reduces readability and makes the codebase harder for teams to navigate. {v4}
      match_positional_field_names_on_assignment: true  # [INFO] Positional field name should match the variable being assigned. This naming violation reduces readability and makes the codebase harder for teams to navigate. {v5}
      missing_use_result_annotation: true  # [INFO] Function returns a value without @useResult annotation. Callers may accidentally ignore the return value, leading to missed error handling, lost data transformations, or incorrectly assuming the function has side effects when it does not. {v5}
      move_records_to_typedefs: true  # [INFO] Inline record with >3 fields reduces readability. Complex record types are easier to read as typedefs. This pattern matching usage can cause unexpected behavior or miss important type information. {v5}
      move_variable_outside_iteration: true  # [INFO] Variable declared and assigned inside a loop body produces the same value on every iteration. Recreating the same object or computing the same expression repeatedly wastes CPU cycles and puts unnecessary pressure on the garbage collector. {v4}
      no_empty_block: true  # [INFO] An empty code block (i.e., {}) does not perform any action and may indicate incomplete code, a forgotten implementation, or a placeholder left by mistake. Empty blocks can confuse maintainers and may hide bugs or unfinished features.
      no_equal_switch_case: true  # [INFO] Multiple switch cases contain identical body code. Duplicated case logic increases maintenance cost because changes must be applied to every copy, and missed updates cause inconsistent behavior across cases that are meant to be equivalent. {v4}
      no_equal_switch_expression_cases: true  # [INFO] Multiple switch expression cases produce identical result values. Duplicate results increase maintenance cost because changes must be applied to every copy, and they obscure whether the cases were truly intended to behave the same. {v4}
      no_magic_number: true  # [INFO] Unexplained numeric literal makes the code harder to understand, maintain, and update consistently. When the same value appears in multiple locations, a typo in one creates a subtle bug. Readers cannot determine whether the number represents a timeout, a threshold, a count, or an index without surrounding context. {v7}
      no_magic_string: true  # [INFO] Unexplained string literal makes the code harder to understand, maintain, and update consistently. Duplicate string values across the codebase lead to inconsistencies when one occurrence is updated but others are missed, causing hard-to-trace bugs in routing, API calls, or status checks. {v7}
      no_object_declaration: true  # [INFO] Member declared with type Object, which erases all type information. Accessing any property or method requires an unsafe downcast, bypassing compile-time type checking and risking runtime cast errors that could have been prevented with a more specific type. {v4}
      pass_optional_argument: true  # [INFO] Function call omits important optional parameter, relying on default value. Future readers must hunt for the function definition to understand the omitted behavior, making code harder to comprehend and maintain at the call site. {v4}
      prefer_abstract_dependencies: true  # [INFO] Depends on concrete implementation. Tight coupling prevents substitution. Dependencies should depend on abstractions (interfaces/abstract classes), not concrete implementations, following the Dependency Inversion Principle. {v5}
      prefer_abstract_final_static_class: true  # [INFO] Class with only static members must be declared abstract final to prevent instantiation. Creating instances of a static-only class is meaningless and misleads readers about its intended usage. {v4}
      prefer_adaptive_dialog: true  # [INFO] AlertDialog without adaptive styling. May look non-native on iOS. Dialogs should adapt to the platform (Material on Android, Cupertino on iOS) for native look and feel. {v3}
      prefer_add_all: true  # [INFO] Looping with forEach or for-in to call add() one element at a time is verbose and slower than addAll(), which can pre-allocate capacity and copy elements in bulk. This pattern also obscures the intent of batch insertion. {v4}
      prefer_addition_subtraction_assignments: true  # [INFO] Use compound assignment operator. This numeric literal usage can cause precision errors or make the intended value unclear. {v4}
      prefer_any_or_every: true  # [INFO] Collection filtered with where() only to check isEmpty/isNotEmpty. The where() call creates an intermediate lazy iterable and allocates a closure, while any() and every() short-circuit on the first matching element without creating intermediate objects. {v5}
      prefer_assigning_await_expressions: true  # [INFO] Inline await expression. Harder to debug and inspect intermediate values. Consequence: Extracting to a variable improves readability, makes debugging easier, and helps catch errors sooner. {v5}
      prefer_async_await: true  # [INFO] Using .then() inside an async function can hide errors in nested callbacks and makes code harder to debug, trace, and maintain. This can lead to missed exceptions and subtle bugs in complex async flows. {v6}
      prefer_async_callback: true  # [INFO] VoidCallback discards Futures silently. Errors will be swallowed and callers cannot await completion. {v4}
      prefer_audio_session_config: true  # [INFO] AudioPlayer used without audio session config. May conflict with other audio. Audio session determines how your app interacts with other audio. Without configuration, audio may behave unexpectedly. {v2}
      prefer_autovalidate_on_interaction: true  # [INFO] AutovalidateMode.always triggers form validation on every keystroke and widget rebuild. This causes visible input lag as validators run continuously, fires excessive error messages before the user finishes typing, and degrades the user experience with distracting red error text that appears immediately on empty fields. {v3}
      prefer_background_sync: true  # [INFO] Data sync in initState() only runs when app is open. Consider background sync for better user experience. {v2}
      prefer_base_class: true  # [INFO] Abstract class with shared implementation could be base. Dart 3.0 introduced the base modifier for classes meant to be extended but not implemented directly. {v4}
      prefer_bloc_listener_for_side_effects: true  # [WARNING] Side effects inside BlocBuilder execute on every widget rebuild, causing user-facing errors like duplicate navigation pushes, multiple snackbars stacking on screen, or repeated API calls that waste bandwidth and may corrupt server-side state. {v6}
      prefer_bloc_test_package: true  # [INFO] Use blocTest() from bloc_test package for Bloc testing. The bloc_test package provides blocTest() which is specifically designed for testing Blocs with better state and event assertions. {v3}
      prefer_both_inlining_annotations: true  # [INFO] Only one inlining pragma is present, but the Dart VM and dart2js compilers use different annotations. Without both vm:prefer-inline and dart2js:tryInline, the function is only inlined on one platform, leaving the other without the intended optimization. {v3}
      prefer_builder_for_conditional: true  # [INFO] Complex widget in ternary conditional. Prefer if/return for readability. This introduces unnecessary computational overhead that degrades responsiveness and increases battery drain on mobile. {v1}
      prefer_bytes_builder: true  # [INFO] List<int> with repeated addAll operations detected. Each addAll call may trigger memory reallocation and copying, causing O(n²) performance when building large byte arrays, resulting in slow processing and excessive memory churn. {v5}
      prefer_cached_getter: true  # [INFO] Repeated getter calls recompute expensive values each time, wasting CPU cycles when caching would suffice. {v5}
      prefer_cached_network_image: true  # [INFO] Image.network re-downloads images every time the widget rebuilds or the user navigates back to the screen, wasting bandwidth and causing visible loading flicker. CachedNetworkImage persists images to disk, loads them instantly on subsequent visits, and supports placeholder and error widgets out of the box. {v4}
      prefer_camera_resolution_selection: true  # [INFO] CameraController with max resolution. Prefer app-specific needs. Camera resolution affects performance, battery, and storage. Always specify the desired resolution for your use case. {v2}
      prefer_carousel_view: true  # [INFO] Third-party carousel package adds dependency maintenance overhead, increases app size, and may not follow Material 3 design guidelines. The built-in CarouselView widget (Flutter 3.24+) provides standard M3 carousel behavior with accessibility support, animation curves, and theme integration out of the box. {v3}
      prefer_color_scheme_from_seed: true  # [INFO] Manually constructing a ColorScheme requires specifying 20+ color roles with correct contrast ratios. Missing or mismatched colors cause poor contrast, invisible text, or WCAG accessibility failures. ColorScheme.fromSeed algorithmically generates all roles from a single seed color with guaranteed accessibility compliance. {v4}
      prefer_commenting_analyzer_ignores: true  # [INFO] Analyzer ignore comment must have a preceding explanatory comment. This debug artifact executes in production, potentially exposing internal state or degrading performance. {v5}
      prefer_commenting_future_delayed: true  # [INFO] Unexplained delay is a code smell that often hides race conditions or timing bugs. {v4}
      prefer_compound_assignment_operators: true  # [INFO] Use compound assignment operator. Similar to PreferAdditionSubtractionAssignmentsRule but focuses on less common operators. {v4}
      prefer_const_border_radius: true  # [INFO] Prefer const BorderRadius.all for constant border radius. Non-const BorderRadius constructors are used. This layout configuration can trigger RenderFlex overflow errors or unexpected visual behavior at runtime. {v6}
      prefer_const_string_list: true  # [INFO] This <String>[...] list contains only string literals and could be const. {v3}
      prefer_const_widgets: true  # [INFO] Widget constructor could use const but is recreated on every build() call. Non-const widgets force Flutter to allocate new instances, compare entire subtrees, and perform unnecessary rebuilds. This wastes CPU cycles and battery, degrading frame rendering performance especially in frequently rebuilt parent widgets. {v4}
      prefer_consumer_widget: true  # [ERROR] Wrapping widgets with Consumer adds unnecessary nesting and boilerplate instead of using ConsumerWidget directly. The extra widget layer causes redundant rebuilds and increases the widget tree depth, leading to harder-to-debug rebuild cascades and degraded rendering performance. {v4}
      prefer_context_selector: true  # [INFO] context.watch() accessing property. Use select() for efficiency. watch() rebuilds on any change to the provider. Using select() limits rebuilds to specific property changes. {v2}
      prefer_correct_edge_insets_constructor: true  # [INFO] EdgeInsets constructor is more verbose than necessary when all values are equal or symmetric. Use .all() or .symmetric() to improve readability and communicate intent more clearly. {v5}
      prefer_correct_error_name: true  # [INFO] Catch parameter uses nonstandard name. Use "e" or "error" for consistency and readability. Consistent naming of catch parameters improves readability. {v4}
      prefer_correct_future_return_type: true  # [INFO] Missing Future return type causes callers to handle dynamic, losing type safety and IDE support. {v3}
      prefer_correct_identifier_length: true  # [INFO] Very short (1-2 char) or very long (>30 char) identifiers measurably impact code comprehension speed. Optimal readability is achieved with 8-20 character names. {v6}
      prefer_correct_stream_return_type: true  # [INFO] Missing Stream return type causes listeners to receive dynamic, losing type safety and IDE support. {v5}
      prefer_correct_switch_length: true  # [INFO] Switch statement has fewer cases than the minimum threshold, making the switch overhead unnecessary. A switch with one or two cases is more verbosely expressed than an equivalent if/else and adds boilerplate (case, break) without benefit. {v4}
      prefer_correct_test_file_name: true  # [INFO] Test files not following the _test.dart naming convention are invisible to dart test and CI runners. Non-matching names cause tests to be silently skipped. {v6}
      prefer_cubit_for_simple: true  # [ERROR] Using Bloc for simple state management with few events adds unnecessary boilerplate, indirection, and makes code harder to maintain. This can slow down development and introduce avoidable complexity. {v3}
      prefer_cupertino_for_ios: true  # [INFO] Material widget in iOS-specific code block. Consider using Cupertino equivalent for native iOS feel. {v2}
      prefer_cupertino_for_ios_feel: true  # [INFO] Material widget has Cupertino equivalent for native iOS feel. Material widgets look foreign on iOS. Use Cupertino equivalents or adaptive widgets for native iOS feel. {v2}
      prefer_date_format: true  # [INFO] Raw DateTime formatting ignores user locale. DateTime.toString() and manual formatting produce inconsistent results across locales. Use DateFormat from intl package for proper i18n. {v2}
      prefer_declaring_const_constructor: true  # [INFO] Class could have a const constructor. A class could have a const constructor but doesn't. This class design reduces clarity and can lead to incorrect object initialization. {v5}
      prefer_dedicated_media_query_method: true  # [INFO] MediaQuery.of(context) accessed for a single property. This registers a dependency on the entire MediaQueryData object, causing the widget to rebuild whenever any media query value changes (orientation, padding, text scale), even if only one property is needed. {v6}
      prefer_digit_separators: true  # [INFO] Large number should use digit separators. Digit separators improve readability of large numbers. This numeric literal usage can cause precision errors or make the intended value unclear. {v4}
      prefer_dio_cancel_token: true  # [INFO] Long-running Dio request without CancelToken. Cannot be canceled. Requests must be cancellable to avoid wasting resources when the user navigates away. {v3}
      prefer_dio_over_http: true  # [INFO] Using http package. Dio provides interceptors, cancellation, and structured error handling suited for production apps. {v2}
      prefer_dio_transformer: true  # [INFO] Dio instance without custom transformer for large data. Parsing large JSON responses on the main thread causes jank. Use BackgroundTransformer or compute() for heavy parsing. {v2}
      prefer_encrypted_prefs: true  # [WARNING] Unencrypted sensitive data is exposed via device backup, file browser, or rooted device access. {v2}
      prefer_enhanced_enums: true  # [INFO] Use enhanced enum instead of extension. An enum could use enhanced enum features instead of extensions. {v4}
      prefer_enums_by_name: true  # [INFO] Enum lookup uses firstWhere with name comparison instead of the built-in byName() method. The manual approach is more verbose, less readable, and throws a generic StateError on mismatch instead of the descriptive ArgumentError that byName() provides. {v5}
      prefer_expect_later: true  # [WARNING] Use expectLater() for Future assertions. Quick fix available: Replaces expect with expectLater. Expect() is used with a Future instead of expectLater(). {v5}
      prefer_explicit_function_type: true  # [INFO] Use explicit function type instead of bare "Function". The bare 'Function' type is too permissive and loses type information. Using bare 'Function' type instead of specific function type. {v5}
      prefer_explicit_parameter_names: true  # [INFO] Function type parameters must have descriptive names. Unnamed parameters lose intent and force callers to guess what each positional argument represents. {v4}
      prefer_explicit_semantics: true  # [INFO] Custom build methods lack explicit Semantics, making their child elements invisible or meaningless to screen readers and assistive technologies. All non-trivial elements in the widget tree must provide a semantic label or description for accessibility. {v2}
      prefer_extracting_callbacks: true  # [INFO] Inline callback exceeds a reasonable length, reducing readability and making the build method harder to maintain. Long inline closures obscure widget structure, complicate debugging, and prevent reuse of the callback logic across widgets. {v3}
      prefer_extracting_function_callbacks: true  # [INFO] Large inline callback detected spanning 10+ lines. Inline callbacks make code harder to read, test in isolation, and reuse across multiple call sites, reducing code maintainability and increasing complexity. {v4}
      prefer_family_for_params: true  # [ERROR] Not using .family for parameterized providers creates a new provider instance on each call, breaking caching and causing duplicate providers. This leads to wasted resources, memory leaks, and unpredictable app behavior. {v2}
      prefer_final_class: true  # [INFO] Prefer marking this class as final. Dart 3.0 introduced the final modifier to prevent subclassing. Classes that are not designed for extension must be marked final. {v4}
      prefer_firestore_batch_write: true  # [INFO] Individual writes increase latency and billing costs. Multiple individual write operations are slower and more expensive than batch writes. Use WriteBatch for multiple related operations. {v3}
      prefer_focus_traversal_order: true  # [INFO] Form with multiple inputs in Row/Wrap layout without FocusTraversalOrder. Keyboard navigation may be confusing. {v2}
      prefer_for_loop_in_children: true  # [INFO] Prefer collection-for over List.generate in children. For building widget lists, prefer for-in collection or spread to improve readability and performance. {v2}
      prefer_foreground_service_android: true  # [INFO] Long-running periodic timer without foreground service. Android 8+ aggressively kills background tasks — Timer.periodic and Isolate.spawn are terminated within minutes when the app is backgrounded. Use a foreground service with notification (flutter_foreground_task, android_alarm_manager) for reliable ongoing work like uploads, GPS tracking, or audio. {v1}
      prefer_geocoding_cache: true  # [INFO] Reverse geocoding call without evident caching. Reverse geocoding (coordinates to address) incurs API calls, network latency, and rate limits. The same coordinates almost always resolve to the same address, so caching results dramatically reduces API usage and improves response time. Store results in a local Map or persistent cache keyed by rounded coordinates. {v1}
      prefer_geolocator_accuracy_appropriate: true  # [INFO] LocationAccuracy.high uses GPS and drains battery significantly. LocationAccuracy.high uses GPS and significantly drains battery. For features that don't need precise location, use lower accuracy. {v2}
      prefer_geolocator_last_known: true  # [INFO] getCurrentPosition polls GPS. Prefer getLastKnownPosition for cached location. getLastKnownPosition returns cached location without GPS poll. Use it when fresh location isn't critical. {v2}
      prefer_getx_builder: true  # [WARNING] .obs value accessed outside Obx or GetX builder. The UI will display stale data because reactive variable changes are silently ignored without an observable wrapper that triggers widget rebuilds. {v2}
      prefer_go_router_redirect: true  # [INFO] GoRouter created without a redirect callback. Without redirect, authentication and authorization checks must happen in build(), which briefly shows the protected page before redirecting. Users see a flash of content they should not access, and crawlers or screen readers may capture protected information. Use the redirect callback to intercept navigation before any UI renders. {v1}
      prefer_grace_period_handling: true  # [INFO] Purchase status check only handles PurchaseStatus.purchased without considering pending or grace period states. Apple and Google give users a billing grace period (typically 3-16 days) when their payment method fails. During this time, the subscription is still active but in a grace state. Locking out these users causes churn — they intend to pay but lose access, often uninstalling the app instead of fixing payment. {v1}
      prefer_hive_encryption: true  # [WARNING] Unencrypted Hive box stores data in plaintext. Anyone with device access can read sensitive user data. {v2}
      prefer_image_size_constraints: true  # [INFO] Missing cacheWidth/cacheHeight decodes full resolution into memory. Images decoded at full resolution consume excessive memory. Use cacheWidth or cacheHeight to decode images at display size, significantly reducing memory usage for large images. {v3}
      prefer_implicit_animations: true  # [INFO] Simple animations such as opacity, size, or color changes are implemented with explicit AnimationController and transition widgets. This adds unnecessary complexity and increases the risk of memory leaks if disposal is missed. Prefer using implicit animation widgets like AnimatedOpacity or AnimatedContainer, which are simpler, auto-dispose, and improve code maintainability and reliability. {v2}
      prefer_infinite_scroll_preload: true  # [INFO] Infinite scroll loads next page only at 100%% scroll extent. Users see a loading spinner and must wait for content. Preload the next page at 70-80%% scroll progress so data arrives before the user reaches the end. This creates a seamless experience without visible pauses. Use position.pixels >= position.maxScrollExtent * 0.8 as threshold. {v1}
      prefer_input_formatters: true  # [INFO] Text field has a structured keyboardType (phone, number, datetime) but no inputFormatters. The keyboard type only restricts which keys appear — it does not format, mask, or validate input. Users must manually type dashes, parentheses, and spaces for phone numbers, credit cards, and dates, leading to inconsistent data and entry errors. Add TextInputFormatter to auto-format as the user types. {v1}
      prefer_interface_class: true  # [INFO] Abstract class with only abstract members could be interface. Dart 3.0 introduced the interface modifier for pure contracts. Abstract classes with no implementation should use interface class. {v4}
      prefer_intl_message_description: true  # [INFO] Intl.message missing description (desc). Translators lack context, leading to poor or incorrect translations. {v2}
      prefer_intl_name: true  # [INFO] Intl.message without name parameter. Message extraction tools require the name parameter to identify and track translatable strings across the codebase. {v4}
      prefer_ios_app_intents_framework: true  # [INFO] Legacy SiriKit Intent detected. Consider migrating to App Intents framework (iOS 16+) for better Siri and Shortcuts integration. {v2}
      prefer_ios_context_menu: true  # [INFO] ListTile with multiple actions could benefit from a context menu. iOS users expect long-press for secondary actions. {v2}
      prefer_ios_handoff_support: true  # [INFO] Document or content editing detected. Consider implementing iOS Handoff for continuity across devices. {v2}
      prefer_ios_haptic_feedback: true  # [INFO] Consider adding haptic feedback for important button interactions on iOS. {v2}
      prefer_ios_spotlight_indexing: true  # [INFO] Searchable content detected. Consider indexing with Core Spotlight so users can find content from iOS home screen. {v2}
      prefer_ios_storekit2: true  # [INFO] Consider using StoreKit 2 APIs for new in-app purchase implementations. StoreKit 2 offers better async support and automatic receipt verification. {v3}
      prefer_iso8601_dates: true  # [INFO] Locale-specific date format detected instead of ISO 8601. Non-standard date formats break cross-system interoperability and can cause parsing failures or timezone-related data corruption. {v2}
      prefer_iterable_of: true  # [WARNING] Using .from() performs a runtime cast on each element, which can silently succeed with wrong types and throw later. The .of() constructor enforces type safety at the call site, catching type mismatches immediately. {v4}
      prefer_iterable_operations: true  # [INFO] Calling .toList() at the end of an iterable chain inside a for-in loop forces eager evaluation and allocates an intermediate list that is iterated once and then discarded. This wastes memory and CPU cycles. {v2}
      prefer_keyboard_shortcuts: true  # [INFO] Desktop app should implement keyboard shortcuts for common actions. Desktop users expect Ctrl+S, Ctrl+Z, etc. Implement Shortcuts and Actions for standard keyboard interactions. {v2}
      prefer_lazy_singleton_registration: true  # [INFO] Expensive service (Database, Analytics, Cache) registered as eager singleton. The service initializes immediately at app startup, slowing down launch time and consuming resources even if the service is never used during the session. {v3}
      prefer_list_contains: true  # [INFO] Using indexOf() with a comparison to -1 or 0 to check element presence is verbose and error-prone. The contains() method expresses intent directly, improving readability and reducing off-by-one mistakes. {v2}
      prefer_local_auth: true  # [INFO] Payment/sensitive operation without biometric authentication. Critical operations like payments should require additional authentication to prevent unauthorized access even if the device is unlocked. {v3}
      prefer_logger_over_print: true  # [INFO] Using print() for logging in production code is discouraged because print statements are not filterable, lack log levels, and may expose sensitive information in release builds. Proper logging allows for better control, filtering, and analysis of application events. {v3}
      prefer_match_file_name: true  # [INFO] When the primary class name does not match the file name, developers cannot use IDE file search to find declarations. This measurably slows navigation in large codebases. {v5}
      prefer_maybe_pop: true  # [WARNING] Navigator.pop() called without verifying that a route exists on the navigation stack to pop. When the stack is empty or contains only the root route, this call throws a FlutterError at runtime, crashing the app. On Android, this also bypasses the system back button contract, preventing the app from exiting gracefully. {v3}
      prefer_mock_navigator: true  # [INFO] Navigator usage in test must be mocked for verification. Real navigation in tests can cause issues and makes tests harder to verify. Navigator is used in tests without mocking. {v4}
      prefer_mock_verify: true  # [INFO] Mock setup with when() but no verify(). Test may pass without calling mock. Using verify() ensures that expected method calls actually happened. Without verification, tests may pass even when methods aren't called. {v3}
      prefer_moving_to_variable: true  # [INFO] Expression repeated multiple times within the same scope. Repeated expressions waste CPU recalculating the same value, make code harder to maintain when the logic changes, and increase the chance of typos creating subtle bugs. {v4}
      prefer_named_boolean_parameters: true  # [INFO] Positional boolean parameter detected. Call sites like doThing(true) give no indication what the boolean controls, forcing readers to look up the function signature to understand the intent. {v5}
      prefer_named_extensions: true  # [INFO] Anonymous extension must be named. This naming violation reduces readability and makes the codebase harder for teams to navigate. {v5}
      prefer_named_parameters: true  # [INFO] Function has too many positional parameters. Call sites with multiple positional arguments like create("a", 1, true, null) are error-prone because argument order is easy to confuse and provides no self-documentation. {v6}
      prefer_notification_grouping: true  # [INFO] Multiple notifications shown in loop without groupKey. Notifications will clutter the notification shade. {v2}
      prefer_null_aware_elements: true  # [INFO] Explicit null check with if (x != null) x in collection literals is verbose. Dart 3 supports the ?element syntax, which eliminates the boilerplate and expresses nullable inclusion more concisely. {v5}
      prefer_null_aware_spread: true  # [INFO] Nullable collection spread without null-aware operator. Spreading a nullable list or set without ...? throws a runtime TypeError when the value is null, crashing the collection literal construction instead of gracefully contributing zero elements. {v4}
      prefer_null_object_pattern: true  # [INFO] Optional dependency (Logger, Analytics, etc.) registered as nullable type. Callers must perform null checks before every use, scattering defensive code throughout the application and increasing the risk of NullPointerExceptions. {v5}
      prefer_nullable_provider_types: true  # [INFO] Provider type is non-nullable but create may return null. When a Provider's create callback explicitly returns null, the type parameter must be nullable to prevent runtime errors. {v2}
      prefer_oauth_pkce: true  # [INFO] OAuth authorization request without PKCE (Proof Key for Code Exchange). Mobile OAuth flows without PKCE are vulnerable to authorization code interception — a malicious app registers for the same redirect URI and steals the code before your app receives it. PKCE binds the authorization request to the token exchange with a code verifier/challenge pair, preventing interception. All mobile OAuth flows must use PKCE per RFC 7636. {v1}
      prefer_opacity_widget: true  # [INFO] Use Opacity widget for complex child widgets. StatefulWidget is created inline in a list builder. This layout configuration can trigger RenderFlex overflow errors or unexpected visual behavior at runtime. {v6}
      prefer_optimistic_updates: true  # [INFO] setState called after an await expression. The UI will not update until the async operation completes, making the app feel slow and unresponsive. Consider updating the state optimistically before the await and rolling back on failure for a snappier user experience. {v1}
      prefer_overlay_portal: true  # [INFO] Consider using OverlayPortal instead of OverlayEntry. {v4}
      prefer_parentheses_with_if_null: true  # [INFO] Add parentheses to clarify if-null expression precedence. If-null operator (??) is used without parentheses in potentially ambiguous expressions. This excessive complexity makes the code harder to understand, test, and maintain. {v4}
      prefer_pattern_destructuring: true  # [INFO] Multiple positional record field accesses could use destructuring. When accessing multiple positional fields from the same record variable, pattern destructuring is clearer and more idiomatic (Dart 3.0+). {v2}
      prefer_permission_request_in_context: true  # [INFO] Permission requested at app startup (main or initState) instead of in response to user action. Users see permission dialogs before understanding why the app needs access, leading to higher denial rates and a confusing first-launch experience. Platform guidelines (Apple, Google) recommend requesting permissions just-in-time when the user performs a relevant action. {v1}
      prefer_physics_simulation: true  # [INFO] Drag-release should use physics simulation for natural feel. Abruptly stopping animations on release feels unnatural. Use spring or friction physics for smooth deceleration. {v2}
      prefer_private_extension_type_field: true  # [INFO] Extension type representation field must be private. Extension type fields must be private for encapsulation. {v4}
      prefer_proxy_provider: true  # [INFO] Provider.create() accesses other providers. Use ProxyProvider instead. When a Provider needs to depend on another provider's value, using a plain Provider with context.read() or context.watch() is fragile and error-prone. ProxyProvider ensures proper dependency tracking and rebuild behavior. {v2}
      prefer_public_exception_classes: true  # [INFO] Private exception classes cannot be caught by name outside this library, forcing generic catch blocks. {v4}
      prefer_pump_and_settle: true  # [INFO] Use pumpAndSettle() after interactions to wait for animations. After tap(), drag(), or other interactions, using pumpAndSettle() ensures all animations complete before assertions. Using pump() alone may miss animations or scheduled frames. {v2}
      prefer_result_pattern: true  # [INFO] Throwing exceptions for recoverable errors like validation failures forces try-catch blocks at every call site and obscures control flow. This makes error handling inconsistent, increases boilerplate, and causes callers to miss failure cases entirely when they forget to add try-catch. {v5}
      prefer_rich_text_for_complex: true  # [INFO] Multiple Text widgets in a Row create separate text layout blocks that cannot wrap as a single paragraph. If the combined text exceeds the available width, each Text clips or overflows independently instead of wrapping naturally. Text.rich with multiple TextSpan children lays out as a single text block with proper line wrapping. {v4}
      prefer_riverpod_auto_dispose: true  # [INFO] Provider declared without the autoDispose modifier retains its state and resources indefinitely even after all listening child widgets are destroyed from the widget tree. This causes memory leaks, stale data accumulation, and unnecessary background computation that grows over the app lifetime, degrading performance progressively. {v3}
      prefer_riverpod_select: true  # [INFO] ref.watch() accessing single field. Use .select() for efficiency. This pattern increases maintenance cost and the likelihood of introducing bugs during future changes. {v2}
      prefer_safe_area_consumer: true  # [INFO] SafeArea placed directly inside a Scaffold body is often redundant because Scaffold already insets its body below the AppBar and above the BottomNavigationBar. Doubling up on safe area handling wastes vertical space and can push content further from the edges than intended. {v4}
      prefer_scaffold_messenger_maybeof: true  # [INFO] ScaffoldMessenger.of throws a FlutterError if no ScaffoldMessenger ancestor exists, crashing the app in contexts like dialogs, overlays, or tests without a Scaffold. Using maybeOf returns null instead, allowing graceful fallback when the messenger is unavailable. {v4}
      prefer_sealed_events: true  # [INFO] Non-sealed events allow subclassing anywhere, preventing compiler exhaustiveness checks in switch statements. {v2}
      prefer_search_anchor: true  # [INFO] showSearch with SearchDelegate uses an imperative API that bypasses the widget tree, cannot access InheritedWidgets from the parent context, and follows Material 2 patterns. SearchAnchor (Flutter 3.10+) provides a declarative, widget-based search with full M3 styling and theme integration. {v4}
      prefer_select_for_partial: true  # [ERROR] Watching the entire provider when only one field is needed causes unnecessary widget tree rebuilds, wasting memory and CPU cycles and reducing app performance. This makes your UI less efficient and responsive. {v2}
      prefer_selectable_text: true  # [INFO] Long-form text displayed with the Text widget cannot be selected or copied by users, frustrating those who need to copy error messages, addresses, phone numbers, or reference codes. SelectableText enables native text selection with copy support at no additional performance cost. {v3}
      prefer_semantic_widget_names: true  # [INFO] Generic Container widget used where a more specific widget communicates intent. Container combines padding, decoration, alignment, and sizing in one opaque widget, making it unclear which feature is actually needed. Specific widgets like SizedBox, DecoratedBox, Padding, or Align are more readable and more efficient. {v2}
      prefer_semantics_container: true  # [INFO] Semantics widget wraps a multi-child layout (Column, Row, Wrap, ListView) without container: true. Screen readers navigate each child individually rather than treating the group as a single logical unit, confusing users who expect related content to be announced together. Set container: true to group related semantic information. {v1}
      prefer_set_for_lookup: true  # [INFO] Calling contains() on a List performs a linear O(n) scan through every element, while a Set uses hash-based O(1) lookup. For collections used primarily for membership testing, this causes unnecessary performance degradation. {v5}
      prefer_shell_route_for_persistent_ui: true  # [INFO] Multiple GoRoute builders duplicate the same bottomNavigationBar, drawer, or NavigationRail instead of sharing a single wrapper. Each route rebuilds the persistent UI independently, causing duplicated code, inconsistent selection state across tabs, visual flicker during navigation, and increased memory usage from redundant widget trees. {v2}
      prefer_shell_route_shared_layout: true  # [INFO] GoRoute with Scaffold builder may duplicate layout code. Multiple routes with the same Scaffold layout cause code duplication and inconsistent behavior. ShellRoute provides a shared wrapper. {v2}
      prefer_shorthands_with_constructors: true  # [INFO] Lambda wraps a constructor call without adding any logic (e.g. (x) => MyClass(x)). The closure allocates an extra function object on each evaluation and obscures the simple delegation, making the code harder to read at a glance. {v4}
      prefer_shorthands_with_enums: true  # [INFO] Enum accessed using verbose qualification (EnumType.enumValue) where shorthand (.enumValue) is available. This adds unnecessary repetition, makes code harder to read, and increases the chance of errors when refactoring enum names. {v4}
      prefer_shorthands_with_static_fields: true  # [INFO] Static field accessed through unnecessary collection search (firstWhere, where) when direct access is available. This wastes CPU cycles iterating through values and makes code less efficient and harder to understand. {v4}
      prefer_simpler_patterns_null_check: true  # [INFO] Verbose null check pattern reduces readability. Use simpler patterns when possible. Pattern null checks can be simplified. This pattern matching usage can cause unexpected behavior or miss important type information. {v5}
      prefer_single_assertion: true  # [INFO] Test has many assertions. Split into focused tests. Tests with multiple assertions are harder to debug - you only see the first failure. One logical assertion per test clarifies what broke. {v2}
      prefer_single_declaration_per_file: true  # [INFO] File contains multiple top-level class, enum, or extension declarations. Combining unrelated declarations in one file makes it harder to locate definitions, increases merge conflicts when multiple developers edit the same file, and breaks the convention of one-declaration-per-file. {v3}
      prefer_single_widget_per_file: true  # [INFO] File contains multiple public widget classes, making it harder to locate widgets by filename, increasing merge conflicts in team environments, and complicating code navigation. Each public widget deserves its own file for discoverability and maintainability. {v4}
      prefer_sliverfillremaining_for_empty: true  # [INFO] Empty state widget in CustomScrollView uses SliverToBoxAdapter instead of SliverFillRemaining. The empty state content will sit at the top of the scroll area rather than being centered in the viewport. SliverFillRemaining expands to fill remaining space, giving a proper centered empty state experience. {v1}
      prefer_specifying_future_value_type: true  # [INFO] Calling Future.value() without specifying a type returns Future<dynamic>, which loses compile-time safety. This can lead to type errors, runtime bugs, and code that is harder to maintain and refactor. {v5}
      prefer_split_widget_const: true  # [INFO] Large widget subtree with all-const children is rebuilt on every parent setState, even though its output never changes. Extracting it into a separate const widget class allows Flutter to skip rebuilding the entire subtree, reducing frame build times and improving scroll performance. {v6}
      prefer_sqflite_batch: true  # [INFO] Database insert in loop reduces throughput by 10-100x. Inserting rows one at a time is slow. Use batch operations for bulk inserts. Sqflite uses individual inserts in a loop instead of batch. {v3}
      prefer_sqflite_singleton: true  # [INFO] openDatabase called directly. May create multiple connections. Calling openDatabase repeatedly creates connection overhead and may cause locking issues. Use a singleton pattern. {v2}
      prefer_static_class: true  # [INFO] Class contains only static members and acts as a namespace. Static-only classes cannot be instantiated meaningfully, add unnecessary boilerplate, and prevent tree-shaking of unused members in the class. {v6}
      prefer_static_const_widgets: true  # [INFO] Non-static const field is created per instance instead of sharing single compile-time object. {v3}
      prefer_streaming_for_large_files: true  # [INFO] Reading potentially large file into memory. Consider streaming for logs, media, or data exports. {v2}
      prefer_streaming_response: true  # [INFO] bodyBytes loads entire file into memory, causing OutOfMemoryError for large downloads. Stream to disk instead. {v2}
      prefer_switch_expression: true  # [INFO] Switch statement with only return/assignment detected. Using statement syntax for simple value mapping adds unnecessary boilerplate (break statements, case keywords) and makes the code more verbose and harder to scan. {v5}
      prefer_switch_with_enums: true  # [INFO] Enum compared using if-else chain instead of switch statement. Without exhaustiveness checking, adding new enum values will not trigger compile errors in this code location, allowing silent bugs where new cases are unhandled. {v4}
      prefer_switch_with_sealed_classes: true  # [INFO] Sealed class handled with if-else or type checks instead of switch. Missing exhaustiveness verification allows unhandled subtypes to silently pass through, causing runtime errors or incorrect behavior when new subtypes are added. {v5}
      prefer_tap_region_for_dismiss: true  # [INFO] Manual tap-outside detection using GestureDetector or Focus requires tracking tap locations and comparing against widget bounds, which is error-prone and breaks with nested interactive elements. TapRegion (Flutter 3.10+) handles this pattern correctly out of the box, including group regions for linked elements. {v4}
      prefer_test_matchers: true  # [INFO] Generic expect(value, equals(x)) or expect(value, isTrue) used where a more specific matcher exists. Specific matchers produce clearer failure messages that show the actual vs expected difference, reducing debugging time when tests fail. {v4}
      prefer_test_structure: true  # [INFO] Test file should follow proper structure conventions. Test files don't follow proper structure. This reduces test maintainability and makes it harder to identify which behavior failed when tests break. {v4}
      prefer_text_rich: true  # [INFO] RichText widget does not inherit DefaultTextStyle or respect textScaler from the widget tree, causing inconsistent text rendering across the app. Text.rich provides the same TextSpan capabilities while automatically inheriting the ambient text style and scaling settings. {v6}
      prefer_text_theme: true  # [INFO] Hardcoded TextStyle with inline fontSize, fontWeight, and color values scatters typography decisions throughout the codebase. When the design system changes, every hardcoded style must be found and updated manually. Theme.textTheme centralizes typography so that a single theme change propagates consistently to all text widgets. {v4}
      prefer_theme_extensions: true  # [INFO] ThemeData.copyWith used for custom colors. Prefer ThemeExtension. ThemeExtension provides type-safe, documented custom theme properties. Ad-hoc fields on ThemeData are not standardized and harder to maintain. {v2}
      prefer_typed_prefs_wrapper: true  # [INFO] Direct SharedPreferences access with string literal key. Scattered keys are error-prone. {v2}
      prefer_typedef_for_callbacks: true  # [INFO] Use typedef for repeated function types. Duplicate inline function types reduce readability and make signature changes error-prone across the codebase. {v5}
      prefer_unique_test_names: true  # [INFO] Duplicate test name found. Tests with the same fully-qualified name (group hierarchy + test name) reduce maintainability and make it harder to identify which behavior failed when tests break. Tests in different group() blocks are NOT duplicates. {v6}
      prefer_unmodifiable_collections: true  # [INFO] Equatable class exposes mutable collection field. External code can modify the collection contents without creating a new instance, breaking Equatable's equality contract and causing inconsistent state where equal objects have different contents. {v3}
      prefer_unwrapping_future_or: true  # [INFO] FutureOr type detected requiring manual type checking and unwrapping. This forces runtime type inspection (is Future checks) and adds branching complexity, making the code harder to understand and more error-prone. {v4}
      prefer_url_launcher_uri_over_string: true  # [ERROR] launchUrl called with Uri.parse() on a string literal instead of constructing a Uri object directly. Uri.parse() defers validation to runtime, where malformed strings throw FormatException and crash the app. {v2}
      prefer_use_callback: true  # [INFO] Inline closure passed as callback in a HookWidget build method. Every rebuild creates a new closure instance, which breaks referential equality and defeats hook memoization. Child widgets receiving this callback will rebuild unnecessarily. Use useCallback to memoize the function and maintain stable references across rebuilds. {v1}
      prefer_utc_datetimes: true  # [INFO] Local DateTime values shift meaning when serialized and deserialized across time zones, causing off-by-hours bugs in timestamps, scheduling, and data synchronization. Storing and transmitting dates in UTC eliminates timezone ambiguity and ensures consistent behavior across devices and servers. {v6}
      prefer_uuid_v4: true  # [INFO] Prefer UUID v4 over v1 to improve randomness and privacy. UUID v1 is time-based and includes MAC address, which may leak information. UUID v4 is random and more suitable for most use cases. {v3}
      prefer_visible_for_testing_on_members: true  # [INFO] Member exposed solely for testing lacks the @visibleForTesting annotation. Without the annotation, the analyzer cannot warn when production code accidentally calls the test-only member, breaking the intended encapsulation boundary. {v4}
      prefer_weak_references_for_cache: true  # [INFO] Strong cache references prevent garbage collection under memory pressure. Strong references to large objects prevent garbage collection. Use WeakReference for caches that should yield to memory pressure. {v5}
      prefer_webview_javascript_disabled: true  # [WARNING] WebView with JavaScript enabled expands the attack surface for cross-site scripting and remote code execution. Malicious scripts can steal authentication tokens, access device APIs through JavaScript bridges, read local storage, and exfiltrate user data to attacker-controlled servers without detection. {v4}
      prefer_when_guard_over_if: true  # [INFO] Switch case body starts with an if statement that could be expressed as a when guard on the case pattern. Moving the condition into the when clause reduces nesting and lets the switch handle the filtering directly. {v2}
      prefer_widget_private_members: true  # [WARNING] Non-final public field in a widget class breaks the immutability contract of Flutter widgets. Mutable widget fields can cause unpredictable rebuilds, stale state, and hard-to-trace rendering bugs because the framework assumes widgets are immutable after construction. {v6}
      prefer_widget_state_mixin: true  # [INFO] Use WidgetStateMixin for interaction states. Widgets use generic names like Container instead of semantic alternatives. This violates the widget lifecycle, risking setState-after-dispose errors or silent state corruption. {v5}
      prefer_wildcard_pattern: true  # [INFO] Unused pattern variable should use wildcard (_). If a variable is declared but never used, use _ instead. This pattern matching usage can cause unexpected behavior or miss important type information. {v4}
      proper_super_calls: true  # [WARNING] Super lifecycle method called in wrong order. In State classes, super.initState() must be called first, and super.dispose() must be called last. {v5}
      require_analytics_event_naming: true  # [INFO] Analytics event name does not follow snake_case convention. Inconsistent naming fragments dashboards, breaks funnel queries, and makes cross-platform analysis unreliable. Most analytics platforms recommend or require snake_case for event names. {v1}
      require_android_backup_rules: true  # [INFO] Sensitive data in SharedPreferences may be included in Android auto-backup. {v3}
      require_animation_curve: true  # [INFO] Animation uses the default linear curve, which often results in unnatural or robotic motion. Without specifying a curve, transitions may feel abrupt and lack the smoothness users expect. Always wrap with CurvedAnimation or use .animate() with a curve parameter to create more natural, visually appealing animations that enhance user experience. {v2}
      require_animation_status_listener: true  # [INFO] AnimationController.forward() called without addStatusListener to detect completion. The animation will run but your application cannot respond when it finishes, preventing cleanup, sequencing, or triggering follow-up actions. {v2}
      require_api_error_mapping: true  # [INFO] Raw API exceptions are exposed to users, leaking implementation details and providing unhelpful error messages. This can confuse users and expose sensitive information such as server paths, stack traces, or internal status codes. Always catch specific exceptions and map them to domain errors with clear, actionable messages to protect against information disclosure and improve error recovery. {v4}
      require_arrange_act_assert: true  # [INFO] Test should follow Arrange-Act-Assert pattern. AAA pattern makes tests more readable and maintainable. This weakens test quality, making failures harder to diagnose and reducing confidence in the test suite. {v3}
      require_audio_focus_handling: true  # [INFO] Audio playback should configure AudioSession for proper focus handling. Without proper audio session handling, your app may conflict with other audio sources (music apps, calls, navigation). Use audio_session package to configure proper audio focus behavior. {v2}
      require_biometric_fallback: true  # [INFO] Biometric-only auth locks out users with damaged sensors. Not all devices support biometrics, and users must have an alternative authentication method. {v6}
      require_bloc_observer: true  # [ERROR] Without a BlocObserver, state transitions and errors are invisible, making it extremely difficult to debug production issues, track bugs, or monitor app health. This can lead to undetected failures and poor user experience. {v3}
      require_bloc_repository_injection: true  # [INFO] Bloc creates its own repository. This makes testing difficult and violates dependency injection. {v2}
      require_bloc_selector: true  # [INFO] BlocBuilder accessing single field. Use BlocSelector instead. Using BlocSelector instead of BlocBuilder when you only need one field prevents unnecessary rebuilds when other fields change. {v2}
      require_bloc_transformer: true  # [INFO] Bloc on<Event> without transformer processes all events sequentially. Without EventTransformer, rapid events are processed sequentially. Use transformers for debouncing, throttling, or concurrent processing. {v1}
      require_cache_eviction_policy: true  # [WARNING] Unbounded cache consumes memory until app crashes with OutOfMemoryError after extended use. {v3}
      require_cache_key_uniqueness: true  # [INFO] Cache key type may have unstable hashCode. Cache keys must be deterministic. Using objects without stable hashCode/equality as cache keys causes missed cache hits. {v2}
      require_cached_image_device_pixel_ratio: true  # [INFO] CachedNetworkImage has fixed width or height without considering devicePixelRatio. A 200px image looks crisp on a 1x display but blurry on 2x and 3x screens (most modern phones). Without DPR scaling, images appear pixelated on high-density devices or waste bandwidth on low-density ones. Scale dimensions by MediaQuery.of(context).devicePixelRatio or use memCacheWidth/memCacheHeight for memory-efficient DPR-aware sizing. {v1}
      require_cancel_token: true  # [WARNING] Async request without cancellation continues after a StatefulWidget disposes its State, wasting bandwidth and causing setState errors. Not canceling can lead to memory leaks, wasted network connections, and crashes from calling setState on a disposed State object after the parent removes the child from the widget tree. {v4}
      require_certificate_pinning: true  # [WARNING] HttpClient without certificate pinning accepts any valid certificate, making it vulnerable to man-in-the-middle attacks. Attackers on the same network can intercept, read, and modify all HTTPS traffic including authentication tokens, personal data, and financial information without detection. {v6}
      require_completer_error_handling: true  # [WARNING] Using a Completer in a try-catch block without calling completeError in the catch block can cause the Future to hang forever if an error occurs. This leads to memory leaks, dangling stream subscriptions, and unclosed connection handles that degrade app stability over time. {v3}
      require_complex_logic_comments: true  # [INFO] Complex method lacks explanatory comments. Complex logic must have comments explaining the reasoning. {v5}
      require_connectivity_check: true  # [INFO] Network calls without connectivity check cause poor UX with long timeouts and unhelpful error messages. {v4}
      require_content_type_check: true  # [INFO] Parsing response without Content-Type check. May fail on error responses. APIs may return different content types on error. Parsing JSON without checking Content-Type may fail unexpectedly. {v4}
      require_crashlytics_user_id: true  # [INFO] Crashlytics setup without setUserIdentifier. Crashes will be anonymous. Setting a user identifier helps track crashes to specific users to improve debugging and support. Without it, crashes are anonymous. {v2}
      require_custom_painter_shouldrepaint: true  # [INFO] Always returning true from shouldRepaint causes unnecessary repaints and wastes resources. Always returning true from shouldRepaint causes unnecessary repaints. Compare relevant fields to determine if repaint is actually needed. {v2}
      require_deep_equality_collections: true  # [WARNING] Collections (List, Map, Set) in Equatable props are compared by reference, not by contents. This causes false negatives in equality checks, leading to subtle bugs, missed UI updates, broken state management, and wasted rebuilds. In production, this can result in persistent UI glitches, incorrect state restoration, and hard-to-diagnose logic errors. Collections with identical contents but different references will not compare as equal, undermining the reliability of Equatable-based state classes. {v3}
      require_deep_link_testing: true  # [INFO] `[HEURISTIC]` Route uses object parameter instead of ID. Consider using path parameters for deep link support. {v2}
      require_di_scope_awareness: true  # [INFO] Review DI scope: singleton retains state, factory creates each time. Misusing DI scopes causes lifecycle bugs: - singleton: Created once, lives forever - lazySingleton: Created on first access, lives forever - factory: Created fresh each time. {v2}
      require_dialog_result_handling: true  # [INFO] showDialog result must be awaited or handled. Dialogs that return values (like confirmation dialogs) must have their results awaited and processed. Ignoring the result can lead to missed user actions. {v2}
      require_dio_interceptor_error_handler: true  # [INFO] InterceptorsWrapper without onError handler. Errors may be unhandled. Interceptors without error handling let errors propagate unexpectedly. This pattern increases maintenance cost and the likelihood of introducing bugs during future changes. {v3}
      require_dio_retry_interceptor: true  # [INFO] Dio instance without retry interceptor. Network failures are common on mobile. Without retry logic, transient failures cause unnecessary errors. {v2}
      require_dio_ssl_pinning: true  # [WARNING] Making authentication or sensitive API requests with Dio without SSL pinning exposes your app to man-in-the-middle (MITM) attacks, where attackers can intercept or modify network traffic. This is a critical security risk for login, registration, and token endpoints. Always configure SSL pinning for all sensitive endpoints to protect user credentials and data. See https://pub.dev/packages/dio#ssl-pinning. {v3}
      require_directional_widgets: true  # [INFO] Non-directional widget. Layout will be wrong for RTL languages (Arabic, Hebrew). This prevents proper localization, causing text to display incorrectly for users in non-English locales. {v5}
      require_drag_alternatives: true  # [INFO] `[HEURISTIC]` Drag-based widget without obvious button alternative. Some users cannot perform drag gestures. {v2}
      require_edge_case_tests: true  # [INFO] Test file may be missing edge case tests (empty, null, boundary). Test boundary conditions: empty lists, null values, max int, empty strings, unicode, negative numbers. Edge cases cause most production bugs. {v3}
      require_enum_unknown_value: true  # [WARNING] Enum parsing without a fallback value throws an ArgumentError when the input string does not match any enum member. Backend API changes or new enum values added server-side will crash the app in production for all users until a client update is deployed. {v6}
      require_error_boundary: true  # [ERROR] Top-level MaterialApp or CupertinoApp is missing an error boundary in its build tree. Without a dedicated error handler, uncaught exceptions will crash the entire application, leaving users with a blank or frozen screen and no recovery path. All production apps must provide a visible fallback UI for unexpected errors. {v2}
      require_error_context: true  # [INFO] Error message is missing contextual details such as entity IDs, operation names, or relevant state. Without this context, production crash reports and logs become impossible to correlate with specific user actions, and debugging requires reproducing the exact conditions that caused the failure. {v5}
      require_error_logging: true  # [WARNING] Caught error is not logged to any logging framework or crash reporting service. Silent catch blocks make production debugging impossible because errors leave no trace in logs, crash reports, or monitoring dashboards. Without logging, you cannot detect, alert on, or diagnose failures reported by users. {v2}
      require_error_message_context: true  # [INFO] Form validation returns a generic error message without explaining what the user did wrong or how to fix it. Vague messages like "Invalid" or "Required" frustrate users, increase form abandonment rates, and fail accessibility standards that require specific, actionable error feedback to help users correct their input. {v3}
      require_example_in_documentation: true  # [INFO] Public class documentation should include an example. Complex classes benefit from example usage in their docs. {v4}
      require_exception_documentation: true  # [INFO] Thrown exceptions must be documented. Methods that throw should document the exceptions. Exception documentation is missing. {v4}
      require_exclude_semantics_justification: true  # [INFO] ExcludeSemantics without justification makes accessibility audits harder to pass. {v5}
      require_exif_handling: true  # [INFO] Image.file may show photos rotated. Prefer EXIF handling. Photos from cameras often have EXIF orientation metadata. Without handling, images may appear rotated incorrectly. {v2}
      require_extend_equatable: true  # [INFO] Class overrides operator == but does not extend Equatable. Equatable provides consistent hashCode and equality implementations with less boilerplate and fewer opportunities for bugs. {v2}
      require_file_handle_close: true  # [WARNING] File handles (e.g., RandomAccessFile) must be closed to release system resources. Failing to close files can cause resource leaks, file access errors, and may prevent other apps or processes from accessing the file. This can lead to app crashes, data loss, and degraded device performance. {v2}
      require_finally_cleanup: true  # [INFO] Cleanup code such as close(), dispose(), or cancel() is placed in a catch block instead of a finally block. This means cleanup only runs when an exception occurs, causing resource leaks of file handles, database connections, or stream subscriptions during normal execution when no error is thrown. {v2}
      require_firebase_app_check: true  # [INFO] Firebase initialization without App Check activation. Firebase App Check helps protect your backend resources from abuse. Without it, your Firebase services are vulnerable to unauthorized access. {v2}
      require_firebase_app_check_production: true  # [WARNING] Calling Firebase.initializeApp() without activating Firebase App Check leaves your backend unprotected against request forgery. Any HTTP client can call your Cloud Functions and access Firestore or RTDB using the public API key. Add FirebaseAppCheck.instance.activate() after initialization. {v1}
      require_form_field_controller: true  # [INFO] TextFormField has no controller, onSaved, or initialValue. When the parent StatefulWidget triggers a rebuild via setState(), the field value is silently discarded because there is no mechanism to persist or retrieve the user input. This causes typed data to vanish unpredictably and forces users to re-enter their input repeatedly. {v2}
      require_form_restoration: true  # [WARNING] Form with 5+ TextEditingController fields lacks RestorationMixin. When the operating system kills the app in the background to reclaim memory, all user input is permanently lost. Users who spent time filling out a lengthy form return to find every field empty and must re-enter all data from scratch. {v2}
      require_freezed_json_converter: true  # [WARNING] Freezed class with DateTime/Color field may need a JsonConverter for correct serialization. Missing converters can cause runtime errors, silent data loss, and broken API contracts. This is a common source of serialization bugs in complex models. {v2}
      require_future_timeout: true  # [WARNING] Executing a long-running Future (such as network or I/O operations) without a timeout can cause your app to hang indefinitely if the operation never completes. This can freeze the UI and degrade user experience. {v3}
      require_future_wait_error_handling: true  # [INFO] Future.wait without eagerError: false. Partial results lost on failure. When one Future in Future.wait fails, all results are lost by default. Use eagerError: false to get partial results on failure. {v2}
      require_geolocator_battery_awareness: true  # [WARNING] High-accuracy continuous location tracking without battery consideration drains battery quickly. {v2}
      require_geolocator_error_handling: true  # [WARNING] Location requests must be wrapped in error handling logic. Failing to handle errors can cause app crashes, lost user trust, and poor reviews—especially when location permissions are denied or hardware fails. Unhandled exceptions may also prevent your app from passing app store review. {v4}
      require_geolocator_service_enabled: true  # [WARNING] Location requests must check if the location service (GPS) is enabled before attempting to access position data. Failing to check service status can cause runtime errors, user confusion, and app store rejection due to non-compliance with platform requirements. On devices where GPS is disabled, location requests will fail, resulting in poor UX and potentially lost functionality. {v3}
      require_geolocator_stream_cancel: true  # [WARNING] Position stream subscriptions must be canceled when no longer needed. Failing to cancel subscriptions leads to battery drain, memory leaks, and background location updates that persist after the UI is disposed. This can degrade device performance and violate privacy expectations. {v3}
      require_geolocator_timeout: true  # [WARNING] Calling getCurrentPosition without a timeLimit can cause the request to hang indefinitely if the device cannot acquire a location fix (e.g., poor GPS signal, airplane mode). This can freeze your UI and frustrate users. {v3}
      require_getit_reset_in_tests: true  # [WARNING] GetIt singletons are not automatically reset between tests. If you do not call GetIt.I.reset(), state from one test can leak into others, causing unpredictable, flaky, or misleading results. This can hide real bugs and make tests unreliable. {v3}
      require_getx_binding_routes: true  # [INFO] GetPage without binding parameter. GetPage without a binding forces manual controller creation and lifecycle management in widgets. GetX routes should use Bindings for dependency injection. {v2}
      require_getx_controller_dispose: true  # [WARNING] GetxController holds TextEditingController or StreamSubscription fields but does not override onClose() to dispose them. Undisposed controllers and subscriptions leak native resources and continue processing events after the screen is removed, causing crashes. {v3}
      require_go_router_refresh_listenable: true  # [INFO] GoRouter has a redirect callback but no refreshListenable. When authentication state changes (login, logout, token expiry), the router does not re-evaluate redirects, leaving users stranded on protected pages after logout or blocked from authenticated pages after login. {v3}
      require_heading_semantics: true  # [INFO] Missing header semantics prevents screen reader users from navigating by headings. {v5}
      require_hive_box_close: true  # [WARNING] Hive database box opened but not closed in dispose. This leaves file handles open, prevents database compaction, causes memory leaks, and can lead to data corruption or app crashes over time. Unclosed boxes may also block updates and degrade device performance. {v3}
      require_hive_database_close: true  # [WARNING] Database opened but no close() method found. This creates a resource leak risk, leading to memory exhaustion, file locks, and possible data loss. Unclosed databases can prevent compaction and degrade app reliability. {v4}
      require_hive_migration_strategy: true  # [INFO] @HiveType with gaps in @HiveField indices. This suggests fields were removed without migration. {v2}
      require_http_client_close: true  # [WARNING] Unclosed HttpClient leaks socket connections and memory, eventually exhausting system resources. {v5}
      require_image_cache_management: true  # [INFO] Loading many images without cache management causes unbounded memory growth. The default ImageCache retains decoded images indefinitely, and without explicit eviction, memory usage climbs until the OS kills the app. Users on devices with limited RAM will experience crashes and degraded multitasking. {v3}
      require_image_compression: true  # [INFO] Camera image captured without compression. Large files waste bandwidth. Phone cameras produce large images (5-20MB). Uploading uncompressed images wastes bandwidth and storage. Compress before upload. {v2}
      require_image_dimensions: true  # [INFO] Network image without explicit dimensions causes layout shifts (CLS) when the image loads, as the widget expands from zero to the image size. This pushes surrounding content around, creates a jarring user experience, and negatively impacts web Core Web Vitals scores. {v2}
      require_image_disposal: true  # [WARNING] Failing to dispose of images or image streams can lead to memory leaks, increased memory usage, and eventual app crashes, especially in image-heavy applications. This is critical for apps that load or manipulate images dynamically. See https://docs.flutter.dev/perf/memory#images. {v6}
      require_image_error_fallback: true  # [INFO] Image.network used without an errorBuilder callback. When the network request fails due to connectivity issues, 404 errors, or server timeouts, Flutter displays a broken image icon placeholder. Users see an ugly, unexplained error state instead of a meaningful fallback such as a retry button or alternative content that maintains the visual layout. {v2}
      require_image_picker_error_handling: true  # [WARNING] Using pickImage, pickVideo, or pickMultiImage without null checks or error handling can result in null dereference errors or unhandled exceptions if the user cancels the picker or a platform error occurs. This can crash your app or cause unpredictable UI states. {v2}
      require_image_picker_result_handling: true  # [WARNING] pickImage returns null when user cancels. Missing null check causes NoSuchMethodError crash. {v2}
      require_input_sanitization: true  # [WARNING] Unsanitized user input in SQL or commands enables injection attacks, allowing data theft or system compromise. {v6}
      require_ios_accessibility_labels: true  # [INFO] Interactive widget without Semantics wrapper. VoiceOver users cannot identify this element. {v2}
      require_ios_age_rating_consideration: true  # [INFO] Feature requiring age rating consideration detected. Verify App Store Connect age rating matches app content. {v2}
      require_ios_app_clip_size_limit: true  # [INFO] App Clip detected. Ensure App Clip bundle stays under 10 MB. Large dependencies can exceed this limit. {v2}
      require_ios_ats_exception_documentation: true  # [INFO] HTTP URL detected. If intentional, document the ATS exception required in Info.plist with a comment. {v2}
      require_ios_background_audio_capability: true  # [INFO] Audio playback detected. If audio should play in background, enable Background Modes > Audio capability in Xcode. {v2}
      require_ios_background_mode: true  # [INFO] Background task pattern detected. iOS requires specific capabilities for background execution. {v2}
      require_ios_background_refresh_declaration: true  # [INFO] Background task scheduling detected. Ensure UIBackgroundModes includes "fetch" in Info.plist for background refresh. {v2}
      require_ios_biometric_fallback: true  # [INFO] Biometric authentication detected. Ensure fallback authentication (passcode) is available for devices without biometrics. {v2}
      require_ios_carplay_setup: true  # [INFO] CarPlay-related code detected. CarPlay requires Apple approval and specific entitlements. {v2}
      require_ios_certificate_pinning: true  # [INFO] Sensitive API endpoint detected. Consider implementing SSL certificate pinning for additional security. {v2}
      require_ios_database_conflict_resolution: true  # [INFO] Database sync detected. Ensure conflict resolution is implemented for multi-device sync scenarios. {v2}
      require_ios_entitlements: true  # [INFO] Feature detected that requires iOS entitlement/capability. Enable in Xcode Signing & Capabilities. {v2}
      require_ios_focus_mode_awareness: true  # [INFO] Notification without interruption level detected. iOS Focus mode may silence notifications. Set appropriate interruption level. {v2}
      require_ios_icloud_kvstore_limitations: true  # [INFO] iCloud Key-Value Storage has 1 MB limit and 1024 keys max. Use only for small preferences. {v2}
      require_ios_keychain_accessibility: true  # [INFO] Keychain write detected. Consider specifying iOS accessibility level for security. {v2}
      require_ios_keychain_sync_awareness: true  # [INFO] Keychain write of sensitive key detected. Consider if this should sync across devices via iCloud Keychain. {v2}
      require_ios_launch_storyboard: true  # [INFO] iOS app detected. Ensure LaunchScreen.storyboard is properly configured. Apps without launch screen are rejected. {v2}
      require_ios_live_activities_setup: true  # [INFO] Live Activity usage detected. Ensure ActivityKit capability and Widget Extension are configured. {v2}
      require_ios_orientation_handling: true  # [INFO] Orientation lock detected. Ensure Info.plist declares supported orientations and UI handles all locked orientations. {v2}
      require_ios_pasteboard_privacy_handling: true  # [INFO] Clipboard access detected. On iOS 16+, users see a notification when apps read the clipboard. Only access after explicit user action. {v2}
      require_ios_promotion_display_support: true  # [INFO] Manual frame timing detected. ProMotion displays run at 120Hz. Use Flutter animations for automatic frame rate adaptation. {v2}
      require_ios_quick_note_awareness: true  # [INFO] Document viewing detected. Consider implementing NSUserActivity for iOS Quick Note compatibility. {v2}
      require_ios_review_prompt_frequency: true  # [INFO] In-app review detected. Apple limits StoreKit prompts to 3x per year. Track and limit prompt frequency. {v2}
      require_ios_scene_delegate_awareness: true  # [INFO] App lifecycle handling detected. On iOS 13+, consider using scene-based lifecycle for multi-window support. {v2}
      require_ios_share_sheet_uti_declaration: true  # [INFO] File sharing with custom type detected. Ensure UTI is declared in Info.plist for custom file types. {v2}
      require_ios_siri_intent_definition: true  # [INFO] Siri Shortcuts usage detected. Ensure Intent Definition file exists in Xcode and SiriKit capability is enabled. {v2}
      require_ios_universal_links_domain_matching: true  # [INFO] Deep link pattern detected. Ensure apple-app-site-association paths match exactly for Universal Links to work. {v2}
      require_ios_version_check: true  # [WARNING] iOS version-specific feature detected without version check. Crashes may occur on older iOS versions. {v2}
      require_ios_voiceover_gesture_compatibility: true  # [INFO] Custom gesture without accessibility action. VoiceOver users may not be able to perform this action. {v2}
      require_ios_widget_extension_capability: true  # [INFO] Home Screen widget usage detected. Ensure Widget Extension target and App Groups are configured in Xcode. {v2}
      require_isar_close_on_dispose: true  # [WARNING] If you do not close the Isar instance in dispose(), file handles and system resources will leak. This can cause database corruption, prevent reopening the database, and lead to crashes or data loss on app restart. {v1}
      require_isar_inspector_debug_only: true  # [WARNING] Enabling Isar Inspector in production exposes internal database details and can create security risks or performance issues. Inspector should only be enabled in debug mode to protect user data and app integrity. {v1}
      require_isar_links_load: true  # [ERROR] Accessing IsarLinks without calling load() first will return incorrect or empty results. This leads to subtle data bugs, such as missing related records, and can break app features that depend on linked data. {v1}
      require_isolate_for_heavy: true  # [WARNING] Heavy computation such as JSON decoding, image processing, or data parsing runs on the main thread, blocking the UI event loop. This prevents frame rendering, freezes animations, and makes the app unresponsive to user input. On lower-end devices, operations exceeding 16ms per frame cause visible stutter and dropped frames that degrade the user experience. {v2}
      require_isolate_kill: true  # [WARNING] Unkilled Isolate continues consuming CPU and memory, and may send messages to disposed handlers causing crashes. {v3}
      require_item_extent_for_large_lists: true  # [INFO] ListView with many items but no itemExtent forces Flutter to lay out every child widget to calculate scroll extent. This causes expensive initial rendering, prevents efficient jump-to-index operations, and degrades scroll bar accuracy, resulting in slow list initialization and janky scrolling. {v5}
      require_json_schema_validation: true  # [WARNING] JSON API response used without validation. Malformed data may crash the app or cause unexpected behavior. {v2}
      require_keyboard_type: true  # [INFO] Text field label suggests email or phone input but no keyboardType is specified. Without TextInputType.emailAddress or TextInputType.phone, users see a generic text keyboard lacking the @ key, .com shortcut, or numeric layout. This causes input errors, slows data entry, and hurts accessibility for users who rely on specialized keyboard layouts. {v3}
      require_keys_in_animated_lists: true  # [ERROR] Without keys, AnimatedList animations target wrong items after insert/remove operations. {v5}
      require_live_region: true  # [INFO] Dynamic content that changes (such as error messages, notifications, or status updates) should use Semantics with liveRegion enabled. Without liveRegion, screen readers will not announce updates, leaving users unaware of important changes. This is critical for accessibility in apps with real-time or changing content. {v5}
      require_locale_aware_formatting: true  # [INFO] Manual date/number formatting ignores locale. Will display wrong format for users. Dates, numbers, and currencies should use locale-aware formatting. {v4}
      require_locale_for_text: true  # [INFO] Text formatting methods (toUpperCase, toLowerCase, number formatting) without an explicit locale use the device default, producing different results across regions. For example, Turkish locale uppercases "i" to "I" (with a dot), breaking string comparisons and identifiers unexpectedly. {v2}
      require_log_level_for_production: true  # [INFO] Verbose log method called without a debug-mode guard. In production builds, verbose logging exposes internal application state, degrades performance, and may leak sensitive information to device logs accessible by other apps. {v1}
      require_multi_provider: true  # [INFO] Nested Provider widgets. Use MultiProvider to improve readability. Nested Provider widgets create deep indentation. MultiProvider flattens the tree and is easier to read and maintain. {v2}
      require_navigation_result_handling: true  # [INFO] Navigator push method is called as a fire-and-forget statement without awaiting or assigning its Future result. Navigator.push() returns a Future<T?> that resolves to the value passed to Navigator.pop(result). Ignoring this return value means the calling screen cannot react to user actions on the pushed screen, leading to stale UI, missed data updates, and broken back-navigation workflows that frustrate users. {v2}
      require_notification_action_handling: true  # [INFO] Notification with action buttons lacks an onDidReceiveNotificationResponse handler. Users who tap notification action buttons will see no response, breaking the expected interaction flow and frustrating users who may abandon the feature or uninstall the app entirely. {v2}
      require_notification_initialize_per_platform: true  # [WARNING] Missing platform-specific initialization settings (android: or iOS: parameters) causes notifications to fail silently, breaking critical app functionality. Users on the unconfigured platform will never receive time-sensitive alerts, security notifications, or important updates. {v4}
      require_notification_permission_android13: true  # [WARNING] Notification displayed without checking POST_NOTIFICATIONS permission on Android 13+. On API level 33 and above, notifications fail silently without this runtime permission, causing users to miss critical alerts, messages, and updates with no error feedback to the developer or the user. {v3}
      require_number_formatting_locale: true  # [INFO] Using NumberFormat or its named constructors without an explicit locale can lead to inconsistent number formatting, such as decimal separators and grouping, depending on the user’s device or system settings. This can cause confusion or misinterpretation of numbers. {v2}
      require_offline_indicator: true  # [INFO] Connectivity is checked, but there is no offline indicator shown to users. Without a clear indicator, users may not understand why features are unavailable or why requests fail. Always show a banner, snackbar, or icon when connectivity is lost to improve transparency and user experience. {v4}
      require_overflow_box_rationale: true  # [INFO] OverflowBox used without comment explaining why overflow is needed. This layout configuration can trigger RenderFlex overflow errors or unexpected visual behavior at runtime. {v2}
      require_parameter_documentation: true  # [INFO] Parameters must be documented. Parameters must be documented to explain their purpose. Parameter documentation is missing for public methods. {v5}
      require_pending_changes_indicator: true  # [INFO] `[HEURISTIC]` Pending changes collection without UI notification. Users cannot see sync status. {v2}
      require_permission_denied_handling: true  # [WARNING] Requesting permissions without handling denied or permanently denied states leaves users unable to proceed, with no explanation or way to enable permissions. This can block critical app features and frustrate users. {v4}
      require_permission_status_check: true  # [WARNING] Accessing camera, location, or other gated features without checking permission status can cause runtime exceptions or crashes if the user has denied permission. This can break critical app flows and frustrate users. {v4}
      require_placeholder_for_network: true  # [INFO] Network image without a placeholder or loadingBuilder shows blank space while the image downloads, giving users no indication that content is loading. On slow connections this blank period can last several seconds, making the UI appear broken or unresponsive. {v4}
      require_platform_channel_cleanup: true  # [WARNING] Active platform channel handler receives callbacks after dispose, causing setState on unmounted widget. {v5}
      require_platform_check: true  # [WARNING] Platform-specific API from dart:io used without a platform guard. On Flutter web, dart:io classes throw UnsupportedError at runtime because they are unavailable in the browser environment. This crashes the app immediately when the code path executes, making the web version completely unusable for affected features. {v2}
      require_plural_handling: true  # [INFO] Simple plural logic fails for many languages (Russian, Arabic have complex plural rules). Different languages have different plural rules (e.g., Russian has singular, few, many; Arabic has six forms). {v4}
      require_pop_result_type: true  # [INFO] Awaited route push without type parameter. Return type will be dynamic. {v2}
      require_public_api_documentation: true  # [INFO] Public API must be documented. Public classes, methods, and properties must have doc comments to help other developers understand their purpose. {v4}
      require_pump_after_interaction: true  # [INFO] Widget test may need pump() or pumpAndSettle() after interaction. Widget tests need to pump to process the widget lifecycle. This weakens test quality, making failures harder to diagnose and reducing confidence in the test suite. {v6}
      require_response_caching: true  # [INFO] GET request without caching. Static or rarely-changing data wastes bandwidth on every call. GET responses for static or slowly-changing data must be cached to reduce bandwidth usage and improve responsiveness. {v5}
      require_retry_logic: true  # [INFO] Network call does not implement retry logic, so transient failures (e.g., temporary network loss, server hiccups) will not recover automatically. This can lead to poor reliability and frustrated users. Always wrap network calls with retry() or implement exponential backoff for SocketException/TimeoutException to improve robustness. {v6}
      require_return_documentation: true  # [INFO] Return value must be documented. Non-void methods should document what they return. Return value documentation is missing. {v4}
      require_route_transition_consistency: true  # [INFO] Multiple route transition types (MaterialPageRoute, CupertinoPageRoute, PageRouteBuilder) are mixed in the same file. Mixing slide, fade, and zoom transitions within a single app produces a jarring, unprofessional navigation experience that disorients users and undermines perceived app quality. {v4}
      require_scroll_tests: true  # [INFO] Widget test creates scrollable widget but does not test scrolling. Scrollable widgets must be tested for scroll behavior to catch overflow issues and ensure content is accessible. {v2}
      require_secure_storage: true  # [WARNING] SharedPreferences stores data in plain XML. On rooted/jailbroken devices, attackers extract credentials for account takeover. {v7}
      require_semantic_colors: true  # [INFO] Color variable is named by its visual appearance (e.g., redColor, blueBackground) rather than its semantic purpose (e.g., errorColor, primaryBackground). Appearance-based color names become misleading when themes change, dark mode inverts colors, or branding updates alter the palette. Developers reading the code assume the color is literally red, leading to confusion when it is actually orange after a theme update, and making it impossible to safely refactor theme colors without auditing every usage site. {v2}
      require_session_timeout: true  # [INFO] Authentication sign-in without session timeout handling. Sessions without timeout remain valid forever if tokens are stolen or compromised. This is a security risk that allows indefinite access to user accounts. Implement idle timeout and absolute session limits after successful authentication. {v1}
      require_should_rebuild: true  # [WARNING] InheritedWidget missing updateShouldNotify. Causes unnecessary rebuilds. This violates the widget lifecycle, risking setState-after-dispose errors or silent state corruption. {v4}
      require_snackbar_action_for_undo: true  # [INFO] SnackBar for delete/remove without undo action. Users can't recover. Destructive actions should provide an undo option. SnackBars are perfect for this pattern with their action button. {v2}
      require_sqflite_close: true  # [WARNING] Database connections must be closed when no longer needed. Leaving databases open causes resource leaks, file locks, and can prevent other apps or processes from accessing the database. This may lead to app crashes, data corruption, and degraded device performance. {v3}
      require_sqflite_error_handling: true  # [WARNING] All database operations (insert, update, query, etc.) must handle errors such as disk full, corruption, or constraint violations. Unhandled database errors will crash the app, cause data loss, and make recovery difficult. This is a critical reliability and data integrity issue. {v5}
      require_sqflite_migration: true  # [WARNING] Implementing onUpgrade without checking oldVersion can cause all migrations to run for every upgrade, corrupting user data and breaking app updates. This is especially dangerous for users upgrading from recent versions. {v4}
      require_sqflite_transaction: true  # [INFO] Multiple sequential writes should use transaction for atomicity. Multiple sequential writes should use transactions for atomicity and better performance. {v2}
      require_stateful_shell_route_tabs: true  # [INFO] ShellRoute with tab-like navigation may lose state on tab switch. Regular ShellRoute recreates child widgets on tab switch, losing state. StatefulShellRoute preserves each tab's state. {v2}
      require_stepper_state_management: true  # [INFO] Stepper widget without form state preservation. Flutter Stepper does not persist form data across step navigation by default — when a user goes back to a previous step, text fields lose their values. Use GlobalKey<FormState>, TextEditingController, or AutomaticKeepAliveClientMixin to preserve input across step transitions and prevent data loss. {v1}
      require_stream_on_done: true  # [INFO] Stream.listen() is called without an onDone handler. When the stream completes, you will not be notified, which can lead to missed cleanup or UI updates. {v2}
      require_structured_logging: true  # [INFO] String concatenation in logs wastes CPU building strings even when logging is disabled. String concatenation in log messages wastes CPU cycles constructing strings even when logging is disabled. Use structured logging with placeholders or log levels. {v2}
      require_submit_button_state: true  # [INFO] Submit button with an async onPressed handler lacks a loading state guard. Without disabling the button during submission, users can tap repeatedly and trigger duplicate network connection requests, duplicate database writes, and duplicate payment transactions. Each extra tap fires another async operation, wasting server memory and bandwidth while producing confusing duplicate entries. {v2}
      require_sync_error_recovery: true  # [WARNING] Sync operation without error recovery detected. Failed syncs should retry and notify user of unrecoverable errors. {v2}
      require_tab_state_preservation: true  # [INFO] TabBarView children may lose state on tab switch. By default, tab content is disposed when switching tabs. Use AutomaticKeepAliveClientMixin to preserve state across tab switches. {v2}
      require_test_cleanup: true  # [INFO] Test creates resources without tearDown cleanup. Tests that create files, directories, or database entries should clean up in tearDown to prevent test pollution and disk space issues. {v3}
      require_test_groups: true  # [INFO] Many tests without group() organization. Group related tests. Large test files without groups are hard to navigate and maintain. Use group() to organize related tests by feature or scenario. {v3}
      require_test_keys: true  # [INFO] Interactive widget in test code lacks a Key, making it difficult to locate with find.byKey() in widget tests. Without a Key, tests rely on fragile finders like find.byType() that break when multiple instances of the same widget type exist in the tree. {v4}
      require_test_setup_teardown: true  # [INFO] Test file lacks setUp/tearDown for shared resources. Tests should properly set up and clean up resources. This weakens test quality, making failures harder to diagnose and reducing confidence in the test suite. {v5}
      require_text_input_tests: true  # [INFO] Widget test creates text input but does not test user input. Text fields must be tested for user input, validation, and submission behavior. This reduces test maintainability and makes it harder to identify which behavior failed when tests break. {v3}
      require_text_overflow_in_row: true  # [WARNING] Text child element inside a Row build tree has no overflow handling. When text content exceeds the available width, Flutter renders yellow and black diagonal overflow stripes that break the visual layout. Users see unreadable, clipped content with an ugly error indicator instead of gracefully truncated or wrapped text. {v3}
      require_theme_color_from_scheme: true  # [INFO] Hardcoded Color value (e.g. Colors.blue, Color(0xFF...)) does not adapt to light/dark theme, high-contrast mode, or dynamic color (Material You). This breaks theming consistency and accessibility, as the color may become unreadable against the themed background in alternate modes. {v4}
      require_token_refresh: true  # [INFO] Auth service stores access token but may lack refresh logic. Access tokens expire. Without refresh logic, users get logged out unexpectedly. Implement proactive token refresh. {v2}
      require_typed_api_response: true  # [INFO] Untyped API access via dynamic keys loses type safety. Typos cause runtime errors, not compile errors. {v4}
      require_typed_di_registration: true  # [INFO] Dependency injection registration lacks explicit type parameter, relying on type inference. Type inference can fail or infer incorrect types when implementations differ from interfaces, causing runtime resolution errors that could be caught at compile time. {v6}
      require_update_callback: true  # [INFO] ProxyProvider.update ignores the previous value. This may cause resource leaks. ProxyProvider.update is called whenever a dependency changes. If the callback doesn't properly handle the previous parameter, it may cause memory leaks or miss important cleanup logic. {v2}
      require_update_should_notify: true  # [ERROR] If an InheritedWidget does not override updateShouldNotify, all dependents rebuild on every change, causing unnecessary rebuilds, degraded performance, and battery drain. This can make your app slow and unresponsive. {v5}
      require_url_launcher_error_handling: true  # [WARNING] Calling launchUrl without error handling can cause your app to crash or leave users stranded if the URL cannot be opened (e.g., missing browser, invalid URL, or platform restrictions). This results in a poor user experience and can break critical app flows. {v2}
      require_webview_error_handling: true  # [WARNING] WebView without an error handler displays a blank white page when network requests fail, SSL errors occur, or resources cannot be loaded. Users see no feedback about what went wrong and have no way to retry, creating a confusing and broken experience that appears as an app crash. {v3}
      require_webview_navigation_delegate: true  # [WARNING] Without navigation delegate, WebView can navigate to malicious or phishing sites. {v3}
      require_widget_key_strategy: true  # [INFO] Inconsistent key usage in itemBuilder - some returns have keys, others do not. Keys help Flutter efficiently update widget trees. Without keys or with inconsistent keys, Flutter may rebuild more than necessary. {v1}
      use_existing_destructuring: true  # [INFO] Property accessed through the original object despite an existing destructured variable that already holds the same value. The redundant access obscures the data flow and ignores the destructuring that was set up to simplify property access. {v5}
      use_existing_variable: true  # [INFO] New variable created with the same value as an existing in-scope variable. The duplicate adds an unnecessary name to the scope, increases cognitive load, and risks divergence if one copy is later modified while the other is not. {v4}
      verify_documented_parameters_exist: true  # [WARNING] Documentation references a parameter that does not exist in the signature. {v3}

      #
      # --- TIER 4: COMPREHENSIVE (102 rules) ---
      #
      avoid_cached_image_in_build: true  # [WARNING] Variable cacheKey in build method defeats caching. Using a changing cacheKey in build causes the image to reload on every rebuild, defeating the purpose of caching. {v2}
      avoid_dio_without_base_url: true  # [INFO] Dio request with full URL. Prefer setting baseUrl. Using full URLs in each request is error-prone. Set baseUrl once. {v2}
      avoid_excessive_widget_depth: true  # [WARNING] Deep widget nesting rebuilds entire subtree on changes, hurting performance and making code hard to maintain. {v2}
      avoid_finalizer_misuse: true  # [INFO] Finalizer used for non-native resources. Prefer explicit dispose(). Dart Finalizers run non-deterministically and add GC overhead. Prefer explicit dispose() methods. Finalizers are only for native resource cleanup as a safety net. {v2}
      avoid_getx_global_state: true  # [INFO] Global GetX state (Get.put/Get.find) makes testing difficult. Using Get.put() for global state makes testing difficult and creates implicit dependencies. Prefer reactive state with GetBuilder. {v2}
      avoid_image_rebuild_on_scroll: true  # [WARNING] Image.network in ListView.builder will rebuild on scroll. Images in scrollable lists will be rebuilt on every scroll, causing unnecessary network requests and poor performance. {v2}
      avoid_isar_embedded_large_objects: true  # [WARNING] Using @embedded for large objects will duplicate the data in every record, causing excessive storage use and slow queries. For shared or large objects, this can make your database unmanageable. {v1}
      avoid_isar_float_equality_queries: true  # [WARNING] Querying floats for exact equality is unreliable: due to rounding errors, you may miss matching records or get inconsistent results. This can break features that depend on accurate data retrieval. {v1}
      avoid_isar_string_contains_without_index: true  # [WARNING] Running contains or matches queries on string fields without a full-text index will force Isar to scan every record, making queries extremely slow and potentially freezing your app as data grows. {v1}
      avoid_late_without_guarantee: true  # [WARNING] late field throws a LateInitializationError at runtime if accessed before assignment, and Dart provides no compile-time guarantee that initialization has occurred. This creates a hidden crash risk that is difficult to catch in testing and may only surface in specific user flows or edge cases. {v2}
      avoid_nested_try_statements: true  # [INFO] Nested try statements found. Deeply nested error handling is hard to read and maintain. Nested try-catch blocks make code harder to read and maintain. Extract nested logic into separate functions. {v2}
      avoid_non_null_assertion: true  # [WARNING] Non-null assertion operator (!) throws a runtime exception if the value is null, crashing the app. The resulting _CastError provides no context about which variable was null or why, making production crashes from error reports and stack traces alone difficult to diagnose and reproduce. {v2}
      avoid_redundant_requests: true  # [INFO] API call in build() or similar may cause redundant requests. Multiple widgets or methods requesting the same data simultaneously wastes bandwidth and server resources. Deduplicate concurrent requests. {v5}
      avoid_redundant_semantics: true  # [INFO] Semantics widget wraps an Image that already has a semanticLabel. The screen reader announces the image description twice — once from the Semantics label and once from the Image semanticLabel — creating a confusing and repetitive experience for users relying on assistive technology. Remove the outer Semantics wrapper or the Image semanticLabel. {v1}
      avoid_sqflite_read_all_columns: true  # [INFO] SELECT * fetches unnecessary columns, wasting memory and bandwidth. Using SELECT * fetches all columns from the database, which: - Wastes memory by loading unused data - Increases network/disk bandwidth - Breaks when table schema changes (new columns appear unexpectedly) - Prevents SQLite query optimization. {v2}
      avoid_type_casts: true  # [WARNING] Type cast with "as" may throw at runtime. Type casts with as can throw at runtime. Prefer is checks or pattern matching for safer type narrowing. {v2}
      avoid_unremovable_callbacks_in_listeners: true  # [WARNING] Anonymous function cannot be removed from listener. This violates the widget lifecycle, risking setState-after-dispose errors or silent state corruption. {v6}
      avoid_unsafe_setstate: true  # [WARNING] setState() called without a mounted check. Calling setState() after a widget has been unmounted (e.g., after an async operation completes) can cause errors. Always check mounted before calling setState() in async contexts. {v6}
      prefer_ble_mtu_negotiation: true  # [INFO] BLE data transfer without MTU negotiation causes slow, fragmented transfers. BLE default MTU is only 23 bytes (20 bytes payload). Without negotiating a larger MTU, data transfers are fragmented into many small packets, causing poor throughput and increased latency. Always request MTU negotiation before transferring data. {v2}
      prefer_bloc_transform: true  # [INFO] Search/input event without transformer. Prefer debounce/throttle. For events like search queries, use EventTransformer to debounce or throttle, preventing excessive API calls. {v2}
      prefer_cached_image_fade_animation: true  # [INFO] CachedNetworkImage without fadeInDuration causes abrupt image pop-in. CachedNetworkImage has a default fadeInDuration of 500ms, which works well for most cases. However, explicitly setting this value signals intentional UX design and allows customization for different contexts: - Fast transitions: 150-200ms for thumbnail grids - Smooth transitions: 300-400ms for hero images - No transition: Duration.zero for instant display. {v3}
      prefer_cached_paint_objects: true  # [INFO] Creating Paint objects inside paint() causes new allocations every frame and hurts performance. Paint objects created in paint() are recreated every frame, causing unnecessary allocations. Move to class fields to improve performance. {v2}
      prefer_change_notifier_proxy: true  # [INFO] Provider.of without listen:false in callback. Use context.read() or add listen: false. This pattern increases maintenance cost and the likelihood of introducing bugs during future changes. {v2}
      prefer_coarse_location_when_sufficient: true  # [INFO] High accuracy location uses more battery. Prefer coarse location. Precise GPS location uses more battery and feels more invasive to users. For city-level features (weather, local stores), use coarse location instead. {v2}
      prefer_compute_over_isolate_run: true  # [INFO] Use compute() instead of Isolate.run(). For simple, quick operations, the overhead of spawning an isolate is greater than running inline. Use compute() for heavy work. {v2}
      prefer_constrained_generics: true  # [INFO] Generic type parameter has no constraint. Unconstrained type parameters accept any type including null, which can lead to unexpected behavior. This weakens type safety, allowing errors to reach runtime where they crash instead of being caught at compile time. {v4}
      prefer_constructor_injection: true  # [INFO] Setter/method injection hides dependencies. Use constructor injection. Constructor injection makes dependencies explicit and ensures objects are fully initialized when created. Setter injection allows partially initialized objects and makes dependencies implicit. {v2}
      prefer_context_read_in_callbacks: true  # [WARNING] context.watch must not be used in callbacks. Using context.watch in button callbacks or event handlers will cause unnecessary rebuilds. Use context.read for one-time access in callbacks. {v2}
      prefer_cubit_for_simple_state: true  # [INFO] Bloc with single event type. Use Cubit for simpler code. Bloc is designed for complex state management with multiple events. When a Bloc only has one event type, a Cubit is simpler and more direct. {v3}
      prefer_dio_base_options: true  # [INFO] Repeated options in Dio requests. Use BaseOptions. Repeated headers/timeouts across requests must be in BaseOptions. {v2}
      prefer_element_rebuild: true  # [INFO] Conditional return of different widget types destroys Elements. Returning the same widget type with same key reuses Elements. Changing widget types or keys destroys Elements, losing state and causing expensive rebuilds. {v2}
      prefer_equatable_mixin: true  # [INFO] Use EquatableMixin instead of extending Equatable. Using EquatableMixin is preferred when: - The class already extends another class - You want to preserve the class hierarchy. {v2}
      prefer_equatable_stringify: true  # [INFO] Equatable class does not override stringify to true. Overriding stringify to true provides better debugging output by including field values in toString() instead of just the class name. {v2}
      prefer_fake_platform: true  # [INFO] Platform-dependent widget in test without mock. Platform channels (camera, GPS, storage) need fakes in tests. Use TestDefaultBinaryMessengerBinding to mock platform responses. {v2}
      prefer_freezed_default_values: true  # [INFO] Freezed nullable field could use @Default annotation instead. Using @Default annotation instead of nullable types provides clearer intent and better code when the field must have a default value rather than being truly optional. {v2}
      prefer_future_wait: true  # [INFO] Sequential awaits could run in parallel with Future.wait. {v2}
      prefer_geolocator_distance_filter: true  # [INFO] Location stream subscription without a distanceFilter fires continuous GPS updates at the maximum sensor rate regardless of actual movement. This causes excessive battery drain, unnecessary network requests to location services, and high CPU usage from processing redundant position updates that provide no new information. {v2}
      prefer_go_router_redirect_auth: true  # [INFO] Authentication check detected inside a GoRoute builder instead of the router-level redirect callback. Scattering auth logic across individual page builders duplicates code, creates inconsistent enforcement, and allows new routes to accidentally skip authentication. {v3}
      prefer_http_connection_reuse: true  # [INFO] HTTP client created inside method. Connection overhead on every call. Each new HTTP client requires DNS lookup, TCP handshake, and TLS negotiation. Reusing connections is much more efficient. {v4}
      prefer_image_picker_request_full_metadata: true  # [INFO] pickImage collects EXIF metadata (GPS, timestamps) by default. By default, image_picker includes full EXIF metadata (GPS location, camera info, timestamps). If your app doesn't need this metadata, set requestFullMetadata: false to improve privacy and reduce permissions needed. {v3}
      prefer_image_precache: true  # [INFO] Large images without precaching cause visible loading delays and layout shifts when they appear on screen. {v5}
      prefer_immutable_annotation: true  # [INFO] Equatable class is not annotated with @immutable. Equatable classes must be immutable to ensure correct equality behavior. The @immutable annotation documents this intent and enables additional static analysis. {v2}
      prefer_immutable_bloc_events: true  # [WARNING] Mutable event fields can be changed during processing, causing inconsistent state and debugging nightmares. {v2}
      prefer_immutable_bloc_state: true  # [WARNING] Mutable state fields break equality comparison, causing BlocBuilder to miss or duplicate updates. {v2}
      prefer_immutable_provider_arguments: true  # [INFO] Mutable provider arguments cause unpredictable rebuilds. Provider arguments must be immutable for consistent behavior. Mutable arguments can lead to unexpected state changes within the provider callback. {v3}
      prefer_immutable_selector_value: true  # [INFO] Selector uses mutable type that may cause incorrect rebuilds. Selector values must be immutable to ensure proper comparisons. Mutable values are used in Provider Selector. {v2}
      prefer_inherited_widget_cache: true  # [INFO] Multiple .of(context) calls for same type. Cache in local variable. Multiple .of(context) calls for the same type trigger redundant lookups. Cache the result in a local variable to improve performance. {v2}
      prefer_isar_async_writes: true  # [INFO] Using writeTxnSync in build methods will block the UI thread, causing your app to freeze, stutter, or become unresponsive. This leads to poor user experience and can trigger platform watchdogs to kill your app. {v1}
      prefer_isar_batch_operations: true  # [INFO] Using put() in a loop for many records is extremely slow: each call triggers a separate database write. This can make your app hang or take minutes to save data. Batch operations like putAll() are up to 100x faster and prevent UI freezes. {v1}
      prefer_isar_composite_index: true  # [INFO] Querying multiple fields together without a composite index will force Isar to scan every record, making queries slow and unscalable as your data grows. Composite indexes enable fast, efficient lookups for multi-field queries. {v1}
      prefer_isar_index_for_queries: true  # [INFO] Querying fields without an @Index annotation forces Isar to scan the entire collection, resulting in slow queries and poor performance as your data grows. Indexed fields enable fast lookups and scalable apps. {v1}
      prefer_isar_lazy_links: true  # [INFO] Using IsarLinks<T>() for large linked collections loads all linked records at once, which can slow down your app and waste memory. IsarLinks.lazy() loads records on demand for better performance. {v1}
      prefer_isar_query_stream: true  # [INFO] Using Timer.periodic or manual polling for reactive queries is inefficient and can drain battery, waste CPU, and miss real-time updates. Isar watch() streams are event-driven and update instantly when data changes. {v1}
      prefer_item_extent: true  # [INFO] ListView without itemExtent recalculates layout on every scroll. When all items have the same height, specifying itemExtent improves scroll performance by avoiding per-item layout calculations. {v3}
      prefer_itemextent_when_known: true  # [INFO] Add itemExtent to improve scroll performance. When all list items have the same height, setting itemExtent allows Flutter to optimize layout calculations, improving scroll performance significantly. {v2}
      prefer_layout_builder_over_media_query: true  # [WARNING] MediaQuery.of in list item builder. Causes unnecessary rebuilds. MediaQuery.of in list item builders causes rebuilds on every scroll. Use LayoutBuilder or pass dimensions from parent to improve performance. {v2}
      prefer_lazy_box_for_large: true  # [INFO] Regular box loads all entries into memory at once. Regular boxes load all data into memory at open time. For large datasets, use lazy boxes that load values on demand. {v4}
      prefer_notifier_over_state: true  # [INFO] StateProvider exposes raw state to uncontrolled mutation. StateProvider is fine for simple state but Notifier provides: - Encapsulated business logic - Methods instead of raw state mutation - Better testability. {v2}
      prefer_prototype_item: true  # [INFO] ListView.builder without prototypeItem measures each child separately. prototypeItem allows Flutter to determine item sizes from a single prototype widget, which is more efficient than calculating each item. {v3}
      prefer_provider_extensions: true  # [INFO] Long provider access chain is hard to read. Long chains like context.read<A>().read<B>().value are hard to read. Use extension methods. {v2}
      prefer_redirecting_superclass_constructor: true  # [INFO] Constructor forwards parameters to super() without modification, which can be simplified with Dart 3 super parameters. {v2}
      prefer_sealed_bloc_events: true  # [INFO] Bloc event base class must be sealed. Sealed event classes ensure exhaustive pattern matching in handlers and prevent unexpected event subtypes from being created. {v2}
      prefer_sealed_bloc_state: true  # [INFO] Bloc state base class must be sealed. Sealed state classes ensure exhaustive pattern matching in widgets and prevent unexpected state subtypes from being created. {v2}
      prefer_selector_over_consumer: true  # [INFO] Consumer accessing single property. Use Selector for granular rebuilds. Consumer rebuilds on any change to the provider. Selector only rebuilds when the selected value changes, providing more granular control. {v3}
      prefer_selector_widget: true  # [INFO] Consumer rebuilds entire subtree. Prefer Selector for targeted rebuilds. Using Consumer to rebuild an entire widget tree when only part needs updating is wasteful. Use Selector to rebuild only what changed. {v2}
      prefer_sliver_list_delegate: true  # [INFO] Use SliverChildBuilderDelegate to improve performance with large lists. This layout configuration can trigger RenderFlex overflow errors or unexpected visual behavior at runtime. {v6}
      prefer_sorted_imports: true  # [INFO] Imports within a group are not sorted alphabetically (A-Z by URI). Unsorted imports make it harder to locate specific dependencies and increase the likelihood of merge conflicts when multiple developers add imports to the same file. {v1}
      prefer_symbol_over_key: true  # [INFO] Use a constant Key instead of string literal. Using Symbols for test keys provides compile-time checking and better refactoring support compared to magic string keys. {v2}
      prefer_test_variant: true  # [INFO] Similar tests could use variant for different configurations. Duplicate tests for different screen sizes, locales, or themes should use testVariants for cleaner code and better coverage reporting. {v3}
      prefer_typed_data: true  # [INFO] List<int> for binary data wastes memory. Use Uint8List instead. Uint8List is more memory-efficient for binary data and provides better interoperability with native code and I/O operations. List<int> uses 8 bytes per element vs 1 byte for Uint8List. {v3}
      prefer_typedefs_for_callbacks: true  # [INFO] Inline function type could be a typedef. Inline function types are harder to read and reuse. Suggests using typedefs for callback function types. {v2}
      prefer_void_callback: true  # [INFO] Use VoidCallback instead of void Function(). Using VoidCallback typedef is cleaner and more conventional in Flutter. {v2}
      require_accessibility_tests: true  # [WARNING] Widget tests should include accessibility checks. Widget tests should verify accessibility to catch issues early. Use meetsGuideline matcher for automated accessibility testing. {v2}
      require_add_automatic_keep_alives_off: true  # [INFO] Long lists with addAutomaticKeepAlives: true (default) can cause memory issues. addAutomaticKeepAlives: true (the default) keeps list items alive in memory even when scrolled off-screen. For long lists, this can cause excessive memory usage. Set it to false to improve memory efficiency. {v3}
      require_animated_builder_child: true  # [WARNING] AnimatedBuilder without a child parameter rebuilds the entire subtree on every animation frame. Static widgets that do not depend on the animation value are needlessly reconstructed 60 times per second, wasting CPU cycles and causing jank in complex widget trees. {v2}
      require_animation_tests: true  # [INFO] Animated widget test should use pump with duration. Animations need time to complete. Tests must use pumpAndSettle or pump(duration) to advance animation frames. {v3}
      require_async_error_documentation: true  # [WARNING] Async function with await expressions does not document thrown exceptions or handle errors internally. Unhandled async errors propagate as uncaught Future failures that can crash the app, produce silent data loss, or leave the UI in an inconsistent state with no recovery path. {v5}
      require_bloc_consumer_when_both: true  # [INFO] Nested BlocListener + BlocBuilder. Use BlocConsumer instead. When you need both listener (for side effects) and builder (for UI), BlocConsumer provides a cleaner single-widget solution. {v3}
      require_bloc_error_state: true  # [INFO] Bloc state sealed class must have an error case. Bloc states should include an error case for proper error handling. {v1}
      require_bloc_event_sealed: true  # [INFO] Bloc event hierarchy should use sealed class for exhaustive matching. Using sealed classes for Bloc events enables exhaustive pattern matching and prevents invalid event subtypes. {v2}
      require_bloc_loading_state: true  # [INFO] Async Bloc handler should emit loading state. Async operations should emit loading state to show UI feedback. {v1}
      require_bloc_repository_abstraction: true  # [INFO] Bloc depends on concrete repository. Use abstract interface for testability. Blocs should depend on abstract repository interfaces, not concrete implementations. This enables testing and swapping implementations. {v2}
      require_covariant_documentation: true  # [INFO] Covariant parameter must be documented. Covariant parameters weaken type safety and can cause runtime errors. They must be documented to explain why they're necessary. {v5}
      require_dio_singleton: true  # [INFO] Use a singleton Dio instance. Creating multiple Dio instances wastes resources and makes interceptor configuration inconsistent. {v2}
      require_freezed_explicit_json: true  # [INFO] Freezed class with nested objects may need explicit_to_json in build.yaml. Freezed requires explicit_to_json: true in build.yaml to correctly serialize nested objects into JSON. Without this setting, nested toJson() calls produce Instance-of strings instead of actual JSON data. {v3}
      require_futureor_documentation: true  # [INFO] FutureOr return type must be documented. FutureOr can be confusing for API consumers and must be documented. FutureOr is used in public API without documentation. {v4}
      require_getx_binding: true  # [INFO] Get.put() in widget. Use Bindings for lifecycle management. GetX controllers must be registered via Bindings for proper lifecycle management and dependency injection. {v2}
      require_go_router_typed_params: true  # [INFO] go_router pathParameters value accessed without type conversion. All path parameters are strings, and assigning them directly to int, double, or bool variables causes a runtime TypeError that crashes the app when users navigate to the route. {v2}
      require_hive_type_id_management: true  # [INFO] @HiveType found. Ensure typeId is unique and documented in a central registry. Hive typeIds must be unique and stable. Changing or duplicating typeIds corrupts stored data. Track typeIds in a central registry. {v2}
      require_image_picker_source_choice: true  # [INFO] Hardcoded ImageSource. Prefer offering user a choice. Users must be able to choose between camera and gallery. {v1}
      require_intl_locale_initialization: true  # [INFO] Intl package used without Intl.defaultLocale initialization. The intl package requires Intl.defaultLocale to be set for proper locale handling. Without initialization, date/number formatting and pluralization may use unexpected system defaults, causing inconsistent behavior across platforms and devices. {v3}
      require_list_preallocate: true  # [INFO] Using List.add() inside a loop without preallocating the list causes repeated memory reallocations (O(n^2) time), which slows down your app and wastes resources. This is especially problematic for large lists or performance-critical code. {v1}
      require_notification_timezone_awareness: true  # [INFO] Scheduled notification should use TZDateTime instead of DateTime. Scheduled notifications require timezone-aware datetime handling to ensure notifications fire at the correct time across timezone changes, daylight saving time transitions, and for users in different time zones. {v3}
      require_permission_rationale: true  # [INFO] Permission request without checking shouldShowRequestRationale. Android established convention is to explain why the app needs a permission before requesting it using shouldShowRequestRationale. {v3}
      require_physics_for_nested_scroll: true  # [WARNING] Nested scrollable widget lacks NeverScrollableScrollPhysics, causing competing scroll gestures between parent and child. This produces unpredictable scroll behavior, jank, and a confusing user experience where swipes affect the wrong scrollable. {v3}
      require_repaint_boundary: true  # [INFO] When animating complex widgets without a RepaintBoundary, the entire widget subtree is repainted on every frame, causing jank, dropped frames, and high CPU/GPU usage. This degrades performance, especially in lists or nested animations. {v2}
      require_rethrow_preserve_stack: true  # [WARNING] Using "throw e" in a catch block creates a new stack trace starting from the throw site, discarding the original stack trace that shows where the error actually occurred. This makes debugging significantly harder because the error origin is lost. The rethrow keyword preserves the original stack trace. {v2}
      require_staggered_animation_delays: true  # [INFO] List item animations are not staggered, resulting in all items animating simultaneously. This creates a chaotic and unnatural cascade effect, making it hard for users to follow the UI changes. Use index-based delays (e.g., Interval(index * 0.1, ..)) to stagger animations for a smooth, visually appealing transition. {v2}
      require_test_documentation: true  # [INFO] Complex test lacks documentation. Complex integration tests with unusual setup or assertions need comments explaining the test scenario and why it matters. {v2}
      require_text_form_field_in_form: true  # [WARNING] TextFormField outside a Form ancestor cannot participate in form-level validation. Calling formKey.currentState.validate() will not reach this field, and its validator callback will never execute. The field behaves identically to a plain TextField but misleads readers into thinking validation is wired up. {v2}
      require_url_launcher_mode: true  # [INFO] launchUrl() without mode parameter has inconsistent behavior across platforms. Without explicit LaunchMode, URL launching behavior varies by platform: - iOS: Opens in-app Safari View Controller by default - Android: Opens external browser by default - Web: Opens in same tab by default. {v2}
      require_webview_progress_indicator: true  # [INFO] WebView without progress indicator. Users see no loading feedback. WebView page loads can take significant time. Without a progress indicator, users may think the app is frozen or broken. Show loading state while content loads. {v2}

      #
      # ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
      #   ◆ STYLISTIC RULES (OPT-IN)
      # ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
      #
      # Formatting, ordering, naming conventions.
      # Enable with: dart run saropa_lints:init --tier <tier> --stylistic

      #
      # ┌─────────────────────────────────────────────────────────────────┐
      # │  ✓ ENABLED STYLISTIC (49 rules)                                         │
      # └─────────────────────────────────────────────────────────────────┘
      #
      avoid_parameter_reassignment: true  # Parameter is being reassigned. This hides the original input value. {v3}
      binary_expression_operand_order: true  # Preferring a specific operand order in binary expressions is a stylistic convention. Both orderings produce equivalent compiled code. Enable via the stylistic tier. {v3}
      double_literal_format: true  # Formatting double literals in a specific way (e.g., 1.0 vs 1.) is a stylistic preference. All forms represent the same value with no performance difference. Enable via the stylistic tier. {v4}
      no_boolean_literal_compare: true  # Comparing a boolean to true/false literally (x == true) instead of using the value directly (x) is a stylistic choice with no correctness or performance impact. Enable via the stylistic tier. {v3}
      prefer_all_named_parameters: true  # Function has 3 or more positional parameters that lack self-documenting call sites. This is an opinionated rule - not included in any tier by default. {v2}
      prefer_arrow_functions: true  # Function body contains only a single return statement. Block syntax adds unnecessary braces and visual noise for simple expressions; convert to arrow syntax (=>) to signal a pure, single-expression return. {v3}
      prefer_blank_line_before_constructor: true  # Adding blank lines before constructors is a formatting preference with no impact on code behavior or performance. Enable via the stylistic tier. {v4}
      prefer_blank_line_before_method: true  # Adding blank lines before methods is a formatting preference with no impact on code behavior or performance. Enable via the stylistic tier. {v4}
      prefer_boolean_prefixes: true  # Boolean variable must have a prefix (is/has/can/should/will/did). This rule only checks class fields and top-level variables, not local variables inside functions/methods. Local variables are implementation details and don't need strict naming conventions. {v7}
      prefer_boolean_prefixes_for_locals: true  # Prefixing boolean local variables with is/has/can/should is a naming convention with no impact on code behavior or performance. Enable via the stylistic tier. {v4}
      prefer_boolean_prefixes_for_params: true  # Prefixing boolean parameters with is/has/can/should is a naming convention with no impact on code behavior or performance. Enable via the stylistic tier. {v4}
      prefer_camel_case_method_names: true  # Method name does not follow lowerCamelCase convention. Non-standard casing breaks IDE autocompletion and makes the API inconsistent with Dart SDK and package conventions. {v2}
      prefer_catch_over_on: true  # "on Object catch" is equivalent to a bare "catch" — both catch everything with the same static type for the exception variable. The redundant "on Object" clause adds visual noise without changing behavior. Remove it to clarify that this is an unfiltered catch-all handler. {v4}
      prefer_class_over_record_return: true  # Method returns a record type, which lacks named fields and dedicated methods. This is an opinionated rule - not included in any tier by default. {v4}
      prefer_conditional_expressions: true  # Using ternary expressions instead of if-else is a code shape preference. Both compile to equivalent code with no performance impact. Enable via the stylistic tier. {v4}
      prefer_correct_callback_field_name: true  # Using onXxx naming for callback fields is a Dart API convention. Callback naming does not affect code behavior or performance. Enable via the stylistic tier. {v5}
      prefer_correct_handler_name: true  # Using handleXxx or onXxx naming for handler methods is a convention. Handler naming does not affect code behavior or performance. Enable via the stylistic tier. {v3}
      prefer_correct_setter_parameter_name: true  # Using value as the setter parameter name is a Dart convention. The parameter name does not affect setter behavior or performance. Enable via the stylistic tier. {v4}
      prefer_descriptive_bool_names: true  # Boolean should use a descriptive prefix (is, has, can, should, etc.) or action verb. This rule is suitable for the professional tier. {v5}
      prefer_doc_comments_over_regular: true  # Use doc comments (///) instead of regular comments (//) for public API documentation. This is an opinionated rule - not included in any tier by default. {v5}
      prefer_doc_straight_apostrophe: true  # Use straight apostrophe (') instead of Right Single Quotation Mark (') in documentation. This is an opinionated rule - not included in any tier by default. {v4}
      prefer_duration_constants: true  # Using named Duration constants instead of inline constructors is a naming convention. Both create identical Duration objects with no performance difference. Enable via the stylistic tier. {v2}
      prefer_error_suffix: true  # The "Exception" suffix is verbose and inconsistent with Dart core types like StateError and ArgumentError. Use "Error" for brevity. {v2}
      prefer_exception_suffix: true  # Exception classes missing the "Exception" suffix are harder to identify as throwable types during code review and IDE search. {v2}
      prefer_expect_over_assert_in_tests: true  # An assert() call was found in test code. Assertions are silently skipped in release mode and provide poor failure messages; use expect() with matchers instead for reliable and descriptive test failures. {v3}
      prefer_explicit_type_arguments: true  # Generic type without explicit type arguments. Explicit type arguments improve code clarity and prevent accidental type inference issues. Collections with types inferred from context are skipped. {v6}
      prefer_for_in: true  # Using for-in loops instead of index-based for loops is a stylistic preference. Both have equivalent performance for most Dart collections. Enable via the stylistic tier. {v4}
      prefer_grouped_imports: true  # Imports are not grouped by type (dart:, package:, relative). Ungrouped imports make it harder to locate dependencies at a glance; organize them into dart:, package:, and relative sections separated by blank lines. {v4}
      prefer_immediate_return: true  # Returning an expression directly instead of assigning to a variable first is a stylistic preference. Both produce the same compiled output. Enable via the stylistic tier. {v6}
      prefer_inline_callbacks: true  # Callback references a separate method, forcing readers to jump away to understand behavior. This is an opinionated rule - not included in any tier by default. {v3}
      prefer_kebab_tag_name: true  # Tag name should follow naming conventions. Widget tag names don't follow conventions. This naming violation reduces readability and makes the codebase harder for teams to navigate. {v4}
      prefer_list_first: true  # Using [0] instead of .first is a stylistic choice. The .first getter calls [0] internally — there is no performance benefit. Enable via the stylistic tier. {v4}
      prefer_list_last: true  # Using [length-1] instead of .last is a stylistic choice. The .last getter does the same indexing internally — no performance benefit. Enable via the stylistic tier. {v3}
      prefer_no_commented_out_code: true  # Commented-out code clutters the codebase. Delete it - git preserves history. Prose comments and special markers like TODO, FIXME, and test directives are automatically skipped. {v5}
      prefer_no_continue_statement: true  # Loop body uses a continue statement to skip to the next iteration. Continue statements break the linear flow of the loop, forcing readers to mentally track which paths reach the end of the body and which jump back to the loop header. {v2}
      prefer_private_underscore_prefix: true  # Instance field is public without documentation, exposing internal state. This is an opinionated rule - not included in any tier by default. {v3}
      prefer_pushing_conditional_expressions: true  # Moving conditional logic into return expressions is a code shape preference. No performance or correctness difference between forms. Enable via the stylistic tier. {v4}
      prefer_rethrow_over_throw_e: true  # Using throw e resets the stack trace to the current frame, losing the original call site information. Use rethrow to preserve the full stack trace for easier debugging. {v3}
      prefer_returning_condition: true  # Returning a condition directly instead of if-else with true/false is a simplification preference. No performance or correctness difference. Enable via the stylistic tier. {v4}
      prefer_returning_conditional_expressions: true  # Returning a ternary expression instead of if-else is a stylistic preference. Both compile to the same code with no performance difference. Enable via the stylistic tier. {v2}
      prefer_returning_conditionals: true  # Simplifying return logic to a single conditional expression is a code shape preference. Both forms produce equivalent compiled output. Enable via the stylistic tier. {v4}
      prefer_returning_shorthands: true  # Simplifying return expressions to shorter forms is a code style preference. Both produce equivalent compiled output with no performance impact. Enable via the stylistic tier. {v5}
      prefer_single_quotes: true  # Double quotes detected where single quotes would suffice. Prefer single quotes for consistency with Dart style conventions and to reduce visual noise in string literals. {v6}
      prefer_snake_case_files: true  # File name does not follow snake_case convention. Non-standard file names break import autocompletion and make the project structure harder to navigate on case-sensitive file systems. {v4}
      prefer_static_members_first: true  # A static member appears after an instance member in the class body. Mixing declaration order makes it harder to locate class-level constants and factories; move all static members above instance members. {v4}
      prefer_trailing_comma: true  # Adding trailing commas in multi-line constructs is a formatting preference that affects diff readability. No performance or correctness impact. Enable via the stylistic tier. {v4}
      prefer_trailing_comma_always: true  # A multi-line argument list or collection is missing a trailing comma. Add one so dart format keeps each element on its own line, producing cleaner diffs and easier reordering. {v4}
      prefer_trailing_underscore_for_unused: true  # Naming unused variables with a trailing underscore is a convention. The Dart compiler handles unused variables the same either way. Enable via the stylistic tier. {v4}
      prefer_wildcard_for_unused_param: true  # Unused parameter obscures intent and signals incomplete API design. Replacing it with a _ wildcard (Dart 3.7+) makes the function signature self-documenting, communicating that the parameter exists for interface conformance but is intentionally ignored in this implementation. {v4}

      #
      # ┌─────────────────────────────────────────────────────────────────┐
      # │  ✗ DISABLED STYLISTIC (129 rules)                                       │
      # └─────────────────────────────────────────────────────────────────┘
      #
      avoid_shrink_wrap_in_scroll: false  # shrinkWrap: true causes O(n) layout cost and defeats lazy loading. shrinkWrap: true causes O(n) layout cost and defeats lazy loading. However, shrinkWrap is sometimes required (e.g. ListView inside a Column) and is safe when paired with NeverScrollableScrollPhysics and a small bounded itemCount. This is a stylistic preference for Slivers over shrinkWrap — see avoid_shrinkwrap_in_scrollview for the context-aware rule that targets the genuinely dangerous nested-scrollable case. {v6}
      avoid_small_text: false  # Font size is smaller than 12.0, which reduces readability for users with low vision. This is an opinionated rule - not included in any tier by default. Default minimum is 12 logical pixels. {v4}
      enforce_parameters_ordering: false  # Ordering parameters in a specific sequence is a convention preference. Parameter order does not affect performance or compiled output. Enable via the stylistic tier. {v3}
      enum_constants_ordering: false  # Ordering enum constants alphabetically is a stylistic preference. Enum constant order does not affect runtime behavior or performance. Enable via the stylistic tier. {v2}
      format_comment_style: false  # Enforcing specific comment formatting conventions is a stylistic preference. Comment format has no impact on code behavior or performance. Enable via the stylistic tier. {v3}
      map_keys_ordering: false  # Ordering map keys alphabetically is a stylistic preference for readability. Key order does not affect map behavior or performance at runtime. Enable via the stylistic tier. {v4}
      prefer_absolute_imports: false  # Relative import detected instead of the preferred absolute package import. Absolute imports provide a canonical path that avoids breakage when files are moved and improves cross-file searchability. {v5}
      prefer_addall_over_spread: false  # The spread operator creates a new list allocation on every use, which can be wasteful for in-place mutations. Use addAll() for an explicit imperative merge that avoids unnecessary copies. {v2}
      prefer_arguments_ordering: false  # Named arguments are not in alphabetical order. Unsorted arguments force reviewers to scan the entire call site to spot missing or duplicated parameters. {v3}
      prefer_await_over_then: false  # The .then() chain obscures sequential async logic and complicates error handling. Rewrite with await for clearer control flow. {v3}
      prefer_blank_line_after_declarations: false  # Variable declarations run directly into the logic that follows without a visual break. A blank line separates setup from behavior and improves scanability. {v3}
      prefer_blank_line_before_case: false  # Adding blank lines before case clauses is a formatting preference with no impact on code behavior or performance. Enable via the stylistic tier. {v4}
      prefer_blank_line_before_return: false  # Adding blank lines before return statements is a formatting preference with no impact on code behavior or performance. Enable via the stylistic tier. {v5}
      prefer_blank_lines_between_members: false  # Class members are not separated by blank lines. Dense class bodies force readers to visually parse where one member ends and the next begins, slowing navigation. {v2}
      prefer_bloc_event_suffix: false  # Suffixing Bloc event class names with Event is a naming convention. The suffix does not affect Bloc behavior or performance. Enable via the stylistic tier. {v2}
      prefer_bloc_state_suffix: false  # Suffixing Bloc state class names with State is a naming convention. The suffix does not affect Bloc behavior or performance. Enable via the stylistic tier. {v2}
      prefer_borderradius_circular: false  # Use BorderRadius.circular() instead of BorderRadius.all(Radius.circular()). This is an opinionated rule - not included in any tier by default. {v3}
      prefer_capitalized_comment_start: false  # Comment should start with capital letter. Prose comments should start with a capital letter for readability. Commented-out code and continuation comments are automatically detected and skipped. {v4}
      prefer_cascade_over_chained: false  # Consecutive method calls on the same variable repeat the target name unnecessarily. Cascade notation (..) signals that all operations mutate the same object. {v1}
      prefer_center_over_align: false  # Center is identical to Align(alignment: Alignment.center) at runtime — same class, same behavior. Purely stylistic preference with no performance benefit. Enable via the stylistic tier. {v3}
      prefer_chained_over_cascade: false  # Cascade notation is unfamiliar to developers from other languages and breaks builder patterns. Separate statements are explicit about each operation. {v1}
      prefer_clip_r_superellipse: false  # ClipRRect uses circular arcs for rounded corners, which produce a visible transition between straight edges and curves. Use ClipRSuperellipse for smoother continuous corners matching iOS design language. {v2}
      prefer_clip_r_superellipse_clipper: false  # Use ClipRSuperellipse instead of ClipRRect for smoother continuous corners. This is an opinionated rule — not included in any tier by default. {v2}
      prefer_collection_if_over_ternary: false  # Ternary operator with spread is used to conditionally include collection elements. This pattern is harder to read; use collection-if syntax instead for clearer, more idiomatic Dart. {v2}
      prefer_compact_class_members: false  # Unnecessary blank lines between class members inflate the class body. Compact layout lets the full class definition fit on fewer lines for a quick overview. {v2}
      prefer_compact_declarations: false  # Unnecessary blank line after variable declarations pushes related logic further from the variables it uses, increasing the vertical distance readers must scan. {v3}
      prefer_concatenation_over_interpolation: false  # String interpolation was used where explicit concatenation is preferred. Use the + operator to build strings for a consistent style that keeps expressions visually separated from literal text. {v2}
      prefer_concise_variable_names: false  # Variable name exceeds 30 characters, which reduces readability and makes code harder to scan. Shorten it to a concise name that still conveys its purpose. {v2}
      prefer_constructor_assertion: false  # Use a constructor assertion instead of a factory for simple debug-only precondition checks. This is an opinionated rule - not included in any tier by default. {v2}
      prefer_constructor_body_assignment: false  # Constructor uses this.field shorthand which prevents adding validation or transformation logic. Use an explicit initializer list or body assignment instead to keep the constructor flexible. {v2}
      prefer_container_over_sizedbox: false  # A SizedBox was used where a Container would provide better consistency and easier future extension. Replace it with a Container to allow adding decoration, padding, or alignment without a widget swap. {v3}
      prefer_curly_apostrophe: false  # Use Right Single Quotation Mark (') instead of straight apostrophe (') in strings. This is an opinionated rule - not included in any tier by default. {v4}
      prefer_default_enum_case: false  # Exhaustive enum switches break at compile time when new values are added, requiring immediate updates across the codebase. A default case handles unknown values gracefully. {v1}
      prefer_descriptive_bool_names_strict: false  # Boolean should use a descriptive prefix (is, has, can, should, etc.). This rule is suitable for the pedantic tier. {v3}
      prefer_descriptive_variable_names: false  # Variable name is shorter than 3 characters, making its purpose unclear without reading surrounding context. Use a descriptive name that communicates intent at the point of use. {v3}
      prefer_doc_curly_apostrophe: false  # Use Right Single Quotation Mark (') instead of straight apostrophe (') in documentation. This is an opinionated rule - not included in any tier by default. {v5}
      prefer_dot_shorthand: false  # Fully qualified enum reference detected where the type is already known from context. Use dot shorthand (.value) available in Dart 3 to reduce verbosity while preserving type safety. {v2}
      prefer_double_quotes: false  # String literal uses single quotes instead of double quotes. Mixing quote styles creates inconsistent formatting that distracts during code review. {v6}
      prefer_dynamic_over_object: false  # Type is declared as Object? where dynamic is intended. Object? forces explicit casts on every access which adds verbosity; use dynamic to signal that static type checking is intentionally bypassed. {v4}
      prefer_early_return: false  # Deeply nested if blocks increase cognitive load and make code harder to follow. Early returns flatten the structure and clarify preconditions. {v7}
      prefer_edgeinsets_only: false  # EdgeInsets.symmetric() hides which sides receive padding, making future per-side adjustments harder. Use EdgeInsets.only() to declare each side explicitly so values can be changed independently. {v3}
      prefer_edgeinsets_symmetric: false  # EdgeInsets.only() was used with equal left/right or top/bottom values, which adds unnecessary repetition. Use EdgeInsets.symmetric() to express mirrored padding concisely. {v4}
      prefer_exhaustive_enums: false  # Prefer exhaustive enum cases instead of default. Exhaustive cases catch missing logic at compile time and prevent silent failures. {v2}
      prefer_expanded_over_flexible: false  # Flexible with fit: FlexFit.tight is equivalent to Expanded, adding unnecessary verbosity. Use Expanded directly for clearer intent and less boilerplate. {v3}
      prefer_explicit_boolean_comparison: false  # A nullable boolean expression is used without an explicit comparison, which can hide null-is-false behavior and confuse readers. Add == true to make the intent clear and self-documenting. {v4}
      prefer_explicit_colors: false  # Theme.of(context).colorScheme requires a BuildContext lookup at runtime, adding indirection. Use explicit Colors constants for predictable output without runtime context dependency. {v2}
      prefer_explicit_null_assignment: false  # The ??= operator hides the null-check control flow, making it harder to debug and log. Use an explicit if-null-then-assign block for step-by-step clarity. {v3}
      prefer_explicit_this: false  # Instance field accessed without an explicit this. prefix, making it ambiguous whether the identifier refers to a local variable or a field. Add this. to clarify ownership and improve readability. {v3}
      prefer_explicit_types: false  # Variable uses var instead of an explicit type annotation. Without a visible type, readers must inspect the right-hand side or hover in the IDE to determine the declared type. {v4}
      prefer_factory_for_validation: false  # Use a factory constructor for validation logic instead of constructor assertions. This is an opinionated rule - not included in any tier by default. {v2}
      prefer_fail_test_case: false  # This custom lint always fails (test hook). Formerly: always_fail_test_case. Test-only rule that always reports a lint at the start of the file. {v3}
      prefer_fake_over_mock: false  # Test uses mock with verify(). A hand-written fake keeps the test simpler and avoids mock-framework coupling. {v4}
      prefer_fields_before_methods: false  # A method appears before a field declaration in this class. Placing fields first improves readability by showing the data model before behavior. Move all field declarations above methods. {v4}
      prefer_fixme_format: false  # FIXME comment is missing the required author tag. Without an owner, FIXMEs become orphaned and unactionable; use the format FIXME(author): description so the issue is trackable. {v3}
      prefer_flat_imports: false  # Import block uses blank-line grouping that fragments the import list. A flat, alphabetically sorted import block is easier to scan and produces fewer merge conflicts. {v4}
      prefer_flexible_over_expanded: false  # Expanded widget detected, which hides its fit parameter. Use Flexible with an explicit fit argument instead for greater clarity and easier adjustments to flex behavior. {v3}
      prefer_future_void_function_over_async_callback: false  # Prefer explicit Future<void> Function() instead of AsyncCallback. Enforces using explicit Future-returning callbacks instead of AsyncCallback. {v3}
      prefer_generic_exception: false  # Custom exception classes add boilerplate without proportional benefit. Use generic Exception to keep error handling simple. {v1}
      prefer_getter_over_method: false  # Using a getter instead of a zero-parameter method is a Dart API style preference. Both produce identical compiled code with no performance difference. Enable via the stylistic tier. {v4}
      prefer_given_when_then_comments: false  # Test body has three or more statements but lacks structural comments. Without Arrange/Act/Assert or Given/When/Then markers, readers must infer the setup, action, and verification phases. Add phase comments for clarity. {v1}
      prefer_grouped_by_purpose: false  # Parameters alternate frequently between required and optional, scattering related arguments. Group parameters by purpose instead so callers can understand each logical group at a glance. {v2}
      prefer_grouped_expectations: false  # Isolating every assertion into its own test duplicates setup logic and inflates the test suite. Group related assertions to reduce boilerplate. {v1}
      prefer_guard_clauses: false  # Wrapping the entire function body in an if block obscures preconditions and increases nesting. Guard clauses make preconditions explicit and help prevent bugs. {v3}
      prefer_if_null_over_ternary: false  # Null-checking ternary detected where the ?? operator expresses the same intent more concisely. Replace with ?? to reduce verbosity and follow idiomatic Dart conventions. {v4}
      prefer_implicit_boolean_comparison: false  # Comparing a boolean expression to a boolean literal (== true or == false) is redundant and adds visual noise. Remove the comparison and use the expression directly for cleaner, idiomatic Dart. {v4}
      prefer_import_group_comments: false  # Import group is missing a doc-comment section header. Adding section headers like "/// Dart imports" before each group makes the import block self-documenting and helps developers quickly identify which group a new import belongs to. {v1}
      prefer_inferred_type_arguments: false  # Explicit generic type arguments match what the compiler already infers from context. Redundant type parameters add visual noise without providing additional type safety, and they must be manually updated if the underlying types change. {v3}
      prefer_initializing_formals: false  # Constructor assigns a parameter directly to a field via the initializer list. Use this.field syntax in the parameter list instead to reduce boilerplate and improve readability. {v3}
      prefer_instance_members_first: false  # An instance member appears after a static member in the class body. Placing instance members first highlights per-object state before shared class-level members; reorder so all instance declarations precede static ones. {v4}
      prefer_interpolation_over_concatenation: false  # String concatenation with the + operator was detected where interpolation would be cleaner. Concatenation adds visual noise and is less idiomatic in Dart. Use $-interpolation for improved readability. {v2}
      prefer_keys_with_lookup: false  # Map iteration uses .entries instead of .keys with explicit lookup. Iterating with .keys and map[key] is a familiar loop pattern that matches common imperative styles. {v2}
      prefer_late_over_nullable: false  # Use late instead of nullable for lazily initialized fields that are always set before first access. This is an opinionated rule - not included in any tier by default. {v2}
      prefer_lower_camel_case_constants: false  # Constant name does not follow lowerCamelCase convention. Inconsistent casing breaks IDE autocompletion expectations and diverges from the Dart style guide. {v3}
      prefer_map_entries_iteration: false  # Iterating a map via .keys and then performing a separate map[key] lookup on each iteration duplicates work. Use map.entries to access both key and value in a single pass. {v2}
      prefer_material_theme_colors: false  # Hardcoded Colors.* constant detected in a widget color parameter. Hardcoded colors ignore the active theme and break dark mode support. Use Theme.of(context).colorScheme for consistent theming. {v2}
      prefer_member_ordering: false  # Class members are not in conventional order. Members must be ordered: fields, constructors, methods. {v4}
      prefer_methods_before_fields: false  # A field declaration appears before a method in this class. Placing methods first highlights the public API before implementation details. Move all method declarations above fields. {v4}
      prefer_mutable_collections: false  # Function returns an unmodifiable collection. Immutable return types force callers to copy the collection before modification, adding allocation overhead and boilerplate. {v3}
      prefer_named_imports: false  # Package import without show/hide combinators detected. Unfiltered imports pull the entire package namespace into scope, increasing the risk of name collisions and making it unclear which symbols the file actually depends on. {v5}
      prefer_no_blank_line_before_return: false  # A blank line before the return statement adds unnecessary vertical space that separates the return value from its context. Remove it to keep the function body compact. {v3}
      prefer_no_blank_line_inside_blocks: false  # Blank line at the start or end of a block body wastes vertical space and creates visual inconsistency with the surrounding indentation structure. {v2}
      prefer_no_getter_prefix: false  # Getter with 'get' prefix is redundant. Dart convention omits it. Formerly: avoid_getter_prefix. A getter name starts with 'get'. {v2}
      prefer_null_aware_assignment: false  # An if-null-then-assign pattern was detected that can be simplified. Replace the verbose null check and assignment block with the ??= operator for a concise, idiomatic single-expression assignment. {v3}
      prefer_nullable_over_late: false  # Field uses late initialization, which risks a LateInitializationError at runtime if read before assignment. Use a nullable type instead so the compiler enforces null checks at every access point. {v4}
      prefer_object_over_dynamic: false  # Type is declared as dynamic, which disables static type checking and allows any member access without compile-time verification. Use Object? instead to retain type safety while still accepting any runtime type. {v4}
      prefer_on_over_catch: false  # A bare "catch" clause catches all exception types indiscriminately, which can mask unexpected errors and hinder targeted recovery. Use "on ExceptionType" to restrict handling to known failures. {v1}
      prefer_one_widget_per_file: false  # Multiple widget classes are defined in a single file, which makes it harder to locate widgets by filename. Move each widget class into its own file so names map directly to files for faster navigation. {v3}
      prefer_period_after_doc: false  # Doc comment missing period at end. Incomplete sentences reduce clarity and professionalism in API docs. {v3}
      prefer_positive_conditions: false  # Prefer a positive condition with the branches swapped for readability. {v3}
      prefer_positive_conditions_first: false  # Guard clauses with negated conditions push the happy path deeper into the function. Positive conditions make the primary logic path prominent and easier to understand. {v2}
      prefer_private_members_first: false  # A public member appears before a private member in the class body. Declaring private members first groups internal state at the top so implementation details are established before the public API. {v4}
      prefer_public_members_first: false  # A public member appears after a private member in the class body. Declaring public members first surfaces the external API at the top, making the class easier to consume. {v4}
      prefer_record_over_equatable: false  # Simple Equatable class with only final fields and no custom methods detected. Dart 3 records provide built-in equality and immutability with far less boilerplate. Replace with a typedef record. {v5}
      prefer_relative_imports: false  # An absolute package import was used for a file within the same package. Relative imports simplify refactoring and clearly signal local dependencies. Replace the absolute import with a relative path. {v6}
      prefer_required_before_optional: false  # Required parameters appear after optional ones, forcing callers to scan past defaults to find mandatory arguments. Place required parameters first for a clearer API signature. {v2}
      prefer_richtext_over_text_rich: false  # Text.rich() inherits DefaultTextStyle implicitly, which can cause unexpected styling. Use RichText instead for explicit control over the base text style without hidden theme inheritance. {v3}
      prefer_screaming_case_constants: false  # Constant name does not use SCREAMING_SNAKE_CASE convention. Without visual distinction, constants blend with regular variables and their immutable intent is lost. {v5}
      prefer_self_documenting_tests: false  # Structure comments like Arrange/Act/Assert add noise to well-written tests. Self-documenting code with clear variable names and assertions is more maintainable. {v1}
      prefer_sentence_case_comments: false  # Comment starts with a lowercase letter. Inconsistent capitalization in comments reduces readability and gives the codebase an unfinished appearance. {v4}
      prefer_single_blank_line_max: false  # Multiple consecutive blank lines waste vertical space and create visual gaps that break reading flow. One blank line is sufficient to separate logical sections. {v2}
      prefer_single_exit_point: false  # Multiple return statements (early exits) make it harder to ensure cleanup, logging, and consistent resource management at the end of a function. This can lead to missed cleanup, inconsistent state, and bugs that are difficult to trace. Refactor to a single exit point so all cleanup and logging happens reliably before returning. {v1}
      prefer_single_expectation_per_test: false  # Test contains multiple logical assertions that verify unrelated behaviors. This is an opinionated rule - not included in any tier by default. {v4}
      prefer_sized_box_square: false  # Using SizedBox(width: x, height: x) instead of SizedBox.square(dimension: x) is a stylistic choice — same widget at runtime, no performance benefit. Enable via the stylistic tier. {v3}
      prefer_sizedbox_over_container: false  # A Container is used only for width/height sizing, which adds unnecessary decoration and padding layers. Use SizedBox instead for a lighter widget with clearer intent. {v3}
      prefer_sliver_prefix: false  # Prefixing sliver widget class names with Sliver is a naming convention. It does not affect widget behavior or performance. Enable via the stylistic tier. {v5}
      prefer_sorted_parameters: false  # Named parameters are not in alphabetical order. Unsorted parameters force developers to scan all parameters to find the one they need, and inconsistent ordering across functions creates unnecessary cognitive load during code review. {v6}
      prefer_sorted_pattern_fields: false  # Unsorted pattern fields slow code review and make refactoring error-prone. Alphabetical ordering provides a predictable scanning path, reduces merge conflicts when multiple developers modify the same pattern, and makes it easier to spot missing or duplicate fields at a glance. {v5}
      prefer_sorted_record_fields: false  # Unsorted record fields slow code review and make refactoring error-prone. Alphabetical ordering provides a predictable scanning path, reduces merge conflicts when multiple developers modify the same record definition, and makes it easier to spot duplicate or missing fields. {v6}
      prefer_spacing_over_sizedbox: false  # Using SizedBox for gaps instead of the spacing parameter is a stylistic API choice. Both achieve the same layout with no performance difference. Enable via the stylistic tier. {v4}
      prefer_specific_exceptions: false  # A generic Exception is thrown instead of a domain-specific type. Generic exceptions prevent callers from catching specific failures and limit targeted error recovery; define and throw a custom exception class. {v2}
      prefer_spread_over_addall: false  # Collection uses addAll() instead of the spread operator. The spread syntax [...list1, ...list2] is a declarative, expression-level merge that avoids mutation. {v2}
      prefer_straight_apostrophe: false  # A Right Single Quotation Mark (U+2019) was found where a straight apostrophe (U+0027) is expected. Curly quotes cause inconsistent string delimiters and can break tooling. Replace with a straight apostrophe. {v5}
      prefer_super_parameters: false  # Constructor parameter is forwarded directly to super() instead of using Dart 3 super-parameter syntax. This adds unnecessary boilerplate and repetition; use super.paramName to shorten the signature. {v3}
      prefer_switch_statement: false  # Switch expressions limit flexibility for side effects and debugging. Switch statements support imperative logic, breakpoints, and multi-line case bodies. {v1}
      prefer_sync_over_async_where_possible: false  # Marking a function async when it only returns a synchronous value adds unnecessary Future wrapping overhead and obscures intent. {v3}
      prefer_ternary_over_collection_if: false  # Collection-if expression could be replaced with a ternary-spread pattern for explicit control over the empty-case branch and consistent syntax. {v2}
      prefer_ternary_over_if_null: false  # The ?? operator hides the null-check branching logic, reducing visibility into both code paths. Use an explicit ternary (value != null ? value : fallback) for full control over each branch. {v5}
      prefer_test_data_builder: false  # Complex object construction in test duplicates setup logic across multiple test cases. Builder pattern for test objects is cleaner than constructors with many parameters and makes tests more readable. {v4}
      prefer_test_name_descriptive: false  # Test name is not descriptive. Rigid patterns make test failures harder to diagnose and understand. This is an opinionated rule - not included in any tier by default. {v1}
      prefer_test_name_should_when: false  # Test name does not follow the "should X when Y" pattern, making it harder to understand the expected behavior and triggering condition at a glance. Restructure as "should [behavior] when [condition]". {v2}
      prefer_text_rich_over_richtext: false  # RichText widget does not inherit DefaultTextStyle, requiring manual base style setup. Text.rich() inherits the theme automatically and produces less boilerplate. {v3}
      prefer_then_over_await: false  # The await keyword introduces implicit suspension points that break functional composition. Use .then() for explicit Future chaining. {v2}
      prefer_todo_format: false  # TODO comment is missing the required author and description format. Use TODO(author): description so the comment is trackable, searchable, and attributable to an owner. {v3}
      prefer_type_over_var: false  # Preferring explicit type annotations over var is a style choice. Both produce identical compiled code. Conflicts with prefer_var_over_explicit_type. Enable via the stylistic tier. {v4}
      prefer_use_prefix: false  # Prefixing Flutter Hooks function names with use is a naming convention. The prefix does not affect hook behavior or performance. Enable via the stylistic tier. {v2}
      prefer_var_over_explicit_type: false  # An explicit type annotation is redundant when the right-hand side already makes the type obvious. Use var to reduce visual noise and let the initializer communicate the type. {v3}
      prefer_wheretype_over_where_is: false  # Collection uses where((e) => e is T) for type filtering, which requires a manual cast afterward. Use whereType<T>() instead to filter and cast in one step for cleaner, type-safe code. {v2}
      prefer_widget_methods_over_classes: false  # Simple widget class with a short build method detected. A separate class adds unnecessary boilerplate when the widget could be a method in the parent, giving direct access to parent state. {v2}
      unnecessary_trailing_comma: false  # Removing trailing commas in single-line constructs is a formatting preference. No impact on code behavior or performance. Enable via the stylistic tier. {v4}

      #
      # ------------------------------------------------------------------------
      #   ✗ DISABLED RULES (ABOVE COMPREHENSIVE TIER)
      # ------------------------------------------------------------------------
      #
      # These rules are in higher tiers. To enable:
      #   1. Choose a higher tier with --tier <tier>
      #   2. Or manually set to true in USER CUSTOMIZATIONS above

      #
      # ┌ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐
      #   TIER 5: PEDANTIC (11 rules disabled)
      # └ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘
      #
      avoid_duplicate_test_assertions: false  # [INFO] Duplicate test assertion detected. Duplicate test assertions are made. This reduces test maintainability and makes it harder to identify which behavior failed when tests break. {v6}
      avoid_nullable_widget_methods: false  # [INFO] A method returning Widget? forces every call site to handle the null case, which clutters the widget tree with null checks and ternary expressions. Nullable widget returns also break composability because parent widgets expecting a non-null child cannot directly use the result. Returning a placeholder widget such as SizedBox.shrink() for empty states keeps the return type non-nullable and makes the widget tree consistent and predictable. {v2}
      avoid_object_creation_in_hot_loops: false  # [WARNING] Creating objects inside hot loops triggers frequent garbage collection pauses that freeze the UI thread. Each allocation adds GC pressure proportionally to iteration count, causing visible jank, dropped frames during animations, and degraded scrolling performance on lower-end devices. {v5}
      avoid_returning_widgets: false  # [INFO] Defining methods that return widgets (other than the build method) can make your widget tree harder to read, test, and maintain. This practice hides widget structure in private methods, reducing code clarity and making it more difficult to leverage Flutter’s hot reload and widget inspector tools. {v5}
      prefer_custom_single_child_layout: false  # [INFO] Deeply nested positioning widgets. Prefer CustomSingleChildLayout. This layout configuration can trigger RenderFlex overflow errors or unexpected visual behavior at runtime. {v2}
      prefer_feature_folder_structure: false  # [INFO] File in type-based folder. Prefer feature-based organization. Group files by feature (/auth, /profile) instead of type (/bloc, /ui) to improve scalability. {v3}
      prefer_providing_intl_description: false  # [INFO] Intl.message without description. Descriptions help translators understand context and produce accurate translations. {v3}
      prefer_providing_intl_examples: false  # [INFO] Intl.message with args but no examples. Examples help translators see how placeholders are used and produce grammatically correct translations. {v3}
      prefer_small_length_files: false  # [INFO] File has more than 200 code lines (comments and blank lines excluded). Smaller files are easier to understand and maintain. {v5}
      prefer_small_length_test_files: false  # [INFO] Test file has more than 400 code lines (comments and blank lines excluded). While test files typically need more room than production code, very large test files can still indicate poor organization. Split by feature, scenario, or test category. {v3}
      prefer_specific_numeric_types: false  # [INFO] Prefer int or double over num to improve type safety. This weakens type safety, allowing errors to reach runtime where they crash instead of being caught at compile time. {v4}

      #
      # ┌ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐
      #   TIER 4: COMPREHENSIVE (1 rules disabled)
      # └ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘
      #
      avoid_isar_web_limitations: false  # [WARNING] Isar sync APIs (e.g., putSync, getSync) will throw runtime errors or silently fail on web platforms. This can break your app for web users and cause data loss or missing features. {v1}

      #
      # ┌ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐
      #   TIER 3: PROFESSIONAL (16 rules disabled)
      # └ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘
      #
      avoid_macos_deprecated_security_apis: false  # [WARNING] Deprecated macOS Security API detected. Use modern equivalents. macOS Security framework has deprecated several APIs that may cause issues with notarization or future macOS versions. {v2}
      avoid_max_path_risk: false  # [INFO] Deeply nested path construction detected. This may exceed Windows' 260-character MAX_PATH limit. {v3}
      avoid_touch_only_gestures: false  # [INFO] GestureDetector with only onTap. Missing desktop/web interactions. Desktop and web apps support secondary click (right-click) and hover. Touch-only gesture handlers reduce accessibility on these platforms. {v4}
      prefer_macos_keyboard_shortcuts: false  # [INFO] macOS apps should implement standard keyboard shortcuts. macOS users expect standard keyboard shortcuts like Cmd+S (Save), Cmd+Z (Undo), Cmd+C/V/X (Copy/Paste/Cut). Flutter provides Shortcuts and CallbackShortcuts widgets to implement these. {v2}
      prefer_macos_menu_bar_integration: false  # [INFO] macOS apps should use PlatformMenuBar for native menu integration. macOS apps should integrate with the system menu bar for a native experience. PlatformMenuBar provides this integration in Flutter. {v2}
      prefer_native_file_dialogs: false  # [INFO] Use native file dialogs on desktop. Desktop platforms have native file dialogs that users expect. Using custom dialogs creates inconsistent UX. {v2}
      prefer_url_strategy_for_web: false  # [INFO] Flutter web defaults to hash-based URLs (e.g. /#/page) which are unfriendly to search engines, break social media link previews, and look unprofessional. Path-based URLs (/page) enable proper SEO indexing, shareable deep links, and server-side routing support. {v2}
      prefer_xdg_directory_convention: false  # [INFO] Manual XDG directory path construction detected. This ignores XDG environment overrides. {v3}
      require_linux_font_fallback: false  # [INFO] Platform-specific font used without fontFamilyFallback. This font may not exist on Linux. {v3}
      require_macos_entitlements: false  # [ERROR] Feature detected that requires macOS entitlement. Sandboxed apps crash without proper entitlements. {v2}
      require_macos_file_access_intent: false  # [INFO] Direct file path access detected. macOS sandboxed apps require user intent (file picker, drag-drop) for file access. {v2}
      require_macos_hardened_runtime: false  # [INFO] Operation detected that may require Hardened Runtime entitlement. Ensure proper entitlements are configured for notarization. {v2}
      require_macos_window_restoration: false  # [INFO] macOS window configuration detected. Consider implementing window state restoration for better UX. {v2}
      require_web_renderer_awareness: false  # [INFO] kIsWeb check without renderer consideration. Behavior may vary. Flutter web has different renderers (HTML, CanvasKit, Skia) with different capabilities. Code assuming one renderer may fail on others. {v3}
      require_window_size_constraints: false  # [INFO] Desktop app without minimum window size allows users to resize the window below the minimum layout dimensions, causing text overflow, clipped buttons, and broken layouts. Setting a minimum size prevents the window from reaching dimensions where the UI cannot function properly. {v2}
      require_windows_single_instance_check: false  # [INFO] runApp() called without single-instance check. Users may open duplicate windows. {v3}

      #
      # ┌ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐
      #   TIER 2: RECOMMENDED (18 rules disabled)
      # └ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘
      #
      avoid_case_sensitive_path_comparison: false  # [WARNING] File path compared without case normalization. Windows filesystem is case-insensitive. {v3}
      avoid_forward_slash_path_assumption: false  # [WARNING] Path built with "/" concatenation. This is not idiomatic on Windows. {v3}
      avoid_gesture_only_interactions: false  # [WARNING] GestureDetector is used without providing keyboard or accessibility support, making the interaction inaccessible to users with motor disabilities or those relying on switch control. This excludes users who cannot use touch input and fails to meet accessibility standards for interactive elements. {v6}
      avoid_hardcoded_drive_letters: false  # [WARNING] Hardcoded Windows drive letter path detected. This breaks on other drives, users, or platforms. {v3}
      avoid_hardcoded_unix_paths: false  # [WARNING] Hardcoded Unix path detected. This breaks under different users, containers, or non-standard layouts. {v3}
      avoid_hover_only: false  # [WARNING] Interactive elements respond only to hover events, excluding mobile and touch users as well as people with motor disabilities. All actionable child elements in the widget tree must support both pointer and touch interactions for universal accessibility. {v3}
      avoid_macos_full_disk_access: false  # [WARNING] Accessing protected paths detected. Consider using NSOpenPanel for user-selected file access instead of Full Disk Access. {v2}
      avoid_macos_hardened_runtime_violations: false  # [ERROR] Pattern detected that may violate macOS Hardened Runtime. Apps must pass notarization for distribution. {v2}
      avoid_secure_storage_on_web: false  # [WARNING] flutter_secure_storage uses localStorage on web, which is not secure. Sensitive data stored in localStorage may be exposed to attackers, browser extensions, or other scripts, violating user privacy and security requirements. This can lead to credential theft, data breaches, and app store rejection. {v4}
      avoid_x11_only_assumptions: false  # [WARNING] X11-specific code detected. Most Linux distros now default to Wayland. {v3}
      prefer_deferred_loading_web: false  # [INFO] Large package imported eagerly increases initial bundle size. Web apps should defer loading large libraries to improve initial load time. Use deferred loading for heavy packages. {v4}
      require_focus_indicator: false  # [WARNING] GestureDetector or InkWell lacks visible focus indicator. Keyboard and assistive technology users cannot determine which element currently has focus, preventing effective navigation and interaction with your application. {v4}
      require_macos_app_transport_security: false  # [WARNING] HTTP URL detected. macOS enforces App Transport Security. Use HTTPS or declare exception in Info.plist. {v2}
      require_macos_sandbox_entitlements: false  # [WARNING] Feature requiring macOS sandbox entitlement detected. Ensure entitlements file includes required permissions. {v2}
      require_macos_sandbox_exceptions: false  # [WARNING] Feature requiring macOS sandbox entitlement detected. App Store apps must declare entitlements. {v2}
      require_macos_window_size_constraints: false  # [INFO] macOS app without window size constraints may resize to unusable dimensions. {v2}
      require_menu_bar_for_desktop: false  # [INFO] Desktop app without PlatformMenuBar lacks standard keyboard shortcuts. Desktop apps must have a menu bar for keyboard shortcuts and standard desktop interactions. {v4}
      require_window_close_confirmation: false  # [INFO] Desktop app should handle window close confirmation. Desktop apps with unsaved data should confirm before closing to prevent data loss. This introduces unnecessary computational overhead that degrades responsiveness and increases battery drain on mobile. {v3}

      #
      # ┌ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┐
      #   TIER 1: ESSENTIAL (6 rules disabled)
      # └ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ─ ┘
      #
      avoid_macos_catalyst_unsupported_apis: false  # [WARNING] API detected that is not available on Mac Catalyst. Add platform check if supporting Mac Catalyst. {v2}
      avoid_platform_channel_on_web: false  # [ERROR] MethodChannel throws MissingPluginException on web, crashing the application. {v4}
      avoid_sudo_shell_commands: false  # [ERROR] Process invocation with elevated privileges detected. Apps should not assume root access. {v3}
      avoid_web_only_dependencies: false  # [ERROR] Importing web-only libraries such as dart:html, dart:js, or dart:indexed_db in a Flutter or Dart app that targets mobile or desktop platforms will cause the app to crash with UnsupportedError at startup. This makes the app completely unusable for non-web users and can result in poor user experience, negative reviews, and lost users. Web-only dependencies must be isolated to web-specific code. {v3}
      require_cors_handling: false  # [ERROR] HTTP calls from a Flutter web app fail silently without proper CORS headers from the server. The browser blocks cross-origin requests by default, causing network errors that prevent data loading, authentication, and API communication. Users see blank screens or broken functionality with no clear error message. {v3}
      require_macos_notarization_ready: false  # [INFO] macOS app detected. Ensure notarization is configured for distribution. Apps without notarization show security warnings. {v2}


