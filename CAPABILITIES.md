# Capabilities Index

**Release 1.4.0** · Generated 2026-06-10

A complete, per-symbol catalog of every public utility in `saropa_dart_utils` — for teams evaluating or adopting the library. Covers **1859 public symbols** across **385 files**.

Each file is independently importable for minimal bundle size (`import 'package:saropa_dart_utils/<path>';`), or import the barrel `package:saropa_dart_utils/saropa_dart_utils.dart` for everything.

> Generated by `tool/gen_capabilities.dart` from the Dart AST under `lib/` — every public declaration, documented or not. Run it after adding utilities to keep this complete.

---

## Categories

- [Async](#async) — 118 symbols
- [Base64](#base64) — 7 symbols
- [Bool](#bool) — 13 symbols
- [Caching](#caching) — 31 symbols
- [Collections](#collections) — 197 symbols
- [DateTime](#datetime) — 287 symbols
- [Double](#double) — 14 symbols
- [Enum](#enum) — 4 symbols
- [Gesture](#gesture) — 36 symbols
- [Graph](#graph) — 38 symbols
- [HTML](#html) — 7 symbols
- [Hex](#hex) — 4 symbols
- [Integer](#integer) — 12 symbols
- [Iterable](#iterable) — 99 symbols
- [JSON](#json) — 27 symbols
- [List](#list) — 64 symbols
- [Map](#map) — 91 symbols
- [Niche](#niche) — 22 symbols
- [Number](#number) — 74 symbols
- [Object & Null](#object--null) — 27 symbols
- [Parsing](#parsing) — 106 symbols
- [Random](#random) — 1 symbols
- [Regex](#regex) — 6 symbols
- [Stats](#stats) — 69 symbols
- [String](#string) — 390 symbols
- [Testing](#testing) — 8 symbols
- [URL & Path](#url--path) — 50 symbols
- [UUID](#uuid) — 5 symbols
- [Validation](#validation) — 52 symbols

---

## Async

### `async/async_barrier_utils.dart`

Async barrier: wait for N events — roadmap #676.

`import 'package:saropa_dart_utils/async/async_barrier_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `AsyncBarrierUtils` | class | Barrier that completes when [count] signals received. |
| `AsyncBarrierUtils` | constructor | Creates a barrier that completes after [count] calls to [signal]. |
| `count` | field | Total number of signals required for the barrier to complete. |
| `signal` | method | Decrements the remaining count; completes the barrier when count reaches zero. |
| `future` | getter | Future that completes when the barrier has received [count] signals. |
| `toString` | method |  |

### `async/async_more_utils.dart`

`import 'package:saropa_dart_utils/async/async_more_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `race` | function | Async More: race, allSettled, retry N times. |
| `allSettled` | function | Attempts all [futures] and returns results (values or error+stackTrace pairs). |
| `retryTimes` | function | Retries [fn] up to [times] attempts; rethrows on final failure. |

### `async/async_mutex_utils.dart`

Async mutex with tryLock — roadmap #652.

`import 'package:saropa_dart_utils/async/async_mutex_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `AsyncMutexUtils` | class | Async mutex: only one holder at a time. |
| `acquire` | method | Waits until the mutex is available, then acquires it. |
| `tryLock` | method | Acquires the mutex immediately if not locked; returns false if already held. |
| `release` | method | Releases the mutex; unblocks one waiter if any. |
| `run` | method | Acquires the mutex, runs [fn], then releases; ensures release on exception. |
| `toString` | method |  |

### `async/async_semaphore_utils.dart`

Async semaphore with permits (roadmap #651).

`import 'package:saropa_dart_utils/async/async_semaphore_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `AsyncAction` | typedef | Callback that produces a future result. |
| `AsyncSemaphoreUtils` | class | Semaphore: at most [permits] concurrent acquisitions. |
| `AsyncSemaphoreUtils` | constructor | Creates a semaphore allowing at most [permits] concurrent acquisitions. |
| `permits` | field | Maximum number of concurrent acquisitions allowed. |
| `run` | method | Acquires a permit, runs [fn], then releases; ensures release on exception. |
| `acquire` | method | Waits for a permit, then acquires it (decrements available count). |
| `release` | method | Releases one permit (increments available count); unblocks one waiter if any. |
| `toString` | method |  |

### `async/batch_async_utils.dart`

Callback that transforms an item asynchronously.

`import 'package:saropa_dart_utils/async/batch_async_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `AsyncMapper` | typedef | Callback that transforms an item asynchronously. |
| `mapBatched` | function | Batch async (e.g. |

### `async/batch_flush_utils.dart`

Batch/flush: collect events and flush on size/time — roadmap #671.

`import 'package:saropa_dart_utils/async/batch_flush_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `BatchFlushUtils` | class | Collects items and flushes when [batchSize] reached or [flush] called. |
| `BatchFlushUtils` | constructor | Creates a batcher that flushes via [onFlush] once [batchSize] items accumulate (or when [flush] is called explicitly). |
| `batchSize` | field | Size at which the buffer is automatically flushed. |
| `onFlush` | field | Callback invoked with the buffered batch when a flush occurs. |
| `add` | method | Appends [item]; flushes when buffer length reaches the batch size. |
| `flush` | method | Invokes [onFlush] with current buffer contents and clears the buffer. |
| `length` | getter | Current number of items in the buffer. |
| `toString` | method |  |

### `async/cancel_previous_exception.dart`

Returns a wrapper that cancels previous in-flight calls when invoked again; the superseded future throws [CancelPreviousException].

`import 'package:saropa_dart_utils/async/cancel_previous_exception.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `cancelPrevious` | function | Returns a wrapper that cancels previous in-flight calls when invoked again; the superseded future throws [CancelPreviousException]. |
| `CancelPreviousException` | class | Thrown when a call was superseded by a newer [cancelPrevious] invocation. |

### `async/circuit_breaker_utils.dart`

Circuit breaker (open/half-open/closed) — roadmap #657.

`import 'package:saropa_dart_utils/async/circuit_breaker_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `circuitBreakerDefaultResetTimeout` | field | Default duration the circuit stays open before allowing a retry (half-open). |
| `CircuitBreakerUtils` | class | Simple circuit breaker: after [failureThreshold] failures, open for [resetTimeout]. |
| `CircuitBreakerUtils` | constructor | Creates a breaker that opens after [failureThreshold] consecutive failures and stays open for [resetTimeout] before allowing a retry. |
| `failureThreshold` | getter | Number of failures before the circuit opens. |
| `resetTimeout` | getter | Duration the circuit stays open before allowing a retry (half-open). |
| `isClosed` | getter | True when the circuit is closed (requests are allowed). |
| `isOpen` | getter | True when the circuit is open (requests are blocked). |
| `recordSuccess` | method | Resets failure count and closes the circuit. |
| `recordFailure` | method | Records a failure; opens the circuit when the failure threshold is reached. |
| `canAttempt` | getter | True if a call is allowed (closed or reset timeout elapsed). |
| `toString` | method |  |

### `async/debounce_utils.dart`

`import 'package:saropa_dart_utils/async/debounce_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `VoidCallback` | typedef | Callback with no arguments. |
| `debounce` | function | Debounce (time) — invokes [fn] after [delay] of no further calls. |

### `async/delay_utils.dart`

Delay by duration (Future.delayed wrapper).

`import 'package:saropa_dart_utils/async/delay_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `delay` | function | Delay by duration (Future.delayed wrapper). |

### `async/exponential_backoff_utils.dart`

Exponential backoff helper (roadmap #677).

`import 'package:saropa_dart_utils/async/exponential_backoff_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `exponentialBackoffDefaultBase` | field | Default base delay for exponential backoff. |
| `exponentialBackoff` | function | Returns delay for attempt [attempt] (0-based): base * 2^attempt, capped at [maxDelay]. |

### `async/heartbeat_utils.dart`

Heartbeat/keepalive for long-running tasks — roadmap #675.

`import 'package:saropa_dart_utils/async/heartbeat_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `HeartbeatUtils` | class | Calls [onBeat] every [interval] until [stop] or [dispose] is called. |
| `HeartbeatUtils` | constructor | Creates a heartbeat that invokes [onBeat] every [interval] once [start] is called. |
| `interval` | getter | Interval between heartbeat callbacks. |
| `onBeat` | getter | Callback invoked every [interval] while running. |
| `dispose` | method | Releases the timer; call when the heartbeat is no longer needed. |
| `start` | method | Starts the periodic heartbeat; cancels any existing timer. |
| `stop` | method | Stops the heartbeat and cancels the timer. |
| `toString` | method |  |

### `async/idempotent_async_utils.dart`

Idempotent async wrapper (deduplicate concurrent calls by key) — roadmap #668.

`import 'package:saropa_dart_utils/async/idempotent_async_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `IdempotentAsyncUtils` | class | Deduplicates in-flight calls: same [key] reuses the same future until it completes. |
| `run` | method | Runs [fn] for [key]; concurrent calls with the same key share one future. |
| `toString` | method |  |

### `async/memoize_future_utils.dart`

`import 'package:saropa_dart_utils/async/memoize_future_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `memoizeFuture` | function | Cache single async result (memoize Future). |

### `async/observability_utils.dart`

Observability helpers: wrap operations with timing and outcome hooks — roadmap #680.

`import 'package:saropa_dart_utils/async/observability_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `observeAsync` | function | Runs the async [operation], measures its wall-clock duration, and reports the outcome through the optional hooks, then returns the result (or rethrows the op... |
| `observeSync` | function | Synchronous counterpart of [observeAsync]: times [operation], reports the outcome through the optional hooks, and returns the result (or rethrows after [onEr... |

### `async/race_cancel_utils.dart`

Race with cancellation (first success wins, cancel rest) — roadmap #667.

`import 'package:saropa_dart_utils/async/race_cancel_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `raceFirst` | function | Returns result of first future that completes successfully; others are not cancelled (fire-and-forget). |

### `async/rate_limiter_utils.dart`

Token-bucket rate limiter — roadmap #670.

`import 'package:saropa_dart_utils/async/rate_limiter_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `TokenBucketRateLimiter` | class | A token bucket: `capacity` tokens max, refilling at `tokensPerSecond`. |
| `TokenBucketRateLimiter` | constructor | Creates a limiter refilling at [tokensPerSecond] (> 0) up to [capacity] (≥ 1) tokens. |
| `tokensPerSecond` | field | Steady-state refill rate (tokens added per second of elapsed time). |
| `capacity` | field | Maximum tokens the bucket can hold — the largest single burst allowed. |
| `tryAcquire` | method | Tries to spend [tokens] (default 1). |
| `timeUntilAvailable` | method | How long until [tokens] (default 1) would be available, or [Duration.zero] if they are available now. |
| `availableTokens` | method | The tokens currently available (after accrual), as a fractional count. |
| `toString` | method |  |

### `async/resource_pool_utils.dart`

Async resource pool with a fixed maximum size — roadmap #666.

`import 'package:saropa_dart_utils/async/resource_pool_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `ResourceFactory` | typedef | Creates a fresh pooled resource. |
| `ResourceDisposer` | typedef | Disposes a pooled resource (close a socket, end a connection). |
| `ResourcePool` | class | A bounded, reusing pool of at most [maxSize] resources of type [T]. |
| `ResourcePool` | constructor | Creates a pool that lazily builds up to [maxSize] resources via [create], optionally disposing idle ones through [onDispose] when [close] drains the pool. |
| `maxSize` | field | Maximum number of resources that may exist (idle + in use) at once. |
| `idleCount` | getter | Resources free for immediate reuse. |
| `inUseCount` | getter | Resources currently borrowed (created but not idle). |
| `waitingCount` | getter | Borrowers currently waiting for a resource. |
| `use` | method | Borrows a resource for [action], returning it to the pool afterward even if [action] throws. |
| `acquire` | method | Acquires a resource: reuses an idle one, else creates a new one if below [maxSize], else waits FIFO for a release. |
| `release` | method | Returns [resource] to the pool. |
| `close` | method | Closes the pool: disposes every idle resource, fails every waiting borrower with [StateError], and blocks new acquisitions. |
| `toString` | method |  |

### `async/retry_policy_utils.dart`

Retry policy (fixed, backoff, jitter) — roadmap #656.

`import 'package:saropa_dart_utils/async/retry_policy_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `retryPolicyDefaultDelay` | field | Default delay between retry attempts. |
| `retryPolicyDefaultBackoffBase` | field | Default base delay for exponential backoff in [retryWithJitter]. |
| `retryPolicyDefaultBackoffJitter` | field | Default jitter range for [retryWithJitter]. |
| `retryWithPolicy` | function | Retries [fn] up to [maxAttempts] with [delay] between attempts. |
| `retryWithJitter` | function | Exponential backoff with jitter: delay = base * 2^attempt + random(0, jitter). |

### `async/retry_utils.dart`

`import 'package:saropa_dart_utils/async/retry_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `retryWithBackoff` | function | Retry with backoff (exponential or linear). |

### `async/sequential_async_utils.dart`

Sequential async map (one after another).

`import 'package:saropa_dart_utils/async/sequential_async_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `mapSequential` | function | Sequential async map (one after another). |

### `async/stream_buffer_utils.dart`

Stream buffering (bufferCount, bufferTime-style) — roadmap #664.

`import 'package:saropa_dart_utils/async/stream_buffer_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `bufferCount` | function | Buffers [stream] into lists of size [count]. |

### `async/stream_combine_utils.dart`

Stream join/zip/combineLatest operators — roadmap #661.

`import 'package:saropa_dart_utils/async/stream_combine_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `zipStreams` | function | Pairs [a] and [b] by index, emitting `combine(aᵢ, bᵢ)` for each index until either stream completes (the standard "zip" operator). |
| `combineLatestStreams` | function | Emits `combine(latestA, latestB)` whenever EITHER [a] or [b] emits, but only once both have produced at least one value (the standard "combineLatest"). |
| `_Cell` | constructor |  |
| `value` | field |  |

### `async/stream_debounce_utils.dart`

Debounce a stream: emit an item only after a quiet gap.

`import 'package:saropa_dart_utils/async/stream_debounce_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `debounceStream` | function | Returns a stream that re-emits values from [source], but suppresses any value that is followed by another within [duration]. |

### `async/stream_window_utils.dart`

Stream windowing (time- and count-based) — roadmap #660.

`import 'package:saropa_dart_utils/async/stream_window_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `windowCount` | function | Buffers [stream] into lists of [count] elements. |

### `async/task_scheduler_utils.dart`

Priority task scheduler with a concurrency limit — roadmap #655.

`import 'package:saropa_dart_utils/async/task_scheduler_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `ScheduledTask` | typedef | Produces the future result of one scheduled task. |
| `TaskScheduler` | class | Schedules async tasks under a fixed [concurrency] cap, dispatching the highest-priority waiter whenever a slot opens. |
| `TaskScheduler` | constructor | Creates a scheduler that runs at most [concurrency] tasks concurrently. |
| `concurrency` | field | Maximum number of tasks allowed to run at the same time. |
| `running` | getter | Number of tasks currently executing. |
| `pending` | getter | Number of tasks waiting for a slot. |
| `schedule` | method | Schedules [task] to run when a slot is free, returning a future that completes with its result (or its error — a failed task never stalls the scheduler; the... |
| `toString` | method |  |
| `_PendingTask` | constructor |  |
| `priority` | field |  |
| `sequence` | field |  |
| `start` | field |  |
| `outranks` | method | Whether this task should stay ahead of [other] in the queue: strictly higher priority, or equal priority but submitted earlier. |

### `async/throttle_utils.dart`

`import 'package:saropa_dart_utils/async/throttle_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `VoidCallback` | typedef | Callback with no arguments. |
| `throttle` | function | Throttle (time) — max one call per interval. |

### `async/timeout_fallback_utils.dart`

`import 'package:saropa_dart_utils/async/timeout_fallback_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `timeoutWithFallback` | function | Timeout with fallback value. |

### `async/timeout_policy_utils.dart`

Async timeout with fallback — roadmap #669.

`import 'package:saropa_dart_utils/async/timeout_policy_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `withTimeout` | function | Runs [fn] with [timeout]; on timeout returns [fallback] or rethrows. |

---

## Base64

### `base64/base64_utils.dart`

`import 'package:saropa_dart_utils/base64/base64_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `Base64Utils` | class | Utility class for Base64 encoding/decoding and text compression. |
| `compressText` | method | Compresses a string using gzip and encodes it as Base64. |
| `decompressText` | method | Decompresses a Base64-encoded gzipped string back to its original form. |

### `base64/gzip_codec_io.dart`

IO-based gzip implementation using `dart:io`.

`import 'package:saropa_dart_utils/base64/gzip_codec_io.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `gzipEncode` | function | Gzip-encodes [bytes] using `dart:io`. |
| `gzipDecode` | function | Gzip-decodes [bytes] using `dart:io`. |

### `base64/gzip_codec_stub.dart`

Stub gzip implementation for platforms without `dart:io` (e.g., web).

`import 'package:saropa_dart_utils/base64/gzip_codec_stub.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `gzipEncode` | function | Stub gzip implementation for platforms without `dart:io` (e.g., web). |
| `gzipDecode` | function | Gzip-decodes the given bytes. |

---

## Bool

### `bool/bool_iterable_extensions.dart`

`import 'package:saropa_dart_utils/bool/bool_iterable_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `BoolIterableExtensions` on `Iterable<bool>` | extension | Saropa extensions for [List]s of [bool]s |
| `mostOccurrences` | method | Finds the most common value in the list. |
| `leastOccurrences` | method | Finds the least common value in the list. |
| `anyTrue` | getter | Checks if any element in the iterable is `true`. |
| `anyFalse` | getter | Checks if any element in the iterable is `false`. |
| `countTrue` | getter | Counts the number of `true` values in the iterable. |
| `countFalse` | getter | Counts the number of `false` values in the iterable. |
| `reverse` | getter | Reverses the boolean values in the list. |

### `bool/bool_string_extensions.dart`

`import 'package:saropa_dart_utils/bool/bool_string_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `BoolStringExtensions` on `String` | extension | Saropa extensions for converting [String] to [bool] |
| `toBoolNullable` | method | Converts a case-insensitive string of 'true' or 'false' to a boolean. |
| `toBool` | method | Converts a case-insensitive string of 'true' to a boolean. |
| `BoolStringNullableExtensions` on `String?` | extension | Saropa extensions for converting [bool] to [String] |
| `toBool` | method | Converts a case-insensitive string of 'true' to a boolean. |

---

## Caching

### `caching/cache_interface.dart`

Generic synchronous cache interface + a write-through async adapter — roadmap #523.

`import 'package:saropa_dart_utils/caching/cache_interface.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `Cache` | class | The common contract shared by the in-memory caches ([LruCache], [TtlCache], [SizeLimitCache]): look up a key, store a value, clear everything. |
| `get` | method | Returns the value cached under [key], or null if absent or expired. |
| `set` | method | Stores [value] under [key], applying this cache's eviction policy. |
| `clear` | method | Removes every entry. |
| `WriteThroughCache` | class | Wraps a synchronous [Cache] with an async [loader], turning a cache miss into a single load whose result is stored (the read-through / write-through pattern... |
| `WriteThroughCache` | constructor | Wraps [cache], filling misses by awaiting [loader]. |
| `getOrLoad` | method | Returns the cached value for [key]; on a miss, loads it once via the loader, stores it, and returns it. |

### `caching/lru_cache.dart`

`import 'package:saropa_dart_utils/caching/lru_cache.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `LruCache` | class | LRU cache (max size, pure Dart). |
| `LruCache` | constructor | Creates a cache holding at most [maxSize] entries (must be positive). |
| `maxSize` | getter | Maximum number of entries; oldest is evicted when exceeded. |
| `get` | method | Returns the value for [key], or null; promotes [key] to most recently used. |
| `set` | method | Associates [key] with [value]; evicts oldest entry if at capacity. |
| `clear` | method | Removes all entries and resets the cache to empty. |
| `length` | getter | Current number of entries. |
| `toString` | method |  |

### `caching/memoize_sync_utils.dart`

Memoize sync function (by argument equality).

`import 'package:saropa_dart_utils/caching/memoize_sync_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `memoize1` | function | Memoize sync function (by argument equality). |
| `singleValueCache` | function | Returns a function that computes once and returns cached value. |

### `caching/size_limit_cache.dart`

`import 'package:saropa_dart_utils/caching/size_limit_cache.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `SizeLimitCache` | class | Cache with size limit (evict oldest). |
| `SizeLimitCache` | constructor | Creates a cache holding at most [maxSize] entries (must be positive). |
| `maxSize` | getter | Maximum number of entries; oldest is evicted when exceeded. |
| `get` | method | Returns the value for [key], or null. |
| `set` | method | Associates [key] with [value]; evicts oldest entry if at capacity. |
| `clear` | method | Removes all entries. |
| `toString` | method |  |

### `caching/ttl_cache.dart`

`import 'package:saropa_dart_utils/caching/ttl_cache.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `TtlCache` | class | TTL cache (expire after duration). |
| `TtlCache` | constructor | Creates a cache whose entries each expire [ttl] after being stored. |
| `ttl` | getter | Time-to-live for each entry; expired entries return null from [get]. |
| `get` | method | Returns the value for [key] if not expired, or null. |
| `set` | method | Stores [value] under [key] with a fresh expiry [ttl] from now. |
| `clear` | method | Removes all entries. |
| `toString` | method |  |

---

## Collections

### `collections/balanced_partition_utils.dart`

Balanced partitioning (K partitions with similar sums) — roadmap #474.

`import 'package:saropa_dart_utils/collections/balanced_partition_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `balancedPartitionIndices` | function | Greedy: assign each element of [values] to current smallest partition. |

### `collections/bimap_utils.dart`

Bi-directional map (key↔value both unique) — roadmap #514.

`import 'package:saropa_dart_utils/collections/bimap_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `BimapUtils` | class | Simple bimap: forward and reverse lookup; keys and values must be unique. |
| `put` | method | Associates [key] with [value]; replaces any existing mapping for either. |
| `get` | method | Value for [key], or null. |
| `getKey` | method | Key for [value], or null. |
| `containsKey` | method | Whether [key] is present. |
| `containsValue` | method | Whether [value] is present. |
| `removeByKey` | method | Removes the entry with the given key. |
| `removeByValue` | method | Removes the entry with the given value. |
| `length` | getter | Number of key-value pairs. |
| `toString` | method |  |

### `collections/bin_packing_utils.dart`

Greedy bin packing (items into bins with capacities) — roadmap #475.

`import 'package:saropa_dart_utils/collections/bin_packing_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `firstFitBinPacking` | function | Assigns each [itemWeights] to a bin; [capacity] per bin. |

### `collections/bloom_filter_utils.dart`

Bloom filter with tunable false positive rate — roadmap #455.

`import 'package:saropa_dart_utils/collections/bloom_filter_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `BloomFilterUtils` | class | Simple Bloom filter: add elements, test membership (may have false positives). |
| `BloomFilterUtils` | constructor | Creates a filter sized for [expectedCount] elements at the target [falsePositiveRate] (default 1%). |
| `expectedCount` | getter | Expected number of elements (used for sizing). |
| `falsePositiveRate` | getter | Target false positive rate (e.g. |
| `add` | method | Adds [element] to the filter (idempotent). |
| `mightContain` | method | True if [element] might have been added (may have false positives). |
| `toString` | method |  |

### `collections/chunk_overlap_utils.dart`

Chunk + overlap windows for streaming — roadmap #465.

`import 'package:saropa_dart_utils/collections/chunk_overlap_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `chunksWithOverlap` | function | Splits [list] into chunks of [chunkSize] with [overlap] between consecutive chunks. |

### `collections/columnar_view_utils.dart`

Columnar view of list<Map<String, Object?>> for analytics — roadmap #470.

`import 'package:saropa_dart_utils/collections/columnar_view_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `columnValues` | function | Returns a single column [key] from [rows] as List<Object?>. |
| `toColumnar` | function | Returns map of key -> list of values for all keys in [rows]. |

### `collections/damerau_levenshtein_utils.dart`

Edit distance with transpositions (Damerau–Levenshtein) — roadmap #443.

`import 'package:saropa_dart_utils/collections/damerau_levenshtein_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `damerauLevenshteinDistance` | function | Returns Damerau–Levenshtein distance (insert, delete, substitute, transpose). |

### `collections/dedup_set_expiry_utils.dart`

Deduplicating set with expiry (for idempotency) — roadmap #511.

`import 'package:saropa_dart_utils/collections/dedup_set_expiry_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `DedupSetExpiryUtils` | class | Tracks "seen" keys; after [expiry] they are forgotten. |
| `DedupSetExpiryUtils` | constructor | Creates a deduplicating set that forgets each key once [expiry] has elapsed since it was added. |
| `expiry` | getter | Duration after which a key is forgotten. |
| `add` | method | Adds [key] if not recently seen; returns true if added, false if already present. |
| `contains` | method | True if [key] was added and not yet expired. |
| `toString` | method |  |

### `collections/dependency_resolver_utils.dart`

Dependency resolver with version constraints — roadmap #540.

`import 'package:saropa_dart_utils/collections/dependency_resolver_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `PackageManifest` | class | One available package version and the constraints it places on its own dependencies (`depName → constraint string`, e.g. |
| `PackageManifest` | constructor | Creates a manifest for [name] at [version] depending on [dependencies]. |
| `name` | field | The package name. |
| `version` | field | This package's version (dotted/semver string). |
| `dependencies` | field | Constraints this version imposes: dependency name → constraint string. |
| `DependencyResolution` | class | The outcome of a successful resolution: the chosen [versions] per package and an [installOrder] with dependencies ahead of the packages that need them. |
| `DependencyResolution` | constructor | Creates a resolution result. |
| `versions` | field | Resolved package name → chosen version. |
| `installOrder` | field | Package names ordered so every dependency precedes its dependents. |
| `DependencyResolutionException` | class | Thrown when resolution cannot complete: an unknown package, no version satisfying the constraints, or a dependency cycle. |
| `DependencyResolutionException` | constructor | Creates an exception describing why resolution failed. |
| `message` | field | Human-readable failure reason. |
| `toString` | method |  |
| `resolveDependencies` | function | Resolves [root] (`depName → constraint`) against [universe] (every available package version), returning the chosen versions and a topological install order. |

### `collections/difference_array_utils.dart`

Difference arrays for efficient range updates — roadmap #484.

`import 'package:saropa_dart_utils/collections/difference_array_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `DifferenceArrayUtils` | class | Difference array: apply [add] to range [l..r] in O(1), then recover array with [toArray]. |
| `DifferenceArrayUtils` | constructor | Creates a difference array backing a logical array of [length] zeros. |
| `addRange` | method | Adds [delta] to the logical range [l..r] (inclusive) in O(1). |
| `toArray` | method | Builds the final array from the difference array (prefix sum). |
| `toString` | method |  |

### `collections/disjoint_set_utils.dart`

Disjoint-set / union-find with path compression (roadmap #496).

`import 'package:saropa_dart_utils/collections/disjoint_set_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `DisjointSetUtils` | class | Union-find over integer elements 0 .. |
| `DisjointSetUtils` | constructor | Creates a union-find structure with [n] singleton sets numbered 0 .. |
| `find` | method | Representative (root) of the set containing [x]. |
| `union` | method | Merges the sets containing [x] and [y], using union-by-rank to keep the trees shallow. |
| `connected` | method | True if [x] and [y] are in the same set. |
| `toString` | method |  |

### `collections/greedy_set_cover_utils.dart`

Greedy set cover approximation — roadmap #447.

`import 'package:saropa_dart_utils/collections/greedy_set_cover_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `greedySetCover` | function | Returns subset of indices into [sets] that covers [universe]. |

### `collections/histogram_utils.dart`

Histogram builder with fixed and quantile-based bins — roadmap #473.

`import 'package:saropa_dart_utils/collections/histogram_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `histogramFixed` | function | Build histogram: [values] binned by [edges] (sorted). |
| `histogramQuantile` | function | Bin boundaries at quantiles (e.g. |

### `collections/interval_scheduling_utils.dart`

Interval scheduling: max non-overlapping intervals (roadmap #445).

`import 'package:saropa_dart_utils/collections/interval_scheduling_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `IntervalSchedulingUtils` | class | Interval with [start] and [end] (inclusive or exclusive per usage). |
| `IntervalSchedulingUtils` | constructor | Creates an interval spanning [start] to [end]. |
| `start` | getter | Start of the interval. |
| `end` | getter | End of the interval. |
| `toString` | method |  |
| `maxNonOverlappingIntervals` | function | Returns a maximal set of non-overlapping intervals from [intervals], chosen by earliest end time (greedy). |

### `collections/interval_tree_utils.dart`

Interval tree for overlap (stabbing) queries — roadmap #494.

`import 'package:saropa_dart_utils/collections/interval_tree_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `IntervalEntry` | class | One inclusive interval `[low, high]` carrying a [value]. |
| `IntervalEntry` | constructor | Creates an interval `[low, high]` (inclusive) labeled with [value]. |
| `low` | field | Inclusive lower bound. |
| `high` | field | Inclusive upper bound. |
| `value` | field | The payload this interval carries (an event, range owner, etc.). |
| `contains` | method | Whether [point] lies within `[low, high]` (inclusive). |
| `overlaps` | method | Whether this interval overlaps the inclusive range `[low, high]` given by [otherLow]/[otherHigh]. |
| `operator ==` | operator |  |
| `hashCode` | getter |  |
| `toString` | method |  |
| `_Node` | constructor |  |
| `entry` | field |  |
| `left` | field |  |
| `right` | field |  |
| `maxHigh` | field | Largest `high` in this node's subtree; the augmentation that lets a query skip a branch once its reach falls short of the query's lower bound. |
| `IntervalTree` | class | An immutable, balanced interval tree over a fixed set of [IntervalEntry]s. |
| `IntervalTree` | constructor | Builds a balanced tree from [entries] (any order). |
| `size` | getter | Number of intervals in the tree. |
| `isEmpty` | getter | Whether the tree holds no intervals. |
| `queryPoint` | method | All intervals that contain [point], in ascending `low` order. |
| `queryRange` | method | All intervals overlapping the inclusive range `[low, high]`, in ascending `low` order. |
| `hasOverlap` | method | Whether any interval overlaps `[low, high]`. |

### `collections/inverted_index_utils.dart`

In-memory inverted index for small datasets — roadmap #456.

`import 'package:saropa_dart_utils/collections/inverted_index_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `buildInvertedIndex` | function | Builds term -> list of document indices containing that term. |

### `collections/kmeans_utils.dart`

K-means clustering (small K, small N) — roadmap #449.

`import 'package:saropa_dart_utils/collections/kmeans_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `kmeans2D` | function | Runs up to [maxIterations] of K-means; [points] are 2D, returns cluster index per point. |

### `collections/knapsack_utils.dart`

0/1 Knapsack solver with reconstruction — roadmap #444.

`import 'package:saropa_dart_utils/collections/knapsack_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `KnapsackUtils` | class | Item with weight and value. |
| `KnapsackUtils` | constructor | Creates a knapsack item with the given [weight] (cost) and [value] (profit). |
| `weight` | getter | Item weight (cost). |
| `value` | getter | Item value (profit). |
| `toString` | method |  |
| `knapsack01` | function | Solves the 0/1 knapsack: (max value, indices of chosen items). |

### `collections/lcs_sequence_utils.dart`

Longest common subsequence (LCS) of two lists.

`import 'package:saropa_dart_utils/collections/lcs_sequence_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `longestCommonSubsequence` | function | Returns one longest common subsequence of [a] and [b], compared with `==`. |
| `longestCommonSubsequenceLength` | function | Returns the length of the longest common subsequence of [a] and [b]. |

### `collections/lcs_substring_utils.dart`

Longest common substring (not subsequence) for two lists/strings (roadmap #442).

`import 'package:saropa_dart_utils/collections/lcs_substring_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `longestCommonSubstringLength` | function | Returns the length of the longest common substring of [a] and [b]. |
| `longestCommonSubstring` | function | Returns one longest common substring of [a] and [b] (empty if none). |

### `collections/lis_utils.dart`

Longest increasing subsequence (LIS) with reconstruction (roadmap #441).

`import 'package:saropa_dart_utils/collections/lis_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `lisLength` | function | Returns length of LIS of [list] using comparable order, and optionally the indices of one such subsequence (reconstruction). |
| `lisIndices` | function | Returns one LIS as a list of indices into [list]. |

### `collections/multi_criteria_sort_utils.dart`

Multi-criteria sort with weighted comparators — roadmap #462.

`import 'package:saropa_dart_utils/collections/multi_criteria_sort_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `sortByCriteria` | function | Returns a new list with [list]'s elements sorted by [compare]. |
| `thenBy` | function | Builds a comparator that uses [primary] and then [thenByCompare] for ties. |

### `collections/multi_key_group_utils.dart`

Group and aggregate by several keys at once — roadmap #477.

`import 'package:saropa_dart_utils/collections/multi_key_group_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `MultiKey` | class | A composite grouping key built from several selector values. |
| `MultiKey` | constructor | Wraps the ordered selector [values] that make up this composite key. |
| `values` | field | The selector values in selector order (e.g. |
| `operator ==` | operator |  |
| `hashCode` | getter |  |
| `toString` | method |  |
| `groupByKeys` | function | Groups [items] by applying every selector in [keys], bucketing rows whose composite key is equal. |
| `aggregateByKeys` | function | Groups [items] by [keys] and reduces each bucket with [aggregator], returning a map from composite key to the aggregate (e.g. |

### `collections/multiset_utils.dart`

Multi-set (bag) union/intersection/difference — roadmap #464.

`import 'package:saropa_dart_utils/collections/multiset_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `multisetUnion` | function | Bag: count per element. |
| `multisetIntersection` | function | Returns the multiset intersection of [a] and [b], keeping each shared element with the minimum of its two counts. |
| `multisetDifference` | function | Returns the multiset difference [a] minus [b], subtracting counts and dropping any element whose remaining count falls to zero or below. |

### `collections/nway_merge_utils.dart`

N-way merge of multiple sorted iterables — roadmap #457.

`import 'package:saropa_dart_utils/collections/nway_merge_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `nWayMerge` | function | Merges [iterables] (each sorted by natural order) into one sorted list. |

### `collections/online_mean_variance_utils.dart`

Online mean/variance for numeric streams — roadmap #466.

`import 'package:saropa_dart_utils/collections/online_mean_variance_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `OnlineMeanVarianceUtils` | class | Welford's online algorithm: add samples one by one, get mean and variance. |
| `count` | getter | Number of samples added. |
| `mean` | getter | Current mean. |
| `variance` | getter | Sample variance (Bessel-corrected). |
| `standardDeviation` | getter | Square root of [variance]. |
| `add` | method | Adds one sample [x]; updates mean and variance in O(1). |
| `toString` | method |  |

### `collections/pivot_unpivot_utils.dart`

Pivot and unpivot for tabular data (list of maps) — roadmap #469.

`import 'package:saropa_dart_utils/collections/pivot_unpivot_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `pivot` | function | Pivot: one row per [rowKey] value, one column per [colKey] value, [valueKey] as cell. |

### `collections/prefix_frequency_utils.dart`

Prefix frequency table (for autocomplete) — roadmap #481.

`import 'package:saropa_dart_utils/collections/prefix_frequency_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `prefixFrequencyTable` | function | Builds map: prefix -> count of strings having that prefix. |

### `collections/priority_map_utils.dart`

Priority map (priority -> queues) — roadmap #528.

`import 'package:saropa_dart_utils/collections/priority_map_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `PriorityMapUtils` | class | Map of priority (lower = higher priority) to queue of items. |
| `add` | method | Enqueues [value] under [priority], creating the queue if needed. |
| `removeFirst` | method | Removes and returns the next item in priority order (front of the first queue), or null when the map is empty. |
| `isEmpty` | getter | True when no items remain. |
| `toString` | method |  |

### `collections/quickselect_utils.dart`

Quickselect: the k-th smallest/largest element without fully sorting.

`import 'package:saropa_dart_utils/collections/quickselect_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `nthSmallest` | function | Returns the element that would sit at 0-based index [k] if [items] were sorted by [compare], without paying for a full sort. |
| `nthLargest` | function | Returns the [k]-th largest element (0-based: `k = 0` is the maximum), or `null` if [k] is out of range. |

### `collections/reservoir_sampling_utils.dart`

Reservoir sampling for streaming data (roadmap #452).

`import 'package:saropa_dart_utils/collections/reservoir_sampling_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `reservoirSample` | function | Takes a sample of [k] items from [items] with equal probability per item (single pass). |

### `collections/ring_buffer_utils.dart`

Ring buffer (bounded queue with overwrite) — roadmap #500.

`import 'package:saropa_dart_utils/collections/ring_buffer_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `RingBufferUtils` | class | Fixed-capacity ring buffer. |
| `RingBufferUtils` | constructor | Creates a ring buffer holding at most [capacity] elements. |
| `capacity` | getter | Maximum number of elements; when full, oldest is overwritten. |
| `add` | method | Appends [value]; overwrites oldest if at capacity. |
| `removeFirst` | method | Removes and returns the oldest element, or null when the buffer is empty. |
| `length` | getter | Current number of elements (0 to [capacity]). |
| `isEmpty` | getter | True when the buffer has no elements. |
| `isNotEmpty` | getter | True when the buffer has at least one element. |
| `toList` | method | Elements in order (oldest first). |
| `toString` | method |  |

### `collections/rolling_hash_utils.dart`

Rolling hash (Rabin–Karp style) for substring search — roadmap #482.

`import 'package:saropa_dart_utils/collections/rolling_hash_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `rollingHash` | function | Polynomial rolling hash of [s] in range [start, end). |
| `rollingHashSearch` | function | Returns index of [pattern] in [text] using rolling hash, or -1. |

### `collections/row_column_table_utils.dart`

Row-oriented vs column-oriented table conversion — roadmap #525.

`import 'package:saropa_dart_utils/collections/row_column_table_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `columnarToRows` | function | From columnar map (key -> list of values) to list of row maps. |

### `collections/run_detection_utils.dart`

Run detection (equal or increasing sequences with metadata) — roadmap #472.

`import 'package:saropa_dart_utils/collections/run_detection_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `RunDetectionUtils` | class | One run: start index, length, constant value or null for increasing. |
| `RunDetectionUtils` | constructor | Creates a run beginning at [start], spanning [length] elements, with the shared [value] (or null for a non-constant run). |
| `start` | getter | Start index in the source list. |
| `length` | getter | Length of the run. |
| `value` | getter | Constant value for the run, or null for non-constant runs. |
| `toString` | method |  |
| `runsEqual` | function | Detects runs of equal consecutive values in [list]. |

### `collections/seeded_shuffle_utils.dart`

Deterministic shuffler with seed — roadmap #529.

`import 'package:saropa_dart_utils/collections/seeded_shuffle_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `shuffleWithSeed` | function | Returns a new list with elements shuffled using [seed]. |

### `collections/sliding_window_aggregate_utils.dart`

Sliding window aggregations (min/max/sum/avg over moving window) — roadmap #451.

`import 'package:saropa_dart_utils/collections/sliding_window_aggregate_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `WindowAggregate` | enum | Aggregation kind for a sliding window. |
| `min` | enum value | Smallest value in each window. |
| `max` | enum value | Largest value in each window. |
| `sum` | enum value | Sum of all values in each window. |
| `avg` | enum value | Arithmetic mean of the values in each window. |
| `slidingWindow` | function | Returns a list where each element is the [agg] of [values] over a window of length [size]. |

### `collections/stream_quantile_utils.dart`

Stream quantile estimation (P²-style approximate) — roadmap #453.

`import 'package:saropa_dart_utils/collections/stream_quantile_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StreamQuantileUtils` | class | Approximate percentile from a stream of values (single pass, fixed memory). |
| `StreamQuantileUtils` | constructor | Creates an estimator for the quantile at [p], where [p] ranges from 0 to 1 (e.g. |
| `p` | getter | The target quantile in the range 0 to 1. |
| `add` | method | Feeds [value] into the estimator for inclusion in the quantile estimate. |
| `quantile` | getter | Current approximate quantile at [p] (e.g. |
| `toString` | method |  |

### `collections/string_pool_utils.dart`

Memory-conscious string pool (deduplicate repeating strings) — roadmap #518.

`import 'package:saropa_dart_utils/collections/string_pool_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringPoolUtils` | class | Interner: returns canonical instance for equal strings. |
| `StringPoolUtils` | constructor | Creates a string pool. |
| `intern` | method | Returns the canonical string equal to [s]; deduplicates repeated strings. |
| `size` | getter | Number of distinct interned strings. |
| `toString` | method |  |

### `collections/time_bucket_utils.dart`

Time-bucketed aggregation — roadmap #468.

`import 'package:saropa_dart_utils/collections/time_bucket_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `bucketByTime` | function | Groups [events] (DateTime) into buckets of [bucketSize]; returns map of bucketStart -> count. |

### `collections/top_k_heap_utils.dart`

Top-K by key via min-heap — roadmap #459.

`import 'package:saropa_dart_utils/collections/top_k_heap_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `topKIndices` | function | Returns indices of top [k] elements in [values] by [keyOf] (largest first). |

### `collections/trie_utils.dart`

Trie (prefix tree) with insert/delete/prefix search (roadmap #491).

`import 'package:saropa_dart_utils/collections/trie_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `TrieUtils` | class | Simple trie for string keys: insert, delete, prefix search. |
| `TrieUtils` | constructor | Creates an empty trie. |
| `insert` | method | Inserts [key] into the trie; empty key is ignored. |
| `search` | method | True if [key] was inserted as a complete word (not merely a prefix). |
| `startsWith` | method | True if some key has [prefix] as a prefix. |
| `delete` | method | Removes [key] from the trie if present. |
| `keysWithPrefix` | method | All keys with prefix [prefix]. |
| `toString` | method |  |
| `isEnd` | getter |  |
| `isEnd` | setter |  |
| `getChild` | method |  |
| `getOrCreateChild` | method |  |
| `removeChild` | method |  |
| `hasNoChildren` | getter |  |
| `childEntries` | getter |  |
| `toString` | method |  |

### `collections/weighted_interval_utils.dart`

Weighted interval scheduling (max weight, DP) — roadmap #446.

`import 'package:saropa_dart_utils/collections/weighted_interval_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `WeightedIntervalUtils` | class | Interval with weight. |
| `WeightedIntervalUtils` | constructor | Creates an interval spanning [start] to [end] with the given [weight]. |
| `start` | getter | Start of the interval. |
| `end` | getter | End of the interval. |
| `weight` | getter | Weight (value) contributed when this interval is selected. |
| `toString` | method |  |
| `maxWeightIntervals` | function | Returns the maximum total weight of non-overlapping intervals (DP by end time). |

### `collections/window_functions_utils.dart`

Window functions (lag, lead, row_number) over ordered data — roadmap #471.

`import 'package:saropa_dart_utils/collections/window_functions_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `lag` | function | Lag: value at index [i - offset]; null if out of range. |
| `lead` | function | Lead: value at index [i + offset]; null if out of range. |
| `rowNumber` | function | Row number (1-based index). |
| `rank` | function | Rank: 1-based rank; equal values get same rank, next rank skips. |

---

## DateTime

### `datetime/business_calendar_utils.dart`

Holiday-aware business calendar — roadmap #593.

`import 'package:saropa_dart_utils/datetime/business_calendar_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `BusinessCalendar` | class | A working-day calendar parameterized by a holiday set and a weekend definition. |
| `BusinessCalendar` | constructor | Creates a calendar. |
| `weekendDays` | field | The `DateTime.weekday` values treated as non-working (default Sat + Sun). |
| `isWeekend` | method | Whether [date] falls on a configured weekend day. |
| `isHoliday` | method | Whether [date]'s calendar day is in the holiday set. |
| `isBusinessDay` | method | Whether [date] is a working day: neither a weekend nor a holiday. |
| `nextBusinessDay` | method | The first business day strictly after [date]. |
| `previousBusinessDay` | method | The first business day strictly before [date]. |
| `addBusinessDays` | method | Adds [n] business days to [date], skipping weekends and holidays. |
| `businessDaysBetween` | method | Counts business days in `[start, end)` — inclusive of [start], exclusive of [end]. |
| `businessDaysIn` | method | Lists the business days in `[start, end)` in ascending order — inclusive of [start], exclusive of [end]. |

### `datetime/date_constant_extensions.dart`

`import 'package:saropa_dart_utils/datetime/date_constant_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `DateConstantExtensions` on `DateTime` | extension | `DateConstantExtensions` is an extension on the `DateTime` class in Dart. |
| `isUnixEpochDate` | getter | Checks if the year, month, and day of the `DateTime` instance match those of the Unix epoch date (January 1, 1970). |
| `isUnixEpochDateTime` | getter | Checks if the `DateTime` instance is exactly equal to the Unix epoch date and time (00:00:00 UTC, January 1, 1970). |

### `datetime/date_constants.dart`

`import 'package:saropa_dart_utils/datetime/date_constants.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `DateConstants` | class | Utility class providing static constants for commonly used date/time values. |
| `minMonth` | field | Minimum valid month number (January). |
| `maxMonth` | field | Maximum valid month number (December). |
| `maxYear` | field | Maximum valid year in DateTime (9999). |
| `maxHour` | field | Maximum valid hour (23 for 24-hour format, 0-23 range). |
| `maxMinuteOrSecond` | field | Maximum valid minute or second (59 for 0-59 range). |
| `maxMillisecondOrMicrosecond` | field | Maximum valid millisecond or microsecond (999 for 0-999 range). |
| `minDaysInAnyMonth` | field | Minimum number of days that exist in any month (February in non-leap years). |
| `daysToAddToGetNextMonth` | field | Days to add to safely reach next month (28 + 4 = 32 days > any month). |
| `daysInFebLeapYear` | field | Number of days in February during a leap year. |
| `defaultLeapYearCheckYear` | field | Default year to use for leap year calculations when year is not specified (chosen as a leap year). |
| `leapYearModulo4` | field | Modulo divisor for basic leap year check (divisible by 4). |
| `leapYearModulo100` | field | Modulo divisor for century leap year exception (divisible by 100). |
| `leapYearModulo400` | field | Modulo divisor for century leap year exception override (divisible by 400). |
| `dayStartHour` | field | Hour threshold for start of "day" time (after 7am). |
| `dayEndHour` | field | Hour threshold for end of "day" time (before 6pm/18:00). |
| `unixEpochDate` | field | January 1st, 1970 is known as the Unix epoch. |
| `daysPerWeek` | field | Number of days in a week. |
| `lastDayOffsetInWeek` | field | Days from start of week to end of week (0-based index of last weekday). |
| `monthsPerQuarter` | field | Number of months per quarter. |
| `isoWeekOffset` | field | Offset used in the ISO 8601 week number formula. |
| `isoWeekReferenceDay` | field | December day used to determine ISO weeks in a year. |
| `yearStringWidth` | field | Number of digits in a year for zero-padded formatting. |
| `minutesPerHour` | field | Number of minutes in one hour. |
| `decemberLastDay` | field | Last day of December. |
| `coppaMinAge` | field | Minimum age for non-child content access under COPPA. |
| `MonthUtils` | class | Utility class for month name operations. |
| `monthLongNames` | field | Full month names indexed by month number (1-12). |
| `monthShortNames` | field | Abbreviated month names indexed by month number (1-12). |
| `monthNumbers` | field | List of all month numbers (1-12). |
| `getMonthLongName` | method | Returns the full name of the given [month] (1-12), or `null` if invalid. |
| `getMonthShortName` | method | Returns the abbreviated name of the given [month] (1-12), or `null` if [month] is `null` or invalid. |
| `WeekdayUtils` | class | Utility class for weekday name operations. |
| `dayLongNames` | field | Full weekday names indexed by DateTime weekday constant (1 = Monday, 7 = Sunday). |
| `dayShortNames` | field | Abbreviated weekday names indexed by DateTime weekday constant. |
| `getDayLongName` | method | Returns the full name of the given [dayOfWeek] (1=Monday, 7=Sunday), or `null` if [dayOfWeek] is `null` or invalid. |
| `getDayShortName` | method | Returns the abbreviated name of the given [dayOfWeek] (1=Monday, 7=Sunday), or `null` if [dayOfWeek] is `null` or invalid. |
| `SerialDateUtils` | class | Utility class for serial date string parsing. |
| `serialToDateTime` | method | Parses a serial date string (ISO 8601 format) to DateTime. |

### `datetime/date_format_preset_utils.dart`

Dashboard date-format presets (short / medium / long) — roadmap #615.

`import 'package:saropa_dart_utils/datetime/date_format_preset_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `DateFormatNames` | class | Month and weekday names used by the medium/long presets, injectable so the presets can render in any language without a locale database. |
| `DateFormatNames` | constructor | Creates a name set. |
| `months` | field | Full month names, January..December (index = month - 1). |
| `monthsShort` | field | Abbreviated month names, Jan..Dec (index = month - 1). |
| `weekdays` | field | Full weekday names, Monday..Sunday (index = weekday - 1). |
| `english` | field | English default used when no localized names are supplied. |
| `formatDateShort` | function | Short preset: unambiguous ISO-8601 calendar date `yyyy-MM-dd` (e.g. |
| `formatDateMedium` | function | Medium preset: abbreviated month, day, year (e.g. |
| `formatDateLong` | function | Long preset: weekday, full month, day, year (e.g. |

### `datetime/date_time_arithmetic_extensions.dart`

`import 'package:saropa_dart_utils/datetime/date_time_arithmetic_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `DateTimeArithmeticExtensions` on `DateTime` | extension | Extensions on [DateTime] for add, subtract, and difference operations. |
| `getTimeDifferenceMs` | method | Returns the time difference in milliseconds between this [DateTime] and [compareTo], or `null` if [compareTo] is `null`. |
| `addYears` | method | Adds the specified number of years to the current [DateTime]. |
| `addMonths` | method | Adds the specified number of months to the current [DateTime]. |
| `addDays` | method | Adds the specified number of days to the current [DateTime]. |
| `addHours` | method | Adds the specified number of hours to the current [DateTime]. |
| `addMinutes` | method | Adds the specified number of minutes to the current [DateTime]. |
| `subtractMinutes` | method | Subtracts the specified number of minutes from the current [DateTime]. |
| `subtractHours` | method | Subtracts the specified number of hours from the current [DateTime]. |
| `subtractMonths` | method | Subtracts the specified number of months from the current [DateTime]. |
| `subtractYears` | method | Subtracts the specified number of years from the current [DateTime]. |
| `subtractDays` | method | Subtracts the specified number of days from the current [DateTime]. |

### `datetime/date_time_bounds_extensions.dart`

`import 'package:saropa_dart_utils/datetime/date_time_bounds_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `DateTimeBoundsExtensions` on `DateTime` | extension | Start/end of day, week, month, quarter, year; quarter; next weekday; isWeekend. |
| `startOfDay` | getter | Start of day (00:00:00.000000). |
| `endOfDay` | getter | End of day (23:59:59.999999). |
| `startOfWeek` | method | Start of week. |
| `endOfWeek` | method | End of week (last day 23:59:59.999999). |
| `startOfMonth` | getter | Start of month (first day 00:00:00). |
| `endOfMonth` | getter | End of month (last day 23:59:59.999999). |
| `quarter` | getter | Quarter (1–4). |
| `startOfQuarter` | getter | Start of quarter (first day of quarter month 00:00:00). |
| `endOfQuarter` | getter | End of quarter (last day of quarter month 23:59:59.999999). |
| `startOfYear` | getter | Start of year (Jan 1 00:00:00). |
| `endOfYear` | getter | End of year (Dec 31 23:59:59.999999). |
| `isWeekend` | getter | True if Saturday or Sunday. |
| `nextWeekday` | method | Next date that is a weekday (skips Saturday/Sunday). |
| `sameTimeOn` | method | Same calendar date as the receiver with time components from [other]. |

### `datetime/date_time_business_days_utils.dart`

`import 'package:saropa_dart_utils/datetime/date_time_business_days_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `businessDaysBetween` | function | Business days between two dates (excludes weekends; no holiday support). |
| `addBusinessDays` | function | Adds [n] business days to [date] (skips weekends). |

### `datetime/date_time_calendar_extensions.dart`

`import 'package:saropa_dart_utils/datetime/date_time_calendar_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `DateTimeCalendarExtensions` on `DateTime` | extension | Extensions on [DateTime] for age, calendar, formatting, and time alignment. |
| `calculateAgeFromNow` | method | Calculates the age based on the current date. |
| `calculateAgeFromDate` | method | Calculates the age based on a given date. |
| `mostRecentSunday` | getter | Returns the most recent Sunday before or on this date. |
| `mostRecentWeekday` | method | Returns the most recent occurrence of the specified weekday. |
| `dayOfYear` | getter | Returns the day of the year (1-366). |
| `weekOfYear` | getter | Returns a raw week-of-year value used internally. |
| `numOfWeeks` | method | Returns the number of ISO weeks in the specified year. |
| `weekNumber` | method | Returns the ISO 8601 week number, correctly handling year boundaries. |
| `toSerialString` | getter | Converts this DateTime to a serial string format (yyyyMMdd'T'HHmmss). |
| `toSerialStringDay` | getter | Converts this DateTime to a serial day string format (yyyyMMdd). |
| `getUtcTimeFromLocal` | method | Converts this local [DateTime] to UTC using the given timezone [offset]. |
| `setTime` | method | Sets the time of the current [DateTime] to the specified [TimeOfDay]. |
| `alignDateTime` | method | Returns a new [DateTime] aligned to the specified [alignment] duration. |

### `datetime/date_time_clamp_extensions.dart`

`import 'package:saropa_dart_utils/datetime/date_time_clamp_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `DateTimeClampExtensions` on `DateTime` | extension | Clamp [DateTime] to a range. |
| `clampTo` | method | Returns this [DateTime] clamped to [min] and [max]. |

### `datetime/date_time_comparison_extensions.dart`

`import 'package:saropa_dart_utils/datetime/date_time_comparison_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `DateTimeComparisonExtensions` on `DateTime` | extension | Extensions on [DateTime] for comparison, range, and equality checks. |
| `isAfterNow` | method | Checks if the date is in the future compared to the current date and time. |
| `isBeforeNow` | method | Checks if the date is in the past compared to the current date and time. |
| `isBeforeNullable` | method | Returns `false` if [other] is `null`, otherwise returns the result of comparing this [DateTime] with [other] using the `isBefore` method. |
| `isAfterNullable` | method | Returns `false` if [other] is `null`, otherwise returns the result of comparing this [DateTime] with [other] using the `isAfter` method. |
| `isYearCurrent` | method | Checks if the date is in the current calendar year. |
| `isSameDateOrAfter` | method | Returns true if this date (date-only) is the same as or after [other]. |
| `isSameDateOrBefore` | method | Returns true if this date (date-only) is the same as or before [other]. |
| `toDateOnly` | method | Removes the time component from the [DateTime] object, returning only the date part (year, month, and day). |
| `isAnnualDateInRange` | method | Returns `true` if this [DateTime] is within the specified [range]. |
| `isBetweenRange` | method | Returns `true` if this [DateTime] is within the specified [range], `false` if [range] is `null`. |
| `isBetween` | method | Returns `true` if this [DateTime] is between [start] and [end]. |
| `isDateAfterToday` | method | Returns true if this date is strictly after today (i.e., tomorrow or later). |
| `isToday` | method | Returns `true` if this [DateTime] matches the current date (today). |
| `isSameDateOnly` | method | Checks if the current [DateTime] has the same date (year, month, day) as another [DateTime]. |
| `isSameDayMonth` | method | Checks if the current [DateTime] has the same day and month as another [DateTime]. |
| `isSameMonth` | method | Checks if the current [DateTime] has the same month as another [DateTime]. |

### `datetime/date_time_extensions.dart`

`import 'package:saropa_dart_utils/datetime/date_time_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `DateTimeExtensions` on `DateTime` | extension | Extensions on the [DateTime] class to provide additional functionality. |
| `getNthWeekdayOfMonthInYear` | method | Returns the date of the [n]th occurrence of [dayOfWeek] within the current instance's month and year, or `null` if it does not exist (e.g., 5th Friday in Feb... |
| `isUnder13` | method | Returns `true` if the date (of birth) is under 13 years old. |
| `generateDayList` | method | Returns a list of [DateTime] objects for consecutive days. |
| `prevDay` | method | Returns the previous day. |
| `nextDay` | method | Returns the next day. |
| `isLeapYear` | method | Checks if the current year is a leap year using [DateTimeUtils.isLeapYear]. |
| `yearStart` | getter | Gets the start date of the year. |
| `yearEnd` | getter | Gets the end date of the year. |
| `isMidnight` | getter | Checks if the current time is exactly midnight (00:00:00.000000). |
| `toDateInYear` | method | Ignores the year of the current [DateTime] and returns a new [DateTime] with the specified [setYear]. |
| `dayOfMonthOrdinal` | method | Returns the ordinal representation of the day of the month. |

### `datetime/date_time_fiscal_extensions.dart`

`import 'package:saropa_dart_utils/datetime/date_time_fiscal_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `DateTimeFiscalExtensions` on `DateTime` | extension | Fiscal year start/end (configurable start month). |
| `fiscalYear` | method | Fiscal year when year starts in [startMonth] (1–12). |
| `startOfFiscalYear` | method | Start of fiscal year (first day of [startMonth] at 00:00:00). |
| `endOfFiscalYear` | method | End of fiscal year (last day of (startMonth-1) at 23:59:59.999999). |

### `datetime/date_time_list_extensions.dart`

`import 'package:saropa_dart_utils/datetime/date_time_list_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `DateTimeListMinMaxExtensions` on `List<DateTime>` | extension | Min/max of list of [DateTime]; sort; generate date range. |
| `minOrNull` | getter | Returns the earliest [DateTime], or `null` if empty. |
| `maxOrNull` | getter | Returns the latest [DateTime], or `null` if empty. |
| `sortedByDate` | method | Returns a new list sorted by date (earliest first). |
| `dateRange` | function | Generates a range of dates from [start] to [end] inclusive, stepping by [stepDays] days. |

### `datetime/date_time_more_extensions.dart`

`import 'package:saropa_dart_utils/datetime/date_time_more_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `DateTimeMoreExtensions` on `DateTime` | extension | DateTime More: is same day, is morning/afternoon/evening, duration between, within last N, etc. |
| `isSameDay` | method | True if this date is the same calendar day as [other]. |
| `isMorning` | getter | True if hour is in 5..11 (morning). |
| `isAfternoon` | getter | True if hour is in 12..16 (afternoon). |
| `isEvening` | getter | True if hour is >= 17 or < 5 (evening/night). |
| `isWithinLastDays` | method | True if this is on or after [now] minus [n] days and not after [now]. |
| `isWithinLastHours` | method | True if this is within the last [n] hours before [now]. |
| `durationBetween` | function | Returns the absolute [Duration] between [a] and [b], regardless of order. |
| `monthsBetween` | function | Returns the first day of each month from [start] through [end] inclusive. |
| `yearsBetween` | function | List of years from [start].year through [end].year (inclusive). |

### `datetime/date_time_nullable_extensions.dart`

`import 'package:saropa_dart_utils/datetime/date_time_nullable_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `DateTimeNullableExtensions` on `DateTime?` | extension | Extension methods for nullable DateTime (`DateTime?`) to enhance null-aware DateTime comparisons, especially useful for sorting and ordering operations where... |
| `isBeforeNullable` | method | Checks if this nullable DateTime is before another nullable DateTime for sorting purposes. |
| `compareDateTimeNullable` | method | Compares two nullable DateTimes for sorting purposes. |

### `datetime/date_time_overlap_utils.dart`

Overlap of two date ranges.

`import 'package:saropa_dart_utils/datetime/date_time_overlap_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `dateRangeOverlap` | function | Overlap of two date ranges. |

### `datetime/date_time_range_utils.dart`

`import 'package:saropa_dart_utils/datetime/date_time_range_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `DateTimeRangeExtensions` on `DateTimeRange` | extension | Extension on [DateTimeRange] to provide additional functionality |
| `isNthDayOfMonthInRange` | method | Returns `true` if the [n]th occurrence of [dayOfWeek] in the given [month] falls within this date range. |
| `inRange` | method | Returns `true` if the given [date] is within this range. |
| `isNowInRange` | method | Returns `true` if [now] is within this range. |

### `datetime/date_time_relative_utils.dart`

`import 'package:saropa_dart_utils/datetime/date_time_relative_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `relativeTimeString` | function | Returns a relative time string: "2 hours ago", "in 3 days", "just now". |

### `datetime/date_time_timezone_extensions.dart`

`import 'package:saropa_dart_utils/datetime/date_time_timezone_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `DateTimeTimezoneOffsetExtensions` on `DateTime` | extension | Timezone offset string (e.g. |
| `timeZoneOffsetString` | getter | Offset string for the local timezone (e.g. |

### `datetime/date_time_utils.dart`

`import 'package:saropa_dart_utils/datetime/date_time_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `DateTimeUtils` | class | A utility class for working with [DateTime] objects. |
| `calculateAgeAtDeath` | method | Calculates the age at death based on the date of birth (DOB) and date of death (DOD). |
| `extractYear` | method | Extracts a 4-digit year from a given string. |
| `tomorrow` | method | Returns the date for tomorrow at the specified time. |
| `isDateMonthFirst` | method | Checks if the given [localeName] uses a month-first date format. |
| `convertDaysToYearsAndMonths` | method | Returns a human-readable string representing [days] as years, months, and optionally remaining days, or `null` if [days] is `null` or less than 1. |
| `firstDayNextMonth` | method | Returns the first day of the month following the given [month] and [year], or `null` if [month] is invalid. |
| `maxDate` | method | Returns the later of two dates. |
| `minDate` | method | Returns the earlier of two dates. |
| `isLeapYear` | method | Checks if the given year is a leap year. |
| `monthDayCount` | method | Returns the number of days in the given [month] and [year]. |
| `isValidDateParts` | method | Returns `true` if all provided date/time components are within valid ranges. |

### `datetime/date_time_week_extensions.dart`

`import 'package:saropa_dart_utils/datetime/date_time_week_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `DateTimeWeekInMonthExtensions` on `DateTime` | extension | Week number in month; ISO week string format/parse. |
| `weekNumberInMonth` | getter | Week number within the month (1-based). |
| `toIsoWeekString` | getter | ISO week string (e.g. |
| `parseIsoWeekString` | function | Parses ISO week string "2026-W09" to the Monday of that week. |

### `datetime/duration_format_utils.dart`

`import 'package:saropa_dart_utils/datetime/duration_format_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `formatDuration` | function | Formats [Duration] as a short string (e.g. |

### `datetime/duration_parse_utils.dart`

Parses duration string like "1.5h", "90m", "2d 3h 30m".

`import 'package:saropa_dart_utils/datetime/duration_parse_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `parseDuration` | function | Parses duration string like "1.5h", "90m", "2d 3h 30m". |

### `datetime/injectable_clock_utils.dart`

Injectable clock for tests (consistent "now") — roadmap #614.

`import 'package:saropa_dart_utils/datetime/injectable_clock_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `InjectableClockUtils` | class | Abstract clock; implementations provide a deterministic or live "now". |
| `now` | method | Current time for this clock. |
| `SystemClock` | class | Live clock. |
| `now` | method |  |
| `toString` | method |  |
| `FixedClock` | class | Fixed clock for tests. |
| `FixedClock` | constructor | Creates a clock whose [now] always returns the fixed [DateTime]. |
| `now` | method |  |
| `toString` | method |  |
| `defaultClock` | getter | Current clock used by code that depends on injectable time. |
| `defaultClock` | setter | Sets the injectable clock (e.g. |
| `_ClockHolder` | constructor |  |
| `clock` | getter |  |
| `clock` | setter |  |
| `toString` | method |  |

### `datetime/iso_interval_parse_utils.dart`

ISO 8601 time-interval parser — roadmap #646.

`import 'package:saropa_dart_utils/datetime/iso_interval_parse_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `_IsoDuration` | constructor |  |
| `years` | field |  |
| `months` | field |  |
| `days` | field |  |
| `time` | field |  |
| `applyTo` | method | Applies this duration to [base] in direction [sign] (+1 forward, -1 back), preserving [base]'s time-of-day fields and UTC-ness. |
| `parseIsoInterval` | function | Parses an ISO 8601 time interval into a [DateTimeRange]. |

### `datetime/period_split_utils.dart`

Period splitting (by month/week) — roadmap #610.

`import 'package:saropa_dart_utils/datetime/period_split_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `splitByMonth` | function | Splits [start]..[end] into consecutive months. |

### `datetime/quiet_hours_utils.dart`

"Quiet hours" helper — roadmap #613.

`import 'package:saropa_dart_utils/datetime/quiet_hours_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `QuietWindow` | class | A daily quiet window in minutes past midnight. |
| `QuietWindow` | constructor | Creates a window from [startMinute] to [endMinute] (0..1440), wrapping when start > end. |
| `startMinute` | field | Inclusive start minute past midnight. |
| `endMinute` | field | Exclusive end minute past midnight. |
| `containsMinute` | method | Whether [minuteOfDay] falls inside this window, honoring midnight wrap. |
| `QuietHours` | class | A set of daily quiet windows with point-in-time and "when does quiet end" queries. |
| `QuietHours` | constructor | Creates quiet hours from [windows] (applied every day). |
| `QuietHours.daily` | constructor | Convenience for a single daily window `[startMinute, endMinute)`. |
| `windows` | field | The configured quiet windows. |
| `isQuiet` | method | Whether [at] falls inside any quiet window (minute resolution). |
| `quietUntil` | method | If [at] is within quiet hours, the instant quiet ends (so a deferred notification can be scheduled for then); otherwise null. |

### `datetime/recurrence_iterator_utils.dart`

Expand a parsed RRULE into concrete occurrences — roadmap #592.

`import 'package:saropa_dart_utils/datetime/recurrence_iterator_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `expandRecurrence` | function | Lazily yields the occurrences of [rule] at and after [start], in ascending order. |

### `datetime/relative_date_bucket_utils.dart`

Relative date bucketing ("today", "yesterday", "last 7 days") — roadmap #616.

`import 'package:saropa_dart_utils/datetime/relative_date_bucket_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `relativeDateBucket` | function | Bucket label for [date] relative to [today]. |

### `datetime/rrule_parse_utils.dart`

RFC 5545 recurrence-rule (RRULE) parser — a practical subset, roadmap #591.

`import 'package:saropa_dart_utils/datetime/rrule_parse_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `RecurFrequency` | enum | How often a recurrence repeats. |
| `daily` | enum value | Every [RecurrenceRule.interval] days. |
| `weekly` | enum value | Every [RecurrenceRule.interval] weeks. |
| `monthly` | enum value | Every [RecurrenceRule.interval] months. |
| `yearly` | enum value | Every [RecurrenceRule.interval] years. |
| `RecurFrequency` | constructor |  |
| `token` | field | The RRULE keyword (`DAILY`, `WEEKLY`, …). |
| `RecurWeekday` | enum | A day of the week, carrying both its RRULE code and its `DateTime.weekday` number so the iterator can match without a second lookup table. |
| `monday` | enum value | Monday (`MO`). |
| `tuesday` | enum value | Tuesday (`TU`). |
| `wednesday` | enum value | Wednesday (`WE`). |
| `thursday` | enum value | Thursday (`TH`). |
| `friday` | enum value | Friday (`FR`). |
| `saturday` | enum value | Saturday (`SA`). |
| `sunday` | enum value | Sunday (`SU`). |
| `RecurWeekday` | constructor |  |
| `isoWeekday` | field | Matches `DateTime.weekday` (Monday = 1 … Sunday = 7). |
| `code` | field | The RRULE two-letter code (`MO`, `TU`, …). |
| `RecurrenceRule` | class | An immutable, parsed RRULE. |
| `RecurrenceRule` | constructor | Creates a rule. |
| `frequency` | field | The base repeat unit (daily/weekly/monthly/yearly). |
| `interval` | field | Repeat every N units (≥ 1). |
| `count` | field | Stop after this many occurrences, or null for unbounded / [until]-bounded. |
| `until` | field | Stop on/after this instant (inclusive), or null. |
| `byWeekDays` | field | Restrict to these weekdays (chiefly for WEEKLY); empty means no filter. |
| `byMonthDays` | field | Restrict to these days of the month (1..31, or negative from month end). |
| `byMonths` | field | Restrict to these months (1..12), chiefly for YEARLY. |
| `weekStart` | field | First day of the week (affects weekly interval math); defaults to Monday. |
| `operator ==` | operator |  |
| `hashCode` | getter |  |
| `toString` | method |  |
| `frequency` | field |  |
| `interval` | field |  |
| `count` | field |  |
| `until` | field |  |
| `byWeekDays` | field |  |
| `byMonthDays` | field |  |
| `byMonths` | field |  |
| `weekStart` | field |  |
| `apply` | method | Dispatches one parsed `NAME=VALUE` part to the matching field. |
| `parseRrule` | function | Parses an RRULE string (with or without a leading `RRULE:`) into a [RecurrenceRule]. |

### `datetime/sla_calculator_utils.dart`

SLA / due-date calculator over business hours + holidays — roadmap #595.

`import 'package:saropa_dart_utils/datetime/sla_calculator_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `OpenWindow` | class | A single open period within a day, expressed as minutes from midnight: `[startMinute, endMinute)`. |
| `OpenWindow` | constructor | Creates a window from [startMinute] to [endMinute] (minutes past midnight). |
| `startMinute` | field | Inclusive start, minutes past midnight. |
| `endMinute` | field | Exclusive end, minutes past midnight. |
| `BusinessHours` | class | A weekly schedule of open windows keyed by `DateTime.weekday` (Mon = 1 … Sun = 7). |
| `BusinessHours` | constructor | Creates a schedule from [windowsByWeekday]. |
| `BusinessHours.uniform` | constructor | Same open window every listed weekday (default Monday–Friday). |
| `windowsFor` | method | The open windows for [weekday] (Mon = 1 … Sun = 7), empty if closed. |
| `SlaCalculator` | class | Computes deadlines and elapsed working time against a [BusinessHours] schedule, optionally skipping holidays from a [BusinessCalendar]. |
| `SlaCalculator` | constructor | Creates a calculator for [hours], optionally holiday-aware via [calendar]. |
| `hours` | field | The weekly open-hours schedule. |
| `isOpen` | method | Whether [at] falls inside an open window on a working day. |
| `addWorkingTime` | method | The instant [amount] of working time after [start], counting only open-hour time and skipping holidays/closed days. |
| `workingTimeBetween` | method | The amount of working time within `[start, end)`, counting only open-hour time on working days. |

### `datetime/time_emoji_utils.dart`

`import 'package:saropa_dart_utils/datetime/time_emoji_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `TimeEmojiUtils` | class | Utility class for handling time-related emojis, specifically for day/night representation. |
| `sunEmoji` | field | Emoji representing the sun (☀️). |
| `moonEmoji` | field | Emoji representing the moon (🌙). |
| `getEmojiDayOrNight` | method | Returns `sunEmoji` if the given hour is during daytime (7am inclusive to 6pm exclusive), otherwise returns `moonEmoji`. |
| `EmojiDateTimeExtensions` on `DateTime` | extension | Extension on [DateTime] to easily access day/night emoji representation. |
| `emojiDayOrNight` | getter | Returns [TimeEmojiUtils.sunEmoji] if the [DateTime]'s hour is during daytime (7am inclusive to 6pm exclusive), otherwise returns [TimeEmojiUtils.moonEmoji]. |

### `datetime/time_rounding_utils.dart`

Flexible time rounding (nearest 5/10/15 min, ceiling/floor) — roadmap #600.

`import 'package:saropa_dart_utils/datetime/time_rounding_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `roundMinutes` | function | Rounds [minutes] to nearest [step] (e.g. |

### `datetime/timebox_exception.dart`

Timebox: run within time budget — roadmap #618.

`import 'package:saropa_dart_utils/datetime/timebox_exception.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `timebox` | function | Runs [fn]; if not completed in [timeout], returns [onTimeout] result (or throws). |
| `TimeboxException` | class | Thrown when [timebox] exceeds the allowed duration and no [onTimeout] was provided. |
| `TimeboxException` | constructor | Creates the exception with a [message] describing the timeout. |
| `message` | getter | Description of the timeout. |
| `toString` | method |  |

---

## Double

### `double/double_close_to_extensions.dart`

`import 'package:saropa_dart_utils/double/double_close_to_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `DoubleCloseToExtensions` on `double` | extension | Tolerance-based floating-point comparison. |
| `isCloseTo` | method | Returns `true` if this value is within tolerance of [other]. |

### `double/double_extensions.dart`

`import 'package:saropa_dart_utils/double/double_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `DoubleExtensions` on `double` | extension | Extension methods for [double] values providing formatting and conversion utilities. |
| `hasDecimals` | getter | Returns `true` if this double has a non-zero fractional part. |
| `toPercentage` | method | Returns this double formatted as a percentage string. |
| `formatDouble` | method | Returns this double formatted as a string with the specified number of decimal places. |
| `forceBetween` | method | Clamps this value to be within the range [from] to [to]. |
| `toPrecision` | method | Returns this double truncated to the given [precision] after the decimal point. |
| `formatPrecision` | method | Returns this double formatted as a string with smart decimal handling. |

### `double/double_iterable_extensions.dart`

`import 'package:saropa_dart_utils/double/double_iterable_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `DoubleIterableExtensions` on `Iterable<double>` | extension | Extension on Iterable&lt;double&gt; to provide additional properties and methods specifically for lists of doubles. |
| `smallestOccurrence` | method | Finds the smallest occurrence in the list. |
| `biggestOccurrence` | method | Finds the biggest occurrence in the list. |
| `mostOccurrences` | method | Finds the most common value in the list. |
| `leastOccurrences` | method | Finds the least common value in the list. |

---

## Enum

### `enum/enum_iterable_extensions.dart`

`import 'package:saropa_dart_utils/enum/enum_iterable_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `EnumIterableExtensions` on `Iterable<T>` | extension | Saropa extensions for [List]s of [Enum]s |
| `mostOccurrences` | method | Returns a [MapEntry] with the most common value and its frequency, or `null` if the iterable is empty. |
| `byNameTry` | method | Finds and returns the first enum value in this list whose [name] property matches the given [name], or `null` if no such value is found. |
| `sortedEnumValues` | method | Returns a list of enum values sorted alphabetically by name. |

---

## Gesture

### `gesture/gesture_utils.dart`

`import 'package:saropa_dart_utils/gesture/gesture_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `GestureUtils` | class | This is a utility class that contains static methods related to gesture processing. |
| `getSwipeSpeed` | method | Method to get the swipe speed based on the thresholds defined in [_swipeSpeedThresholds]. |
| `swipeMagnitudeThresholds` | field | Map for swipe magnitude thresholds. |

### `gesture/swipe_properties.dart`

`import 'package:saropa_dart_utils/gesture/swipe_properties.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `SwipeSpeed` | enum | Enum for swipe speed. |
| `minimal` | enum value | For very slow swipes. |
| `slow` | enum value | For slow swipes. |
| `normal` | enum value | For regular speed swipes. |
| `fast` | enum value | For fast swipes. |
| `SwipeDirection` | enum | Enum for swipe direction. |
| `left` | enum value | For swipes to the left. |
| `right` | enum value | For swipes to the right. |
| `up` | enum value | For swipes up. |
| `down` | enum value | For swipes down. |
| `SwipeMagnitude` | enum | Enum for swipe magnitude. |
| `minimal` | enum value | For very small swipes. |
| `small` | enum value | For small swipes. |
| `medium` | enum value | For medium swipes. |
| `large` | enum value | For large swipes. |
| `massive` | enum value | For very large swipes. |
| `SwipeAngle` | enum | Enum for swipe angle. |
| `horizontal` | enum value | For horizontal swipes. |
| `diagonal` | enum value | For diagonal swipes. |
| `vertical` | enum value | For vertical swipes. |
| `SwipeProperties` | class | Record for a swipe gesture. |
| `SwipeProperties` | constructor | Creates a new [SwipeProperties] record. |
| `direction` | field | The direction of the swipe. |
| `speed` | field | The speed of the swipe. |
| `magnitude` | field | The magnitude of the swipe. |
| `angle` | field | The angle of the swipe. |
| `toString` | method | Meaningful string for debugging and logging (avoids default "Instance of 'Swipe'"). |
| `SwipePropsExt` on `DragEndDetails` | extension | The DragEndDetailsProperties extension adds additional properties to [DragEndDetails] instances, allowing you to easily determine the direction and speed of... |
| `swipeDirection` | getter | Method to get the swipe direction. |
| `swipeSpeed` | getter | Method to get the swipe speed. |
| `swipeMagnitude` | getter | Method to get the swipe magnitude. |
| `swipeAngle` | getter | Method to get the swipe angle. |
| `swipe` | getter | Method to get a Swipe record for the swipe gesture. |

---

## Graph

### `graph/astar_utils.dart`

A* shortest path with pluggable heuristic — roadmap #534.

`import 'package:saropa_dart_utils/graph/astar_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `astar` | function | A* from [start] to [goal]. |

### `graph/bfs_dfs_utils.dart`

BFS/DFS traversal with hooks (roadmap #532).

`import 'package:saropa_dart_utils/graph/bfs_dfs_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `bfs` | function | BFS from [start]; calls [visit](node, depth) for each node. |
| `dfs` | function | DFS from [start]; calls [visit](node, depth). |

### `graph/bipartite_utils.dart`

Bipartite graph check and partitioning — roadmap #546.

`import 'package:saropa_dart_utils/graph/bipartite_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `bipartitePartition` | function | Returns (true, left, right) if bipartite, else (false, [], []). |

### `graph/connected_components_utils.dart`

Connected components (and optionally SCC) — roadmap #537.

`import 'package:saropa_dart_utils/graph/connected_components_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `connectedComponents` | function | Returns list of components (each component = list of node indices). |

### `graph/critical_path_utils.dart`

Critical path (longest path in DAG) — roadmap #550.

`import 'package:saropa_dart_utils/graph/critical_path_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `criticalPathDistances` | function | Longest path from [start] to each node (DAG). |

### `graph/dag_scheduler_utils.dart`

DAG-based task scheduler (topological + priorities) — roadmap #558.

`import 'package:saropa_dart_utils/graph/dag_scheduler_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `dagSchedule` | function | Returns schedule (list of node indices) respecting topology; [priority] lower = earlier. |

### `graph/dijkstra_utils.dart`

Dijkstra shortest path on weighted graphs (roadmap #533).

`import 'package:saropa_dart_utils/graph/dijkstra_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `dijkstraDistances` | function | Returns shortest distances from [source] to all nodes; unreachable = infinity. |
| `dijkstraWithParents` | function | Returns (distances, parent array). |

### `graph/floyd_warshall_utils.dart`

All-pairs shortest paths (Floyd–Warshall) — roadmap #536.

`import 'package:saropa_dart_utils/graph/floyd_warshall_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `floydWarshall` | function | Returns distance matrix where row i, column j gives shortest path i→j (infinity if none). |

### `graph/graph_diff_utils.dart`

Graph diff (added/removed/changed edges) — roadmap #553.

`import 'package:saropa_dart_utils/graph/graph_diff_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `graphEdgeDiff` | function | Compares two adjacency lists; returns (added edges, removed edges). |

### `graph/graph_utils.dart`

Graph representation helpers: adjacency list / weighted edges (roadmap #531).

`import 'package:saropa_dart_utils/graph/graph_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `GraphUtils` | class | Weighted directed edge. |
| `GraphUtils` | constructor | Creates a directed edge from node [from] to node [to] with optional [weight] (defaults to 1.0). |
| `from` | getter | Source node index. |
| `to` | getter | Target node index. |
| `weight` | getter | Edge weight (cost). |
| `toString` | method |  |
| `WeightedAdjacency` | typedef | Adjacency list: for each node, list of (neighbor, weight). |
| `buildWeightedGraph` | function | Builds [WeightedAdjacency] from [edges]; [nodeCount] = max node index + 1. |
| `Adjacency` | typedef | Unweighted: list of neighbors per node. |
| `buildGraph` | function | Builds an unweighted [Adjacency] list from [edges], where [nodeCount] is the max node index + 1. |

### `graph/hierarchy_utils.dart`

Hierarchy flattener and builder from flat list with parent ids — roadmap #556, #557.

`import 'package:saropa_dart_utils/graph/hierarchy_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `HierarchyUtils` | class | Node with id and optional parentId for tree building. |
| `HierarchyUtils` | constructor | Creates a node with the given [id] and optional [parentId] (null for a root node). |
| `id` | getter | Unique node identifier. |
| `parentId` | getter | Parent node id; null for roots. |
| `toString` | method |  |
| `flattenHierarchy` | function | Flattens tree: [nodes] has (id, parentId). |
| `hierarchyFromParentIds` | function | Builds parent map from flat list: id -> parentId (roots have null). |

### `graph/line_simplify_utils.dart`

Line simplification (Douglas–Peucker) — roadmap #547.

`import 'package:saropa_dart_utils/graph/line_simplify_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `LineSimplifyUtils` | class | Point for polyline. |
| `LineSimplifyUtils` | constructor | Creates a 2D point at coordinates ([x], [y]). |
| `x` | getter | X coordinate. |
| `y` | getter | Y coordinate. |
| `toString` | method |  |
| `douglasPeuckerIndices` | function | Douglas–Peucker line simplification: returns indices of [points] to keep within [epsilon] tolerance. |

### `graph/mst_utils.dart`

Minimum spanning tree (Kruskal) — roadmap #538.

`import 'package:saropa_dart_utils/graph/mst_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `kruskalMST` | function | MST from [edges] (GraphUtils); returns (mst edges, total cost). |

### `graph/topological_sort_utils.dart`

Topological sort with cycle detection — roadmap #539.

`import 'package:saropa_dart_utils/graph/topological_sort_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `topologicalSort` | function | Returns sorted node indices, or null if cycle detected. |

### `graph/tree_utils.dart`

Tree utilities (LCA, depth, subtree size) — roadmap #555.

`import 'package:saropa_dart_utils/graph/tree_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `lowestCommonAncestor` | function | Parent array: entry at index i is the parent of node i; root has -1. |
| `treeDepths` | function | Returns depth of each node (root = 0). |

---

## HTML

### `html/html_entity_data.dart`

Length of the longest key in [htmlNamedEntities].

`import 'package:saropa_dart_utils/html/html_entity_data.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `htmlEntityMaxKeyLength` | field | Length of the longest key in [htmlNamedEntities]. |
| `htmlEntityMaxLegacyLength` | field | Longest legacy entity (without trailing semicolon) in [htmlNamedEntities]. |
| `htmlNamedEntities` | field | HTML5 named character reference map. |

### `html/html_utils.dart`

`import 'package:saropa_dart_utils/html/html_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `HtmlUtils` | class | Utility class for HTML text processing. |
| `unescape` | method | Converts HTML entities to their corresponding characters. |
| `removeHtmlTags` | method | Removes all HTML tags from a string, leaving only the text content. |
| `toPlainText` | method | Strips HTML tags and unescapes HTML entities in one operation. |

---

## Hex

### `hex/hex_utils.dart`

`import 'package:saropa_dart_utils/hex/hex_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `HexExtensions` on `String` | extension | Extension methods for [String] to facilitate hexadecimal conversions. |
| `hexToInt` | method | Converts a hexadecimal string to an `int`. |
| `HexIntExtensions` on `int` | extension | Extension methods for [int] to facilitate hexadecimal conversions. |
| `intToHex` | method | Converts an `int` value to a hexadecimal `String`. |

---

## Integer

### `int/int_extensions.dart`

`import 'package:saropa_dart_utils/int/int_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `IntExtensions` on `int` | extension | `IntExtensions` is an extension on the `int` class in Dart. |
| `countDigits` | method | This function counts the number of digits in an integer. |
| `forceBetween` | method | Clamps this integer to be within the inclusive range [[from], [to]]. |

### `int/int_iterable_extensions.dart`

`import 'package:saropa_dart_utils/int/int_iterable_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `IntIterableExtensions` on `Iterable<int>` | extension | NOTE: Dart's type system doesn't consider int to be a subtype of Comparable&lt;int&gt;, even though int does implement Comparable&lt;num&gt; |
| `mostOccurrences` | method | Returns an [Occurrence] of the most common value and its frequency, or `null` if the iterable is empty. |
| `leastOccurrences` | method | Returns an [Occurrence] of the least common value and its frequency, or `null` if the iterable is empty. |

### `int/int_nullable_extensions.dart`

`import 'package:saropa_dart_utils/int/int_nullable_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `IntNullableExtensions` on `int?` | extension | Null-safe comparison helpers for nullable [int] values. |
| `compareToIntNullable` | method | Returns a negative value if this is less than [second], zero if they are equal, and a positive value if this is greater than [second]. |

### `int/int_string_extensions.dart`

`import 'package:saropa_dart_utils/int/int_string_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `IntStringExtensions` on `int` | extension | `IntStringExtensions` is an extension on the `int` class in Dart. |
| `ordinal` | method | Returns an ordinal number of `String` type for any integer |

### `int/int_utils.dart`

`import 'package:saropa_dart_utils/int/int_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `IntUtils` | class | `IntUtils` is a utility class in Dart that provides static methods for performing operations on integers. |
| `findGreatestCommonDenominator` | method | Returns the greatest common denominator of [a] and [b], or `null` if the inputs are invalid or recursion exceeds [maxDepth]. |

---

## Iterable

### `iterable/comparable_iterable_extensions.dart`

`import 'package:saropa_dart_utils/iterable/comparable_iterable_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `ComparableIterableExtensions` on `Iterable<T>` | extension | Min/max lookups for iterables whose elements implement [Comparable]. |
| `smallestOccurrence` | method | Finds the smallest occurrence in the list. |
| `biggestOccurrence` | method | Finds the biggest occurrence in the list. |

### `iterable/iterable_cartesian_extensions.dart`

`import 'package:saropa_dart_utils/iterable/iterable_cartesian_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `IterableCartesianExtensions` on `Iterable<T>` | extension | Cartesian product of two iterables. |
| `cartesian` | method | Cartesian product: all pairs (a, b) for a in this, b in [other]. |

### `iterable/iterable_diff_extensions.dart`

Diff two lists: added, removed, unchanged.

`import 'package:saropa_dart_utils/iterable/iterable_diff_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `IterableDiffExtensions` on `Iterable<T>` | extension | Diff two lists: added, removed, unchanged. |
| `diff` | method | Returns (added, removed, unchanged) relative to [other]. |

### `iterable/iterable_extensions.dart`

`import 'package:saropa_dart_utils/iterable/iterable_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `ElementPredicate` | typedef | A function that tests whether an element of type [T] satisfies a condition. |
| `GeneralIterableExtensions` on `Iterable<T>` | extension | General-purpose aggregation, windowing, and frequency helpers for iterables. |
| `mostOccurrences` | method | Finds the most common value in the list. |
| `leastOccurrences` | method | Returns an [Occurrence] of the least common value and its frequency, or `null` if the iterable is empty. |
| `randomElement` | method | Returns a random element from this iterable. |
| `containsAll` | method | Returns true if this iterable contains all elements from [other]. |
| `countWhere` | method | Returns the number of elements that satisfy the given [predicate]. |
| `chunks` | method | Splits this iterable into chunks of size [size]. |
| `partition` | method | Partitions elements into two lists: those that satisfy [predicate] and those that do not. |
| `groupBy` | method | Groups elements by the key produced by [keyOf]. |
| `slidingWindow` | method | Sliding windows of size [windowSize]. |
| `distinctBy` | method | Distinct elements by key; keeps first occurrence of each key. |
| `sortBy` | method | Sorts by [keyOf] and returns a new list. |
| `zipWithIndex` | method | Zip with index: [(0, e0), (1, e1), ...]. |
| `takeEveryNth` | method | Takes every [n]-th element (1-based: first, then 1+n, 1+2n, ...). |
| `skipEveryNth` | method | Skips every [n]-th element. |
| `dedupeConsecutive` | method | Removes consecutive duplicate elements (keeps first of each run). |

### `iterable/iterable_first_last_extensions.dart`

`import 'package:saropa_dart_utils/iterable/iterable_first_last_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `IterableFirstLastWhereExtensions` on `Iterable<T>` | extension | First/last where with default. |
| `firstWhereOrElse` | method | First element satisfying [predicate], or [orElse] if none. |
| `lastWhereOrElse` | method | Last element satisfying [predicate], or [orElse] if none. |

### `iterable/iterable_flatten_deep_extensions.dart`

`import 'package:saropa_dart_utils/iterable/iterable_flatten_deep_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `IterableFlattenDeepExtensions` on `Iterable<dynamic>` | extension | Flatten to depth N or fully deep. |
| `flattenDeep` | method | Flattens recursively to [depth] levels. |

### `iterable/iterable_flatten_extensions.dart`

`import 'package:saropa_dart_utils/iterable/iterable_flatten_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `IterableFlattenExtensions` on `Iterable<Iterable<E>>` | extension | Flatten nested iterables. |
| `flatten` | method | Flattens one level: Iterable<Iterable<E>> → Iterable<E>. |

### `iterable/iterable_group_by_extensions.dart`

`import 'package:saropa_dart_utils/iterable/iterable_group_by_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `IterableGroupByTransformExtensions` on `Iterable<T>` | extension | Group by key with optional value transform. |
| `groupByTransform` | method | Groups by [keyOf] and transforms values with [valueTransform]. |

### `iterable/iterable_indexed_extensions.dart`

`import 'package:saropa_dart_utils/iterable/iterable_indexed_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `IterableIndexedExtensions` on `Iterable<T>` | extension | Indexed map and fold. |
| `mapIndexed` | method | Maps each element with its index: (index, element) -> newValue. |
| `foldIndexed` | method | Fold with index: (prev, index, element) -> next. |

### `iterable/iterable_list_ops_extensions.dart`

`import 'package:saropa_dart_utils/iterable/iterable_list_ops_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `IterableListOpsExtensions` on `Iterable<T>` | extension | List/set operations: difference, intersection, union, interleave. |
| `difference` | method | Elements in this iterable that are not in [other]. |
| `intersection` | method | Elements that appear in both this and [other]. |
| `union` | method | Distinct union of this and [other] (all elements, no duplicates). |
| `interleave` | method | Interleaves this with [other]: [a0, b0, a1, b1, ...]. |

### `iterable/iterable_map_not_null_extensions.dart`

`import 'package:saropa_dart_utils/iterable/iterable_map_not_null_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `IterableMapNotNullExtensions` on `Iterable<T>` | extension | Map-and-filter-nulls in a single pass. |
| `mapNotNull` | method | Applies [selector] to each element and yields only the non-null results. |
| `IterableWhereNotNullExtensions` on `Iterable<T?>` | extension | Drops `null` entries from an iterable of nullable elements. |
| `whereNotNull` | method | Returns the non-null elements as an `Iterable<T>`. |

### `iterable/iterable_min_max_extensions.dart`

Min/max by key (return element).

`import 'package:saropa_dart_utils/iterable/iterable_min_max_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `IterableMinMaxByExtensions` on `Iterable<T>` | extension | Min/max by key (return element). |
| `minBy` | method | Element with minimum [keyOf] value; null if empty. |
| `maxBy` | method | Element with maximum [keyOf] value; null if empty. |

### `iterable/iterable_more_extensions.dart`

`import 'package:saropa_dart_utils/iterable/iterable_more_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `IterableTakeDropLast` on `Iterable<T>` | extension | Collections More: take/drop last, replace first/all, cycle, pad, unzip, segment, consecutive pairs, etc. |
| `takeLast` | method | Last [n] elements; all if n >= length. |
| `dropLast` | method | All but the last [n] elements. |
| `IterableReplace` on `List<T>` | extension | Replace first or all occurrences in a list. |
| `replaceFirst` | method | New list with first occurrence of [value] replaced by [replacement]. |
| `replaceAllValues` | method | New list with every [value] replaced by [replacement]. |
| `IterableCycle` on `List<T>` | extension | Infinite cycle over list elements. |
| `cycle` | method | Lazy infinite iterable repeating this list. |
| `IterablePad` on `List<T>` | extension | Pad list to a minimum length. |
| `padTo` | method | New list padded with [fill] to at least [length] elements. |
| `unzip2` | function | Splits [pairs] into two lists: first elements and second elements. |
| `IterableSegment` on `Iterable<T>` | extension | Segment iterable into lists where consecutive pairs satisfy [predicate]. |
| `segmentBy` | method | When [predicate](prev, next) is false, starts a new segment. |
| `IterableConsecutive` on `List<T>` | extension | Consecutive pairs from a list. |
| `consecutivePairs` | method | Adjacent pairs of elements; empty if length < 2. |
| `IterableArgMinMax` on `Iterable<T>` | extension | Index of min/max by a comparable key. |
| `argMinBy` | method | Index of the element with minimum [keyOf]; null if empty. |
| `argMaxBy` | method | Index of the element with maximum [keyOf]; null if empty. |
| `IterableAllEqual` on `Iterable<T>` | extension | Check if all elements are equal. |
| `allEqual` | getter | True if empty or all elements are equal. |
| `IterableCountBy` on `Iterable<T>` | extension | Count occurrences of each element. |
| `countBy` | method | Map of element to occurrence count. |
| `IterableScan` on `Iterable<T>` | extension | Prefix scan (running reduce). |
| `scan` | method | List of [initial] and each step of [combine](acc, element). |

### `iterable/iterable_none_extensions.dart`

`import 'package:saropa_dart_utils/iterable/iterable_none_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `IterableNoneExtensions` on `Iterable<T>` | extension | The boolean complement of [Iterable.any]. |
| `none` | method | Returns `true` if no element satisfies [predicate]. |

### `iterable/iterable_pairs_extensions.dart`

`import 'package:saropa_dart_utils/iterable/iterable_pairs_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `IterablePairsExtensions` on `Iterable<T>` | extension | All pairs (i, j) with i < j. |
| `allPairs` | method | All unordered pairs [(a, b)] where a appears before b in this iterable. |

### `iterable/iterable_sort_extensions.dart`

`import 'package:saropa_dart_utils/iterable/iterable_sort_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `IterableSortByExtensions` on `Iterable<T>` | extension | Sort by multiple keys (thenBy). |
| `sortByThenBy` | method | Sorts by [keyOf] then by [thenBy] if present. |

### `iterable/iterable_split_extensions.dart`

`import 'package:saropa_dart_utils/iterable/iterable_split_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `IterableSplitExtensions` on `Iterable<T>` | extension | Split at index or at first element matching predicate. |
| `splitAt` | method | Splits into two lists at [index]. |
| `splitAtFirstWhere` | method | Splits at the first element where [predicate] is true. |

### `iterable/iterable_stable_sort_extensions.dart`

`import 'package:saropa_dart_utils/iterable/iterable_stable_sort_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `IterableStableSortExtensions` on `Iterable<T>` | extension | Stable sorting that preserves the input order of equal elements. |
| `stableSortBy` | method | Returns a new list sorted by [keyOf], keeping equal-key elements in their original relative order. |
| `stableSort` | method | Returns a new list sorted by [compare], keeping elements that compare equal in their original relative order. |

### `iterable/iterable_sum_by_extensions.dart`

`import 'package:saropa_dart_utils/iterable/iterable_sum_by_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `IterableSumByExtensions` on `Iterable<T>` | extension | Numeric aggregation by a selector function. |
| `sumBy` | method | Sums [selector] applied to each element. |
| `averageBy` | method | Mean of [selector] applied to each element, or `null` if the iterable is empty. |

### `iterable/iterable_symmetric_diff_extensions.dart`

`import 'package:saropa_dart_utils/iterable/iterable_symmetric_diff_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `IterableSymmetricDifferenceExtensions` on `Iterable<T>` | extension | Symmetric difference (elements in A or B but not both). |
| `symmetricDifference` | method | Symmetric difference: elements in this or [other] but not in both. |

### `iterable/occurrence.dart`

`import 'package:saropa_dart_utils/iterable/occurrence.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `Occurrence` | class | Represents a value and its frequency count within a collection. |
| `Occurrence` | constructor | Creates an [Occurrence] with the given [value] and [count]. |
| `value` | field | The value that was found in the collection. |
| `count` | field | How many times [value] appears in the collection. |
| `operator ==` | operator |  |
| `hashCode` | getter |  |
| `toString` | method |  |

### `iterable/run_length_utils.dart`

`import 'package:saropa_dart_utils/iterable/run_length_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `RunLengthEncodeExtensions` on `Iterable<T>` | extension | Run-length encode/decode for iterables. |
| `runLengthEncode` | method | Run-length encodes this iterable into (value, count) pairs. |
| `runLengthDecode` | function | Run-length decode: expand (value, count) pairs into a list. |

---

## JSON

### `json/json_epoch_scale.dart`

Scale for epoch timestamp parsing.

`import 'package:saropa_dart_utils/json/json_epoch_scale.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `JsonEpochScale` | enum | Scale for epoch timestamp parsing. |
| `seconds` | enum value | Seconds since Unix epoch. |
| `milliseconds` | enum value | Milliseconds since Unix epoch. |
| `microseconds` | enum value | Microseconds since Unix epoch. |

### `json/json_iterables_utils.dart`

`import 'package:saropa_dart_utils/json/json_iterables_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `JsonIterablesUtils` | class | Utility class for JSON encoding of iterables. |
| `jsonEncode` | method | Returns the JSON-encoded string representation of [iterable]. |

### `json/json_type_utils.dart`

`import 'package:saropa_dart_utils/json/json_type_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `JsonTypeUtils` | class | Utility class for converting JSON-decoded values to specific Dart types. |
| `toListMap` | method | Returns a list of maps extracted from [valueList], or `null` if empty or `null`. |
| `countIterableJson` | method | Returns the item count from [json], which may be an iterable or a [separator]-delimited string. |
| `toStringListJson` | method | Returns a list of strings parsed from [json], splitting on [separator] if the value is a string, or `null` if conversion is not possible. |
| `toIntListJson` | method | Returns a list of integers parsed from [json], or `null` if conversion is not possible. |
| `toIntJson` | method | Returns an integer parsed from [json], or `null` if conversion is not possible. |
| `toBoolJson` | method | Returns a boolean parsed from [json], using [isCaseSensitive] to control string comparison, or `null` if conversion is not possible. |
| `toDoubleJson` | method | Returns a double parsed from [json], or `null` if conversion is not possible. |
| `toDoubleListJson` | method | Returns a list of doubles parsed from [json], or `null` if conversion is not possible. |
| `toStringJson` | method | Returns a string from [json] with optional transformations controlled by [shouldTrim], [shouldUppercase], [shouldLowercase], and [shouldCapitalize], or `null... |
| `toDateTimeJson` | method | Returns a [DateTime] parsed from [json], or `null` if conversion is not possible. |
| `toDateTimeEpochJson` | method | Returns a [DateTime] from the epoch timestamp [json] interpreted at the given [scale], or `null` if [json] is not a valid integer. |
| `toListDynamic` | method | Returns [value] as a list if it is one, or `null` otherwise. |

### `json/json_utils.dart`

`import 'package:saropa_dart_utils/json/json_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `JsonUtils` | class | Utility class for JSON parsing and validation. |
| `jsonDecodeToMap` | method | Returns a decoded `Map` from the given [jsonString], or `null` if decoding fails. |
| `jsonDecodeSafe` | method | Safely decodes a JSON string, returning null on error. |
| `isJson` | method | Returns `true` if [value] appears to be valid JSON. |
| `cleanJsonResponse` | method | Returns a cleaned version of the JSON response [value] by stripping outer double-quotes and unescaping any inner escaped quotes (`\"`), or `null` if the resu... |
| `tryJsonDecode` | method | Returns a decoded `Map` from the JSON string [value], optionally cleaning the input first when [shouldCleanInput] is `true`, or `null` if decoding fails. |
| `tryJsonDecodeListMap` | method | Returns a list of maps decoded from the JSON string [value], or `null` if decoding fails. |
| `tryJsonDecodeList` | method | Returns a list of strings decoded from the JSON string [value], or `null` if decoding fails. |

---

## List

### `list/list_binary_search_extensions.dart`

`import 'package:saropa_dart_utils/list/list_binary_search_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `ListBinarySearchExtensions` on `List<T>` | extension | Binary search and merge sorted on lists. |
| `binarySearchIndex` | method | Returns the index of [value] in this sorted list, or -1 if not found. |
| `binarySearchInsertPoint` | method | Returns the insertion point for [value] (index where it would be inserted to preserve order). |
| `mergeSorted` | function | Merge two sorted lists into one sorted list. |

### `list/list_default_empty_extensions.dart`

`import 'package:saropa_dart_utils/list/list_default_empty_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `ListDefaultEmptyExtension` on `List<T>?` | extension | Default empty list/map for null. |
| `orEmpty` | method | This list, or an empty list if null. |
| `MapDefaultEmptyExtension` on `Map<K, V>?` | extension | Default empty map for null. |
| `orEmpty` | method | This map, or an empty map if null. |
| `ListSecondThirdExtension` on `List<T>` | extension | Second/third element or null. |
| `secondOrNull` | getter | Element at index 1, or null if length < 2. |
| `thirdOrNull` | getter | Element at index 2, or null if length < 3. |

### `list/list_extensions.dart`

`import 'package:saropa_dart_utils/list/list_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `ListExtensions` on `List<T>` | extension | Extension methods for [List]. |
| `equalsIgnoringOrder` | method | Compares this list to [other], ignoring the order of elements. |
| `topOccurrence` | method | Returns the element that occurs most frequently in the list. |
| `addNotNull` | method | Adds a value to the list only if it is not null. |
| `limit` | method | Returns a new list containing the first `count` elements of this list. |
| `lastOrNull` | getter | Returns the last element in the list, or `null` if the list is empty. |
| `itemAt` | method | Returns the element at the specified index in the list, or `null` if the index is out of bounds or null. |
| `nullIfEmpty` | method | Returns `null` if the list is empty, otherwise returns the list itself. |
| `takeSafe` | method | Returns a new list containing at most the first [count] elements of this list, in a safe manner. |
| `exclude` | method | Returns a new list containing elements from this list, excluding any elements that are present in the `items` list. |
| `containsAny` | method | Checks if this list contains any element that is also present in the `inThis` list. |

### `list/list_lower_extensions.dart`

`import 'package:saropa_dart_utils/list/list_lower_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `ListLowerExtensions` on `List<T>` | extension | List ops: swap, reverse copy, insert/replace at index, safe get, default if empty. |
| `swapAt` | method | New list with elements at [i] and [j] swapped; in range only. |
| `reversedCopy` | method | New list with elements in reverse order. |
| `insertAt` | method | New list with [value] inserted at [index] (clamped to 0..length). |
| `replaceAt` | method | New list with element at [index] replaced by [value]; no-op if index out of range. |
| `getOrNull` | method | Element at [index], or null if out of range. |
| `orDefault` | method | This list, or [defaultValue] if empty. |
| `firstOrCompute` | method | First element, or result of [compute] if empty. |
| `ListSingleExtension` on `List<T>` | extension | Single element or null. |
| `singleOrNull` | getter | The only element if length == 1, otherwise null. |
| `IterableToSetExtension` on `Iterable<T>` | extension | Convert iterable to set. |
| `toSetFrom` | method | Set of distinct elements. |
| `IterableToListExtension` on `Iterable<T>` | extension | Convert iterable to list. |
| `toListFrom` | method | New list containing all elements. |

### `list/list_nullable_extensions.dart`

`import 'package:saropa_dart_utils/list/list_nullable_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `ListExtensionsNullable` on `List<T>?` | extension | Extension methods for nullable [List<T>?]. |
| `isListNullOrEmpty` | getter | Checks if the nullable list is either `null` or empty. |
| `isNotListNullOrEmpty` | getter | Checks if the nullable list is neither `null` nor empty. |

### `list/list_of_list_extensions.dart`

`import 'package:saropa_dart_utils/list/list_of_list_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `ListOfListExtension` on `List<List<T>>` | extension | Utilities for nested lists (matrices): totals, flattening, copying, and matrix-string rendering. |
| `totalLength` | getter | sum the total number of items |
| `totalUniqueLength` | getter | Getting the length of [toFlattenedList] |
| `toFlattenedList` | method | Extension method that returns a flattened list of unique elements of type T. |
| `getChildListLengths` | method | Returns a list containing the lengths of the child lists in parentList. |
| `copy` | method | Returns `true` after copying the elements of this list into [destination], or `false` if the dimensions do not match. |
| `clone` | method | Returns a new list that is an exact duplicate of this list. |
| `toMatrixString` | method | Converts a two-dimensional matrix into a string with comma-separated values and line breaks between rows. |

### `list/list_rotate_extensions.dart`

`import 'package:saropa_dart_utils/list/list_rotate_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `ListRotateExtensions` on `List<T>` | extension | Rotate list left or right. |
| `rotate` | method | Returns a new list rotated left by [n] (positive) or right by -[n]. |

### `list/list_shuffle_seeded_extensions.dart`

`import 'package:saropa_dart_utils/list/list_shuffle_seeded_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `ListShuffleSeededExtensions` on `List<T>` | extension | Shuffle with seed (reproducible). |
| `shuffleWithSeed` | method | Returns a new list with elements shuffled using [seed]. |

### `list/list_string_extensions.dart`

`import 'package:saropa_dart_utils/list/list_string_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `ListStringExtensions` on `List<String>` | extension | Extensions on List<String> for common prefix/suffix. |
| `commonPrefix` | method | Returns the longest common prefix of all strings in this list. |
| `commonSuffix` | method | Returns the longest common suffix of all strings in this list. |

### `list/list_top_k_extensions.dart`

`import 'package:saropa_dart_utils/list/list_top_k_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `ListTopKExtensions` on `List<T>` | extension | Top K elements (partial sort). |
| `topK` | method | Returns the [k] smallest elements by [compare]. |

### `list/make_list_extensions.dart`

`import 'package:saropa_dart_utils/list/make_list_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `MakeListExtensions` on `T?` | extension | This is an extension on the nullable generic type T? |
| `toListIfNotNull` | method | This method converts an object to a list if it's not null. |

### `list/unique_list_extensions.dart`

`import 'package:saropa_dart_utils/list/unique_list_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `KeyExtractor` | typedef | Extracts a key of type [E] from an element of type [T]. |
| `UniqueIterableExtensions` on `Iterable<T>` | extension | Deduplication helpers for any iterable, preserving encounter order. |
| `toUnique` | method | Returns a new list with duplicate elements removed, preserving order. |
| `UniqueListExtensionsUniqueBy` on `List<T>` | extension | Deduplication helpers that compare elements by an extracted key. |
| `toUniqueBy` | method | Returns a new list with unique elements based on the provided [keyExtractor] function. |
| `toUniqueByInPlace` | method | Modifies the original list to contain only unique elements based on the provided [keyExtractor] function. |

---

## Map

### `map/deep_equality_utils.dart`

Deep equality for nested maps and lists.

`import 'package:saropa_dart_utils/map/deep_equality_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `deepEquals` | function | Deep equality: compares maps and lists recursively; primitives by value. |

### `map/deep_freeze_utils.dart`

Deep freeze: recursively unmodifiable views of maps, lists, and sets.

`import 'package:saropa_dart_utils/map/deep_freeze_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `deepFreeze` | function | Returns a deeply-unmodifiable copy of [value]. |

### `map/map_deep_merge_extensions.dart`

`import 'package:saropa_dart_utils/map/map_deep_merge_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `MapDeepMergeExtensions` on `Map<String, dynamic>` | extension | Deep merge: nested maps are merged recursively; later source overwrites. |
| `deepMerge` | method | Merges [other] into this map recursively. |

### `map/map_deep_utils.dart`

Deep copy for maps and lists.

`import 'package:saropa_dart_utils/map/map_deep_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `deepCopyMap` | function | Deep copy for maps and lists. |
| `deepCopyList` | function | Recursively deep-copies [source], cloning any nested maps and lists so the result shares no mutable structure with the original. |

### `map/map_default_extensions.dart`

`import 'package:saropa_dart_utils/map/map_default_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `MapDefaultExtensions` | class | Default map: returns a default value for missing keys. |
| `MapDefaultExtensions` | constructor | Creates a default map with the wrapped map and [defaultValue]. |
| `defaultValue` | field | Value returned for missing keys. |
| `operator []` | operator |  |
| `operator []=` | operator |  |
| `clear` | method |  |
| `keys` | getter |  |
| `remove` | method |  |
| `values` | getter |  |
| `addAll` | method |  |
| `addEntries` | method |  |
| `map` | method |  |
| `removeWhere` | method |  |
| `putIfAbsent` | method |  |
| `forEach` | method |  |
| `entries` | getter |  |
| `length` | getter |  |
| `isEmpty` | getter |  |
| `isNotEmpty` | getter |  |
| `containsKey` | method |  |
| `containsValue` | method |  |
| `cast` | method |  |
| `update` | method |  |
| `updateAll` | method |  |
| `toString` | method |  |
| `MapWithDefaultExt` on `Map<K, V>` | extension | Extension to wrap a map with a default value for missing keys. |
| `withDefault` | method | Returns a [MapDefaultExtensions] view that returns [defaultValue] for missing keys. |

### `map/map_diff_utils.dart`

Map diff: keys added, removed, changed.

`import 'package:saropa_dart_utils/map/map_diff_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `mapDiff` | function | Map diff: keys added, removed, changed. |

### `map/map_extensions.dart`

`import 'package:saropa_dart_utils/map/map_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `MapExt` on `Map<K, V>` | extension | Extension methods for generic maps. |
| `nullIfEmpty` | method | Returns this map, or `null` if it is empty. |
| `getRandomListExcept` | method | Returns up to [count] random entries from this map, excluding any keys present in [ignoreList]. |
| `StringMapExtensions` on `Map<String, dynamic>` | extension | Extension methods for String-keyed dynamic maps. |
| `formatMap` | method | Returns a human-readable string representation of this map with indentation. |
| `getChildString` | method | Returns the value for [childKey] as a `String`, or `null` if the key is missing or the value is not a `String`. |
| `getGrandchild` | method | Returns the nested value at [childKey] → [grandChildKey], or `null` if either key is missing or the child is not a map. |
| `getGrandchildString` | method | Returns the nested value at [childKey] → [grandChildKey] as a `String`, or `null` if either key is missing or the value is not a `String`. |
| `getGreatGrandchild` | method | Returns the nested value at [childKey] → [grandChildKey] → [greatGrandChildKey], or `null` if any key is missing. |
| `getGreatGrandchildString` | method | Returns the nested value at [childKey] → [grandChildKey] → [greatGrandChildKey] as a `String`, or `null` if any key is missing or the value is not a `String`. |
| `getValue` | method | Returns the value for [key] as a `Map<String, dynamic>`, or `null` if the key is missing or the value cannot be converted. |
| `removeKeys` | method | Returns `true` after removing all keys in [removeKeysList] from this map. |
| `toKeySorted` | method | Returns a new `Map<String, dynamic>` with keys sorted alphabetically. |
| `MapExtensions` | class | Utility class for map operations. |
| `countItems` | method | Returns the total number of items across all iterable values in [inputMap]. |
| `mapToggleValue` | method | Toggles [value] in the list at [key] within [map]. |
| `mapAddValue` | method | Adds [value] to the list at [key] within [map], creating the list if absent. |
| `mapRemoveValue` | method | Removes all occurrences of [value] from the list at [key] within [map]. |
| `mapContainsValue` | method | Returns `true` if the list at [key] within [map] contains [value]. |
| `toMapStringDynamic` | method | Returns [json] as a `Map<String, dynamic>`, or `null` if conversion is not possible. |

### `map/map_flatten_extensions.dart`

`import 'package:saropa_dart_utils/map/map_flatten_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `MapFlattenExtensions` on `Map<String, dynamic>` | extension | Flatten/unflatten keys (e.g. |
| `flattenKeys` | method | Flattens nested keys to dot-separated keys. |
| `unflattenKeys` | method | Unflattens dot-separated keys into nested maps. |

### `map/map_from_entries_extensions.dart`

`import 'package:saropa_dart_utils/map/map_from_entries_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `MapFromEntriesExtensions` on `Map<K, V>` | extension | Map from list of entries / entries to list. |
| `toEntriesList` | method | Returns entries as list of pairs. |
| `mapFromEntries` | function | Build map from iterable of (key, value) pairs. |

### `map/map_invert_extensions.dart`

`import 'package:saropa_dart_utils/map/map_invert_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `MapInvertExtensions` on `Map<K, V>` | extension | Invert map (K->V to V->K). |
| `invert` | method | Inverts keys and values. |

### `map/map_merge_extensions.dart`

`import 'package:saropa_dart_utils/map/map_merge_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `MapMergeListExtensions` on `List<Map<K, V>>` | extension | Merge list of maps. |
| `mergeAll` | method | Merges all maps; later maps overwrite earlier for same key. |

### `map/map_more_extensions.dart`

`import 'package:saropa_dart_utils/map/map_more_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `MapFromIterableExtension` on `Iterable<T>` | extension | Map More: from iterable, find key by value, rename key, ensure key, etc. |
| `toMapWith` | method | Builds a map from [keyOf] and [valueOf] for each element; later elements overwrite. |
| `MapKeysValuesList` on `Map<K, V>` | extension | Keys/values as lists. |
| `keysList` | getter | All keys in iteration order. |
| `valuesList` | getter | All values in iteration order. |
| `MapFindKey` on `Map<K, V>` | extension | Find key for a value. |
| `findKeyByValue` | method | First key that maps to [value], or null. |
| `MapRenameKey` on `Map<K, V>` | extension | Rename keys in a copy. |
| `renameKey` | method | New map with [oldKey] renamed to [newKey]; unchanged if oldKey absent. |
| `renameKeys` | method | New map with keys renamed according to [oldToNew]. |
| `MapEnsureKey` on `Map<K, V>` | extension | Ensure a key exists. |
| `ensureKey` | method | If [key] is absent, sets this[key] = ifAbsent(). |
| `MapUpsert` on `Map<K, V>` | extension | Insert or update by key. |
| `upsert` | method | If [key] absent: this[key] = insert(); else this[key] = update(this[key]). |

### `map/map_nested_extensions.dart`

`import 'package:saropa_dart_utils/map/map_nested_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `getNested` | function | Get/set nested value by path (list of keys). |
| `setNested` | function | Sets [value] at the nested location addressed by [path] in [map], creating intermediate maps as needed. |

### `map/map_nullable_extensions.dart`

`import 'package:saropa_dart_utils/map/map_nullable_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `MapNullableExtensions` on `Map<dynamic, dynamic>?` | extension | Extension for nullable maps. |
| `isMapNullOrEmpty` | getter | Returns true if this map is null or empty. |
| `isNotMapNullOrEmpty` | getter | Returns true if this map is not null and not empty. |

### `map/map_pick_omit_extensions.dart`

`import 'package:saropa_dart_utils/map/map_pick_omit_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `MapPickOmitExtensions` on `Map<K, V>` | extension | Pick and omit keys from maps. |
| `pick` | method | Returns a new map containing only entries whose keys are in [keys]. |
| `omit` | method | Returns a new map containing all entries whose keys are not in [keys]. |

### `map/map_transform_extensions.dart`

`import 'package:saropa_dart_utils/map/map_transform_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `MapTransformExtensions` on `Map<K, V>` | extension | Map values, map keys, filter by key/value. |
| `mapValues` | method | New map with same keys, values transformed by [f]. |
| `mapKeys` | method | New map with same values, keys transformed by [f]. |
| `filterKeys` | method | New map with only entries where [keyPredicate](key) is true. |
| `filterValues` | method | New map with only entries where [valuePredicate](value) is true. |

---

## Niche

### `niche/checksum_utils.dart`

Simple additive checksum for integrity.

`import 'package:saropa_dart_utils/niche/checksum_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `additiveChecksum` | function | Simple additive checksum for integrity. |

### `niche/color_utils.dart`

`import 'package:saropa_dart_utils/niche/color_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `hexToRgb` | function | Color hex to RGB. |
| `rgbToHex` | function | Packs [r], [g], and [b] channels into a fully opaque `0xAARRGGBB` color int. |
| `luminance` | function | Returns the relative luminance of an sRGB color per the WCAG 2.x formula. |
| `contrastRatio` | function | Returns the WCAG contrast ratio between two sRGB colors. |

### `niche/hash_utils.dart`

Simple hash for cache key from objects.

`import 'package:saropa_dart_utils/niche/hash_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `simpleHash` | function | Simple hash for cache key from objects. |

### `niche/name_utils.dart`

`import 'package:saropa_dart_utils/niche/name_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `abbreviateName` | function | Abbreviate name, initials from name. |
| `initialsFromName` | function | Returns the uppercase initials for [name]. |

### `niche/natural_sort_utils.dart`

`import 'package:saropa_dart_utils/niche/natural_sort_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `naturalCompare` | function | Sort strings naturally (human: a2 before a10). |
| `NaturalSortExtension` on `List<String>` | extension | Adds natural (human-friendly) sorting to a `List<String>`. |
| `sortedNatural` | method | Returns a new list sorted naturally so `a2` comes before `a10`. |

### `niche/niche_more_utils.dart`

`import 'package:saropa_dart_utils/niche/niche_more_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `hexDump` | function | Renders [bytes] as a classic hex dump: offset, hex columns, and ASCII gutter. |
| `parseHexToBytes` | function | Parses a [hex] string into its byte values. |
| `bytesToHex` | function | Returns the lowercase, zero-padded hex string for [bytes], with no separators. |
| `maskCreditCard` | function | Masks all but the last [visibleLast] digits of [digits] with [maskChar]. |
| `stripControlChars` | function | Removes ASCII control characters (`0x00`–`0x1F` and `0x7F`) from [s]. |
| `isAsciiOnly` | function | Returns `true` if every code unit in [s] is within the ASCII range (`< 128`). |
| `truncateToByteLength` | function | Truncates [s] so its UTF-8 encoding does not exceed [maxBytes] bytes. |

### `niche/pad_format_utils.dart`

Pad number with leading zeros.

`import 'package:saropa_dart_utils/niche/pad_format_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `padWithZeros` | function | Returns [value] as a string left-padded with zeros to at least [length] chars. |
| `formatFileSize` | function | Formats [bytes] as a human-readable size using binary (1024) units up to TB. |

### `niche/random_string_utils.dart`

`import 'package:saropa_dart_utils/niche/random_string_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `randomAlphanumeric` | function | Returns a random alphanumeric string of [length] characters. |

### `niche/string_diff_utils.dart`

Diff two strings (line-by-line).

`import 'package:saropa_dart_utils/niche/string_diff_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `stringDiffLines` | function | Diff two strings (line-by-line). |

---

## Number

### `num/math_utils.dart`

GCD, LCM, and related integer math.

`import 'package:saropa_dart_utils/num/math_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `gcd` | function | Greatest common divisor of [a] and [b]. |
| `lcm` | function | Least common multiple. |

### `num/num_clamp_extensions.dart`

`import 'package:saropa_dart_utils/num/num_clamp_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `NumClampToIntExtensions` on `num` | extension | Clamp to int (round then clamp). |
| `clampToInt` | method | Rounds to nearest int then clamps to [min]..[max]. |

### `num/num_compact_parse_extensions.dart`

Parse compact number ("1.2K" → 1200).

`import 'package:saropa_dart_utils/num/num_compact_parse_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `parseCompactNumber` | function | Parse compact number ("1.2K" → 1200). |

### `num/num_extensions.dart`

`import 'package:saropa_dart_utils/num/num_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `NumberExtensions` on `num` | extension | Extension methods for `num` covering sign checks and string length. |
| `isNotZeroOrNegative` | getter | Returns `true` if the number is not zero and not negative. |
| `isZeroOrNegative` | getter | Returns `true` if the number is zero or negative. |
| `length` | method | Returns the number of characters in the string representation of this number. |
| `NumberNullableExtensions` on `num?` | extension | Extension methods for nullable `num?`. |
| `isNotNullZeroOrNegative` | getter | Returns `true` if the nullable number is not null, not zero, and not negative. |
| `isNullZeroOrNegative` | getter | Returns `true` if the nullable number is null, zero, or negative. |
| `isNullOrZero` | getter | Returns `true` if the nullable number is null or zero. |
| `isNotNullOrZero` | getter | Returns `true` if the nullable number is not null and not zero. |
| `isGreaterThanZero` | getter | Returns `true` if the nullable number is not null and greater than zero. |
| `isGreaterThanOne` | getter | Returns `true` if the nullable number is not null and greater than one. |
| `toDoubleOrNull` | method | Converts this nullable num to a double, or returns null if null. |
| `toIntOrNull` | method | Converts this nullable num to an int, or returns null if null. |

### `num/num_factorial_utils.dart`

Factorial with overflow guard.

`import 'package:saropa_dart_utils/num/num_factorial_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `factorial` | function | Factorial with overflow guard. |

### `num/num_format_extensions.dart`

`import 'package:saropa_dart_utils/num/num_format_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `NumFormatExtensions` on `num` | extension | Format numbers: significant digits, compact (1.2K). |
| `roundToSignificantDigits` | method | Rounds to [significantDigits] significant digits. |
| `toCompactString` | method | Formats as compact string (e.g. |

### `num/num_iterable_extensions.dart`

Sum, count, average for iterable of num.

`import 'package:saropa_dart_utils/num/num_iterable_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `NumIterableSumExtensions` on `Iterable<num>` | extension | Sum, count, average for iterable of num. |
| `sum` | getter | Sum of elements. |
| `count` | getter | Count of elements. |
| `average` | getter | Average (mean). |

### `num/num_lerp_utils.dart`

Lerp, inverse lerp, map value from one range to another.

`import 'package:saropa_dart_utils/num/num_lerp_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `lerp` | function | Lerp, inverse lerp, map value from one range to another. |
| `inverseLerp` | function | Returns where [value] falls between [a] and [b] as a fraction in `0.0`–`1.0`. |
| `mapRange` | function | Remaps [value] from the input range `[fromMin, fromMax]` to the output range `[toMin, toMax]`. |

### `num/num_locale_utils.dart`

`import 'package:saropa_dart_utils/num/num_locale_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `parseNumberLocale` | function | Parse/format number with locale (thousands sep, decimals). |
| `formatNumberLocale` | function | Formats number with [decimalPlaces] and optional [groupSep] (e.g. |

### `num/num_min_max_utils.dart`

Min/max of two or N numbers.

`import 'package:saropa_dart_utils/num/num_min_max_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `minOf` | function | Min/max of two or N numbers. |
| `maxOf` | function | Max of two numbers (NumUtils has maxOf with null handling). |
| `minOfMany` | function | Min of iterable; null if empty. |
| `maxOfMany` | function | Max of iterable; null if empty. |

### `num/num_modulo_utils.dart`

`import 'package:saropa_dart_utils/num/num_modulo_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `modulo` | function | Modulo that handles negative (e.g. |

### `num/num_more_extensions.dart`

`import 'package:saropa_dart_utils/num/num_more_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `NumMoreExtensions` on `num` | extension | Number More: clamp non-negative, isInteger, round half up, truncate decimals, percentage, degrees/radians, etc. |
| `clampNonNegative` | method | Returns this number rounded to the nearest int, with negatives clamped to `0`. |
| `isInteger` | getter | Returns `true` if this number has no fractional part. |
| `truncateToDecimals` | method | Truncates this number to [places] decimal places without rounding. |
| `percentageChangeFrom` | method | Returns the fractional change from [from] to this value. |
| `percentageOf` | method | Returns this value as a fraction of [total]. |
| `degreesToRadians` | function | Converts an angle in degrees ([deg]) to radians. |
| `radiansToDegrees` | function | Converts an angle in radians ([rad]) to degrees. |
| `normalizeAngle360` | function | Normalizes [degrees] into the range `[0, 360)`. |
| `normalizeAngle180` | function | Normalizes [degrees] into the range `(-180, 180]`. |
| `digitSum` | function | Returns the sum of the decimal digits of [n]. |
| `isPowerOfTwo` | function | Returns `true` if [n] is a positive power of two (`1, 2, 4, 8, …`). |
| `nextPowerOfTwo` | function | Returns the smallest power of two greater than or equal to [n]. |
| `isqrt` | function | Returns the integer square root of [n] (the floor of its real square root). |

### `num/num_prime_utils.dart`

Is prime (small numbers), prime factors.

`import 'package:saropa_dart_utils/num/num_prime_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `isPrime` | function | Is prime (small numbers), prime factors. |
| `primeFactors` | function | Prime factors (small numbers). |

### `num/num_range_extensions.dart`

`import 'package:saropa_dart_utils/num/num_range_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `NumRangeExtensions` on `num` | extension | Extension methods for `num` (and its subtypes like `int`, `double`). |
| `isInRange` | method | Returns true if the number is between [min] and [max] inclusive. |
| `forceInRange` | method | Returns the [min] if the number is less than [min], [max] if greater than [max], or the number itself otherwise. |
| `isNotBetween` | method | Returns `true` if this number is NOT between [from] and [to] inclusive. |
| `isBetween` | method | Returns `true` if this number is between [from] and [to] inclusive. |

### `num/num_range_inclusive_extensions.dart`

`import 'package:saropa_dart_utils/num/num_range_inclusive_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `NumRangeInclusiveExclusiveExtensions` on `num` | extension | Is in closed/open range. |
| `isInRangeExclusive` | method | True if strictly between [min] and [max] (exclusive). |
| `isInRangeInclusive` | method | True if in [min, max] inclusive (alias for isBetween). |

### `num/num_round_multiple_extensions.dart`

`import 'package:saropa_dart_utils/num/num_round_multiple_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `NumRoundMultipleExtensions` on `num` | extension | Round/floor/ceil to multiple. |
| `roundToMultiple` | method | Rounds to nearest multiple of [multiple]. |
| `floorToMultiple` | method | Floors to multiple of [multiple]. |
| `ceilToMultiple` | method | Ceils to multiple of [multiple]. |

### `num/num_safe_division_extensions.dart`

`import 'package:saropa_dart_utils/num/num_safe_division_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `NumSafeDivisionExtensions` on `num` | extension | Safe division (avoid division by zero). |
| `divideSafe` | method | Divides by [divisor]. |
| `safeDivide` | function | Divides [a] by [b]; returns null if [b] is 0. |

### `num/num_stats_utils.dart`

`import 'package:saropa_dart_utils/num/num_stats_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `variance` | function | Variance, standard deviation, median, percentile. |
| `standardDeviation` | function | Standard deviation (square root of variance). |
| `median` | function | Returns the median of [values], or `null` if the collection is empty. |
| `percentile` | function | Returns the [p]th percentile of [values] using linear interpolation. |

### `num/num_utils.dart`

`import 'package:saropa_dart_utils/num/num_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `NumUtils` | class | Utility class for number-related operations. |
| `maxOf` | method | Returns the maximum of [a] and [b], handling `null` values. |
| `generateIntList` | method | Generates a list of integers in ascending order, starting from [start] and ending at [end] (inclusive). |

---

## Object & Null

### `object/assert_utils.dart`

`import 'package:saropa_dart_utils/object/assert_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `assertThat` | function | Assert with message (only in debug if desired). |

### `object/cast_utils.dart`

Cast or null (safe cast).

`import 'package:saropa_dart_utils/object/cast_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `castOrNull` | function | Cast or null (safe cast). |

### `object/coalesce_utils.dart`

Null coalesce chain (first non-null of list).

`import 'package:saropa_dart_utils/object/coalesce_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `coalesce` | function | Null coalesce chain (first non-null of list). |

### `object/copy_with_defaults_utils.dart`

`import 'package:saropa_dart_utils/object/copy_with_defaults_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `copyWithDefaults` | function | Copy with defaults (merge with default object). |

### `object/default_value_extensions.dart`

`import 'package:saropa_dart_utils/object/default_value_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `DefaultValueExtensions` on `T?` | extension | Default map for null (map null to default value). |
| `orDefault` | method | Returns this value if non-null, otherwise [defaultValue]. |

### `object/identity_utils.dart`

Identity equality (same reference).

`import 'package:saropa_dart_utils/object/identity_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `identityEquals` | function | Identity equality (same reference). |

### `object/nullable_more_extensions.dart`

Null-aware helpers for any nullable receiver `T?`: run a side effect, transform, or supply a fallback only when (or unless) the value is null.

`import 'package:saropa_dart_utils/object/nullable_more_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `NullableWhen` on `T?` | extension | Null-aware helpers for any nullable receiver `T?`: run a side effect, transform, or supply a fallback only when (or unless) the value is null. |
| `whenNonNull` | method | Runs [fn] with the value when it is non-null, then returns the original receiver unchanged (null or not) so calls can be chained. |
| `mapNonNull` | method | Transforms a non-null value with [fn], or returns null when the receiver is null. |
| `orElse` | method | Returns the value when non-null, otherwise the result of [compute]. |
| `TryCast` on `T?` | extension | Non-throwing runtime cast for a nullable receiver. |
| `tryCast` | method | Casts the receiver to [U] when its runtime type matches, otherwise returns null. |
| `isType` | function | Returns whether [value] is an instance of [T] at runtime. |
| `asTypeOr` | function | Returns [value] as a [T] when it is one, otherwise [fallback]. |
| `FirstOfTypeExtension` on `List<Object?>` | extension | Searches a heterogeneous list for the first element of a given type. |
| `firstOfType` | method | Returns the first element that is a [T], or null when no element matches. |
| `ToListOrEmpty` on `T?` | extension | Wraps a nullable value into a list, treating null as empty. |
| `toListOrEmpty` | method | Wraps a non-null value in a single-element list, or returns an empty list when null. |

### `object/pipe_compose_utils.dart`

Pipe (chain unary functions).

`import 'package:saropa_dart_utils/object/pipe_compose_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `pipe` | function | Pipe (chain unary functions). |
| `compose` | function | Returns the mathematical composition `f(g(x))`: applies [g] first, then [f]. |
| `once` | function | Returns a callable that runs [block] only on its first invocation and is a no-op thereafter. |

### `object/pipe_utils.dart`

Also/let style (pipe value through function).

`import 'package:saropa_dart_utils/object/pipe_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `also` | function | Also/let style (pipe value through function). |
| `let` | function | Applies [fn] to [value] and returns its result. |

### `object/require_utils.dart`

`import 'package:saropa_dart_utils/object/require_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `requireNonNull` | function | Require non-null (throw descriptive if null). |

### `object/shallow_copy_utils.dart`

Shallow copy list/map.

`import 'package:saropa_dart_utils/object/shallow_copy_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `shallowCopyList` | function | Shallow copy list/map. |
| `shallowCopyMap` | function | Returns a new map with the same entries as [source]. |

---

## Parsing

### `parsing/accept_language_utils.dart`

Parse an HTTP `Accept-Language` header into ranked language ranges.

`import 'package:saropa_dart_utils/parsing/accept_language_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `LanguageRange` | class | A single weighted language range from an `Accept-Language` header. |
| `LanguageRange` | constructor | Creates a range pairing a [tag] (already lower-cased by the parser) with its [quality] weight in 0.0..1.0. |
| `tag` | field | The language tag, lower-cased (e.g. |
| `quality` | field | The quality weight in 0.0..1.0 (absent `q=` defaults to 1.0). |
| `toString` | method |  |
| `parseAcceptLanguage` | function | Parses [header] into language ranges ordered most-preferred first. |

### `parsing/canonicalize_json_utils.dart`

Canonicalization (sort keys, normalize) for JSON-like data — roadmap #639.

`import 'package:saropa_dart_utils/parsing/canonicalize_json_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `canonicalizeJson` | function | Recursively sorts map keys and normalizes numbers. |

### `parsing/changelog_section_utils.dart`

Changelog/semantic version section parser — roadmap #431.

`import 'package:saropa_dart_utils/parsing/changelog_section_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `parseChangelogSections` | function | Extracts sections like "## [1.0.0] - date" and content until next ##. |

### `parsing/config_precedence_utils.dart`

Config precedence resolver: defaults → overlay (roadmap #644).

`import 'package:saropa_dart_utils/parsing/config_precedence_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `mergeConfig` | function | Merges [defaults] with [overlay]; overlay wins for non-null values. |

### `parsing/cron_utils.dart`

Parse a standard 5-field cron expression and compute its next run time.

`import 'package:saropa_dart_utils/parsing/cron_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `CronSchedule` | class | A parsed 5-field cron expression. |
| `minutes` | field | Allowed minute values (0-59). |
| `hours` | field | Allowed hour values (0-23). |
| `daysOfMonth` | field | Allowed day-of-month values (1-31). |
| `months` | field | Allowed month values (1-12). |
| `daysOfWeek` | field | Allowed day-of-week values (0-6, Sunday=0). |
| `isDayOfMonthRestricted` | field | Whether the day-of-month field was something other than `*`. |
| `isDayOfWeekRestricted` | field | Whether the day-of-week field was something other than `*`. |
| `tryParse` | method | Parses [expression] (5 whitespace-separated fields), or returns `null` if it is malformed or any field is out of range. |
| `nextRunAfter` | method | Returns the first matching time strictly after [from] (at minute resolution), or `null` if none occurs within four years. |

### `parsing/csv_dialect_utils.dart`

CSV/TSV dialect detector (roadmap #435).

`import 'package:saropa_dart_utils/parsing/csv_dialect_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `CsvDialectUtils` | class | Detected dialect: delimiter, hasHeader. |
| `CsvDialectUtils` | constructor | Creates a detected dialect with the field [delimiter] and whether the first row is a header ([hasHeader]). |
| `delimiter` | getter | The detected field delimiter, e.g. |
| `hasHeader` | getter | Whether the first row was treated as a header row. |
| `toString` | method |  |
| `detectCsvDialect` | function | Detects the dialect of a CSV/TSV [sample] by inspecting its first line. |

### `parsing/csv_parse_utils.dart`

Parse one CSV line (handle quoted fields, commas inside quotes).

`import 'package:saropa_dart_utils/parsing/csv_parse_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `parseCsvLine` | function | Parse one CSV line (handle quoted fields, commas inside quotes). |

### `parsing/csv_writer_utils.dart`

CSV writer with configurable dialect and RFC 4180 auto-quoting — roadmap #622.

`import 'package:saropa_dart_utils/parsing/csv_writer_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `writeCsvLine` | function | Encodes one row of [fields] as a single CSV line (no trailing newline). |
| `writeCsv` | function | Encodes [rows] as a full CSV document, joining lines with [eol] (default CRLF, the RFC 4180 line terminator). |

### `parsing/email_validation_utils.dart`

Email validation (reasonable regex, not RFC-perfect).

`import 'package:saropa_dart_utils/parsing/email_validation_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `isValidEmail` | function | Returns `true` if [email] looks like a valid address. |

### `parsing/hex_color_utils.dart`

Parse hex color string (#RGB, #RRGGBB, #AARRGGBB).

`import 'package:saropa_dart_utils/parsing/hex_color_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `parseHexColor` | function | Parse hex color string (#RGB, #RRGGBB, #AARRGGBB). |

### `parsing/ini_parser_utils.dart`

INI / `.env` configuration parser — roadmap #626.

`import 'package:saropa_dart_utils/parsing/ini_parser_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `iniGlobalSection` | field | Section key under which entries appearing before any `[section]` header are collected. |
| `parseIni` | function | Parses [input] as INI text into `section → key → value`. |
| `parseEnv` | function | Parses [input] as a `.env` file into a flat `key → value` map. |

### `parsing/isbn_utils.dart`

ISBN-10/13 validation.

`import 'package:saropa_dart_utils/parsing/isbn_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `isValidIsbn10` | function | ISBN-10/13 validation. |
| `isValidIsbn13` | function | Returns `true` if [isbn] is a valid ISBN-13. |

### `parsing/json_diff_patch_utils.dart`

JSON diff/patch (simple key-level) — roadmap #624.

`import 'package:saropa_dart_utils/parsing/json_diff_patch_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `jsonDiffShallow` | function | Returns keys added, removed, or changed between [a] and [b] (one level). |

### `parsing/json_model_mapper_utils.dart`

Read typed fields from decoded JSON, collecting errors instead of throwing — roadmap #637.

`import 'package:saropa_dart_utils/parsing/json_model_mapper_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `JsonModelReader` | class | Reads typed values from a decoded-JSON object, accumulating a [ValidationErrors] list for missing or wrong-typed fields instead of throwing on the first prob... |
| `JsonModelReader` | constructor | Wraps [source], the decoded JSON object to read. |
| `errors` | field | Accumulated errors for every failed read on this object. |
| `requireString` | method | Required [String]; records an error and returns null if absent/non-string. |
| `requireInt` | method | Required [int]; records an error and returns null if absent/non-int. |
| `requireBool` | method | Required [bool]; records an error and returns null if absent/non-bool. |
| `requireDouble` | method | Required number as [double]; an [int] is widened. |
| `optionalString` | method | Optional [String]: returns [fallback] when the key is absent, but still records an error when the key is present with a non-string value (bad data is a real... |
| `requireList` | method | Required homogeneous list. |
| `child` | method | Required nested object as a child reader sharing this reader's error list, so a nested failure surfaces with a dotted [_at] path on the SAME [errors] collect... |

### `parsing/json_path_utils.dart`

Read a value from decoded JSON by a simple dotted/indexed path.

`import 'package:saropa_dart_utils/parsing/json_path_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `getByJsonPath` | function | Returns the value at [path] within decoded JSON [json] (maps/lists/scalars as produced by `jsonDecode`), or `null` if any segment is missing or the path is m... |

### `parsing/json_pretty_print_utils.dart`

Pretty-print decoded JSON with configurable indentation and key sorting — roadmap #436.

`import 'package:saropa_dart_utils/parsing/json_pretty_print_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `prettyPrintJson` | function | Renders a decoded-JSON [value] (the `Map`/`List`/`num`/`String`/`bool`/null tree returned by `jsonDecode`) as an indented string. |

### `parsing/json_schema_utils.dart`

Declarative schema validation for JSON-like data — roadmap #636.

`import 'package:saropa_dart_utils/parsing/json_schema_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `JsonType` | enum | The JSON value kinds a [FieldSchema] can require. |
| `string` | enum value | A `String`. |
| `integer` | enum value | An `int` specifically (not a fractional number). |
| `number` | enum value | Any `num` (int or double). |
| `boolean` | enum value | A `bool`. |
| `list` | enum value | A `List`. |
| `object` | enum value | A `Map` (nested object). |
| `any` | enum value | Any non-null value (no type constraint). |
| `FieldSchema` | class | The constraints on a single field: its [type], whether it must be present ([isRequired]), and an optional [allowed] set of values (an enum). |
| `FieldSchema` | constructor | Describes a field of [type]. |
| `type` | field | The required value kind. |
| `isRequired` | field | Whether an absent (or null) value is an error. |
| `allowed` | field | When non-null, the value must equal one of these (an enum constraint). |
| `validateJsonSchema` | function | Validates a decoded JSON [object] against [schema], returning a [ValidationErrors] collecting every problem (it never throws). |

### `parsing/luhn_utils.dart`

Luhn check (credit card / ID validation).

`import 'package:saropa_dart_utils/parsing/luhn_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `luhnCheck` | function | Luhn check (credit card / ID validation). |

### `parsing/nested_query_parser_utils.dart`

Query string parser with nested keys (a[b][c]) — roadmap #628.

`import 'package:saropa_dart_utils/parsing/nested_query_parser_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `parseNestedQuery` | function | Parses [queryString] into a nested map. |

### `parsing/parse_bool_utils.dart`

Parse simple boolean ("on"/"off", "1"/"0", "yes"/"no").

`import 'package:saropa_dart_utils/parsing/parse_bool_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `parseBool` | function | Parse simple boolean ("on"/"off", "1"/"0", "yes"/"no"). |

### `parsing/parse_list_utils.dart`

`import 'package:saropa_dart_utils/parsing/parse_list_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `parseListFromString` | function | Parse list from string (e.g. |

### `parsing/parser_error_utils.dart`

Rich parser error (line/column, context) — roadmap #647.

`import 'package:saropa_dart_utils/parsing/parser_error_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `ParserErrorUtils` | class | Parser error with position and snippet. |
| `ParserErrorUtils` | constructor | Creates a parser error with a human-readable [message] and optional position ([line], [column]) and source [snippet]. |
| `message` | getter | The human-readable description of what went wrong. |
| `line` | getter | The 1-based line where the error occurred, or `null` if unknown. |
| `column` | getter | The 1-based column where the error occurred, or `null` if unknown. |
| `snippet` | getter | The offending source excerpt, or `null` if none was captured. |
| `toString` | method |  |

### `parsing/parsing_more_utils.dart`

`import 'package:saropa_dart_utils/parsing/parsing_more_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `parseIntBase` | function | Parsing More: parse int with base, validate URL/IP, parse port, validate hex, etc. |
| `isValidUrlLoose` | function | Returns `true` if [s] begins with an `http://` or `https://` scheme. |
| `isValidIpv4` | function | Returns `true` if [s] is a dotted-quad IPv4 address. |
| `parsePortFromHostPort` | function | Extracts the port number from a `host:port` string in [hostPort]. |
| `parseKeyValueLines` | function | Parses `key=value` lines from [input] into a map. |
| `isValidHexString` | function | Returns `true` if [s] consists only of hexadecimal digits. |
| `parseDottedDecimal` | function | Parses the dot-separated integers in [s] into a list. |

### `parsing/phone_normalize_utils.dart`

Phone number normalize (digits only or E.164-ish).

`import 'package:saropa_dart_utils/parsing/phone_normalize_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `normalizePhoneDigits` | function | Phone number normalize (digits only or E.164-ish). |
| `normalizePhoneE164` | function | E.164-ish: digits only, optional leading + preserved as prefix for result. |

### `parsing/range_header_utils.dart`

Parse an HTTP `Range` request header (`bytes=` unit only).

`import 'package:saropa_dart_utils/parsing/range_header_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `ByteRange` | class | One requested byte range. |
| `ByteRange` | constructor | Creates a byte range from [start] and [end]; see the class doc for how a null in either position encodes an open-ended or suffix range. |
| `start` | field | First byte offset (inclusive), or null for a suffix-length range. |
| `end` | field | Last byte offset (inclusive), or null for an open-ended range; for a suffix range it is the count of trailing bytes. |
| `operator ==` | operator |  |
| `hashCode` | getter |  |
| `parseRangeHeader` | function | Parses [header] (e.g. |

### `parsing/semver_utils.dart`

Semver parse and compare.

`import 'package:saropa_dart_utils/parsing/semver_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `SemverUtils` | class | Semver parse and compare. |
| `SemverUtils` | constructor | Creates a semantic version from its [major], [minor], and [patch] numbers, with optional [pre]-release and [build] metadata strings. |
| `major` | getter | The major version number (breaking changes). |
| `minor` | getter | The minor version number (backward-compatible features). |
| `patch` | getter | The patch version number (backward-compatible fixes). |
| `pre` | getter | The pre-release identifier (e.g. |
| `build` | getter | The build metadata (e.g. |
| `parse` | method | Parses [s] into a [SemverUtils], or returns `null` if it is not valid. |
| `compareTo` | method | Compares this version against [other] following semver precedence rules. |
| `toString` | method |  |

### `parsing/size_parse_utils.dart`

Parse size string ("1.5 MB", "512K") to bytes; format bytes to human.

`import 'package:saropa_dart_utils/parsing/size_parse_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `parseSizeToBytes` | function | Parses a human-readable size string in [input] into a byte count. |
| `formatBytesToHuman` | function | Formats a byte count in [bytes] into a human-readable size string. |

### `parsing/validate_non_empty_utils.dart`

Validate non-empty after trim.

`import 'package:saropa_dart_utils/parsing/validate_non_empty_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `isNonEmptyAfterTrim` | function | Validate non-empty after trim. |

### `parsing/varint_utils.dart`

Varint encoding/decoding (Protobuf-style) — roadmap #635.

`import 'package:saropa_dart_utils/parsing/varint_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `decodeVarint` | function | Decodes one varint from [bytes] at [start]. |
| `encodeVarint` | function | Encodes [value] as varint bytes. |

### `parsing/version_compare_utils.dart`

Version compare (semver or dotted).

`import 'package:saropa_dart_utils/parsing/version_compare_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `compareVersions` | function | Version compare (semver or dotted). |

### `parsing/version_parse_utils.dart`

Parse version string (major.minor.patch).

`import 'package:saropa_dart_utils/parsing/version_parse_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `parseVersion` | function | Parse version string (major.minor.patch). |

---

## Random

### `random/common_random.dart`

`import 'package:saropa_dart_utils/random/common_random.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `CommonRandom` | function | Creates a [Random] number generator. |

---

## Regex

### `regex/regex_common_utils.dart`

Common regex: email (simple), phone (digits), URL (loose).

`import 'package:saropa_dart_utils/regex/regex_common_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `regexEmailSimple` | getter | Common regex: email (simple), phone (digits), URL (loose). |
| `regexPhoneDigits` | getter | A loose phone-number pattern: 7 to 15 ASCII digits, with no separators or country prefix. |
| `regexUrlLoose` | getter | A loose URL pattern matching an `http://` or `https://` scheme (case- insensitive) followed by a run of non-whitespace, non-delimiter characters. |

### `regex/regex_match_utils.dart`

Match all (return all matches with groups).

`import 'package:saropa_dart_utils/regex/regex_match_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `matchAll` | function | Match all (return all matches with groups). |
| `replaceAllWithCallback` | function | Replaces every match of [pattern] in [input] with the result of [replace]. |
| `namedGroupMap` | function | Returns the named capture groups of [match] as a map of name to value. |

---

## Stats

### `stats/bucketed_aggregate_utils.dart`

Bucketed aggregation (sum/count/avg/min/max per bucket) — roadmap #570.

`import 'package:saropa_dart_utils/stats/bucketed_aggregate_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `BucketedValues` | typedef | Bucket key and list of values. |
| `bucketBy` | function | Groups [values] by [keyOf](index, value), then returns map of key -> list of values. |
| `bucketAggregate` | function | For each bucket, compute [aggregate] (sum, count, avg, min, max). |

### `stats/cdf_utils.dart`

Empirical CDF and cumulative histogram for numeric samples — roadmap #574.

`import 'package:saropa_dart_utils/stats/cdf_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `CdfPoint` | class | One step of an empirical CDF: a sample [value] and the cumulative probability [p] = (count of samples ≤ value) / n, in `[0, 1]`. |
| `CdfPoint` | constructor | Creates a CDF step at [value] with cumulative probability [p]. |
| `value` | field | A distinct sample value, ascending across the returned list. |
| `p` | field | Fraction of all samples that are ≤ [value]; the last point's [p] is 1.0. |
| `operator ==` | operator |  |
| `hashCode` | getter |  |
| `toString` | method |  |
| `empiricalCdf` | function | Builds the empirical CDF of [values]: one [CdfPoint] per DISTINCT value in ascending order, with `p` the running fraction of samples ≤ that value. |
| `cdfAt` | function | The empirical CDF of [values] evaluated at [x]: the fraction of samples that are ≤ [x], in `[0, 1]`. |
| `cumulativeHistogram` | function | Cumulative histogram: the running total of `histogramFixed(values, edges)`, so bin `i` holds the count of samples in bins `0..i` — the CDF in bin form. |

### `stats/confidence_interval_utils.dart`

Confidence interval for mean (normal approximation) — roadmap #562.

`import 'package:saropa_dart_utils/stats/confidence_interval_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `confidenceInterval95` | function | Approximate 95% CI for mean: (lower, upper). |

### `stats/correlation_utils.dart`

Correlation coefficients (Pearson) — roadmap #563.

`import 'package:saropa_dart_utils/stats/correlation_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `pearsonCorrelation` | function | Pearson correlation between [x] and [y]. |

### `stats/data_normalization_utils.dart`

Data normalization (z-score, min-max scaling) — roadmap #576.

`import 'package:saropa_dart_utils/stats/data_normalization_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `zScoreNormalize` | function | Z-score: (x - mean) / stdDev. |
| `minMaxScale` | function | Min-max scale to [low, high]. |

### `stats/feature_encoding_utils.dart`

Feature scaling/encoding (bucketization, one-hot) — roadmap #589.

`import 'package:saropa_dart_utils/stats/feature_encoding_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `oneHot` | function | One-hot: [value] in [categories] -> list of 0/1 of length [categories]. |
| `bucketize` | function | Bucketize numeric value into bin index for [edges] (sorted boundaries). |

### `stats/funnel_utils.dart`

Funnel analysis (drop-off between ordered steps) — roadmap #580.

`import 'package:saropa_dart_utils/stats/funnel_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `FunnelUtils` | class | Step name and count of users who reached it. |
| `FunnelUtils` | constructor | Creates a funnel step named [name] reached by [count] users. |
| `name` | getter | The step's display name. |
| `count` | getter | Number of users who reached this step. |
| `toString` | method |  |
| `funnelConversionRates` | function | Returns conversion rate from step i to i+1 (and overall to last step). |

### `stats/grouped_stats_utils.dart`

Per-key descriptive statistics over an iterable — roadmap #571.

`import 'package:saropa_dart_utils/stats/grouped_stats_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `NumericStats` | class | Descriptive statistics for one group of numeric values. |
| `NumericStats` | constructor | Creates a stat bundle. |
| `count` | field | Number of values in the group (≥ 1). |
| `sum` | field | Sum of the values. |
| `min` | field | Smallest value. |
| `max` | field | Largest value. |
| `mean` | field | Arithmetic mean (`sum / count`). |
| `operator ==` | operator |  |
| `hashCode` | getter |  |
| `toString` | method |  |
| `count` | field |  |
| `sum` | field |  |
| `min` | field |  |
| `max` | field |  |
| `add` | method |  |
| `toStats` | method |  |
| `groupedStats` | function | Groups [items] by [keyOf] and computes a [NumericStats] bundle over [valueOf] for each group, in a single pass. |

### `stats/linear_regression_utils.dart`

Simple linear regression — roadmap #564.

`import 'package:saropa_dart_utils/stats/linear_regression_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `linearRegression` | function | Simple linear regression y ~ x. |

### `stats/log_transform_utils.dart`

Log/exp transforms for analytics — roadmap #575.

`import 'package:saropa_dart_utils/stats/log_transform_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `log1pSafe` | function | Log transform: log(1 + x) to handle zeros. |
| `expSafe` | function | Exp transform. |
| `logScale` | function | Log-scale list (1 + x then log). |

### `stats/metric_rollup_utils.dart`

Metric roll-up (daily → weekly → monthly) — roadmap #587.

`import 'package:saropa_dart_utils/stats/metric_rollup_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `rollupDailyToPeriod` | function | Aggregates [dailyValues] (list of 7 or 30 values) into one value by [op]. |
| `rollupSum` | function | Sum of list. |
| `rollupAvg` | function | Average of list. |

### `stats/moving_average_utils.dart`

Moving averages: simple, exponential (roadmap #565).

`import 'package:saropa_dart_utils/stats/moving_average_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `simpleMovingAverage` | function | Simple moving average of [values] over window [size]. |
| `exponentialMovingAverage` | function | Exponential moving average: [alpha] in (0,1]; first value = first input. |

### `stats/outlier_mad_utils.dart`

Outlier detection by MAD / Z-score — roadmap #467.

`import 'package:saropa_dart_utils/stats/outlier_mad_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `outlierIndicesByMAD` | function | Flags indices where \|x - median\| / MAD > [threshold]. |

### `stats/percentile_rank_utils.dart`

Percentile rank and inverse percentile — roadmap #583.

`import 'package:saropa_dart_utils/stats/percentile_rank_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `percentileRank` | function | Percentile rank of [value] in [sorted] (0..1). |
| `percentile` | function | Value at percentile [p] (0..1) in [sorted]. |

### `stats/quantile_summary_utils.dart`

Quantile summary (percentiles, median, quartiles) — roadmap #572.

`import 'package:saropa_dart_utils/stats/quantile_summary_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `QuantileSummaryUtils` | class | Pre-computed quantile summary for a list of numbers. |
| `QuantileSummaryUtils` | constructor | Builds a summary from [values], sorting a copy once for repeated queries. |
| `quantile` | method | Returns the value at quantile [p] (0.0–1.0). |
| `min` | getter | Smallest value, or [double.nan] when there are no values. |
| `max` | getter | Largest value, or [double.nan] when there are no values. |
| `median` | getter | The median (50th percentile). |
| `q1` | getter | The first quartile (25th percentile). |
| `q3` | getter | The third quartile (75th percentile). |
| `toString` | method |  |

### `stats/retention_utils.dart`

Retention curves (N-day retention) — roadmap #581.

`import 'package:saropa_dart_utils/stats/retention_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `retentionByDay` | function | [events] = list of (userId, date). |

### `stats/robust_stats_utils.dart`

Robust statistics: MAD, trimmed mean (roadmap #561).

`import 'package:saropa_dart_utils/stats/robust_stats_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `median` | function | Median of [values] (assumes non-empty; sorts a copy). |
| `medianAbsoluteDeviation` | function | Median absolute deviation: median of \|x_i - median(x)\|. |
| `trimmedMean` | function | Trimmed mean: drop [trim] fraction from each tail (0..0.5), then average. |

### `stats/sampling_utils.dart`

Sampling helpers (stratified, systematic) — roadmap #584.

`import 'package:saropa_dart_utils/stats/sampling_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `systematicSample` | function | Systematic sampling: take every [step]-th element starting at [start]. |
| `stratifiedSampleIndices` | function | Stratified: [strata] gives group id per index; sample [perGroup] from each. |

---

## String

### `string/acronym_extract_utils.dart`

Acronym/initialism extractor (e.g.

`import 'package:saropa_dart_utils/string/acronym_extract_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `extractAcronyms` | function | Extracts patterns like "Full Name (ABC)" -> (ABC, Full Name). |

### `string/apply_patch_utils.dart`

Apply patch (edit script) to string with validation (roadmap #403).

`import 'package:saropa_dart_utils/string/apply_patch_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `ApplyPatchUtils` | class | Result of applying a patch. |
| `ApplyPatchUtils` | constructor |  |
| `ApplyPatchSuccess` | class | Patch applied successfully; [text] is the result. |
| `ApplyPatchSuccess` | constructor | Creates a success result wrapping the patched [text]. |
| `text` | getter | The fully patched output text. |
| `toString` | method |  |
| `ApplyPatchConflict` | class | Patch could not be applied; [message] describes the conflict or error. |
| `ApplyPatchConflict` | constructor | Creates a conflict result whose [message] describes why the patch failed. |
| `message` | getter | Human-readable description of the conflict or error that blocked the patch. |
| `toString` | method |  |
| `applyPatch` | function | Applies an edit script [ops] to [baseText] (line-based). |

### `string/between_result.dart`

`import 'package:saropa_dart_utils/string/between_result.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `BetweenResult` | class | Represents the result of extracting content between delimiters. |
| `BetweenResult` | constructor | Creates a [BetweenResult] with the given [content] and [remaining]. |
| `content` | field | The content found between the delimiters. |
| `remaining` | field | The remaining string after removing the delimited section. |
| `operator ==` | operator |  |
| `hashCode` | getter |  |
| `toString` | method |  |

### `string/code_block_extract_utils.dart`

Code block detector/extractor from mixed text (roadmap #422).

`import 'package:saropa_dart_utils/string/code_block_extract_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `extractFirstCodeBlock` | function | Extracts first fenced code block ( |
| `extractAllCodeBlocks` | function | Returns all fenced code blocks as list of (language, code). |

### `string/did_you_mean_utils.dart`

"Did you mean?" over dictionary (Levenshtein) — roadmap #426.

`import 'package:saropa_dart_utils/string/did_you_mean_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `didYouMean` | function | Returns up to [limit] words from [dictionary] closest to [word] by edit distance. |

### `string/diff_render_utils.dart`

Diff → colored/HTML/ANSI unified diff renderer (roadmap #402).

`import 'package:saropa_dart_utils/string/diff_render_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `DiffOutputFormat` | enum | Format for unified diff output. |
| `plain` | enum value | Plain text with +/- prefix (no color). |
| `ansi` | enum value | ANSI escape codes for terminal (green add, red remove). |
| `html` | enum value | HTML spans with classes (add/remove for styling). |
| `renderUnifiedDiff` | function | Renders a list of [DiffOp] to a unified-diff-style string. |

### `string/duplicate_doc_utils.dart`

Near-duplicate document detector via fingerprints — roadmap #438.

`import 'package:saropa_dart_utils/string/duplicate_doc_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `isNearDuplicate` | function | Returns true if [a] and [b] are near-duplicates (cosine similarity >= [threshold]). |
| `clusterNearDuplicates` | function | Groups [documents] into near-duplicate clusters (greedy). |

### `string/email_quote_strip_utils.dart`

Email reply quote stripper (heuristic) — roadmap #424.

`import 'package:saropa_dart_utils/string/email_quote_strip_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `stripEmailReplyQuotes` | function | Strips common reply patterns: lines starting with >, On ... |

### `string/excerpt_utils.dart`

Smart excerpt generator: best snippet around query terms with ellipsis (roadmap #410).

`import 'package:saropa_dart_utils/string/excerpt_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `excerptAround` | function | Returns an excerpt of [text] around the first occurrence of [query], with [contextChars] characters on each side and [ellipsis] at truncation points. |

### `string/fuzzy_search_utils.dart`

Fuzzy search over list of strings with token + edit distance + ranking (roadmap #406).

`import 'package:saropa_dart_utils/string/fuzzy_search_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `FuzzySearchUtils` | class | One candidate from fuzzy search with score and matched text. |
| `FuzzySearchUtils` | constructor | Creates a match at [index] in the original candidate list, holding the matched [text] and its computed relevance [score]. |
| `index` | getter | Position of this match within the original `candidates` list. |
| `text` | getter | The candidate string that matched the query. |
| `score` | getter | Relevance score in `[0, 1]`; higher means a closer match to the query. |
| `toString` | method |  |
| `fuzzySearch` | function | Searches [candidates] for [query]; returns matches sorted by match score descending. |

### `string/glob_utils.dart`

Simple glob matching (**/*.dart style).

`import 'package:saropa_dart_utils/string/glob_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `GlobUtils` | class | Glob-style pattern matching. |
| `match` | method | Returns true if [path] matches [pattern]. |

### `string/html_sanitizer_utils.dart`

HTML sanitizer: allowlist tags/attributes, strip scripts/styles (roadmap #439).

`import 'package:saropa_dart_utils/string/html_sanitizer_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `sanitizeHtml` | function | Removes script and style tag contents, then strips all HTML tags. |
| `stripHtmlTags` | function | Strips all HTML tags and returns plain text (no attributes or script content). |

### `string/human_name_parser_utils.dart`

Human name parser (first/middle/last/suffix) — roadmap #409.

`import 'package:saropa_dart_utils/string/human_name_parser_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `HumanNameParserUtils` | class | Parsed name parts. |
| `HumanNameParserUtils` | constructor | Creates parsed name parts; any unrecognized component is left null. |
| `first` | getter | Given (first) name, or null when none was detected. |
| `middle` | getter | Middle name(s) joined by spaces, or null when none was detected. |
| `last` | getter | Family (last) name, or null when none was detected. |
| `suffix` | getter | Generational/honorific suffix such as "Jr.", "Sr.", or "III"; null if absent. |
| `toString` | method |  |
| `parseHumanName` | function | Simple split: "Last, First Middle" or "First Middle Last". |

### `string/icu_message_utils.dart`

ICU-style message formatting lite: pluralization and `select` (gender) — roadmap #414.

`import 'package:saropa_dart_utils/string/icu_message_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `icuPlural` | function | Selects a plural form for [count] and replaces every `#` in the chosen form with the count. |
| `icuSelect` | function | ICU `select` lite: returns the [cases] entry whose key equals [value], falling back to [other] for any missing or unknown key. |

### `string/levenshtein_utils.dart`

Levenshtein (edit) distance and similarity ratio for strings.

`import 'package:saropa_dart_utils/string/levenshtein_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `LevenshteinUtils` | class | Utilities for Levenshtein edit distance and derived metrics. |
| `distance` | method | Returns the Levenshtein (edit) distance between [a] and [b]. |
| `ratio` | method | Returns a similarity ratio in the range 0.0 (no similarity) to 1.0 (identical). |
| `fuzzyContains` | method | Returns true if [source] contains a substring within [maxDistance] edits of [target], using a sliding window the width of [target]. |

### `string/markdown_plain_utils.dart`

Markdown to plain text (strip markup, keep structure) — roadmap #420.

`import 'package:saropa_dart_utils/string/markdown_plain_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `markdownToPlainText` | function | Strips Markdown markup and returns plain text (headers, lists as lines). |

### `string/markdown_snippet_utils.dart`

Markdown snippet extractor (heading sections, first code block) — roadmap #419.

`import 'package:saropa_dart_utils/string/markdown_snippet_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `extractSectionByHeading` | function | Extracts text under first heading matching [headingPattern] (e.g. |
| `extractFirstCodeBlock` | function | Returns first fenced code block content or null. |

### `string/myers_diff_utils.dart`

Myers diff for strings: minimal edit script (roadmap #401).

`import 'package:saropa_dart_utils/string/myers_diff_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `DiffOpKind` | enum | Kind of diff operation in an edit script. |
| `equal` | enum value | Line (or segment) is unchanged in both inputs. |
| `insert` | enum value | Line exists only in the "new" (right) text — insert into old to get new. |
| `delete` | enum value | Line exists only in the "old" (left) text — delete from old to get new. |
| `MyersDiffUtils` | class | Myers-style minimal edit script between two strings (line-based). |
| `diffLines` | method | Computes a minimal line-based edit script transforming [oldText] into [newText], returned as merged [DiffOp]s (equal, insert, delete). |
| `DiffOp` | class | A single operation in a minimal edit script. |
| `DiffOp` | constructor | Creates an edit operation of the given [kind] carrying its segment [text]. |
| `kind` | getter | The kind of edit (equal, insert, or delete). |
| `text` | getter | The segment text for this operation. |
| `toString` | method |  |
| `_Edit` | constructor |  |
| `toString` | method |  |

### `string/ngram_utils.dart`

N-gram generator for strings — character and word n-grams (roadmap #405).

`import 'package:saropa_dart_utils/string/ngram_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `characterNgrams` | function | Returns character n-grams of [s] with length [n]. |
| `wordNgrams` | function | Returns word n-grams of [s] with [n] words per gram. |

### `string/safe_html_excerpt_utils.dart`

Safe HTML excerpt (truncate without breaking tags) — roadmap #440.

`import 'package:saropa_dart_utils/string/safe_html_excerpt_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `safeHtmlExcerpt` | function | Truncates [html] to about [maxLength] chars, closing open tags. |

### `string/search_index_utils.dart`

Simple in-memory search index (term frequency scoring) — roadmap #407.

`import 'package:saropa_dart_utils/string/search_index_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `SearchIndexUtils` | class | In-memory document index: add docs, search by query (TF scoring). |
| `addDocument` | method | Adds [text] as a document to the index. |
| `search` | method | Returns up to [limit] hits as (documentIndex, score), sorted by score descending. |
| `length` | getter | Number of documents in the index. |
| `getDocument` | method | Returns the document at [i]. |
| `toString` | method |  |

### `string/search_query_parser_utils.dart`

Simplified search query parser (AND/OR, quotes, minus) — roadmap #418.

`import 'package:saropa_dart_utils/string/search_query_parser_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `SearchQueryParserUtils` | class | Simple parser: "a b" = AND, "a OR b", quoted = phrase, -x = exclude. |
| `parseSearchQuery` | method | Parses [query] into a list of [QueryTerm] (words/phrases, AND/OR, minus). |
| `QueryTerm` | class | Parsed query term: phrase or word, optional negated. |
| `QueryTerm` | constructor | Creates a query term for [text]; set [isNegated] for excluded ("-word") terms. |
| `text` | field | The word or quoted phrase to match (without the leading "-" if negated). |
| `isNegated` | field | True when this term must be excluded from results (entered as "-term"). |
| `toString` | method |  |

### `string/sensitive_scrub_utils.dart`

Sensitive data scrubber with pluggable patterns (roadmap #425).

`import 'package:saropa_dart_utils/string/sensitive_scrub_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `SensitiveScrubUtils` | class | A scrubber rule: [pattern] is replaced by [replacement] when matched. |
| `SensitiveScrubUtils` | constructor | Creates a rule that replaces every match of [pattern] with [replacement]. |
| `pattern` | field | Regular expression identifying the sensitive substrings to mask. |
| `replacement` | field | Text substituted in place of each [pattern] match (e.g. |
| `toString` | method |  |
| `defaultScrubRules` | getter | Default rules: mask emails, phone-like digits, card-like numbers, SSN-like. |
| `scrubSensitive` | function | Returns [text] with all matches of [rules] replaced by their replacement string. |

### `string/slug_dedup_utils.dart`

Slug deduplicator: append incremental suffixes based on taken slugs (roadmap #411).

`import 'package:saropa_dart_utils/string/slug_dedup_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `deduplicateSlug` | function | Returns a slug that is not in [takenSlugs]. |

### `string/soundex_utils.dart`

Soundex phonetic encoding for English names.

`import 'package:saropa_dart_utils/string/soundex_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `SoundexUtils` | class | Soundex encoding: words that sound similar produce the same code. |
| `encode` | method | Encodes this string using Soundex (first letter + 3 digits). |
| `soundsLike` | method | Returns true if [a] and [b] have the same Soundex code. |

### `string/spelling_key_lookup_utils.dart`

Spelling-tolerant key lookup (canonical → variants) — roadmap #416.

`import 'package:saropa_dart_utils/string/spelling_key_lookup_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `lookupWithVariants` | function | Lookup that returns canonical key if [query] matches any variant within [maxDistance]. |

### `string/string_analysis_extensions.dart`

`import 'package:saropa_dart_utils/string/string_analysis_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringAnalysisExtensions` on `String` | extension | Extensions on [String] for validation, comparison, and analysis. |
| `isLatin` | method | Returns `true` if this string contains only Latin alphabet characters (a-z, A-Z). |
| `isEquals` | method | Returns `true` if this string equals [other], with options for case-insensitivity via [ignoreCase] and apostrophe normalization via [normalizeApostrophe]. |
| `containsIgnoreCase` | method | Returns `true` if this string contains [other] in a case-insensitive manner. |
| `getFirstDiffChar` | method | Returns the first character where this string and [other] differ, or an empty string if they are identical. |
| `hasInvalidUnicode` | getter | Returns true if this string contains invalid Unicode characters. |
| `removeInvalidUnicode` | method | Returns a new string with all invalid Unicode replacement characters removed. |
| `isVowel` | method | Returns true if this single-character string is a vowel. |
| `hasAnyDigits` | method | Returns true if this string contains any digit characters. |
| `count` | method | Returns the number of non-overlapping occurrences of [find] in this string. |
| `secondIndex` | method | Returns the index of the second occurrence of [char], or `-1` if not found. |
| `extractCurlyBraces` | method | Extracts all text within curly braces from this string. |
| `obscureText` | method | Returns an obscured version of this string using [char] repeated, or `null` if empty. |
| `endsWithAny` | method | Returns `true` if this string ends with any character in [find]. |
| `endsWithPunctuation` | method | Returns `true` if this string ends with punctuation (`.`, `?`, or `!`). |
| `isAny` | method | Returns `true` if this string equals any item in [list]. |

### `string/string_ansi_extensions.dart`

`import 'package:saropa_dart_utils/string/string_ansi_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringAnsiExtensions` on `String` | extension | Strip ANSI escape codes from terminal output. |
| `stripAnsi` | method | Removes ANSI escape sequences (e.g. |

### `string/string_between_extensions.dart`

`import 'package:saropa_dart_utils/string/string_between_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringBetweenExtensions` on `String` | extension | Extension methods for extracting content between delimiters. |
| `betweenBracketsResult` | method | Returns a [BetweenResult] of (content between brackets, remaining string) for the first matching bracket pair found. |
| `betweenBracketsResultLast` | method | Returns a [BetweenResult] like `betweenBracketsResult` but searches from the end. |
| `betweenBrackets` | method | Returns the content between the first matching bracket pair found, or `null` if none is found. |
| `removeBetweenAll` | method | Returns a new string with all content between [start] and [end] delimiters removed. |
| `betweenSplit` | method | Returns a list of all sections found between [start] and [end] delimiters, or `null` if none are found. |
| `betweenResult` | method | Returns a [BetweenResult] of content between [start] and [end] delimiters plus the remaining string, or `null` if not found. |
| `betweenResultLast` | method | Returns a [BetweenResult] like `betweenResult` but searches from the end using [start] and [end] delimiters. |
| `between` | method | Returns the content between the first occurrence of [start] and [end]. |
| `betweenLast` | method | Returns the content between the last occurrence of [start] and [end]. |

### `string/string_case_acronym_extensions.dart`

`import 'package:saropa_dart_utils/string/string_case_acronym_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringCaseAcronymExtensions` on `String` | extension | Case conversion that preserves acronyms (e.g. |
| `toCamelCaseAcronyms` | method | Converts to camelCase, lowercasing acronyms (e.g. |
| `toSnakeCaseAcronyms` | method | Converts to snake_case, lowercasing acronyms (e.g. |

### `string/string_case_extensions.dart`

`import 'package:saropa_dart_utils/string/string_case_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringCaseExtensions` on `String` | extension | Extension methods for [String] to provide advanced case manipulation functionalities. |
| `isAllLetterLowerCase` | getter | Checks if the string contains only lowercase letters (a-z). |
| `isAnyCaseLetter` | getter | Checks if the string contains only letters, regardless of case (a-z, A-Z). |
| `isAllLetterUpperCase` | getter | Checks if the string contains only uppercase letters (A-Z). |
| `capitalizeWords` | method | Capitalizes the first letter of each word in the string, leaving other characters as they are. |
| `lowerCaseFirstChar` | method | Converts the first character of the string to lowercase, leaving the rest of the string unchanged. |
| `upperCaseFirstChar` | method | Converts the first character of the string to uppercase, leaving the rest of the string unchanged. |
| `titleCase` | method | Converts the string to title case, capitalizing the first letter and lowercasing the rest. |
| `toUpperLatinOnly` | method | Converts only the Latin alphabetic characters (a-z) in the string to uppercase, leaving other characters unchanged. |
| `capitalize` | method | Capitalizes the first letter of the string, leaving the rest of the string unchanged. |
| `upperCaseLettersOnly` | method | Extracts and concatenates only the uppercase letters from the string. |
| `findCapitalizedWords` | method | Finds and returns a list of words from the string that are capitalized (start with an uppercase letter). |
| `insertSpaceBetweenCapitalized` | method | Inserts a space between words that are capitalized in a string (CamelCase or PascalCase). |
| `splitCapitalized` | method | Splits a CamelCase or PascalCase string into a list of words based on capitalization. |
| `unCapitalizedWords` | method | Finds and returns a list of words from the string that are uncapitalized (start with a lowercase letter). |

### `string/string_character_extensions.dart`

`import 'package:saropa_dart_utils/string/string_character_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringCharacterExtensions` on `String` | extension | Extension methods for grapheme-aware character operations. |
| `substringCharacter` | method | Gets a grapheme-aware substring. |
| `firstCharacter` | method | Gets the first grapheme cluster (character). |
| `secondCharacter` | method | Gets the second grapheme cluster (character). |
| `graphemeLength` | getter | Gets the grapheme length (number of user-perceived characters). |

### `string/string_csv_extensions.dart`

`import 'package:saropa_dart_utils/string/string_csv_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringCsvExtensions` on `String` | extension | CSV-style quoting (wrap in quotes and escape internal quotes). |
| `wrapCsvQuotes` | method | Wraps this string in double quotes and doubles any internal double quotes. |

### `string/string_diacritics_extensions.dart`

`import 'package:saropa_dart_utils/string/string_diacritics_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringDiacriticsExtensions` on `String` | extension | An extension on the String class to provide utility methods for handling diacritical marks (accents). |
| `containsDiacritics` | method | Accurately checks if the string contains any diacritical characters that can be removed by the `removeDiacritics` method. |
| `removeDiacritics` | method | Returns a new string with diacritical marks replaced by their base ASCII equivalents (e.g., 'é' becomes 'e'). |

### `string/string_extensions.dart`

`import 'package:saropa_dart_utils/string/string_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringExtensions` on `String` | extension | Extensions for presentation, like adding quotes, truncating text, and formatting. |
| `accentedQuoteOpening` | field | Left single curly quotation mark ("smart" opening quote, U+2018). |
| `accentedQuoteClosing` | field | Right single curly quotation mark ("smart" closing quote, U+2019). |
| `accentedDoubleQuoteOpening` | field | Left double curly quotation mark ("smart" opening double quote, U+201C). |
| `accentedDoubleQuoteClosing` | field | Right double curly quotation mark ("smart" closing double quote, U+201D). |
| `ellipsis` | field | Horizontal ellipsis character (U+2026), the single-glyph "\u2026". |
| `doubleChevron` | field | Right-pointing double angle quotation mark "\u00BB" (U+00BB). |
| `apostrophe` | field | Typographic apostrophe (U+2019), preferred over the straight ASCII quote. |
| `hyphen` | field | Unicode hyphen (U+2010), the typographically correct hyphen glyph. |
| `softHyphen` | field | Soft Hyphen character (U+00AD). |
| `newLine` | field | Line feed character (`\n`) used to separate lines of text. |
| `lineBreak` | field | Alias for [newLine]; reads more naturally when joining display text. |
| `blank` | field | Hangul filler (U+3164), a whitespace-like glyph that survives UI trimming where a normal space would be collapsed. |
| `zeroWidth` | field | 'This\u200Bis\u200Ba\u200Bsentence\u200Bwith\u200Bzero-width\u200Bspaces. |
| `nonBreakingSpace` | field | Non-breaking space (U+00A0); keeps adjacent words on the same line. |
| `nonBreakingHyphen` | field | For not breaking words into newline at hyphen in Text. |
| `bullet` | field | Bullet point character. |
| `dot` | field | Alias for `bullet`. |
| `dotJoiner` | field | A dot with spaces for joining items (e.g., "Item 1 • Item 2"). |
| `commonWordEndings` | field | Common word-ending characters for text processing. |
| `wrap` | method | Returns this string wrapped with [before] prepended and [after] appended. |
| `wrapWith` | method | Returns this string wrapped with [before] prepended and [after] appended, or `null` if the string is empty. |
| `wrapSingleQuotes` | method | Returns this string wrapped in single quotes: `'string'`. |
| `wrapDoubleQuotes` | method | Returns this string wrapped in double quotes: `"string"`. |
| `wrapSingleAccentedQuotes` | method | Returns this string wrapped in accented single quotes. |
| `wrapDoubleAccentedQuotes` | method | Returns this string wrapped in accented double quotes. |
| `encloseInParentheses` | method | Returns this string enclosed in parentheses, or `null` if empty. |
| `insertNewLineBeforeBrackets` | method | Returns a new string with a newline character inserted before each opening parenthesis. |
| `truncateWithEllipsis` | method | Returns the string truncated to [cutoff] graphemes with an ellipsis appended. |
| `truncateWithEllipsisPreserveWords` | method | Returns the string truncated to [cutoff] graphemes with an ellipsis, preserving whole words. |
| `reversed` | getter | Returns a new string with all characters reversed. |
| `nullIfEmpty` | method | Returns `null` if the string is empty or contains only whitespace. |
| `substringSafe` | method | Safely gets a substring, preventing [RangeError] (returns empty string for out-of-bounds indices). |
| `lastChars` | method | Get the last [n] graphemes (user-perceived characters) of a string. |
| `last` | method | Returns the last [len] grapheme clusters of this string. |

### `string/string_highlight_extensions.dart`

`import 'package:saropa_dart_utils/string/string_highlight_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringHighlightExtensions` on `String` | extension | Highlight (wrap) substring matches. |
| `highlightSubstring` | method | Wraps each occurrence of [substring] with [before] and [after]. |

### `string/string_indent_extensions.dart`

`import 'package:saropa_dart_utils/string/string_indent_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringIndentExtensions` on `String` | extension | Indent and dedent for multi-line strings. |
| `indentLines` | method | Prepends [prefix] to every line (after splitting by newline). |
| `dedent` | method | Removes the common leading whitespace from every line. |

### `string/string_key_value_extensions.dart`

`import 'package:saropa_dart_utils/string/string_key_value_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringKeyValueExtensions` on `String` | extension | Parse simple key=value pairs from a string. |
| `parseKeyValuePairs` | method | Parses simple key=value pairs separated by whitespace into a map. |

### `string/string_line_extensions.dart`

`import 'package:saropa_dart_utils/string/string_line_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringLineExtensions` on `String` | extension | Extensions for line-based string operations (line breaks, BOM, indent). |
| `normalizeLineBreaks` | method | Normalizes line breaks to [target] (default `\n`). |
| `stripBom` | method | Removes the UTF-8 BOM (U+FEFF) from the start of this string if present. |
| `splitIntoLines` | method | Splits this string into lines, normalizing CRLF/CR/LF to a single newline first. |

### `string/string_lower_extensions.dart`

`import 'package:saropa_dart_utils/string/string_lower_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringLowerExtensions` on `String` | extension | Lower priority string: truncate with ellipsis, pad, repeat, isWhitespaceOnly. |
| `padLeftTo` | method | Pads this string on the left to [length] with [pad]. |
| `padRightTo` | method | Pads this string on the right to [length] with [pad]. |
| `repeatTimes` | method | Returns this string repeated [n] times, or empty if n <= 0. |
| `isWhitespaceOnly` | getter | True if this string is non-empty and contains only whitespace. |
| `ensurePrefix` | method | Returns this string with [prefix] prepended if not already present. |
| `ensureSuffix` | method | Returns this string with [suffix] appended if not already present. |
| `removePrefix` | method | Removes [prefix] from the start if present; otherwise returns this. |
| `removeSuffix` | method | Removes [suffix] from the end if present; otherwise returns this. |
| `StringDefaultEmptyExtension` on `String?` | extension | Null-safe fallbacks for nullable strings (treat null as empty). |
| `orEmpty` | method | Returns this string if non-null, otherwise the empty string. |

### `string/string_manipulation_extensions.dart`

`import 'package:saropa_dart_utils/string/string_manipulation_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringManipulationExtensions` on `String` | extension | Extensions on [String] for character manipulation, removal, and cleaning. |
| `insert` | method | Returns a new string with [newChar] inserted at the specified [position]. |
| `removeLastOccurrence` | method | Returns a new string with the last occurrence of [target] removed. |
| `removeMatchingWrappingBrackets` | method | Returns a new string with the outer matching bracket pair removed. |
| `isBracketWrapped` | method | Returns `true` if this string starts and ends with a matching bracket pair: parentheses, square brackets, curly braces, or angle brackets. |
| `removeWrappingChar` | method | Returns a new string with [char] removed from the beginning and/or end. |
| `removeStart` | method | Returns a new string with [start] removed from the beginning, or `null` if the result is empty. |
| `removeEnd` | method | Returns a new string with [end] removed from the end, if it exists. |
| `removeFirstChar` | method | Returns a new string with the first character removed. |
| `removeLastChar` | method | Returns a new string with the last character removed. |
| `removeFirstLastChar` | method | Returns a new string with both the first and last characters removed. |
| `normalizeApostrophe` | method | Returns a new string with apostrophe variants replaced by a standard single quote. |
| `toAlphaOnly` | method | Returns a new string with all non-letter characters removed. |
| `removeNonAlphaNumeric` | method | Returns a new string with all non-alphanumeric characters removed. |
| `replaceNonNumbers` | method | Replaces all characters that are not digits (0-9) with the [replacement] string. |
| `removeNonNumbers` | method | Returns a new string with all non-digit characters removed. |
| `removeAll` | method | Returns a new string with all occurrences of [pattern] removed. |
| `replaceLastNCharacters` | method | Returns a new string with the last [n] characters replaced by [replacementChar]. |
| `makeNonBreaking` | method | Returns a new string with hyphens and spaces replaced by non-breaking equivalents. |
| `replaceLineBreaks` | method | Returns a new string with all line breaks replaced by [replacement]. |
| `removeLeadingAndTrailing` | method | Returns a new string with leading and trailing occurrences of [find] removed, or `null` if the result is empty. |
| `lettersOnly` | method | Extracts only ASCII letter characters (A-Z, a-z) from this string. |
| `lowerCaseLettersOnly` | method | Extracts only ASCII lowercase letter characters (a-z) from this string. |
| `getEverythingBefore` | method | Returns the substring before the first occurrence of [find]. |
| `getEverythingAfter` | method | Returns the substringSafe after the first occurrence of [find]. |
| `getEverythingAfterLast` | method | Returns the substringSafe after the last occurrence of [find]. |
| `getRandomChar` | method | Returns a random character from this string. |
| `repeat` | method | Returns this string repeated [count] times. |
| `appendNotEmpty` | method | Returns this string with [value] appended, or an empty string if this string is empty. |
| `prefixNotEmpty` | method | Returns this string with [value] prepended, or this string unchanged if empty. |

### `string/string_mask_extensions.dart`

`import 'package:saropa_dart_utils/string/string_mask_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringMaskExtensions` on `String` | extension | Extensions for masking and redacting sensitive string content. |
| `mask` | method | Masks this string, showing only the last [visibleCount] characters; the rest become [maskChar]. |
| `redactEmail` | method | Redacts an email-like string to something like `j***@example.com`. |
| `redactPhone` | method | Redacts a phone-like string: keeps last [visibleCount] digits, masks the rest with [maskChar]. |

### `string/string_more_extensions.dart`

`import 'package:saropa_dart_utils/string/string_more_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringMoreExtensions` on `String` | extension | String More: strip leading/trailing substring, join lines, wrap at N, capitalize sentence, swap case, etc. |
| `stripSubstring` | method | Removes leading and trailing occurrences of [substring] until none remain. |
| `joinLines` | method | Joins lines (split by newline) with [separator]. |
| `wrapAtChars` | method | Wraps this string at [width] character boundaries (grapheme-safe). |
| `capitalizeSentences` | method | Capitalizes the first letter after sentence boundaries (start or after `. |
| `swapCase` | method | Swaps upper-case and lower-case for each character. |
| `removeRepeatedChars` | method | Collapses runs of the same character into one, keeping order. |
| `countOccurrences` | method | Counts non-overlapping occurrences of [substring] in this string. |
| `allIndicesOf` | method | Returns all start indices where [substring] occurs (non-overlapping). |
| `isPalindrome` | method | Returns true if this string reads the same forwards and backwards (optionally ignoring case/punctuation). |
| `reverseWords` | method | Reverses the order of words (split on whitespace). |
| `firstNWords` | method | Returns the first [n] words (split on whitespace). |
| `lastNWords` | method | Returns the last [n] words (split on whitespace), preserving their order. |
| `padToWidth` | method | Pads this string to [width] with [padChar], on the left if [padLeft] else right. |
| `stripHtmlComments` | method | Removes HTML comments (<!-- ... |

### `string/string_nullable_extensions.dart`

`import 'package:saropa_dart_utils/string/string_nullable_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringNullableExtensions` on `String?` | extension | A set of utility methods for working with strings. |
| `isNullOrEmpty` | getter | Extension method to check if a [String] is null or empty. |
| `isNotNullOrEmpty` | getter | IMPORTANT: do not call ?.isNotNullOrEmpty as it will chain to null not a bool Return true if the string is not null and not empty |

### `string/string_number_extensions.dart`

`import 'package:saropa_dart_utils/string/string_number_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringNumberExtensions` on `String` | extension | An extension on the String class to extract the trailing integer from a string. |
| `isNumeric` | method | Returns `true` if this string can be parsed as a number. |
| `toDoubleNullable` | method | Returns this string parsed as a `double`, or `null` if parsing fails. |
| `toIntNullable` | method | Returns this string parsed as an `int`, or `null` if parsing fails. |
| `getTrailingInt` | method | Gets the trailing integer from the string. |

### `string/string_punctuation.dart`

`import 'package:saropa_dart_utils/string/string_punctuation.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringPunctuation` on `String` | extension | Extension on [String] to provide punctuation removal functionality. |
| `punctuationRegex` | field | Regular expression to remove punctuation while preserving alphabetic characters, *numbers**, and whitespace. |
| `removePunctuation` | method | Removes punctuation characters from a string, while preserving alphabetic characters (including accented characters and characters from other languages like... |

### `string/string_regex_extensions.dart`

`import 'package:saropa_dart_utils/string/string_regex_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringRegexExtensions` on `String` | extension | Extensions for regex-related string operations. |
| `escapeForRegex` | method | Escapes this string for use as a literal inside a [RegExp]. |

### `string/string_replace_n_extensions.dart`

`import 'package:saropa_dart_utils/string/string_replace_n_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringReplaceNExtensions` on `String` | extension | Replace first N or last occurrence. |
| `replaceFirstN` | method | Replaces the first [n] occurrences of [pattern] with [replacement]. |
| `replaceLast` | method | Replaces only the last occurrence of [pattern] with [replacement]. |

### `string/string_search_extensions.dart`

`import 'package:saropa_dart_utils/string/string_search_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `SearchMatchType` | enum | Search match type for string matching operations. |
| `contains` | enum value | Match if the string contains the search term. |
| `startsWith` | enum value | Match if the string starts with the search term. |
| `exact` | enum value | Match if the string exactly equals the search term. |
| `StringSearchExtensions` on `String` | extension | Extension methods for string searching and matching. |
| `isEqualsAny` | method | Returns `true` if this string equals any item in [list]. |
| `isContainsDigits` | method | Returns true if this string contains any digits. |
| `isContainsAnyInList` | method | Returns `true` if this string contains any item from [list]. |
| `isContainedInAny` | method | Returns `true` if this string is contained in any item from [list]. |
| `isContainsCaseInsensitive` | method | Returns `true` if this string contains [find] (case-insensitive). |
| `isContainsNullable` | method | Returns `true` if this string contains [find], handling `null` safely. |
| `isMatchAny` | method | Returns `true` if this string matches any item in [list] using the specified [matchType]. |
| `isStartsWithAny` | method | Returns `true` if this string starts with any item in [list]. |
| `getRepeatableLetter` | method | Returns the first character uppercased for use as a repeatable index. |
| `isNotContains` | method | Returns true if this string does NOT contain [find]. |
| `isContainsConditional` | method | Conditional contains check. |
| `isContainsAnyWord` | method | Returns `true` if this string contains any whole word from [searchItems]. |
| `isContainsWord` | method | Returns `true` if this string contains [find] as a whole word. |
| `isNotStartsWith` | method | Returns true if this string does NOT start with [find]. |
| `isStartsWithConditional` | method | Conditional startsWith check. |

### `string/string_slug_extensions.dart`

`import 'package:saropa_dart_utils/string/string_slug_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringSlugExtensions` on `String` | extension | Extensions for generating URL-safe slugs and sanitizing filenames. |
| `toSlug` | method | Converts this string to a URL-safe slug (lowercase, spaces/special → hyphen). |
| `toSlugWithMaxLength` | method | Returns a slug with at most [maxLength] characters, truncating at word boundaries when possible. |
| `sanitizeFilename` | method | Sanitizes this string for use as a filename: removes invalid characters and optional length cap. |

### `string/string_split_extensions.dart`

`import 'package:saropa_dart_utils/string/string_split_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringSplitExtensions` on `String` | extension | Split options that keep delimiters. |
| `splitKeepingDelimiter` | method | Splits by [pattern] and returns list of segments; optionally includes matches as separate elements. |

### `string/string_template_extensions.dart`

`import 'package:saropa_dart_utils/string/string_template_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringTemplateExtensions` on `String` | extension | Extensions for simple template substitution in strings (e.g. |
| `substituteTemplate` | method | Replaces placeholders of the form `{{key}}` with values from [variables]. |

### `string/string_text_extensions.dart`

`import 'package:saropa_dart_utils/string/string_text_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringTextExtensions` on `String` | extension | Extensions on [String] for words, grammar, lines, and text display. |
| `removeConsecutiveSpaces` | method | Returns a new string with consecutive whitespace collapsed into a single space, or `null` if the result is empty. |
| `compressSpaces` | method | Returns the result of collapsing consecutive whitespace, or `null` if empty. |
| `splitCapitalizedUnicode` | method | Returns a list of segments split at capitalized letters (Unicode-aware). |
| `words` | method | Returns this string split into a list of words, or `null` if empty. |
| `firstWord` | method | Returns the first word of this string, or `null` if empty. |
| `secondWord` | method | Returns the second word of this string, or `null` if fewer than two words. |
| `removeSingleCharacterWords` | method | Returns a new string with single-character words removed, or `null` if the result is empty. |
| `firstLines` | method | Returns the first [limit] lines of this string. |
| `trimLines` | method | Returns a new string with each line trimmed and empty lines removed. |
| `multiLinePrefix` | method | Returns a new string with [insertText] prepended to every line. |
| `grammarArticle` | method | Returns the appropriate indefinite article (`'a'` or `'an'`) for this word, or an empty string if input is empty. |
| `possess` | method | Returns the possessive form of this string (e.g., "John's", "boss'"). |
| `pluralize` | method | Returns the plural form of this string based on [count]. |
| `trimWithEllipsis` | method | Returns a truncated version of this string with ellipsis, keeping the first and last [minLength] characters. |
| `collapseMultilineString` | method | Returns this multiline string collapsed into a single line, truncated to [cropLength] characters. |

### `string/string_truncate_middle_extensions.dart`

`import 'package:saropa_dart_utils/string/string_truncate_middle_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringTruncateMiddleExtensions` on `String` | extension | Truncate the middle of a string, keeping both ends visible. |
| `truncateMiddle` | method | Returns this string shortened to [maxLength] by replacing the middle with [ellipsis], keeping the start and end visible. |

### `string/string_unicode_extensions.dart`

`import 'package:saropa_dart_utils/string/string_unicode_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringUnicodeExtensions` on `String` | extension | Unicode normalization. |
| `normalizeUnicodeNfc` | method | Normalizes to NFC (canonical composition) for comparison and storage. |
| `normalizeUnicodeNfd` | method | Normalizes to NFD (canonical decomposition). |

### `string/string_utils.dart`

`import 'package:saropa_dart_utils/string/string_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringUtils` | class | `StringUtils` is a utility class in Dart that provides static methods for manipulating and analyzing strings. |
| `getNthLatinLetterUpper` | method | Returns the n-th letter of the alphabet in uppercase. |
| `getNthLatinLetterLower` | method | Returns the n-th letter of the alphabet in lowercase. |

### `string/string_wildcard_extensions.dart`

`import 'package:saropa_dart_utils/string/string_wildcard_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringWildcardExtensions` on `String` | extension | Extensions for wildcard matching (`*` = any sequence, `?` = single character). |
| `wildcardMatch` | method | Returns true if this string matches the [pattern] with wildcards. |

### `string/string_words_extensions.dart`

`import 'package:saropa_dart_utils/string/string_words_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringWordsExtensions` on `String` | extension | Word count and break long words. |
| `wordCount` | method | Counts words using whitespace as delimiters. |
| `breakLongWords` | method | Inserts [separator] (e.g. |

### `string/string_wrap_extensions.dart`

`import 'package:saropa_dart_utils/string/string_wrap_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `StringWrapExtensions` on `String` | extension | Word wrap and grapheme-safe truncation. |
| `wordWrap` | method | Wraps this string at [columnWidth], breaking at word boundaries when possible. |
| `truncateAtGrapheme` | method | Truncates at grapheme boundary so no emoji or extended grapheme is cut. |

### `string/template_engine_utils.dart`

Simple template engine (conditionals, no eval) — roadmap #413.

`import 'package:saropa_dart_utils/string/template_engine_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `substituteTemplate` | function | Replaces {{key}} in [template] with values from [data]. |

### `string/text_chunk_utils.dart`

Text segmentation into chunks for indexing (size and sentence boundaries) — roadmap #430.

`import 'package:saropa_dart_utils/string/text_chunk_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `chunkText` | function | Splits [text] into chunks of at most [maxChars] characters, trying to break at sentence boundaries (.). |

### `string/text_diff_structured_utils.dart`

Structured diff of two texts by sentences and by words — roadmap #415.

`import 'package:saropa_dart_utils/string/text_diff_structured_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `SeqDiffKind` | enum | The kind of change an edit-script step represents. |
| `equal` | enum value | The value is present in both inputs (unchanged). |
| `insert` | enum value | The value is present only in the new input (added). |
| `delete` | enum value | The value is present only in the old input (removed). |
| `SeqDiffOp` | class | One step of an edit script: a [kind] and the [value] it applies to. |
| `SeqDiffOp` | constructor | Creates an op of [kind] carrying [value]. |
| `kind` | field | Whether this value was kept, added, or removed. |
| `value` | field | The unit (word, sentence, or generic element) this op describes. |
| `operator ==` | operator |  |
| `hashCode` | getter |  |
| `toString` | method |  |
| `diffSequences` | function | Computes the LCS-based edit script that turns [a] into [b]: a list of `equal` / `delete` / `insert` ops in input order. |
| `diffWords` | function | Word-level structured diff of [oldText] vs [newText], reusing `tokenizeWords` to split into words (punctuation stripped). |
| `diffSentences` | function | Sentence-level structured diff of [oldText] vs [newText], reusing `tokenizeSentences` to split on sentence boundaries. |

### `string/text_fingerprint_utils.dart`

Text fingerprinting (simhash-style) — roadmap #417.

`import 'package:saropa_dart_utils/string/text_fingerprint_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `textFingerprint` | function | Simple 32-bit fingerprint: hash of word shingles. |
| `fingerprintDistance` | function | Hamming distance between two 32-bit fingerprints (number of differing bits). |

### `string/text_normalize_pipeline_utils.dart`

Text normalization pipeline (composable steps) — roadmap #408.

`import 'package:saropa_dart_utils/string/text_normalize_pipeline_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `NormalizeStep` | typedef | One normalization step: (name, function). |
| `normalizeText` | function | Applies [steps] in order to [text]. |
| `normalizeLower` | function | Predefined: lowercase. |
| `normalizeTrim` | function | Predefined: trim. |

### `string/text_similarity_utils.dart`

Text similarity score (cosine similarity over TF vectors) — roadmap #437.

`import 'package:saropa_dart_utils/string/text_similarity_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `termFrequencies` | function | Term frequencies for a list of tokens (e.g. |
| `cosineSimilarity` | function | Cosine similarity between two term-frequency maps (0.0 to 1.0). |
| `textToTf` | function | Tokenizes [s] by splitting on non-letters and lowercasing; returns TF map. |
| `textSimilarity` | function | Returns cosine similarity of [a] and [b] when treated as bags of words. |

### `string/tokenize_sentences_utils.dart`

Tokenize text into sentences and words (roadmap #404).

`import 'package:saropa_dart_utils/string/tokenize_sentences_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `tokenizeSentences` | function | Splits [text] into sentences (split on . |
| `tokenizeWords` | function | Splits [text] into words (non-empty runs of letters/numbers). |

### `string/tokenizer_pipeline_utils.dart`

Customizable tokenizer pipeline: ordered regex rules with keep/skip — roadmap #434.

`import 'package:saropa_dart_utils/string/tokenizer_pipeline_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `TokenRule` | class | One tokenizer rule: a [type] label, the [pattern] to match at the cursor, and whether matches are dropped ([skip]) instead of emitted. |
| `TokenRule` | constructor | Creates a rule labelled [type] matching [pattern]. |
| `type` | field | The label attached to tokens this rule produces (e.g. |
| `pattern` | field | The pattern matched as a prefix at the current cursor position. |
| `shouldSkip` | field | When true, matches advance the cursor but emit no token. |
| `Token` | class | A produced token: its rule [type], the matched [value], and the [start] offset into the original input. |
| `Token` | constructor | Creates a token of [type] holding [value], found at [start]. |
| `type` | field | The label from the [TokenRule] that produced this token. |
| `value` | field | The exact matched substring. |
| `start` | field | The 0-based offset of [value] in the original input. |
| `operator ==` | operator |  |
| `hashCode` | getter |  |
| `toString` | method |  |
| `tokenize` | function | Tokenizes [input] by trying [rules] in order at each cursor position; the first rule whose pattern matches as a prefix wins. |
| `_Hit` | constructor |  |
| `rule` | field |  |
| `text` | field |  |

### `string/url_extract_utils.dart`

URL/link extractor with context (roadmap #423).

`import 'package:saropa_dart_utils/string/url_extract_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `UrlExtractUtils` | class | One extracted link with optional label and surrounding snippet. |
| `UrlExtractUtils` | constructor | Creates an extracted link for [url], with an optional anchor [label] and surrounding [snippet] of context. |
| `url` | getter | The extracted URL. |
| `label` | getter | Visible link text/anchor when the URL was part of markup, else null. |
| `snippet` | getter | Surrounding text giving context around the link, or null if unavailable. |
| `toString` | method |  |
| `extractUrlsWithContext` | function | Extracts URLs from [text]; [snippetLength] chars of context before/after. |

---

## Testing

### `testing/debug_utils.dart`

Testing/Debug: pretty-print, dump iterable, assert equals with tolerance, range, repeat, timed.

`import 'package:saropa_dart_utils/testing/debug_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `prettyPrint` | function | Testing/Debug: pretty-print, dump iterable, assert equals with tolerance, range, repeat, timed. |
| `dumpIterable` | function | Renders [it] as a string, truncating to the first [maxItems] elements and appending the total count when the iterable is longer. |
| `assertEqualsWithTolerance` | function | Throws an [AssertionError] when [a] and [b] differ by more than [tolerance]. |
| `rangeInt` | function | Returns integers from [start] (inclusive) toward [end] (exclusive) in increments of [step]. |
| `rangeDouble` | function | Returns doubles from [start] (inclusive) toward [end] (exclusive) in increments of [step]. |
| `repeatValue` | function | Returns a list containing [value] repeated [n] times. |
| `timed` | function | Runs [fn] synchronously and returns the wall-clock [Duration] it took. |
| `retryUntil` | function | Calls [predicate] up to [maxAttempts] times, returning `true` as soon as it succeeds, or `false` if every attempt fails. |

---

## URL & Path

### `url/path_extension_utils.dart`

File extension / without extension / change extension.

`import 'package:saropa_dart_utils/url/path_extension_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `pathExtension` | function | File extension / without extension / change extension. |
| `pathWithoutExtension` | function | Returns [path] with its trailing file extension (and the dot) removed. |
| `pathChangeExtension` | function | Returns [path] with its extension replaced by [newExt]. |

### `url/path_join_utils.dart`

Join path segments (cross-platform, no double slash).

`import 'package:saropa_dart_utils/url/path_join_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `pathJoin` | function | Join path segments (cross-platform, no double slash). |
| `pathNormalize` | function | Normalize path (resolve . |
| `pathRelative` | function | Relative path from base to target. |

### `url/path_more_utils.dart`

URL/Path More: directory from path, base name, path separator, is absolute, etc.

`import 'package:saropa_dart_utils/url/path_more_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `pathDirectory` | function | URL/Path More: directory from path, base name, path separator, is absolute, etc. |
| `pathBaseName` | function | Returns the final segment of [path] (the file or last directory name). |
| `pathSeparator` | getter | The forward-slash separator used by these path helpers. |
| `isPathAbsolute` | function | Whether [path] is absolute — starts with `/` or has a drive letter (`C:`). |
| `pathCollapseSeparators` | function | Collapses runs of `/` or `\` in [path] into a single forward slash. |
| `pathAppend` | function | Joins [segment] onto [path] with a single forward slash. |
| `parseBearerToken` | function | Extracts the token from a `Bearer <token>` [authorizationHeader]. |

### `url/uri_pattern_utils.dart`

URI path-template matcher with typed params — roadmap #630.

`import 'package:saropa_dart_utils/url/uri_pattern_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `UriPattern` | class | A compiled path template such as `/users/{id}/posts/{slug}` that matches concrete request paths and extracts the named segment parameters. |
| `UriPattern` | constructor | Compiles [template] into matchable segments. |
| `match` | method | Matches [path] against this template. |
| `_Segment` | constructor |  |
| `_Segment.parse` | constructor |  |
| `isIntOnly` | field | Whether a captured value must parse as an integer to match. |
| `accept` | method |  |

### `url/url_absolute_utils.dart`

`import 'package:saropa_dart_utils/url/url_absolute_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `isAbsoluteUrl` | function | Is absolute URL / is relative path. |
| `isRelativePath` | function | Whether [path] is a relative path (no leading `/` and no drive letter). |

### `url/url_build_utils.dart`

`import 'package:saropa_dart_utils/url/url_build_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `buildUri` | function | Build URL from base + path + query. |
| `stripFragment` | function | Returns [uri] with its fragment (`#...`) removed. |

### `url/url_canonicalize_utils.dart`

Canonicalize a URL so equivalent links compare equal.

`import 'package:saropa_dart_utils/url/url_canonicalize_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `canonicalizeUrl` | function | Returns a canonical form of [uri]: - scheme and host lower-cased (they are case-insensitive), - the scheme's default port removed — Dart's [Uri] does this au... |

### `url/url_encode_utils.dart`

URL encode/decode (component vs full).

`import 'package:saropa_dart_utils/url/url_encode_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `urlEncodeComponent` | function | Percent-encodes [value] for use as a single URI component. |
| `urlDecodeComponent` | function | Decodes a percent-encoded URI component [value] back to plain text. |
| `safeDecodeUri` | function | Decodes a percent-encoded URI component [value], returning null on failure. |

### `url/url_extensions.dart`

`import 'package:saropa_dart_utils/url/url_extensions.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `flutterImageExtensions` | field | Common image file extensions supported by Flutter. |
| `UriExtensions` on `Uri` | extension | Extension methods for URI manipulation. |
| `removeQuery` | method | Returns a new URI with query parameters removed. |
| `isImageUri` | getter | Returns true if this URI points to an image file. |
| `fileName` | getter | Returns the file name from this URI path, or `null` if the path is empty. |
| `fileExtension` | getter | Returns the file extension from this URI path, or `null` if none exists. |
| `isSecure` | getter | Returns true if this URI uses HTTPS scheme. |
| `addQueryParameter` | method | Returns a new URI with the [key] query parameter set to [value]. |
| `hasQueryParameter` | method | Returns `true` if this URI has a query parameter named [key]. |
| `getQueryParameter` | method | Returns the value of the [key] query parameter, or `null` if not found. |
| `replaceHost` | method | Returns a new URI with the host replaced by [newHost], or this URI if [newHost] is empty. |
| `UriNullableExtensions` on `Uri?` | extension | Extension methods for nullable URI. |
| `isUriNullOrEmpty` | getter | Returns true if this URI is null or has an empty path. |
| `isNotUriNullOrEmpty` | getter | Returns true if this URI is not null and has a non-empty path. |
| `UrlExtensions` | class | Utility class for URL operations. |
| `tryParse` | method | Returns [url] parsed as a URI, or `null` if parsing fails. |
| `isValidUrl` | method | Returns `true` if [url] is a valid URL with a scheme and host. |
| `isValidHttpUrl` | method | Returns `true` if [url] is a valid HTTP or HTTPS URL. |
| `extractDomain` | method | Returns the domain (host) extracted from [url], or `null` if invalid. |

### `url/url_query_utils.dart`

Parse query string to map (and back).

`import 'package:saropa_dart_utils/url/url_query_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `parseQueryString` | function | Parse query string to map (and back). |
| `buildQueryString` | function | Builds a percent-encoded `key=value&...` query string from [params]. |
| `uriWithQueryParams` | function | Add/remove query params (immutable). |

---

## UUID

### `uuid/uuid_utils.dart`

`import 'package:saropa_dart_utils/uuid/uuid_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `UuidUtils` | class | Utility class for UUID validation and manipulation. |
| `isUUID` | method | Validates if the given string is a valid UUID. |
| `addHyphens` | method | Adds hyphens to a 32-character UUID string to create the standard format. |
| `removeHyphens` | method | Removes hyphens from a UUID string to create the compact format. |

### `uuid/uuid_v4_utils.dart`

`import 'package:saropa_dart_utils/uuid/uuid_v4_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `generateUuidV4` | function | Generate random UUID v4. |

---

## Validation

### `validation/cross_field_validation_utils.dart`

Cross-field validation (start < end, one-of required) — roadmap #682.

`import 'package:saropa_dart_utils/validation/cross_field_validation_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `validateStartBeforeEnd` | function | Returns null if valid, else error message. |
| `validateOneOfRequired` | function | Returns null if at least one of [values] is non-null and non-empty (for strings). |

### `validation/data_redaction_utils.dart`

Data redaction policies (masking per field path) — roadmap #692.

`import 'package:saropa_dart_utils/validation/data_redaction_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `RedactMaskFn` | typedef | Maps a field [value] to its masked replacement (e.g. |
| `redactFields` | function | Redacts [data] by applying [mask] to values at keys in [fieldPaths]. |

### `validation/guard_utils.dart`

Defensive coding helpers (guard patterns, invariants) — roadmap #700.

`import 'package:saropa_dart_utils/validation/guard_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `guardArgument` | function | Throws [ArgumentError] if [isConditionMet] is false, with [message]. |
| `guard` | function | Returns [value] if [isConditionMet] else throws [ArgumentError]. |

### `validation/input_shaping_utils.dart`

Input shaping: clamp/normalize numbers, trim/limit strings — roadmap #696.

`import 'package:saropa_dart_utils/validation/input_shaping_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `clampNumber` | function | Clamps [value] to [min, max], then returns as int if [isInt] else double. |
| `shapeString` | function | Trims [s] and truncates to [maxLength] with optional [ellipsis]. |

### `validation/ip_cidr_utils.dart`

IP/CIDR utilities (subnet contains, range checks) — roadmap #686.

`import 'package:saropa_dart_utils/validation/ip_cidr_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `parseIpv4` | function | Parses IPv4 "a.b.c.d" into int (big-endian). |
| `ipInCidr` | function | Checks if [ip] (as int) is in CIDR [network]/[prefixLen]. |

### `validation/jwt_structure_utils.dart`

JWT structural checks (no crypto) — roadmap #688.

`import 'package:saropa_dart_utils/validation/jwt_structure_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `isJwtStructure` | function | Returns true if [token] looks like a JWT (three base64url parts separated by .). |
| `jwtPayload` | function | Decodes payload (middle part) as JSON map; returns null if invalid. |

### `validation/password_strength_utils.dart`

Password strength estimation (entropy heuristics) — roadmap #687.

`import 'package:saropa_dart_utils/validation/password_strength_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `passwordStrengthScore` | function | Simple strength score 0..4 from length and character variety. |

### `validation/path_validator_utils.dart`

Robust file path validators (prevent traversal, normalize) — roadmap #693.

`import 'package:saropa_dart_utils/validation/path_validator_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `isPathSafe` | function | Returns true if [path] does not contain '..' segments that escape [root]. |

### `validation/pii_detector_utils.dart`

PII detector for free-form text — roadmap #691.

`import 'package:saropa_dart_utils/validation/pii_detector_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `detectPii` | function | Detects likely PII patterns; returns list of (patternName, startIndex, endIndex). |

### `validation/safe_parse_utils.dart`

Safe parsing wrappers (no-throw, rich error) — roadmap #695.

`import 'package:saropa_dart_utils/validation/safe_parse_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `ParseFn` | typedef | Parses [source] into a `T`, throwing on failure (caught by [safeParse]). |
| `SafeParseUtils` | class | Result of a safe parse. |
| `valueOrNull` | getter | The parsed value if successful, or null if failed. |
| `ParseOk` | class | Successful parse result holding the parsed [value]. |
| `ParseOk` | constructor | Wraps a successfully parsed [value]. |
| `value` | getter | The parsed value. |
| `valueOrNull` | getter |  |
| `toString` | method |  |
| `ParseErr` | class | Failed parse result with [message] and optional [details]. |
| `ParseErr` | constructor | Creates a failure with an error [message] and optional [details] trace. |
| `message` | getter | Human-readable error message. |
| `details` | getter | Optional stack trace from the parse exception. |
| `valueOrNull` | getter |  |
| `toString` | method |  |
| `safeParse` | function | Parses [source] with [parse]; returns ParseOk or ParseErr. |

### `validation/safe_temp_name_utils.dart`

Safe temp-file naming (randomized, collision-resistant) — roadmap #694.

`import 'package:saropa_dart_utils/validation/safe_temp_name_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `safeTempName` | function | Returns a short random string suitable for temp file names (alphanumeric). |

### `validation/typed_positive_utils.dart`

Typed non-empty/positive wrappers with validation — roadmap #697.

`import 'package:saropa_dart_utils/validation/typed_positive_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `TypedPositiveUtils` | class | Wrapper that guarantees non-empty string (validated at construction). |
| `TypedPositiveUtils` | constructor | Creates a [TypedPositiveUtils] if [value] is non-empty after trim; otherwise throws. |
| `value` | getter | The non-empty string value. |
| `PositiveNumber` | class | Wrapper that guarantees positive number. |
| `PositiveNumber` | constructor | Creates a [PositiveNumber] if [value] is greater than zero; otherwise throws. |
| `value` | getter | The positive number value. |
| `toString` | method |  |

### `validation/validation_error_utils.dart`

Normalized error model (code, message, details) for validation (roadmap #683).

`import 'package:saropa_dart_utils/validation/validation_error_utils.dart';`

| Symbol | Kind | Description |
|--------|------|-------------|
| `ValidationErrorUtils` | class | Single validation error with optional path and code. |
| `ValidationErrorUtils` | constructor | Creates an error with a human-readable [message], optional machine-readable [code], and optional [path] identifying the offending field. |
| `message` | field | Human-readable description of what failed. |
| `code` | getter | Optional machine-readable error code. |
| `path` | getter | Optional path (e.g. |
| `toString` | method |  |
| `ValidationErrors` | class | Aggregates multiple validation errors (roadmap #684). |
| `ValidationErrors` | constructor | Creates a collector, optionally seeded with an existing [list] of errors. |
| `add` | method | Appends a single error. |
| `addAll` | method | Appends all errors from [e]. |
| `isEmpty` | getter | True when there are no errors. |
| `isNotEmpty` | getter | True when there is at least one error. |
| `errors` | getter | Unmodifiable list of all errors. |
| `toString` | method |  |

---

